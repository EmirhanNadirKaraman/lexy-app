fastapi
spacy
uvicorn[standard]
asyncpg
python-dotenv
pydantic[email]
aiofiles

# auth
bcrypt
pyjwt

# migrations (alembic uses sqlalchemy's connection layer; no ORM models are used)
alembic
sqlalchemy
psycopg2-binary

# llm
anthropic

# youtube metadata
yt-dlp

# book reading mode
PyMuPDF
pytesseract
Pillow
regex

# dev / test
httpx
pytest
pytest-asyncio
pytest-xdist
# Pinned deliberately, unlike everything else in this file: ruff's rule set and
# its default selections change between releases, so an unpinned ruff would make
# `ruff check .` report differently machine-to-machine. Config lives in
# /ruff.toml at the repo root.
ruff==0.14.1
