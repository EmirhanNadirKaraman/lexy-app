# Repository audit — 2026-08-03

Generated by the autoloop audit executor (deterministic rendering of reconciled subagent findings; raw agent reports preserved at `.autoloop/audit/20260803T130810Z-5aebac`).

- branch: `autoloop/audit-0001` | HEAD: `85bf26f3df09` | uncommitted files: 0
- requested scope: Audit architecture, tests/CI, security, database migrations, documentation drift, and the ingestion pipeline; reconcile findings against the existing 23-task roadmap, identify stale or duplicate tasks, and propose only evidence-backed additions or corrections.

## Domain coverage — 6/6 domains reported usable output

| domain | usable output | findings |
|---|---|---|
| `docs_drift` | yes | 3 |
| `tests_ci` | yes | 6 |
| `repo_structure` | yes | 8 |
| `security_paths` | yes | 3 |
| `db_migrations` | yes | 2 |
| `ingestion_pipeline` | yes | 5 |

## Validation baseline
- `ruff check .` — **PASS**
  - tail: `All checks passed!`
- `python3 -m pytest autoloop/tests -q` — **PASS**
  - tail: `[32m[32m[1m1109 passed[0m, [33m1 skipped[0m[32m in 223.18s (0:03:43)[0m[0m`
- `python3 -m pytest tests/ -q` — **PASS**
  - tail: `[33m[32m368 passed[0m, [33m[1m5 warnings[0m[33m in 12.19s[0m[0m`

## Confirmed defects (7)
#### tests_ci:ci-test-coverage-missing — Add GitHub Actions workflow jobs: `backend` (cd lexy-app/backend && pytest -n auto), `frontend` (cd 
- severity **high**, confidence **confirmed**, domain `tests_ci`
- files: `.github/workflows/dependency-audit.yml`
- evidence: .github/workflows/dependency-audit.yml contains only `pip-audit` and `npm audit` jobs. No workflow invokes `pytest`, `npm run test`, or test suites before merge to main. CLAUDE.md §11 requires `ruff check .` and docs/TESTS.md §0 documents 1137 backend + 322 frontend + 221 root-pipeline tests (1680 total), none of which execute in CI.
- impact: Code merges to main without running any test suites. Regressions in backend progression rules, frontend interactivity, or pipeline i+1 logic are not caught. CI gap makes test counts in TESTS.md aspirational rather than enforced.
- acceptance: New workflow .github/workflows/tests.yml or equivalent exists; Backend pytest runs with `-n auto` and must pass; Frontend `npm run test` runs and must pass; Root pipeline `pytest tests/` runs and must pass; Ruff check runs and blocks on findings; All four jobs are required status checks on main branch
- validation: ls .github/workflows/ | grep -i test; grep -A 5 'jobs:' .github/workflows/tests.yml | grep -E 'backend|frontend|pipeline|lint'; git log --oneline | head -1 | xargs git show --name-only | grep -c '.py' (verify a PR is testable)

#### tests_ci:linting-not-enforced — Add a `lint` job to the test CI workflow (see ci-test-coverage-missing) that runs `ruff check .` fro
- severity **medium**, confidence **confirmed**, domain `tests_ci`
- files: `CLAUDE.md`, `.github/workflows/dependency-audit.yml`
- symbols: `CLAUDE.md:208-214`
- evidence: CLAUDE.md §11 states: 'Ruff is **part of backend validation, not an optional extra**—run it alongside `pytest` before calling any Python change done.' Baseline documented as 'zero findings (was 153; see the ruff entry in `docs/TESTS.md`)'. No CI job runs `ruff check .` to enforce this rule before merge.
- impact: Lint findings can silently accumulate. A developer working locally on a machine without ruff installed (or an old version) can commit code that fails the 'zero findings' baseline when run on another machine.
- acceptance: `ruff check .` passes with 'All checks passed!' on every commit to main; CI rejects PRs with ruff findings
- validation: cd /repo && ruff check . && echo 'PASS' || echo 'FAIL'

#### tests_ci:test-isolation-not-verified — Once CI test job is added (see ci-test-coverage-missing), verify that running backend tests WITH pyt
- severity **medium**, confidence **confirmed**, domain `tests_ci`
- files: `lexy-app/pytest.ini`, `pytest.ini`, `docs/TESTS.md`
- symbols: `lexy-app/pytest.ini:10`, `pytest.ini:6`, `docs/TESTS.md:11-44`
- evidence: Both pytest.ini files carry `addopts = -p no:randomly` documented in TESTS.md §0 as 'load-bearing'. The rationale: pytest-randomly masks seeds, numpy overflows on any seed ≥2**32, and thinc's seeder is unmasked. TESTS.md documents 'With the block [removed]: 1036 errors'; without it, 'All checks passed!'. The block exists in config but is never validated in CI — no CI runs pytest, so the block's necessity is untested.
- impact: If pytest-randomly is transitively installed in a CI agent's environment and the `-p no:randomly` block is accidentally removed, every test setup/teardown crashes at numpy.random.seed() with ValueError. The load-bearing test isolation would be silently broken.
- acceptance: Backend pytest runs with pytest-randomly installed in the CI environment and still passes (due to block); Documentation in CLAUDE.md §11 or TESTS.md §0 notes the CI verification of the block
- validation: pip install pytest-randomly && cd /repo/lexy-app/backend && pytest -n auto -q && echo 'PASS (block effective)' || echo 'FAIL (block broken)'

#### tests_ci:frontend-tests-not-ci-gated — Add frontend job to test CI workflow: `cd lexy-app/frontend && npm ci && npm run test`. Must pass be
- severity **medium**, confidence **confirmed**, domain `tests_ci`
- files: `lexy-app/frontend/package.json`, `.github/workflows/dependency-audit.yml`
- evidence: TESTS.md documents frontend 'Current count (2026-07-27): 261 tests across 43 files' (line 99) and subsequent updates show 322 tests (line 835). npm audit is CI'd but not `npm run test`. No workflow invokes Vitest.
- impact: Frontend regressions (broken hooks, component state, routing, theme switching) are not caught before merge. A critical bug in SRSReviewPage or BookReaderPage shipping live depends only on manual testing or post-deploy detection.
- acceptance: Frontend vitest runs on every PR; All 322 tests pass or failure blocks merge
- validation: cd /repo/lexy-app/frontend && npm run test 2>&1 | grep -E 'passed|failed'

#### tests_ci:root-pipeline-tests-not-ci-gated — Add pipeline job to test CI workflow: from repo root, `pytest tests/ -q`. Must pass before merge.
- severity **medium**, confidence **confirmed**, domain `tests_ci`
- files: `pytest.ini`, `.github/workflows/dependency-audit.yml`
- evidence: ROADMAP.md line 70: 'root pipeline **221 passed**'. TESTS.md line 2 (root section intro) says 'Pytest tests for pipeline modules. Mostly hermetic (no DB). 207 tests'. No CI workflow invokes `pytest tests/` from repo root.
- impact: Pipeline ingestion (i+1 eligibility, exposure counter, learning units, subtitle cleaner/merger/segmenter) has no CI verification. A change to pipeline.py or exposure_counter.py can silently break the subtitle-scraper's core i+1 logic without CI detection.
- acceptance: Root pipeline pytest runs on every PR; All 221 tests pass or failure blocks merge
- validation: cd /repo && python3 -m pytest tests/ -q 2>&1 | tail -1

#### ingestion_pipeline:ingest-defect-02 — Rewrite test_document_package.py's imports to 'from backend.services...'/'from backend.services.docu
- severity **medium**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/tests/test_document_package.py`
- symbols: `test_document_package.py:13-43`
- evidence: Line 13 'from services import book_import_service' + lines 14-43 bare 'from services.document_package import ...' — the only backend test file not using 'from backend.services import ...'. Requires cwd on sys.path, so bare 'pytest' silently drops all 107 of this file's tests while 'python3 -m pytest' collects them. Documented in CLAUDE.md §11 / COMMON_ERRORS.md §2 and tracked as ingestion_pipeline:ing-03/ing-05, au-015/au-017 — re-verified still open 2026-08-03.
- impact: A contributor who runs the bare 'pytest' console script instead of 'python3 -m pytest' gets a clean-looking pass with A2's entire test suite (107 tests, all of book_import_service coverage) silently uncollected, and may ship an untested change believing it's covered.
- acceptance: cd lexy-app/backend && pytest tests/test_document_package.py -q (bare console script) collects and passes all 107 tests; python3 -m pytest tests/test_document_package.py -q also passes
- validation: cd lexy-app/backend && pytest tests/test_document_package.py -q; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -q

#### ingestion_pipeline:ingest-defect-01 — Only emit checksums_verified when the loop found zero fatal checksum issues, or reword it to 'checke
- severity **low**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/services/document_package/loader.py`
- symbols: `loader.py:verify_checksums:344-437`
- evidence: loader.py:402-430 loops pkg.files adding checksum_mismatch/checksum_missing_file/checksum_path_escape as fatal per bad entry; loader.py:432-437 then unconditionally adds an INFO 'checksums_verified: verified N file(s)' with no gate on whether any of those N just failed. Tracked as ingestion_pipeline:ing-06/task rt-06 in AUDIT_2026-07-30.md; absent from AUDIT_2026-08-02.md; code unchanged as of 2026-08-03 — dropped from tracking, not fixed.
- impact: import_result.json can carry an info entry claiming files were 'verified' in the same run where those same files are listed under errors as checksum_mismatch, confusing anyone reading the info/warning trail without cross-checking validation.checksums (which does correctly report 'fail').
- acceptance: A package with a checksum_mismatch entry produces no checksums_verified info claiming success; test_document_package.py gains a case asserting the two are mutually exclusive
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k Checksums -q


## Security / data-loss risks (8)
#### repo_structure:books-admin-gate — Add Depends(require_admin) to GET /books/packages and POST /books/import, matching the delete_book/d
- severity **high**, confidence **confirmed**, domain `repo_structure`
- files: `lexy-app/backend/routers/books.py`, `docs/SECURITY.md`
- symbols: `routers/books.py:182-185 (list_packages)`, `routers/books.py:188-212 (import_package)`
- evidence: books.py:183,189 depend only on Depends(get_current_user); no require_admin, unlike delete_book/delete_page in the same file and /phrases/seed. book_import_service.import_package takes user_id but document_package/loader.py's package_root is a single shared operator directory and contract.py carries no owner field, so user_id is not an ownership scope. Re-verified unchanged since the 2026-08-02 audit; registry not readable from this worker checkout so I cannot confirm whether a task already tracks it.
- impact: Any authenticated non-admin user can enumerate every operator-curated ingestion package name and force full checksum/structural validation on demand — information disclosure plus an unmetered compute-cost surface open to the whole user base.
- acceptance: Non-admin authenticated user gets 403 admin_required from both endpoints; Admin behavior unchanged; docs/SECURITY.md lists both routes as admin-gated
- validation: rg -n 'require_admin' lexy-app/backend/routers/books.py; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k TestImportEndpoint -q

#### security_paths:sec-01 — Add Depends(require_admin) to both routes, matching the existing pattern; add a regression test asse
- severity **high**, confidence **confirmed**, domain `security_paths`
- files: `lexy-app/backend/routers/books.py`
- symbols: `list_packages:183-185`, `import_package:189-212`
- evidence: books.py:183 list_packages and :189 import_package depend only on Depends(get_current_user), no require_admin — verified directly by reading the file. Every other admin-shaped route (delete_book, /phrases/seed, /admin/lemma-corrections/*) uses require_admin. Still open on 2026-08-03; duplicates already-tracked autoloop/seed_tasks.json:rt-01 and docs/AUDIT_2026-08-02.md repo_structure:sec-01 / ingestion_pipeline:ing-04 / security_paths:sec-01 — do not spawn a second task for this.
- impact: Any authenticated non-admin learner can enumerate every operator-curated book package name and force checksum/structural validation compute on demand.
- acceptance: Non-admin caller gets 403 admin_required from both GET /books/packages and POST /books/import; Admin behavior unchanged; docs/SECURITY.md lists both routes under admin-gated routes
- validation: rg -n 'require_admin' lexy-app/backend/routers/books.py; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -q

#### ingestion_pipeline:ingest-sec-01 — Add Depends(require_admin) to both routes (pattern: routers/phrases.py:36), add a non-admin-403 regr
- severity **high**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/routers/books.py`, `docs/SECURITY.md`
- symbols: `routers/books.py:list_packages:182-185`, `routers/books.py:import_package:188-212`
- evidence: books.py:182-212 gate both /books/packages and /books/import on Depends(get_current_user) only; delete_book/delete_page in the same file check is_admin. INGESTION_PIPELINE.md:306 labels the workflow operator-only. Re-verified 2026-08-03, unchanged since seed_tasks.json rt-01 and AUDIT_2026-08-02.md ing-04/sec-01 — still open, not fixed.
- impact: Any authenticated non-admin user can enumerate every operator-curated package name and force checksum/structural validation of any of them — info disclosure plus an uncapped compute-cost surface, worsening into a data-integrity issue once A3 wires real persistence.
- acceptance: Non-admin authenticated user gets 403 admin_required from GET /books/packages and POST /books/import; Admin behavior unchanged; docs/SECURITY.md lists both routes as admin-gated
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k TestImportEndpoint -q; rg -n 'require_admin' lexy-app/backend/routers/books.py

#### repo_structure:migration-025-downgrade-orphan — Guard downgrade() to refuse when reading_selections rows hold non-empty anchors, or persist/restore 
- severity **medium**, confidence **confirmed**, domain `repo_structure`
- files: `lexy-app/backend/migrations/versions/025_block_token_ids.py`, `lexy-app/backend/models/schemas.py`
- symbols: `025_block_token_ids.py:_tokenize:21-27`, `025_block_token_ids.py:downgrade:64-65`, `schemas.py:ReadingSelectionAnchor:704-707`
- evidence: _tokenize (21-27) assigns a fresh uuid.uuid4() per token every run; downgrade() (64-65) is a bare `ALTER TABLE book_blocks DROP COLUMN tokens`, no guard. schemas.py:706 confirms reading_selections.anchors keys on this same token_id UUID ('replaced token_index for stability across edits'), and the frontend sends `token_id: t.tokenId` when saving a selection, so the anchor genuinely depends on token_id stability. Re-verified unchanged since the 2026-08-02 audit.
- impact: A downgrade-then-upgrade of migration 025 regenerates every token_id, silently orphaning every existing reading_selections.anchors reference — the selection row survives but its anchor no longer resolves against book_blocks.tokens.
- acceptance: downgrade() raises when orphaning anchors would occur, or a round-trip test proves token_ids survive downgrade+upgrade
- validation: cat lexy-app/backend/migrations/versions/025_block_token_ids.py

#### security_paths:data-01 — Make downgrade() refuse when any reading_selections row has a non-empty anchors array, or persist/re
- severity **medium**, confidence **confirmed**, domain `security_paths`
- files: `lexy-app/backend/migrations/versions/025_block_token_ids.py`
- symbols: `_tokenize:21-27`, `downgrade:64-65`
- evidence: Read directly: _tokenize (21-27) calls uuid.uuid4() fresh per token every run; downgrade() (64-65) is a bare ALTER TABLE book_blocks DROP COLUMN tokens with no guard. reading_selections.anchors (migration 009:31-32) stores {block_id, token_id, surface} with no FK. Still open 2026-08-03; duplicates docs/AUDIT_2026-08-02.md db_migrations:db-02 / repo_structure:data-01.
- impact: A downgrade-then-upgrade of migration 025 regenerates every token_id, silently orphaning every existing reading_selections.anchors reference with no error surfaced.
- acceptance: downgrade() raises when orphaning anchors would occur, or a round-trip test proves token_ids survive downgrade+upgrade
- validation: cat lexy-app/backend/migrations/versions/025_block_token_ids.py

#### security_paths:ing-01 — Add a structural validator comparing bbox['page'] (when present) to element['page_index'], flagging 
- severity **medium**, confidence **confirmed**, domain `security_paths`
- files: `lexy-app/backend/services/document_package/validators.py`, `docs/INGESTION_PIPELINE.md`
- symbols: `validators.py:_BBOX_KEYS:21`, `validators.py:validate_bboxes:296-373`
- evidence: Read validators.py directly: _BBOX_KEYS = ("x1","y1","x2","y2") never includes 'page'; page-dims lookup (line 351) keys off element.get('page_index') only, never bbox['page']. Still open 2026-08-03; duplicates docs/AUDIT_2026-08-02.md repo_structure:ing-01 / ingestion_pipeline:ing-01.
- impact: A worker bug or corrupted package where a bbox's own 'page' field disagrees with element.page_index passes every validation gate silently — inert today, a live trap once roadmap A3 wires real persistence off bbox.page.
- acceptance: Synthetic element with mismatched page fails validation with a new issue code
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k Structural -q

#### db_migrations:db-014-01 — Add an idempotent INSERT...SELECT backfill (ON CONFLICT DO NOTHING) into user_video_category in 014'
- severity **medium**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/014_drop_video_genre.py`, `lexy-app/backend/migrations/versions/013_video_category_cleanup.py`
- symbols: `014_drop_video_genre.py:1-4`, `014_drop_video_genre.py:upgrade:21-40`, `013_video_category_cleanup.py:upgrade:37-76`
- evidence: 014_drop_video_genre.py:4 docstring claims liked_genres/disliked_genres are migrated from users.settings into user_video_category, but upgrade() (014:21-40) only adds a column and deletes those JSONB keys (014:32-36) — no INSERT INTO user_video_category exists in 014 or in the prior migration 013 (013:37-76), both checked directly.
- impact: The only JSONB copy of each user's liked/disliked genre preferences is deleted with no backfill in the alembic chain, so genre prefs can be lost on a legacy-seeded DB.
- acceptance: 014_drop_video_genre.py upgrade() contains a backfill INSERT into user_video_category from the JSONB keys, executed before those keys are deleted; Or: the 014 docstring no longer claims a migration it does not perform, and cites where the backfill actually ran; Migrating a DB seeded only with legacy users.settings JSONB and no user_video_category rows preserves the genre preferences
- validation: grep -n 'INSERT INTO user_video_category' lexy-app/backend/migrations/versions/014_drop_video_genre.py lexy-app/backend/migrations/versions/013_video_category_cleanup.py

#### db_migrations:db-014-02 — Add a one-line comment in downgrade() stating the rebuilt settings values reflect current user_video
- severity **low**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/014_drop_video_genre.py`
- symbols: `014_drop_video_genre.py:downgrade:47-64`
- evidence: downgrade() (47-64) rebuilds users.settings.liked_genres/disliked_genres by aggregating current rows from user_video_category — a table upgrade() (21-40) never wrote to. Same 'downgrade fabricates a placeholder instead of restoring' pattern already flagged elsewhere in this chain (migration 013), but not yet noted for 014.
- impact: A downgrade of 014 writes JSONB preference data reconstructed from whatever user_video_category holds at downgrade time (populated by unrelated application code after 014 shipped), not the true pre-upgrade JSONB values — misleading for anyone treating downgrade as a real rollback.
- acceptance: downgrade() in 014_drop_video_genre.py carries a comment documenting that the rebuilt JSONB is not a true restore of pre-upgrade values
- validation: grep -n -B2 'liked_genres' lexy-app/backend/migrations/versions/014_drop_video_genre.py


## Architectural risks (2)
#### repo_structure:docker-boot-crash — Move the Docker build context to the repo root (COPY subtitle-scraper/ + data/ alongside backend/) a
- severity **critical**, confidence **confirmed**, domain `repo_structure`
- files: `lexy-app/Dockerfile`, `lexy-app/docker-compose.yml`, `lexy-app/backend/services/matcher_service.py`
- symbols: `Dockerfile:21-31`, `matcher_service.py:35,43`
- evidence: Dockerfile:28,31 COPY only backend/ and built frontend/dist, never subtitle-scraper/ or data/ (both at repo root, outside the lexy-app/ build context). matcher_service.py:35 computes Path(__file__).resolve().parents[3]/'subtitle-scraper'; in-image WORKDIR=/app so __file__=/app/backend/services/matcher_service.py, making parents[3] '/' -> nonexistent /subtitle-scraper. Line 43 `import phrase_finder` runs at module scope, pulled in before main.py builds the app. Re-verified unchanged since the 2026-08-02 audit (repo_structure:arch-01).
- impact: docker compose up crashes on boot with ModuleNotFoundError: phrase_finder before uvicorn starts — every docker-compose deployment following README.md's own quickstart is unreachable.
- acceptance: docker compose up boots without a startup crash; A smoke test builds the image and gets a 2xx from a health endpoint
- validation: cd lexy-app && docker compose build && docker compose up -d && curl -f http://localhost:8000/api/v1/auth/login -X POST -d '{}' -H 'Content-Type: application/json'

#### repo_structure:taskgen-dedup — In reconcile.py, loosen _is_duplicate_candidate: drop or widen the exact category prefilter and/or l
- severity **medium**, confidence **confirmed**, domain `repo_structure`
- files: `autoloop/audit/reconcile.py`, `autoloop/audit/taskgen.py`, `docs/AUDIT_2026-08-02.md`
- symbols: `reconcile.py:_is_duplicate_candidate:118-148`, `reconcile.py:MERGE_SIMILARITY:73`, `taskgen.py:generate_tasks:98-163`
- evidence: reconcile.py:118-148 gates merging on exact category equality AND affected-file overlap AND Jaccard>=0.7 (threshold reconcile.py:73, token scoring reconcile.py:101-115); taskgen.py:98-163 emits one task per surviving Finding with no later grouping. docs/AUDIT_2026-08-02.md:349 records only one merged pair for the whole run, yet the same books.py admin-gate defect (sec-01/ing-04/sec-01 from three domain agents), the same migration-025 downgrade risk (db-02/data-01) and the same bbox-validator gap (ing-01/ing-01) each survived as separate proposed tasks au-009/au-010/au-011, au-002/au-005, au-003/au-006.
- impact: Independently-phrased duplicate write-ups from different domain agents rarely clear the 0.7 similarity bar, so the task registry accumulates redundant tasks for one defect (33 proposed from ~19 canonical defects on 2026-08-02) — wasted implement budget and a cluttered plan needing hand-triage.
- acceptance: Two synthetic findings describing the same defect under different categories with overlapping affected_files merge into one Finding via reconcile(); A regression test in autoloop/tests/test_audit_reconcile.py locks the merge for a cross-category duplicate pair
- validation: python3 -m pytest autoloop/tests/test_audit_reconcile.py -q


## Documentation drift (7)
#### docs_drift:doc-01 — Recount frontend test files via `find lexy-app/frontend/src -name '*.test.tsx'` and update both docs
- severity **low**, confidence **confirmed**, domain `docs_drift`
- files: `docs/TESTS.md`, `docs/ROADMAP.md`
- symbols: `docs/TESTS.md:99`, `docs/ROADMAP.md:68`
- evidence: docs/TESTS.md line 99 claims '261 tests across 43 files' (current count as of 2026-07-27); docs/ROADMAP.md line 68 claims 'frontend **322 passed across 44 files**' (verified 2026-07-29). Glob search finds exactly 35 frontend test files: 28 in components/, 4 in hooks/, 3 in test/.
- impact: Documentation overstates frontend test file count by 8-9 files. Makes it impossible to verify the claim by counting files. Unknown if test count is also stale.
- acceptance: docs/TESTS.md line 99 reflects actual frontend test file count; docs/ROADMAP.md line 68 reflects actual frontend test file count; Both lines include verification date and command used to count
- validation: find /Users/emir/.autoloop/workers/audit-0001/lexy-app/frontend/src -name '*.test.tsx' | wc -l

#### docs_drift:doc-02 — Remove the sys.path reference from docs/SUMMARY.md since the root pipeline tests now import directly
- severity **low**, confidence **confirmed**, domain `docs_drift`
- files: `docs/SUMMARY.md`
- symbols: `docs/SUMMARY.md:348`
- evidence: docs/SUMMARY.md line 348 references 'conftest.py' and sys.path injection for root pipeline tests, stating the setup 'adds `src/` to path'. However, per CLAUDE.md §10 and the 2026-07-27 deletion history (TODO.md #17), the `src/app/` refactor was deleted entirely, including all sys.path hacks for it. The old setup is stale.
- impact: Documentation describes a hook that no longer exists and was removed over a month ago. New contributors reading this may fail to understand the current pipeline test structure.
- acceptance: docs/SUMMARY.md line 348 no longer mentions sys.path or src/app; root conftest.py has no sys.path.insert for src/ directory
- validation: grep -n 'src.*path' /Users/emir/.autoloop/workers/audit-0001/conftest.py || echo 'not found (expected)'

#### tests_ci:test-count-documentation-aspirational — Once CI is wired (see ci-test-coverage-missing), add a documentation-generation step or a README bad
- severity **low**, confidence **confirmed**, domain `tests_ci`
- files: `docs/TESTS.md`, `docs/ROADMAP.md`
- symbols: `docs/TESTS.md:1-3`, `docs/ROADMAP.md:68-70`
- evidence: TESTS.md header: 'Test inventory and coverage status. **Update this file whenever you add, remove, or rename a test.**' Lines 68-70 of ROADMAP.md: 'Verified on **2026-07-29** (current): backend **1151 passed / 2 skipped**, frontend **322 passed across 44 files**, root pipeline **221 passed**'. Counts are documented as from local runs ("Validation: backend `-n auto` → ...") but CI does not verify them continuously.
- impact: Test counts in documentation can drift from reality. A developer adds a test locally, the count in TESTS.md stays stale because there is no CI enforcement and no human re-checks it on every commit. ROADMAP.md dated 2026-07-29 with counts that may no longer be accurate.
- acceptance: Live test count is visible (badge, in repo, or GitHub UI) and matches actual CI runs; TESTS.md or ROADMAP.md update instructions are executable without manual counting
- validation: cd /repo && grep -n '1151 passed' docs/ROADMAP.md && pytest lexy-app/backend -q 2>&1 | tail -1

#### repo_structure:anchor-format-doc-drift — Update reading_service.py:117's docstring and migration 009's inline comment to describe the current
- severity **low**, confidence **confirmed**, domain `repo_structure`
- files: `lexy-app/backend/services/reading_service.py`, `lexy-app/backend/migrations/versions/009_reading_selections.py`, `lexy-app/backend/models/schemas.py`
- symbols: `reading_service.py:117`, `009_reading_selections.py:31`, `schemas.py:706`
- evidence: reading_service.py:117's docstring and migration 009:31's inline comment both describe anchors as `[{block_id, token_index, surface}]` (pre-025 positional format). schemas.py:706 and the frontend (SelectionPanel.tsx sends `token_id: t.tokenId`) show the live shape is `{block_id, token_id, surface}`, a stable UUID explicitly 'replaced token_index for stability'. grep for 'token_index' across lexy-app/ finds only these two stale comments; no code reads that key.
- impact: A contributor trusting reading_service.py's own docstring when writing new code against `anchors` (a migration, an analytics query, or future A3 persistence work) keys off the wrong field name and silently gets no matches.
- acceptance: reading_service.py and migration 009's anchors-shape comments name token_id, not token_index
- validation: grep -rn token_index lexy-app/

#### repo_structure:claude-md-stale-refs — Fix CLAUDE.md §3's migrations path/count, rewrite §8b's status_marked_known sentence to describe app
- severity **low**, confidence **confirmed**, domain `repo_structure`
- files: `CLAUDE.md`, `docs/SECURITY.md`
- symbols: `CLAUDE.md §3 (Alembic line)`, `CLAUDE.md §8b (status_marked_known paragraph)`, `docs/SECURITY.md:546`
- evidence: CLAUDE.md §3 still says migrations are '25+ files in `alembic/versions/`' — alembic.ini's script_location is `migrations` and the real 37 files live at lexy-app/backend/migrations/versions/; no alembic/versions/ dir exists. CLAUDE.md §8b still says the known-status flip is 'forced to known by word_service.upsert_word_status', though CLAUDE.md's own §10/§12 say that function 'has been removed'/'is gone'. docs/SECURITY.md:546 cites 'CLAUDE.md §7', but current CLAUDE.md's sections run 3→5→6→8 — no §4 or §7 exists.
- impact: CLAUDE.md is the file this repo tells every contributor to read first (§13); three of its own cross-references (and one in SECURITY.md) point at things that no longer exist, misleading whoever follows them.
- acceptance: CLAUDE.md no longer mentions upsert_word_status or alembic/versions/; docs/SECURITY.md:546 resolves to a section that exists
- validation: grep -n upsert_word_status CLAUDE.md; grep -n 'alembic/versions' CLAUDE.md; grep -n '^## ' CLAUDE.md

#### ingestion_pipeline:ingest-doc-01 — Change '+89 backend' to '+107 backend' at docs/TESTS.md:956 to match the figure already stated later
- severity **low**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `docs/TESTS.md`
- symbols: `docs/TESTS.md:956`
- evidence: Line 956's heading states A2 landed '+89 backend' tests. grep -c 'def test_' on test_document_package.py = 96, plus 3 parametrize sites (4+5+5 cases) adding 11 extra collected items = 107, matching the '107 tests' figure the same file states elsewhere (~line 2174) and INGESTION_PIPELINE.md §13. Already flagged as ingestion_pipeline:ing-05/au-024 in AUDIT_2026-08-02.md — re-verified still unresolved 2026-08-03.
- impact: A reader trusting the '+89' heading under-counts test_document_package.py's actual coverage by 18 tests in the file CLAUDE.md designates as the coverage index.
- acceptance: Both figures for test_document_package.py agree within docs/TESTS.md
- validation: grep -n '89 backend\|107 tests' docs/TESTS.md

#### docs_drift:doc-03 — Update line 201 to state clearly that the salvage and deletion are complete events: 'The `src/app/` 
- severity **info**, confidence **confirmed**, domain `docs_drift`
- files: `docs/ROADMAP.md`
- symbols: `docs/ROADMAP.md:201`
- evidence: docs/ROADMAP.md line 201 mentions 'tests for pipeline modules' with a note 'There is no longer a `src/app/` tree or tests targeting one — see TODO #17.' However, docs/TODO.md #17 (starting at line 273) was resolved 2026-07-27 with `src/app/` and 537 tests deleted. The ROADMAP entry correctly notes this resolution, but the phrasing as a current state (no longer exists) rather than a past decision (was deleted) could be clearer for readers unaware of the deletion.
- impact: Minor clarity issue in documentation tense. Does not affect functionality or understanding significantly.
- acceptance: docs/ROADMAP.md line 201 uses past tense for the deletion; Cross-reference to TODO.md #17 remains valid
- validation: grep -A 2 'no longer a.*src/app' /Users/emir/.autoloop/workers/audit-0001/docs/ROADMAP.md


## Missing regression protection (2)
#### repo_structure:ci-missing-gates — Add a new .github/workflows/tests.yml running `ruff check .`, backend `python3 -m pytest -n auto`, r
- severity **high**, confidence **confirmed**, domain `repo_structure`
- files: `.github/workflows/dependency-audit.yml`
- symbols: `.github/workflows/`
- evidence: Glob '.github/workflows/*' returns only dependency-audit.yml (pip-audit + npm audit jobs). CLAUDE.md §11 calls `ruff check .` and the backend/frontend/root pytest suites 'part of backend validation, not an optional extra,' but no workflow invokes pytest, ruff, tsc, or a frontend build. Re-verified unchanged since the 2026-08-02 audit.
  - also reported as security_paths:ci-01: Glob '.github/workflows/*.yml' on 2026-08-03 returns only dependency-audit.yml, which runs pip-audit and npm audit only — no pytest/ruff/tsc anywhere in it. CLAUDE.md §11 calls ruff+pytest 'part of backend validation, not an optional extra.' Duplicates docs/AUDIT_2026-08-02.md tests_ci:ci-01 / repo_structure:ci-01 / tests_ci:test-01 — do not spawn a second task.
- impact: A PR that fails ruff, breaks a backend/frontend/root-pipeline test, or breaks the frontend TypeScript build can merge to main with a fully green required-checks state.
  - also reported as security_paths:ci-01: A PR that fails ruff, breaks a backend/frontend/root-pipeline test, or breaks the TypeScript build can merge to main with a fully green required-checks state.
- acceptance: tests.yml exists and runs on PRs to main; A PR with a failing test or ruff violation shows a failing required check
- validation: ls -la .github/workflows/

#### repo_structure:test-doc-package-import-style — Rewrite the imports to `from backend.services import ...` / `from backend.services.document_package 
- severity **low**, confidence **confirmed**, domain `repo_structure`
- files: `lexy-app/backend/tests/test_document_package.py`
- symbols: `test_document_package.py:13-40`
- evidence: Line 13 `from services import book_import_service` and following lines use bare `from services.document_package import ...` — every other file under lexy-app/backend/tests/ uses `from backend.services import ...` (CLAUDE.md §11 / docs/COMMON_ERRORS.md §2 already document this exact trap). Re-verified unchanged since the 2026-08-02 audit.
- impact: Running the bare `pytest` console script instead of `python3 -m pytest` silently drops this file's ~107 tests from collection with no clear error, so a contributor on the wrong entrypoint can believe an untested ingestion-pipeline change is safe.
- acceptance: File uses from backend.services... imports throughout; bare `pytest tests/test_document_package.py` from lexy-app/backend collects and passes all tests
- validation: cd lexy-app/backend && pytest tests/test_document_package.py -q


## Optional improvements (1)
#### ingestion_pipeline:ingest-improve-01 — Resolve UPLOAD_ROOT lazily inside package_root() (matching PACKAGE_ROOT's call-time read) instead of
- severity **low**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/services/document_package/loader.py`
- symbols: `loader.py:DEFAULT_PACKAGE_ROOT:25`, `loader.py:package_root:89-97`
- evidence: loader.py:25 computes DEFAULT_PACKAGE_ROOT = Path(os.getenv('UPLOAD_ROOT','uploads'))/'packages' once at module import; package_root() (89-97) re-reads os.getenv('PACKAGE_ROOT') fresh on every call. Setting PACKAGE_ROOT after import takes effect immediately; setting UPLOAD_ROOT after import silently does not. test_document_package.py:929 exercises PACKAGE_ROOT via monkeypatch; no test exercises UPLOAD_ROOT for this module. New finding, not in either prior report.
- impact: A test or late-binding deployment that sets UPLOAD_ROOT expecting document_package.package_root() to follow it (as book_service.py's identically-named _UPLOAD_ROOT does) silently gets the real default 'uploads/packages' instead — a same-file inconsistency between two env vars' resolution timing.
- acceptance: Setting UPLOAD_ROOT via monkeypatch after the module is imported changes package_root()'s result in a new regression test
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k package_root -q


## Human decisions needed (0)
(none)

## Rejected items (1)
- [ingestion_pipeline] unsupported speculation — recorded, never promoted to a task — `ingestion_pipeline:ingest-improve-02`

## Deduplicated (1)
- `security_paths:ci-01` folded into `repo_structure:ci-missing-gates`

## Proposed task graph (27 tasks)
To adopt this graph, reply with a `plan` decision containing the tasks below (edit freely — this is a proposal):
```json
[
  {
    "id": "au-001",
    "title": "Add GitHub Actions workflow jobs: `backend` (cd lexy-app/backend && pytest -n auto), `frontend` (...",
    "description": "Priority: P1 | severity high | confidence confirmed | from audit finding tests_ci:ci-test-coverage-missing\nScope: Add GitHub Actions workflow jobs: `backend` (cd lexy-app/backend && pytest -n auto), `frontend` (cd lexy-app/frontend && npm run test), `pipeline` (pytest tests/), and `lint` (ruff check .). All must pass before merge.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Code merges to main without running any test suites. Regressions in backend progression rules, frontend interactivity, or pipeline i+1 logic are not caught. CI gap makes test counts in TESTS.md aspirational rather than enforced.\nEvidence: .github/workflows/dependency-audit.yml contains only `pip-audit` and `npm audit` jobs. No workflow invokes `pytest`, `npm run test`, or test suites before merge to main. CLAUDE.md \u00a711 requires `ruff check .` and docs/TESTS.md \u00a70 documents 1137 backend + 322 frontend + 221 root-pipeline tests (1680 total), none of which execute in CI.\nExpected files: .github/workflows/dependency-audit.yml\nAcceptance criteria: New workflow .github/workflows/tests.yml or equivalent exists; Backend pytest runs with `-n auto` and must pass; Frontend `npm run test` runs and must pass; Root pipeline `pytest tests/` runs and must pass; Ruff check runs and blocks on findings; All four jobs are required status checks on main branch\nValidation: ls .github/workflows/ | grep -i test; grep -A 5 'jobs:' .github/workflows/tests.yml | grep -E 'backend|frontend|pipeline|lint'; git log --oneline | head -1 | xargs git show --name-only | grep -c '.py' (verify a PR is testable)\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-002",
    "title": "Add an idempotent INSERT...SELECT backfill (ON CONFLICT DO NOTHING) into user_video_category in 0...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding db_migrations:db-014-01\nScope: Add an idempotent INSERT...SELECT backfill (ON CONFLICT DO NOTHING) into user_video_category in 014's upgrade() before the JSONB keys are deleted, or correct the docstring to cite where the backfill really happened.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: The only JSONB copy of each user's liked/disliked genre preferences is deleted with no backfill in the alembic chain, so genre prefs can be lost on a legacy-seeded DB.\nEvidence: 014_drop_video_genre.py:4 docstring claims liked_genres/disliked_genres are migrated from users.settings into user_video_category, but upgrade() (014:21-40) only adds a column and deletes those JSONB keys (014:32-36) \u2014 no INSERT INTO user_video_category exists in 014 or in the prior migration 013 (013:37-76), both checked directly.\nExpected files: lexy-app/backend/migrations/versions/014_drop_video_genre.py, lexy-app/backend/migrations/versions/013_video_category_cleanup.py\nAcceptance criteria: 014_drop_video_genre.py upgrade() contains a backfill INSERT into user_video_category from the JSONB keys, executed before those keys are deleted; Or: the 014 docstring no longer claims a migration it does not perform, and cites where the backfill actually ran; Migrating a DB seeded only with legacy users.settings JSONB and no user_video_category rows preserves the genre preferences\nValidation: grep -n 'INSERT INTO user_video_category' lexy-app/backend/migrations/versions/014_drop_video_genre.py lexy-app/backend/migrations/versions/013_video_category_cleanup.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-003",
    "title": "Rewrite test_document_package.py's imports to 'from backend.services...'/'from backend.services.d...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding ingestion_pipeline:ingest-defect-02\nScope: Rewrite test_document_package.py's imports to 'from backend.services...'/'from backend.services.document_package...', matching every other backend test file.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A contributor who runs the bare 'pytest' console script instead of 'python3 -m pytest' gets a clean-looking pass with A2's entire test suite (107 tests, all of book_import_service coverage) silently uncollected, and may ship an untested change believing it's covered.\nEvidence: Line 13 'from services import book_import_service' + lines 14-43 bare 'from services.document_package import ...' \u2014 the only backend test file not using 'from backend.services import ...'. Requires cwd on sys.path, so bare 'pytest' silently drops all 107 of this file's tests while 'python3 -m pytest' collects them. Documented in CLAUDE.md \u00a711 / COMMON_ERRORS.md \u00a72 and tracked as ingestion_pipeline:ing-03/ing-05, au-015/au-017 \u2014 re-verified still open 2026-08-03.\nExpected files: lexy-app/backend/tests/test_document_package.py\nAcceptance criteria: cd lexy-app/backend && pytest tests/test_document_package.py -q (bare console script) collects and passes all 107 tests; python3 -m pytest tests/test_document_package.py -q also passes\nValidation: cd lexy-app/backend && pytest tests/test_document_package.py -q; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-004",
    "title": "Guard downgrade() to refuse when reading_selections rows hold non-empty anchors, or persist/resto...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding repo_structure:migration-025-downgrade-orphan\nScope: Guard downgrade() to refuse when reading_selections rows hold non-empty anchors, or persist/restore original token_ids instead of regenerating.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A downgrade-then-upgrade of migration 025 regenerates every token_id, silently orphaning every existing reading_selections.anchors reference \u2014 the selection row survives but its anchor no longer resolves against book_blocks.tokens.\nEvidence: _tokenize (21-27) assigns a fresh uuid.uuid4() per token every run; downgrade() (64-65) is a bare `ALTER TABLE book_blocks DROP COLUMN tokens`, no guard. schemas.py:706 confirms reading_selections.anchors keys on this same token_id UUID ('replaced token_index for stability across edits'), and the frontend sends `token_id: t.tokenId` when saving a selection, so the anchor genuinely depends on token_id stability. Re-verified unchanged since the 2026-08-02 audit.\nExpected files: lexy-app/backend/migrations/versions/025_block_token_ids.py, lexy-app/backend/models/schemas.py\nAcceptance criteria: downgrade() raises when orphaning anchors would occur, or a round-trip test proves token_ids survive downgrade+upgrade\nValidation: cat lexy-app/backend/migrations/versions/025_block_token_ids.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-005",
    "title": "Make downgrade() refuse when any reading_selections row has a non-empty anchors array, or persist...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding security_paths:data-01\nScope: Make downgrade() refuse when any reading_selections row has a non-empty anchors array, or persist/restore token_ids deterministically across the round-trip.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A downgrade-then-upgrade of migration 025 regenerates every token_id, silently orphaning every existing reading_selections.anchors reference with no error surfaced.\nEvidence: Read directly: _tokenize (21-27) calls uuid.uuid4() fresh per token every run; downgrade() (64-65) is a bare ALTER TABLE book_blocks DROP COLUMN tokens with no guard. reading_selections.anchors (migration 009:31-32) stores {block_id, token_id, surface} with no FK. Still open 2026-08-03; duplicates docs/AUDIT_2026-08-02.md db_migrations:db-02 / repo_structure:data-01.\nExpected files: lexy-app/backend/migrations/versions/025_block_token_ids.py\nAcceptance criteria: downgrade() raises when orphaning anchors would occur, or a round-trip test proves token_ids survive downgrade+upgrade\nValidation: cat lexy-app/backend/migrations/versions/025_block_token_ids.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-006",
    "title": "Add a structural validator comparing bbox['page'] (when present) to element['page_index'], flaggi...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding security_paths:ing-01\nScope: Add a structural validator comparing bbox['page'] (when present) to element['page_index'], flagging a stable issue code on mismatch.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A worker bug or corrupted package where a bbox's own 'page' field disagrees with element.page_index passes every validation gate silently \u2014 inert today, a live trap once roadmap A3 wires real persistence off bbox.page.\nEvidence: Read validators.py directly: _BBOX_KEYS = (\"x1\",\"y1\",\"x2\",\"y2\") never includes 'page'; page-dims lookup (line 351) keys off element.get('page_index') only, never bbox['page']. Still open 2026-08-03; duplicates docs/AUDIT_2026-08-02.md repo_structure:ing-01 / ingestion_pipeline:ing-01.\nExpected files: lexy-app/backend/services/document_package/validators.py, docs/INGESTION_PIPELINE.md\nAcceptance criteria: Synthetic element with mismatched page fails validation with a new issue code\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k Structural -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-007",
    "title": "Add frontend job to test CI workflow: `cd lexy-app/frontend && npm ci && npm run test`. Must pass...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding tests_ci:frontend-tests-not-ci-gated\nScope: Add frontend job to test CI workflow: `cd lexy-app/frontend && npm ci && npm run test`. Must pass before merge.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Frontend regressions (broken hooks, component state, routing, theme switching) are not caught before merge. A critical bug in SRSReviewPage or BookReaderPage shipping live depends only on manual testing or post-deploy detection.\nEvidence: TESTS.md documents frontend 'Current count (2026-07-27): 261 tests across 43 files' (line 99) and subsequent updates show 322 tests (line 835). npm audit is CI'd but not `npm run test`. No workflow invokes Vitest.\nExpected files: lexy-app/frontend/package.json, .github/workflows/dependency-audit.yml\nAcceptance criteria: Frontend vitest runs on every PR; All 322 tests pass or failure blocks merge\nValidation: cd /repo/lexy-app/frontend && npm run test 2>&1 | grep -E 'passed|failed'\nParallelizable: yes",
    "depends_on": [
      "au-001"
    ]
  },
  {
    "id": "au-008",
    "title": "Add a `lint` job to the test CI workflow (see ci-test-coverage-missing) that runs `ruff check .` ...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding tests_ci:linting-not-enforced\nScope: Add a `lint` job to the test CI workflow (see ci-test-coverage-missing) that runs `ruff check .` from repo root. Fail if any findings reported.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Lint findings can silently accumulate. A developer working locally on a machine without ruff installed (or an old version) can commit code that fails the 'zero findings' baseline when run on another machine.\nEvidence: CLAUDE.md \u00a711 states: 'Ruff is **part of backend validation, not an optional extra**\u2014run it alongside `pytest` before calling any Python change done.' Baseline documented as 'zero findings (was 153; see the ruff entry in `docs/TESTS.md`)'. No CI job runs `ruff check .` to enforce this rule before merge.\nExpected files: CLAUDE.md, .github/workflows/dependency-audit.yml\nAcceptance criteria: `ruff check .` passes with 'All checks passed!' on every commit to main; CI rejects PRs with ruff findings\nValidation: cd /repo && ruff check . && echo 'PASS' || echo 'FAIL'\nParallelizable: yes",
    "depends_on": [
      "au-001"
    ]
  },
  {
    "id": "au-009",
    "title": "Add pipeline job to test CI workflow: from repo root, `pytest tests/ -q`. Must pass before merge.",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding tests_ci:root-pipeline-tests-not-ci-gated\nScope: Add pipeline job to test CI workflow: from repo root, `pytest tests/ -q`. Must pass before merge.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Pipeline ingestion (i+1 eligibility, exposure counter, learning units, subtitle cleaner/merger/segmenter) has no CI verification. A change to pipeline.py or exposure_counter.py can silently break the subtitle-scraper's core i+1 logic without CI detection.\nEvidence: ROADMAP.md line 70: 'root pipeline **221 passed**'. TESTS.md line 2 (root section intro) says 'Pytest tests for pipeline modules. Mostly hermetic (no DB). 207 tests'. No CI workflow invokes `pytest tests/` from repo root.\nExpected files: pytest.ini, .github/workflows/dependency-audit.yml\nAcceptance criteria: Root pipeline pytest runs on every PR; All 221 tests pass or failure blocks merge\nValidation: cd /repo && python3 -m pytest tests/ -q 2>&1 | tail -1\nParallelizable: yes",
    "depends_on": [
      "au-001"
    ]
  },
  {
    "id": "au-010",
    "title": "Once CI test job is added (see ci-test-coverage-missing), verify that running backend tests WITH ...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding tests_ci:test-isolation-not-verified\nScope: Once CI test job is added (see ci-test-coverage-missing), verify that running backend tests WITH pytest-randomly in the environment still works because `addopts = -p no:randomly` blocks it. Document this as a regression guard.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: If pytest-randomly is transitively installed in a CI agent's environment and the `-p no:randomly` block is accidentally removed, every test setup/teardown crashes at numpy.random.seed() with ValueError. The load-bearing test isolation would be silently broken.\nEvidence: Both pytest.ini files carry `addopts = -p no:randomly` documented in TESTS.md \u00a70 as 'load-bearing'. The rationale: pytest-randomly masks seeds, numpy overflows on any seed \u22652**32, and thinc's seeder is unmasked. TESTS.md documents 'With the block [removed]: 1036 errors'; without it, 'All checks passed!'. The block exists in config but is never validated in CI \u2014 no CI runs pytest, so the block's necessity is untested.\nExpected files: lexy-app/pytest.ini, pytest.ini, docs/TESTS.md\nAcceptance criteria: Backend pytest runs with pytest-randomly installed in the CI environment and still passes (due to block); Documentation in CLAUDE.md \u00a711 or TESTS.md \u00a70 notes the CI verification of the block\nValidation: pip install pytest-randomly && cd /repo/lexy-app/backend && pytest -n auto -q && echo 'PASS (block effective)' || echo 'FAIL (block broken)'\nParallelizable: no",
    "depends_on": [
      "au-001"
    ]
  },
  {
    "id": "au-011",
    "title": "Add a one-line comment in downgrade() stating the rebuilt settings values reflect current user_vi...",
    "description": "Priority: P1 | severity low | confidence confirmed | from audit finding db_migrations:db-014-02\nScope: Add a one-line comment in downgrade() stating the rebuilt settings values reflect current user_video_category state, not the original pre-upgrade data.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A downgrade of 014 writes JSONB preference data reconstructed from whatever user_video_category holds at downgrade time (populated by unrelated application code after 014 shipped), not the true pre-upgrade JSONB values \u2014 misleading for anyone treating downgrade as a real rollback.\nEvidence: downgrade() (47-64) rebuilds users.settings.liked_genres/disliked_genres by aggregating current rows from user_video_category \u2014 a table upgrade() (21-40) never wrote to. Same 'downgrade fabricates a placeholder instead of restoring' pattern already flagged elsewhere in this chain (migration 013), but not yet noted for 014.\nExpected files: lexy-app/backend/migrations/versions/014_drop_video_genre.py\nAcceptance criteria: downgrade() in 014_drop_video_genre.py carries a comment documenting that the rebuilt JSONB is not a true restore of pre-upgrade values\nValidation: grep -n -B2 'liked_genres' lexy-app/backend/migrations/versions/014_drop_video_genre.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-012",
    "title": "Only emit checksums_verified when the loop found zero fatal checksum issues, or reword it to 'che...",
    "description": "Priority: P1 | severity low | confidence confirmed | from audit finding ingestion_pipeline:ingest-defect-01\nScope: Only emit checksums_verified when the loop found zero fatal checksum issues, or reword it to 'checked N file(s)' so it doesn't imply success.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: import_result.json can carry an info entry claiming files were 'verified' in the same run where those same files are listed under errors as checksum_mismatch, confusing anyone reading the info/warning trail without cross-checking validation.checksums (which does correctly report 'fail').\nEvidence: loader.py:402-430 loops pkg.files adding checksum_mismatch/checksum_missing_file/checksum_path_escape as fatal per bad entry; loader.py:432-437 then unconditionally adds an INFO 'checksums_verified: verified N file(s)' with no gate on whether any of those N just failed. Tracked as ingestion_pipeline:ing-06/task rt-06 in AUDIT_2026-07-30.md; absent from AUDIT_2026-08-02.md; code unchanged as of 2026-08-03 \u2014 dropped from tracking, not fixed.\nExpected files: lexy-app/backend/services/document_package/loader.py\nAcceptance criteria: A package with a checksum_mismatch entry produces no checksums_verified info claiming success; test_document_package.py gains a case asserting the two are mutually exclusive\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k Checksums -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-013",
    "title": "Add Depends(require_admin) to both routes (pattern: routers/phrases.py:36), add a non-admin-403 r...",
    "description": "Priority: P2 | severity high | confidence confirmed | from audit finding ingestion_pipeline:ingest-sec-01\nScope: Add Depends(require_admin) to both routes (pattern: routers/phrases.py:36), add a non-admin-403 regression test, and add both routes to docs/SECURITY.md's admin-gated list.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any authenticated non-admin user can enumerate every operator-curated package name and force checksum/structural validation of any of them \u2014 info disclosure plus an uncapped compute-cost surface, worsening into a data-integrity issue once A3 wires real persistence.\nEvidence: books.py:182-212 gate both /books/packages and /books/import on Depends(get_current_user) only; delete_book/delete_page in the same file check is_admin. INGESTION_PIPELINE.md:306 labels the workflow operator-only. Re-verified 2026-08-03, unchanged since seed_tasks.json rt-01 and AUDIT_2026-08-02.md ing-04/sec-01 \u2014 still open, not fixed.\nExpected files: lexy-app/backend/routers/books.py, docs/SECURITY.md\nAcceptance criteria: Non-admin authenticated user gets 403 admin_required from GET /books/packages and POST /books/import; Admin behavior unchanged; docs/SECURITY.md lists both routes as admin-gated\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k TestImportEndpoint -q; rg -n 'require_admin' lexy-app/backend/routers/books.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-014",
    "title": "Add Depends(require_admin) to GET /books/packages and POST /books/import, matching the delete_boo...",
    "description": "Priority: P2 | severity high | confidence confirmed | from audit finding repo_structure:books-admin-gate\nScope: Add Depends(require_admin) to GET /books/packages and POST /books/import, matching the delete_book/delete_page and /phrases/seed precedent; add both routes to docs/SECURITY.md's admin-gated list.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any authenticated non-admin user can enumerate every operator-curated ingestion package name and force full checksum/structural validation on demand \u2014 information disclosure plus an unmetered compute-cost surface open to the whole user base.\nEvidence: books.py:183,189 depend only on Depends(get_current_user); no require_admin, unlike delete_book/delete_page in the same file and /phrases/seed. book_import_service.import_package takes user_id but document_package/loader.py's package_root is a single shared operator directory and contract.py carries no owner field, so user_id is not an ownership scope. Re-verified unchanged since the 2026-08-02 audit; registry not readable from this worker checkout so I cannot confirm whether a task already tracks it.\nExpected files: lexy-app/backend/routers/books.py, docs/SECURITY.md\nAcceptance criteria: Non-admin authenticated user gets 403 admin_required from both endpoints; Admin behavior unchanged; docs/SECURITY.md lists both routes as admin-gated\nValidation: rg -n 'require_admin' lexy-app/backend/routers/books.py; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k TestImportEndpoint -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-015",
    "title": "Add Depends(require_admin) to both routes, matching the existing pattern; add a regression test a...",
    "description": "Priority: P2 | severity high | confidence confirmed | from audit finding security_paths:sec-01\nScope: Add Depends(require_admin) to both routes, matching the existing pattern; add a regression test asserting 403 for a non-admin caller; update SECURITY.md's admin-gated-routes list.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any authenticated non-admin learner can enumerate every operator-curated book package name and force checksum/structural validation compute on demand.\nEvidence: books.py:183 list_packages and :189 import_package depend only on Depends(get_current_user), no require_admin \u2014 verified directly by reading the file. Every other admin-shaped route (delete_book, /phrases/seed, /admin/lemma-corrections/*) uses require_admin. Still open on 2026-08-03; duplicates already-tracked autoloop/seed_tasks.json:rt-01 and docs/AUDIT_2026-08-02.md repo_structure:sec-01 / ingestion_pipeline:ing-04 / security_paths:sec-01 \u2014 do not spawn a second task for this.\nExpected files: lexy-app/backend/routers/books.py\nAcceptance criteria: Non-admin caller gets 403 admin_required from both GET /books/packages and POST /books/import; Admin behavior unchanged; docs/SECURITY.md lists both routes under admin-gated routes\nValidation: rg -n 'require_admin' lexy-app/backend/routers/books.py; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-016",
    "title": "Move the Docker build context to the repo root (COPY subtitle-scraper/ + data/ alongside backend/...",
    "description": "Priority: P4 | severity critical | confidence confirmed | from audit finding repo_structure:docker-boot-crash\nScope: Move the Docker build context to the repo root (COPY subtitle-scraper/ + data/ alongside backend/) and update docker-compose.yml's build context, or make the phrase_finder import lazy with a clear degraded-mode failure; add a build-and-boot smoke test.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: docker compose up crashes on boot with ModuleNotFoundError: phrase_finder before uvicorn starts \u2014 every docker-compose deployment following README.md's own quickstart is unreachable.\nEvidence: Dockerfile:28,31 COPY only backend/ and built frontend/dist, never subtitle-scraper/ or data/ (both at repo root, outside the lexy-app/ build context). matcher_service.py:35 computes Path(__file__).resolve().parents[3]/'subtitle-scraper'; in-image WORKDIR=/app so __file__=/app/backend/services/matcher_service.py, making parents[3] '/' -> nonexistent /subtitle-scraper. Line 43 `import phrase_finder` runs at module scope, pulled in before main.py builds the app. Re-verified unchanged since the 2026-08-02 audit (repo_structure:arch-01).\nExpected files: lexy-app/Dockerfile, lexy-app/docker-compose.yml, lexy-app/backend/services/matcher_service.py\nAcceptance criteria: docker compose up boots without a startup crash; A smoke test builds the image and gets a 2xx from a health endpoint\nValidation: cd lexy-app && docker compose build && docker compose up -d && curl -f http://localhost:8000/api/v1/auth/login -X POST -d '{}' -H 'Content-Type: application/json'\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-017",
    "title": "In reconcile.py, loosen _is_duplicate_candidate: drop or widen the exact category prefilter and/o...",
    "description": "Priority: P4 | severity medium | confidence confirmed | from audit finding repo_structure:taskgen-dedup\nScope: In reconcile.py, loosen _is_duplicate_candidate: drop or widen the exact category prefilter and/or lower MERGE_SIMILARITY when affected_files overlap is strong, so same-defect findings merge before taskgen assigns ids.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Independently-phrased duplicate write-ups from different domain agents rarely clear the 0.7 similarity bar, so the task registry accumulates redundant tasks for one defect (33 proposed from ~19 canonical defects on 2026-08-02) \u2014 wasted implement budget and a cluttered plan needing hand-triage.\nEvidence: reconcile.py:118-148 gates merging on exact category equality AND affected-file overlap AND Jaccard>=0.7 (threshold reconcile.py:73, token scoring reconcile.py:101-115); taskgen.py:98-163 emits one task per surviving Finding with no later grouping. docs/AUDIT_2026-08-02.md:349 records only one merged pair for the whole run, yet the same books.py admin-gate defect (sec-01/ing-04/sec-01 from three domain agents), the same migration-025 downgrade risk (db-02/data-01) and the same bbox-validator gap (ing-01/ing-01) each survived as separate proposed tasks au-009/au-010/au-011, au-002/au-005, au-003/au-006.\nExpected files: autoloop/audit/reconcile.py, autoloop/audit/taskgen.py, docs/AUDIT_2026-08-02.md\nAcceptance criteria: Two synthetic findings describing the same defect under different categories with overlapping affected_files merge into one Finding via reconcile(); A regression test in autoloop/tests/test_audit_reconcile.py locks the merge for a cross-category duplicate pair\nValidation: python3 -m pytest autoloop/tests/test_audit_reconcile.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-018",
    "title": "Add a new .github/workflows/tests.yml running `ruff check .`, backend `python3 -m pytest -n auto`...",
    "description": "Priority: P5 | severity high | confidence confirmed | from audit finding repo_structure:ci-missing-gates\nScope: Add a new .github/workflows/tests.yml running `ruff check .`, backend `python3 -m pytest -n auto`, root `pytest tests/`, and frontend `npm run test` + `npm run build` on push/PR to main; leave dependency-audit.yml untouched.\n  - also reported as security_paths:ci-01: Add a new .github/workflows/tests.yml running ruff check ., backend python3 -m pytest -n auto, frontend npx tsc --noEmit && npx vitest run, and the root pipeline suite, gating main.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A PR that fails ruff, breaks a backend/frontend/root-pipeline test, or breaks the frontend TypeScript build can merge to main with a fully green required-checks state.\n  - also reported as security_paths:ci-01: A PR that fails ruff, breaks a backend/frontend/root-pipeline test, or breaks the TypeScript build can merge to main with a fully green required-checks state.\nEvidence: Glob '.github/workflows/*' returns only dependency-audit.yml (pip-audit + npm audit jobs). CLAUDE.md \u00a711 calls `ruff check .` and the backend/frontend/root pytest suites 'part of backend validation, not an optional extra,' but no workflow invokes pytest, ruff, tsc, or a frontend build. Re-verified unchanged since the 2026-08-02 audit.\n  - also reported as security_paths:ci-01: Glob '.github/workflows/*.yml' on 2026-08-03 returns only dependency-audit.yml, which runs pip-audit and npm audit only \u2014 no pytest/ruff/tsc anywhere in it. CLAUDE.md \u00a711 calls ruff+pytest 'part of backend validation, not an optional extra.' Duplicates docs/AUDIT_2026-08-02.md tests_ci:ci-01 / repo_structure:ci-01 / tests_ci:test-01 \u2014 do not spawn a second task.\nExpected files: .github/workflows/dependency-audit.yml\nAcceptance criteria: tests.yml exists and runs on PRs to main; A PR with a failing test or ruff violation shows a failing required check\nValidation: ls -la .github/workflows/\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-019",
    "title": "Rewrite the imports to `from backend.services import ...` / `from backend.services.document_packa...",
    "description": "Priority: P5 | severity low | confidence confirmed | from audit finding repo_structure:test-doc-package-import-style\nScope: Rewrite the imports to `from backend.services import ...` / `from backend.services.document_package import ...`, matching every other backend test file.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Running the bare `pytest` console script instead of `python3 -m pytest` silently drops this file's ~107 tests from collection with no clear error, so a contributor on the wrong entrypoint can believe an untested ingestion-pipeline change is safe.\nEvidence: Line 13 `from services import book_import_service` and following lines use bare `from services.document_package import ...` \u2014 every other file under lexy-app/backend/tests/ uses `from backend.services import ...` (CLAUDE.md \u00a711 / docs/COMMON_ERRORS.md \u00a72 already document this exact trap). Re-verified unchanged since the 2026-08-02 audit.\nExpected files: lexy-app/backend/tests/test_document_package.py\nAcceptance criteria: File uses from backend.services... imports throughout; bare `pytest tests/test_document_package.py` from lexy-app/backend collects and passes all tests\nValidation: cd lexy-app/backend && pytest tests/test_document_package.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-020",
    "title": "Resolve UPLOAD_ROOT lazily inside package_root() (matching PACKAGE_ROOT's call-time read) instead...",
    "description": "Priority: P6 | severity low | confidence confirmed | from audit finding ingestion_pipeline:ingest-improve-01\nScope: Resolve UPLOAD_ROOT lazily inside package_root() (matching PACKAGE_ROOT's call-time read) instead of freezing it into a module-level constant at import time.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A test or late-binding deployment that sets UPLOAD_ROOT expecting document_package.package_root() to follow it (as book_service.py's identically-named _UPLOAD_ROOT does) silently gets the real default 'uploads/packages' instead \u2014 a same-file inconsistency between two env vars' resolution timing.\nEvidence: loader.py:25 computes DEFAULT_PACKAGE_ROOT = Path(os.getenv('UPLOAD_ROOT','uploads'))/'packages' once at module import; package_root() (89-97) re-reads os.getenv('PACKAGE_ROOT') fresh on every call. Setting PACKAGE_ROOT after import takes effect immediately; setting UPLOAD_ROOT after import silently does not. test_document_package.py:929 exercises PACKAGE_ROOT via monkeypatch; no test exercises UPLOAD_ROOT for this module. New finding, not in either prior report.\nExpected files: lexy-app/backend/services/document_package/loader.py\nAcceptance criteria: Setting UPLOAD_ROOT via monkeypatch after the module is imported changes package_root()'s result in a new regression test\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k package_root -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-021",
    "title": "Recount frontend test files via `find lexy-app/frontend/src -name '*.test.tsx'` and update both d...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding docs_drift:doc-01\nScope: Recount frontend test files via `find lexy-app/frontend/src -name '*.test.tsx'` and update both docs to match actual count and date.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Documentation overstates frontend test file count by 8-9 files. Makes it impossible to verify the claim by counting files. Unknown if test count is also stale.\nEvidence: docs/TESTS.md line 99 claims '261 tests across 43 files' (current count as of 2026-07-27); docs/ROADMAP.md line 68 claims 'frontend **322 passed across 44 files**' (verified 2026-07-29). Glob search finds exactly 35 frontend test files: 28 in components/, 4 in hooks/, 3 in test/.\nExpected files: docs/TESTS.md, docs/ROADMAP.md\nAcceptance criteria: docs/TESTS.md line 99 reflects actual frontend test file count; docs/ROADMAP.md line 68 reflects actual frontend test file count; Both lines include verification date and command used to count\nValidation: find /Users/emir/.autoloop/workers/audit-0001/lexy-app/frontend/src -name '*.test.tsx' | wc -l\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-022",
    "title": "Remove the sys.path reference from docs/SUMMARY.md since the root pipeline tests now import direc...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding docs_drift:doc-02\nScope: Remove the sys.path reference from docs/SUMMARY.md since the root pipeline tests now import directly from root modules, no longer from `src/`. Verify pytest.ini no longer has the old sys.path.insert line.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Documentation describes a hook that no longer exists and was removed over a month ago. New contributors reading this may fail to understand the current pipeline test structure.\nEvidence: docs/SUMMARY.md line 348 references 'conftest.py' and sys.path injection for root pipeline tests, stating the setup 'adds `src/` to path'. However, per CLAUDE.md \u00a710 and the 2026-07-27 deletion history (TODO.md #17), the `src/app/` refactor was deleted entirely, including all sys.path hacks for it. The old setup is stale.\nExpected files: docs/SUMMARY.md\nAcceptance criteria: docs/SUMMARY.md line 348 no longer mentions sys.path or src/app; root conftest.py has no sys.path.insert for src/ directory\nValidation: grep -n 'src.*path' /Users/emir/.autoloop/workers/audit-0001/conftest.py || echo 'not found (expected)'\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-023",
    "title": "Change '+89 backend' to '+107 backend' at docs/TESTS.md:956 to match the figure already stated la...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding ingestion_pipeline:ingest-doc-01\nScope: Change '+89 backend' to '+107 backend' at docs/TESTS.md:956 to match the figure already stated later in the same file.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A reader trusting the '+89' heading under-counts test_document_package.py's actual coverage by 18 tests in the file CLAUDE.md designates as the coverage index.\nEvidence: Line 956's heading states A2 landed '+89 backend' tests. grep -c 'def test_' on test_document_package.py = 96, plus 3 parametrize sites (4+5+5 cases) adding 11 extra collected items = 107, matching the '107 tests' figure the same file states elsewhere (~line 2174) and INGESTION_PIPELINE.md \u00a713. Already flagged as ingestion_pipeline:ing-05/au-024 in AUDIT_2026-08-02.md \u2014 re-verified still unresolved 2026-08-03.\nExpected files: docs/TESTS.md\nAcceptance criteria: Both figures for test_document_package.py agree within docs/TESTS.md\nValidation: grep -n '89 backend\\|107 tests' docs/TESTS.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-024",
    "title": "Update reading_service.py:117's docstring and migration 009's inline comment to describe the curr...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding repo_structure:anchor-format-doc-drift\nScope: Update reading_service.py:117's docstring and migration 009's inline comment to describe the current `{block_id, token_id, surface}` shape, noting migration 025 as the point of change.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A contributor trusting reading_service.py's own docstring when writing new code against `anchors` (a migration, an analytics query, or future A3 persistence work) keys off the wrong field name and silently gets no matches.\nEvidence: reading_service.py:117's docstring and migration 009:31's inline comment both describe anchors as `[{block_id, token_index, surface}]` (pre-025 positional format). schemas.py:706 and the frontend (SelectionPanel.tsx sends `token_id: t.tokenId`) show the live shape is `{block_id, token_id, surface}`, a stable UUID explicitly 'replaced token_index for stability'. grep for 'token_index' across lexy-app/ finds only these two stale comments; no code reads that key.\nExpected files: lexy-app/backend/services/reading_service.py, lexy-app/backend/migrations/versions/009_reading_selections.py, lexy-app/backend/models/schemas.py\nAcceptance criteria: reading_service.py and migration 009's anchors-shape comments name token_id, not token_index\nValidation: grep -rn token_index lexy-app/\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-025",
    "title": "Fix CLAUDE.md \u00a73's migrations path/count, rewrite \u00a78b's status_marked_known sentence to describe ...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding repo_structure:claude-md-stale-refs\nScope: Fix CLAUDE.md \u00a73's migrations path/count, rewrite \u00a78b's status_marked_known sentence to describe apply_progression(..., status_override=...) instead of the deleted upsert_word_status, and update SECURITY.md:546's cross-reference to a real section (or drop it).\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: CLAUDE.md is the file this repo tells every contributor to read first (\u00a713); three of its own cross-references (and one in SECURITY.md) point at things that no longer exist, misleading whoever follows them.\nEvidence: CLAUDE.md \u00a73 still says migrations are '25+ files in `alembic/versions/`' \u2014 alembic.ini's script_location is `migrations` and the real 37 files live at lexy-app/backend/migrations/versions/; no alembic/versions/ dir exists. CLAUDE.md \u00a78b still says the known-status flip is 'forced to known by word_service.upsert_word_status', though CLAUDE.md's own \u00a710/\u00a712 say that function 'has been removed'/'is gone'. docs/SECURITY.md:546 cites 'CLAUDE.md \u00a77', but current CLAUDE.md's sections run 3\u21925\u21926\u21928 \u2014 no \u00a74 or \u00a77 exists.\nExpected files: CLAUDE.md, docs/SECURITY.md\nAcceptance criteria: CLAUDE.md no longer mentions upsert_word_status or alembic/versions/; docs/SECURITY.md:546 resolves to a section that exists\nValidation: grep -n upsert_word_status CLAUDE.md; grep -n 'alembic/versions' CLAUDE.md; grep -n '^## ' CLAUDE.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-026",
    "title": "Once CI is wired (see ci-test-coverage-missing), add a documentation-generation step or a README ...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding tests_ci:test-count-documentation-aspirational\nScope: Once CI is wired (see ci-test-coverage-missing), add a documentation-generation step or a README badge showing live test counts from the most recent CI run. Or add a pre-commit hook that runs tests and updates TESTS.md.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Test counts in documentation can drift from reality. A developer adds a test locally, the count in TESTS.md stays stale because there is no CI enforcement and no human re-checks it on every commit. ROADMAP.md dated 2026-07-29 with counts that may no longer be accurate.\nEvidence: TESTS.md header: 'Test inventory and coverage status. **Update this file whenever you add, remove, or rename a test.**' Lines 68-70 of ROADMAP.md: 'Verified on **2026-07-29** (current): backend **1151 passed / 2 skipped**, frontend **322 passed across 44 files**, root pipeline **221 passed**'. Counts are documented as from local runs (\"Validation: backend `-n auto` \u2192 ...\") but CI does not verify them continuously.\nExpected files: docs/TESTS.md, docs/ROADMAP.md\nAcceptance criteria: Live test count is visible (badge, in repo, or GitHub UI) and matches actual CI runs; TESTS.md or ROADMAP.md update instructions are executable without manual counting\nValidation: cd /repo && grep -n '1151 passed' docs/ROADMAP.md && pytest lexy-app/backend -q 2>&1 | tail -1\nParallelizable: yes",
    "depends_on": [
      "au-001"
    ]
  },
  {
    "id": "au-027",
    "title": "Update line 201 to state clearly that the salvage and deletion are complete events: 'The `src/app...",
    "description": "Priority: P7 | severity info | confidence confirmed | from audit finding docs_drift:doc-03\nScope: Update line 201 to state clearly that the salvage and deletion are complete events: 'The `src/app/` tree and its 537 tests were deleted 2026-07-27; pipeline tests now import directly from root modules.'\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Minor clarity issue in documentation tense. Does not affect functionality or understanding significantly.\nEvidence: docs/ROADMAP.md line 201 mentions 'tests for pipeline modules' with a note 'There is no longer a `src/app/` tree or tests targeting one \u2014 see TODO #17.' However, docs/TODO.md #17 (starting at line 273) was resolved 2026-07-27 with `src/app/` and 537 tests deleted. The ROADMAP entry correctly notes this resolution, but the phrasing as a current state (no longer exists) rather than a past decision (was deleted) could be clearer for readers unaware of the deletion.\nExpected files: docs/ROADMAP.md\nAcceptance criteria: docs/ROADMAP.md line 201 uses past tense for the deletion; Cross-reference to TODO.md #17 remains valid\nValidation: grep -A 2 'no longer a.*src/app' /Users/emir/.autoloop/workers/audit-0001/docs/ROADMAP.md\nParallelizable: yes",
    "depends_on": []
  }
]
```
