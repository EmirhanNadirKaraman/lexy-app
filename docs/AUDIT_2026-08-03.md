# Repository audit — 2026-08-03

Generated by the autoloop audit executor (deterministic rendering of reconciled subagent findings; raw agent reports preserved at `.autoloop/audit/20260803T133418Z-ba085d`).

- branch: `autoloop/audit-0001` | HEAD: `6b75c667ac15` | uncommitted files: 0
- requested scope: Audit architecture, tests/CI, security, database migrations, documentation drift, and the ingestion pipeline; reconcile findings against the existing 23-task roadmap, identify stale or duplicate tasks, and propose only evidence-backed additions or corrections.

## Domain coverage — 6/6 domains reported usable output

| domain | usable output | findings |
|---|---|---|
| `docs_drift` | yes | 4 |
| `tests_ci` | yes | 9 |
| `repo_structure` | yes | 5 |
| `security_paths` | yes | 12 |
| `db_migrations` | yes | 6 |
| `ingestion_pipeline` | yes | 9 |

## Validation baseline
- `ruff check .` — **PASS**
  - tail: `All checks passed!`
- `python3 -m pytest autoloop/tests -q` — **FAIL (rc=1)**
  - tail: `[31m[31m[1m1 failed[0m, [32m1108 passed[0m, [33m1 skipped[0m[31m in 216.28s (0:03:36)[0m[0m`
- `python3 -m pytest tests/ -q` — **PASS**
  - tail: `[33m[32m368 passed[0m, [33m[1m5 warnings[0m[33m in 8.35s[0m[0m`

## Confirmed defects (4)
#### security_paths:ing-01 — Add a structural validator comparing bbox['page'] (when present) to element['page_index'], flagging 
- severity **medium**, confidence **confirmed**, domain `security_paths`
- files: `lexy-app/backend/services/document_package/validators.py`, `docs/INGESTION_PIPELINE.md`
- symbols: `validators.py:_BBOX_KEYS:21`, `validators.py:validate_bboxes:296-373`
- evidence: _BBOX_KEYS = ("x1","y1","x2","y2") (line 21) never includes 'page'; validate_bboxes (351) keys page-dims lookup off element.get('page_index') only. docs/INGESTION_PIPELINE.md:245-246,261 shows the package format explicitly gives bbox its own 'page' field ('bbox carries page explicitly').
- impact: A worker bug or corrupted package where a bbox's own 'page' field disagrees with the element's page_index passes every validation gate silently.
- acceptance: Synthetic element with mismatched bbox page vs page_index fails validation with a new issue code
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k Structural -q

#### ingestion_pipeline:ing-03 — Add a structural validator flagging bbox['page'] != element['page_index'] with a new stable issue co
- severity **medium**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/services/document_package/validators.py`, `docs/INGESTION_PIPELINE.md`
- symbols: `validators._BBOX_KEYS:21`, `validators.validate_bboxes:296-373`
- evidence: _BBOX_KEYS=('x1','y1','x2','y2') never includes 'page'; validate_bboxes:351-356 keys page dims off element.get('page_index') only, never bbox.get('page'). The doc's own §4 worked example (lines 245-246) shows page_index:11 alongside bbox.page:12 in the same element, unreconciled. Still open per AUDIT_2026-08-03.md security_paths:ing-01 — duplicate, do not spawn a second task.
- impact: A corrupted/buggy package where an element's bbox.page disagrees with its page_index passes every validation gate silently; inert today, a live coordinate-mapping trap once A3 or a review UI keys off bbox.page.
- acceptance: A synthetic element with bbox.page != page_index produces a new stable validation issue code; INGESTION_PIPELINE.md §4 example has bbox.page == page_index
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k Structural -q

#### db_migrations:db-02 — Add a one-line comment in downgrade() noting the rebuilt JSONB is a best-effort reconstruction, not 
- severity **low**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/014_drop_video_genre.py`
- symbols: `014_drop_video_genre.py:downgrade:43-66`
- evidence: downgrade() (47-64) rebuilds users.settings.liked_genres/disliked_genres by aggregating current user_video_category rows — a table upgrade() never wrote to (db-01) and that the running application may have populated long after 014 shipped.
- impact: A downgrade of 014 produces JSONB preference values reflecting current app state, not the true pre-upgrade values, misleading anyone who treats alembic downgrade as a real rollback.
- acceptance: downgrade() carries a comment documenting the reconstruction is not a true restore
- validation: grep -n -B2 'liked_genres' lexy-app/backend/migrations/versions/014_drop_video_genre.py

#### ingestion_pipeline:ing-05 — Only emit checksums_verified when the loop found zero fatal issues in this gate, or reword it to 'ch
- severity **low**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/services/document_package/loader.py`
- symbols: `loader.verify_checksums:344-437`
- evidence: loader.py:402-430 loops pkg.files adding checksum_mismatch/checksum_missing_file/checksum_path_escape as FATAL per bad entry; lines 432-437 then unconditionally add an INFO 'checksums_verified: verified N file(s)' regardless of whether any of those same N entries just failed above. Already open as AUDIT_2026-08-03.md ingest-defect-01 — duplicate, code unchanged.
- impact: import_result.json can carry an info entry claiming files were 'verified' in the same run where those files are simultaneously listed under errors as checksum_mismatch, misleading anyone scanning info/warnings without cross-referencing validation.checksums.
- acceptance: A package with a checksum_mismatch entry produces no checksums_verified info claiming success
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k Checksum -q


## Security / data-loss risks (11)
#### tests_ci:db-01 — Add a pre-001 baseline migration creating the content core (video, word_table, sentence, language_ta
- severity **critical**, confidence **confirmed**, domain `tests_ci`
- files: `lexy-app/backend/alembic/versions`, `lexy-app/backend/alembic.ini`, `docs/COMMON_ERRORS.md`
- symbols: `docs/COMMON_ERRORS.md:§9`, `lexy-app/backend/alembic/versions/006_*`
- evidence: docs/COMMON_ERRORS.md §9 records that a clean DB + `alembic upgrade head` dies at migration 006 (ALTER TABLE video ADD COLUMN channel_id): the chain creates 23 tables and never creates the content core, because there were two schema sources (migrations = app half, the now-deleted table.sql = content half) that cannot be composed.
- impact: No new database can be built from scratch — CI schema validation, new-contributor setup, and fresh deploys all fail at migration 006; the project is un-deployable to a clean DB.
- acceptance: createdb + alembic upgrade head succeeds on an empty database; Resulting schema contains all 37 tables including the content core; pg_dump schema-only diff against the production DB shows no missing tables
- validation: createdb fresh_test; cd lexy-app/backend && DB_NAME=fresh_test alembic upgrade head; psql fresh_test -c '\dt' | grep -E 'video|word_table|phrase'; git log --oneline -- table.sql | head -1

#### repo_structure:arch-02-books-admin-gate — Gate both routes with the same is_admin/require_admin pattern already used by delete_document/delete
- severity **high**, confidence **confirmed**, domain `repo_structure`
- files: `lexy-app/backend/routers/books.py`
- symbols: `books.py:182-185 list_packages`, `books.py:188-212 import_package`, `books.py:384,399 delete_document/delete_page is_admin threading`
- evidence: books.py:183,189 depend only on Depends(get_current_user); no admin check. The same file's delete_document/delete_page (384,399) thread `is_admin=user.get('is_admin', False)` into book_service, which gates elevated behavior on it (book_service.py:790,809) — an established admin-aware pattern that list_packages/import_package skip entirely. Re-confirmed by reading both files this session; already tracked as repo_structure:books-admin-gate / security_paths:sec-01 / au-014/au-015 — do not spawn a second task.
- impact: Any authenticated non-admin user can enumerate every operator-curated package name and force checksum/structural validation compute via POST /books/import, since packages carry no per-user ownership scope.
- acceptance: non-admin caller gets 403 from both GET /books/packages and POST /books/import; admin behavior unchanged
- validation: rg -n 'require_admin|is_admin' lexy-app/backend/routers/books.py

#### security_paths:sec-01 — Add Depends(require_admin) to both routes, matching delete_book/delete_page; add a non-admin-403 reg
- severity **high**, confidence **confirmed**, domain `security_paths`
- files: `lexy-app/backend/routers/books.py`
- symbols: `list_packages:182-185`, `import_package:188-212`
- evidence: books.py:183 and :189 depend only on Depends(get_current_user); no require_admin, unlike delete_book/delete_page in the same file and POST /phrases/seed. Verified directly by reading the file.
- impact: Any authenticated non-admin user can enumerate every operator-curated book package name and force checksum/structural validation compute via POST /books/import.
- acceptance: Non-admin caller gets 403 admin_required from GET /books/packages and POST /books/import; Admin behavior unchanged
- validation: rg -n 'require_admin' lexy-app/backend/routers/books.py; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -q

#### db_migrations:db-01 — Add an INSERT INTO user_video_category ... SELECT ... jsonb_array_elements_text(settings->'liked_gen
- severity **high**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/014_drop_video_genre.py`
- symbols: `014_drop_video_genre.py:upgrade:21-40`, `014_drop_video_genre.py:1-6`
- evidence: Docstring (lines 4-6) claims liked_genres/disliked_genres are 'migrated ... into user_video_category'. upgrade() (21-40) only adds the preference column (23-27), deletes both JSONB keys from users.settings (32-36), and drops video.genre (39-40) — no INSERT INTO user_video_category exists anywhere in this file or in 013.
- impact: Any DB whose users.settings still holds legacy liked_genres/disliked_genres when this migration runs (fresh install from an old backup, or any environment migrating from pre-014 state) permanently loses that preference data with no backfill path.
- acceptance: A DB seeded only with users.settings.liked_genres/disliked_genres (no user_video_category rows) retains those preferences in user_video_category after upgrading through 014; Migration docstring accurately describes what upgrade() performs
- validation: grep -n 'INSERT INTO user_video_category' lexy-app/backend/migrations/versions/014_drop_video_genre.py lexy-app/backend/migrations/versions/013_video_category_cleanup.py

#### ingestion_pipeline:ing-01 — Add Depends(require_admin) to both routes, matching delete_book/phrases.seed; add a non-admin-403 re
- severity **high**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/routers/books.py`
- symbols: `routers.books.list_packages:182-185`, `routers.books.import_package:188-212`
- evidence: books.py:183 list_packages and :191 import_package depend only on Depends(get_current_user); sibling routes in the same file (delete_book/delete_page) and /phrases/seed gate on require_admin. test_document_package.py:987-1034 asserts only 401/403 for unauthenticated, never non-admin-403. Matches AUDIT_2026-08-03.md's already-open books-admin-gate/sec-01/ing-04 (3 duplicate write-ups) — still unresolved, do not spawn a 4th task.
- impact: Any authenticated non-admin learner can enumerate every operator-curated ingestion package name and force full checksum/structural validation compute on demand.
- acceptance: Non-admin authenticated caller gets 403 from GET /books/packages and POST /books/import; Admin behavior unchanged; docs/SECURITY.md lists both routes as admin-gated
- validation: rg -n 'require_admin' lexy-app/backend/routers/books.py; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k TestImportEndpoint -q

#### security_paths:data-01 — Guard downgrade() to refuse when reading_selections rows hold non-empty anchors, or persist/restore 
- severity **medium**, confidence **confirmed**, domain `security_paths`
- files: `lexy-app/backend/migrations/versions/025_block_token_ids.py`, `lexy-app/backend/models/schemas.py`, `lexy-app/backend/migrations/versions/009_reading_selections.py`
- symbols: `025_block_token_ids.py:_tokenize:21-27`, `025_block_token_ids.py:downgrade:64-65`, `schemas.py:706`
- evidence: _tokenize (21-27) assigns fresh uuid.uuid4() per token every invocation; downgrade() (64-65) is a bare ALTER TABLE book_blocks DROP COLUMN tokens, no guard. schemas.py:706 confirms reading_selections.anchors keys on this same token_id UUID ('replaced token_index for stability across edits'), with no FK constraint.
- impact: A downgrade-then-upgrade of migration 025 regenerates every token_id, silently orphaning every existing reading_selections.anchors reference with no error surfaced.
- acceptance: downgrade() raises when orphaning anchors would occur, or a round-trip test proves token_ids survive downgrade+upgrade
- validation: cat lexy-app/backend/migrations/versions/025_block_token_ids.py

#### security_paths:data-02 — Add an idempotent INSERT...SELECT backfill (ON CONFLICT DO NOTHING) into user_video_category inside 
- severity **medium**, confidence **confirmed**, domain `security_paths`
- files: `lexy-app/backend/migrations/versions/014_drop_video_genre.py`, `lexy-app/backend/migrations/versions/013_video_category_cleanup.py`
- symbols: `014_drop_video_genre.py:1-7`, `014_drop_video_genre.py:upgrade:21-40`, `013_video_category_cleanup.py:upgrade:37-76`
- evidence: 014's own docstring (line 4-5) claims liked_genres/disliked_genres are migrated from users.settings into user_video_category. Read both upgrade() bodies directly: 014:21-40 only adds a column and deletes the JSONB keys (32-36); 013:37-76 seeds categories/FKs but contains no INSERT INTO user_video_category from settings data anywhere in the chain.
- impact: The only copy of each legacy user's liked/disliked genre preferences is deleted with no backfill anywhere in the alembic chain, so genre prefs are lost on any DB seeded before migration 014.
- acceptance: 014_drop_video_genre.py upgrade() backfills user_video_category from the JSONB keys before deleting them, or the docstring is corrected to state no backfill occurs
- validation: grep -n 'INSERT INTO user_video_category' lexy-app/backend/migrations/versions/014_drop_video_genre.py lexy-app/backend/migrations/versions/013_video_category_cleanup.py

#### db_migrations:db-03 — Make downgrade() raise when any reading_selections row has a non-empty anchors array, or add a round
- severity **medium**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/025_block_token_ids.py`, `lexy-app/backend/models/schemas.py`
- symbols: `025_block_token_ids.py:_tokenize:21-27`, `025_block_token_ids.py:downgrade:64-65`, `schemas.py:ReadingSelectionAnchor:704-707`
- evidence: _tokenize (21-27) assigns a fresh uuid.uuid4() per token on every run; downgrade() (64-65) is a bare 'ALTER TABLE book_blocks DROP COLUMN tokens' with no guard. schemas.py:706 confirms reading_selections.anchors keys on this same token_id, which deliberately replaced the old positional token_index for stability across edits.
- impact: A downgrade-then-upgrade cycle of migration 025 regenerates every token_id, silently orphaning every existing reading_selections.anchors reference — the selection row survives but its anchor no longer resolves against book_blocks.tokens, with no error surfaced anywhere.
- acceptance: downgrade() refuses when it would orphan existing anchors, or a regression test proves token_id stability across downgrade+upgrade
- validation: cat lexy-app/backend/migrations/versions/025_block_token_ids.py

#### db_migrations:db-04 — Have downgrade() raise (or require an explicit force flag) when any is_system row exists, rather tha
- severity **medium**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/037_system_word_lists.py`
- symbols: `037_system_word_lists.py:39-42`, `037_system_word_lists.py:downgrade:81-89`
- evidence: Docstring (39-42) states real system lists are seeded via 'a separate reviewed script' run AFTER this migration. downgrade() (81-89) unconditionally runs 'DELETE FROM word_lists WHERE is_system' (cascading to word_list_items) with no guard or count check.
- impact: Once the operational seeding script referenced in this migration's own docstring has populated built-in vocabulary lists, downgrading 037 permanently deletes all of that curated content (and its items) with no warning, backup, or confirmation step.
- acceptance: downgrade() refuses (or requires explicit confirmation) when is_system rows exist
- validation: cat lexy-app/backend/migrations/versions/037_system_word_lists.py

#### ingestion_pipeline:ing-02 — Add a validator comparing manifest.counts.pages/elements against len(page_dims)/len(elements), and o
- severity **medium**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/services/document_package/validators.py`
- symbols: `validators.validate_page_dims:131-157`, `validators.validate_page_indices:102-128`
- evidence: validate_page_indices only fires when an element references a page missing from page_dims; validate_page_dims only iterates the page_dims keys that exist. Neither cross-checks manifest.counts.pages against len(page_dims), page_dims key contiguity, nor counts.elements against len(document.elements) — grep for 'counts' in document_package/ shows only result.py reads it, for output not validation. Was flagged as ing-02 in AUDIT_2026-08-02.md but dropped from AUDIT_2026-08-03.md's list; re-confirmed still present.
- impact: A worker bug that drops an entire page (its page_dims entry and every element on it) produces zero fatal/warning issues and imports as if the book were complete.
- acceptance: A synthetic package with one page_dims entry removed (and no elements on that page) fails validation with a new stable issue code; A synthetic package whose counts.elements disagrees with len(elements) fails validation
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k Structural -q

#### db_migrations:db-06 — Log (via RAISE NOTICE or a row-count check) how many rows the DELETE removed, so an unexpected prefe
- severity **low**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/013_video_category_cleanup.py`
- symbols: `013_video_category_cleanup.py:upgrade:66-69`
- evidence: Step 5 (lines 66-69) runs 'DELETE FROM user_video_category WHERE video_category NOT IN (SELECT category FROM video_category)' unconditionally, immediately before adding the FK in step 6 (72-76), with no row count logged.
- impact: Any user preference row referencing a category outside the 18 seeded YouTube categories (e.g. a stale or renamed category from an earlier app version) is permanently deleted with no visibility into how many rows or which users were affected.
- acceptance: Migration emits a visible count of deleted orphan rows during upgrade
- validation: cat lexy-app/backend/migrations/versions/013_video_category_cleanup.py


## Architectural risks (4)
#### repo_structure:arch-01-docker-boot-crash — Rebuild the Docker context from the repo root (COPY subtitle-scraper/ + data/ alongside backend/) or
- severity **critical**, confidence **confirmed**, domain `repo_structure`
- files: `lexy-app/Dockerfile`, `lexy-app/backend/services/matcher_service.py`
- symbols: `Dockerfile:21-31`, `matcher_service.py:35`, `matcher_service.py:43`
- evidence: Dockerfile:28,31 COPY only backend/ and frontend/dist; subtitle-scraper/ and data/ (repo-root siblings) sit outside the lexy-app/ build context. matcher_service.py:35 computes parents[3] of __file__, which resolves to '/' under in-image WORKDIR=/app, giving _SCRAPER_PATH='/subtitle-scraper' (nonexistent); line 43 `import phrase_finder as _pf` runs at module scope. Re-confirmed by reading both files this session; already tracked as repo_structure:docker-boot-crash / au-016 in docs/AUDIT_2026-08-03.md — do not spawn a second task.
- impact: Any router importing matcher_service (phrases.py, matcher.py, chat.py) pulls this in at app startup, so `docker compose up` — README.md's own quickstart — crashes before uvicorn binds a port.
- acceptance: docker compose up boots without crashing; a CI smoke test builds the image and gets a 2xx from a health endpoint
- validation: cd lexy-app && docker compose build && docker compose up -d && curl -f http://localhost:8000/api/v1/auth/login -X POST -d '{}' -H 'Content-Type: application/json'

#### security_paths:arch-01 — Move the Docker build context to the repo root (COPY subtitle-scraper/ + data/ alongside backend/) a
- severity **high**, confidence **confirmed**, domain `security_paths`
- files: `lexy-app/Dockerfile`, `lexy-app/backend/services/matcher_service.py`, `README.md`
- symbols: `Dockerfile:21-31`, `matcher_service.py:35,43`
- evidence: Dockerfile COPYs only backend/ and built frontend/dist (WORKDIR=/app), never subtitle-scraper/ or data/ (both at repo root, outside the lexy-app/ build context). matcher_service.py:35 computes Path(__file__).resolve().parents[3]; in-image this resolves to '/', so line 43's module-scope `import phrase_finder` fails at startup. README.md:24 documents `docker compose up` as the setup path.
- impact: Following README's documented `docker compose up` quickstart crashes the backend at boot with ModuleNotFoundError before uvicorn starts.
- acceptance: docker compose up boots without a startup crash; A smoke test builds the image and gets a 2xx from a health endpoint
- validation: cd lexy-app && docker compose build && docker compose up -d && curl -f http://localhost:8000/api/v1/auth/login -X POST -d '{}' -H 'Content-Type: application/json'

#### tests_ci:arch-01 — Add an integration test in CI that mocks an OpenAI-compatible server response and calls structured()
- severity **medium**, confidence **confirmed**, domain `tests_ci`
- files: `lexy-app/backend/services/llm_provider.py`
- symbols: `lexy-app/backend/services/llm_provider.py`
- evidence: Two LLM backends exist: AnthropicProvider (default) and OpenAICompatibleProvider (opt-in). docs/SECURITY.md S18 documents S18 — LOW risk, opt-in. TESTS.md shows 'OpenAI-compatible provider (+30 tests, same file)' with comprehensive coverage of URL forms, error handling, retry logic. HOWEVER: the provider is only tested in unit tests. No integration test covers an actual structured() call against a live or mocked remote server in CI.
- impact: The OpenAI-compatible backend could break end-to-end and CI would not catch it. A missing dependency (httpx) or a URL-building regression would only surface when an operator tries to use it.
- acceptance: Mock OpenAI-compatible server responds to a structured() call; Response is correctly parsed and returned as a dict; Error paths (malformed JSON, 5xx, connection failure) are tested
- validation: grep -n 'OpenAICompatibleProvider' lexy-app/backend/tests/test_llm_provider.py | head -5; grep -n '@pytest.mark.asyncio' lexy-app/backend/tests/test_llm_provider.py | wc -l

#### security_paths:meta-01 — Loosen _is_duplicate_candidate to drop or widen the exact-category prefilter (e.g. allow a curated s
- severity **medium**, confidence **confirmed**, domain `security_paths`
- files: `autoloop/audit/reconcile.py`
- symbols: `reconcile.py:_is_duplicate_candidate:118-148`, `reconcile.py:MERGE_SIMILARITY:73`
- evidence: _is_duplicate_candidate (118-148) returns False immediately on a.category != b.category (line 143) before any substance comparison, and additionally requires file overlap plus Jaccard similarity >= 0.7 (line 73/101-115) over impact+proposed_action tokens only.
- impact: Independent domain agents that phrase the same defect under a different category label (e.g. 'security' vs 'defect' vs 'missing_test') can never merge regardless of textual similarity, so this exact multi-domain run is likely to re-propose duplicate tasks for defects like the books.py admin-gate gap (sec-01 above) across several domain reports.
- acceptance: Two synthetic findings describing the same defect under different categories with overlapping affected_files merge into one Finding via reconcile(); A regression test in autoloop/tests/test_audit_reconcile.py locks the cross-category merge
- validation: python3 -m pytest autoloop/tests/test_audit_reconcile.py -q


## Documentation drift (12)
#### tests_ci:doc-01 — Verify whether A2 (document-package import) exists and merged. If merged: update SUMMARY.md paths to
- severity **medium**, confidence **confirmed**, domain `tests_ci`
- files: `docs/SUMMARY.md`
- symbols: `docs/SUMMARY.md:354`
- evidence: SUMMARY.md line 354 lists 'services/book_import_service.py | Document-package import orchestration (roadmap A2).' and 'services/document_package/' directory under the heading 'Services'. Glob for `services/book_import_service.py` returns 'lexy-app/backend/routers/books.py', NOT a service file. The A2 work may be unmerged or the entry is stale.
- impact: SUMMARY.md is the file-level index used to navigate the codebase (CLAUDE.md §13). Stale or wrong entries send developers to the wrong location and erode trust in the index.
- acceptance: SUMMARY.md lists only files that exist; All path references resolve correctly; No service/component listed that is in a different directory
- validation: find lexy-app/backend -name 'book_import_service.py' -o -name 'document_package' -type d; find . -path './lexy-app/backend/services/document_package' -type d 2>/dev/null | wc -l; grep -n 'book_import_service\|document_package' docs/SUMMARY.md

#### docs_drift:doc-01 — Correct line 38 to: 'Alembic migrations (37 files in `migrations/versions/`)'
- severity **low**, confidence **confirmed**, domain `docs_drift`
- files: `CLAUDE.md`
- symbols: `CLAUDE.md:38`
- evidence: CLAUDE.md claims 'Alembic migrations (25+ files in `alembic/versions/`)' but actual location is lexy-app/backend/migrations/versions/ (37 files as of current HEAD). Path is inaccurate; migration count is correct.
- impact: Users following the guide will search for alembic/versions/ and not find it, causing confusion about where migrations are stored.
- acceptance: CLAUDE.md references correct path migrations/versions/; Migration count reflects actual current state
- validation: ls -1 lexy-app/backend/migrations/versions/ | wc -l

#### docs_drift:doc-02 — Update CLAUDE.md:111 to reference '(line 259)' instead of '(line 178)'
- severity **low**, confidence **confirmed**, domain `docs_drift`
- files: `CLAUDE.md`
- symbols: `CLAUDE.md:111`
- evidence: CLAUDE.md:111 claims '`apply_progression` wraps the upsert + promotion + SRS update in a single `async with conn.transaction():` (line 178).' Actual line is 259 in lexy-app/backend/services/progression_service.py:259 where `async with conn.transaction():` appears.
- impact: Users trying to navigate to the referenced line in the source will land on wrong code, disrupting their understanding of the progression flow.
- acceptance: Line reference points to actual transaction() call
- validation: grep -n 'async with conn.transaction' lexy-app/backend/services/progression_service.py | head -1

#### docs_drift:doc-03 — Update CLAUDE.md:48 to match docs/TESTS.md baseline: '(261 tests across 43 files)'
- severity **low**, confidence **confirmed**, domain `docs_drift`
- files: `CLAUDE.md`
- symbols: `CLAUDE.md:48`
- evidence: CLAUDE.md:48 claims 'Vitest + jsdom (322 tests across 44 files)' but docs/TESTS.md:100 states 'Current count (2026-07-27): 261 tests across 43 files' as the measured baseline.
- impact: Developers reviewing test coverage metrics will see inconsistent numbers. The TESTS.md page is the authoritative tracker per CLAUDE.md:296, so CLAUDE.md is stale.
- acceptance: Test count in CLAUDE.md matches current docs/TESTS.md
- validation: grep -E 'Current count|^\| File |test.*\.tsx' lexy-app/frontend/src | wc -l

#### repo_structure:arch-03-summary-missing-services — Add one-line entries for rate_limiter.py, srs_backfill_service.py, and srs_cleanup_service.py to doc
- severity **low**, confidence **confirmed**, domain `repo_structure`
- files: `docs/SUMMARY.md`, `lexy-app/backend/services/rate_limiter.py`, `lexy-app/backend/services/srs_backfill_service.py`, `lexy-app/backend/services/srs_cleanup_service.py`
- symbols: `docs/SUMMARY.md:46-82 (services table)`
- evidence: Glob of lexy-app/backend/services/*.py returns 36 files; grepping docs/SUMMARY.md for 'rate_limiter', 'srs_backfill_service', 'srs_cleanup_service' returns zero matches, while the other 33 service files each have an entry in the services table (lines 46-82).
- impact: docs/SUMMARY.md is the file CLAUDE.md §12 designates as the mandatory 'where do I look to change X' index, to be updated 'whenever a file is added' — three real services (rate limiting, two SRS maintenance backfills) are invisible to anyone using it as intended.
- acceptance: all three files have an entry in docs/SUMMARY.md's services table
- validation: grep -n 'rate_limiter\|srs_backfill_service\|srs_cleanup_service' docs/SUMMARY.md

#### security_paths:doc-01 — Correct docs/TESTS.md line 99 to '44 files' (or recount and update both with a stated verification c
- severity **low**, confidence **confirmed**, domain `security_paths`
- files: `docs/TESTS.md`, `docs/ROADMAP.md`, `docs/AUDIT_2026-08-02.md`, `docs/AUDIT_2026-08-03.md`
- symbols: `docs/TESTS.md:99`, `docs/ROADMAP.md:68`
- evidence: Direct glob of lexy-app/frontend/src: 35 *.test.tsx (28 components/ + 4 hooks/ + 3 test/) PLUS 9 *.test.ts (7 api/ + 2 hooks/) = 44 files total. docs/ROADMAP.md:68's '44 files' matches exactly; docs/TESTS.md:99's '43 files' is off by one.
- impact: Both prior audit reports (AUDIT_2026-08-02.md, AUDIT_2026-08-03.md) mis-attributed this drift to ROADMAP.md being wrong; a task built from their acceptance criteria ('update ROADMAP.md line 68') would edit the file that is already correct and leave the actually-stale TESTS.md:99 untouched.
- acceptance: docs/TESTS.md reflects the actual frontend test file count (44), verified by a glob including both .test.tsx and .test.ts
- validation: find lexy-app/frontend/src -name '*.test.tsx' -o -name '*.test.ts' | wc -l

#### security_paths:doc-02 — Remove the conftest.py reference from that SUMMARY.md row, or replace it with an accurate descriptio
- severity **low**, confidence **confirmed**, domain `security_paths`
- files: `docs/SUMMARY.md`
- symbols: `docs/SUMMARY.md:348`
- evidence: SUMMARY.md:348 states 'pytest.ini, conftest.py | Pytest config; conftest adds `src/` to path' for the repo root. A repo-wide glob for conftest.py finds only autoloop/tests/conftest.py, lexy-app/backend/tests/conftest.py, and tests/evaluation/conftest.py — no conftest.py exists at the repo root at all; root pytest.ini carries no sys.path logic.
- impact: A contributor following SUMMARY.md's file index to understand root-pipeline test setup is told a file exists and does something it neither does nor exists to do.
- acceptance: SUMMARY.md no longer claims a root conftest.py exists or adds src/ to path
- validation: find . -maxdepth 1 -name conftest.py; grep -n 'conftest' docs/SUMMARY.md

#### security_paths:doc-03 — Fix CLAUDE.md §3's migration path/count, rewrite §8b's status_marked_known sentence to describe appl
- severity **low**, confidence **confirmed**, domain `security_paths`
- files: `CLAUDE.md`, `docs/SECURITY.md`
- symbols: `CLAUDE.md §3 (Alembic line)`, `CLAUDE.md §8b (status_marked_known paragraph)`, `CLAUDE.md §10`, `docs/SECURITY.md:546`
- evidence: CLAUDE.md §3 says migrations are '25+ files in `alembic/versions/`'; actual path is lexy-app/backend/migrations/versions/ (script_location=migrations in alembic.ini) holding 37 files, no alembic/versions/ dir exists. CLAUDE.md §8b says status_marked_known's flip is 'forced to known by word_service.upsert_word_status', while §10/§12 of the SAME file say that function 'is gone'/'removed'. docs/SECURITY.md:546 cites 'CLAUDE.md §7', but CLAUDE.md's sections are 1,2,3,5,6,8,8b,9,10,11,12,13,14 — no §4 or §7 exists.
- impact: CLAUDE.md is the file this repo tells every contributor to read first; it contradicts itself on which function writes user_word_knowledge status, and both it and SECURITY.md carry cross-references to sections/paths that no longer exist.
- acceptance: CLAUDE.md no longer mentions upsert_word_status as an active function or 'alembic/versions/'; docs/SECURITY.md:546 resolves to a section that exists
- validation: grep -n upsert_word_status CLAUDE.md; grep -n 'alembic/versions' CLAUDE.md; grep -n '^## ' CLAUDE.md

#### ingestion_pipeline:ing-06 — Fix the curl example at INGESTION_PIPELINE.md:318 to use package_name instead of package_path.
- severity **low**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `docs/INGESTION_PIPELINE.md`, `lexy-app/backend/models/schemas.py`
- symbols: `INGESTION_PIPELINE.md:318`, `schemas.py:PackageImportRequest:1009-1022`
- evidence: INGESTION_PIPELINE.md:318's operator walkthrough posts {"package_path": "<document_id>"}, but PackageImportRequest (schemas.py:1018) declares only a required package_name: str (no alias) and routers/books.py:203 reads body.package_name. Flagged in AUDIT_2026-08-02.md security_paths:doc-02 but dropped from AUDIT_2026-08-03.md; re-verified still present.
- impact: An operator following the documented walkthrough verbatim gets a 422 missing-required-field error on package_name instead of a successful import.
- acceptance: The doc's example field name matches PackageImportRequest.package_name
- validation: grep -n 'package_path\|package_name' docs/INGESTION_PIPELINE.md lexy-app/backend/models/schemas.py

#### ingestion_pipeline:ing-07 — Change '+89 backend' to '+107 backend' at docs/TESTS.md:956.
- severity **low**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `docs/TESTS.md`
- symbols: `docs/TESTS.md:956`
- evidence: Line 956 heads A2's entry '+89 backend / +1 file'; grep -c 'def test_' on test_document_package.py = 96, plus parametrize expansions bring the collected total to 107 — the figure TESTS.md itself states later (~line 2174) and INGESTION_PIPELINE.md §13 also cites. Already open as AUDIT_2026-08-03.md ingest-doc-01 — duplicate, unchanged.
- impact: A reader trusting the '+89' heading under-counts test_document_package.py's actual coverage by 18 tests in the file CLAUDE.md designates as the coverage index.
- acceptance: Both figures for test_document_package.py agree within docs/TESTS.md
- validation: grep -n '89 backend\|107 tests' docs/TESTS.md

#### ingestion_pipeline:ing-08 — Reword §5 to 'no database mutation' (consistent with §4's own claim that import_result.json is writt
- severity **low**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `docs/INGESTION_PIPELINE.md`, `lexy-app/backend/services/book_import_service.py`, `lexy-app/backend/routers/books.py`
- symbols: `INGESTION_PIPELINE.md:403-404`, `book_import_service.import_package:116-135`, `book_import_service.dry_run_package:214-227`, `routers.books.import_package:207-211`
- evidence: Doc §5 states dry-run 'runs every gate and builds the full persistence plan, then writes nothing.' But import_package's write_result param defaults True (book_import_service.py:123), and routers/books.py:207-211 calls import_package(dry_run=body.dry_run) without passing write_result=False — confirmed by test_document_package.py:850-859's own test exercising dry_run=True, write_result=True and asserting import_result.json IS written. Only the unused dry_run_package() helper (never called by the router; grep shows zero non-test callers) sets write_result=False.
- impact: An operator relying on the documented 'dry-run writes nothing' guarantee finds the package directory mutated with an import_result.json file after every real dry-run API call — doc and shipped behavior disagree.
- acceptance: docs/INGESTION_PIPELINE.md §5 no longer claims a dry run writes nothing to disk when the shipped default does
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k import_result_is_written -q; grep -n 'writes nothing' docs/INGESTION_PIPELINE.md

#### docs_drift:doc-04 — Add a timestamp and verification method to the claim: 'Pulls transcripts for ~916 YouTube channels (
- severity **info**, confidence **confirmed**, domain `docs_drift`
- files: `CLAUDE.md`
- symbols: `CLAUDE.md:52`
- evidence: CLAUDE.md:52 claims '~916 YouTube channels' in subtitle-scraper but provides no reference to where this measurement was taken or when. No `git log --grep` or dated comment marks the measurement. Unverifiable without access to the production database.
- impact: Low. The order of magnitude (~916) is plausible but undated and unverifiable from code alone.
- acceptance: Claim includes date and pointer to verification source
- validation: grep -c 'youtube_channel_id\|channel_id' subtitle-scraper/seed_data/channels.json || echo 'Count via: jq 'length' seed_data/channels.json'


## Missing regression protection (9)
#### tests_ci:ci-01 — Add GitHub Actions jobs to CI: backend pytest (serial + parallel), frontend vitest, ruff check (from
- severity **high**, confidence **confirmed**, domain `tests_ci`
- files: `.github/workflows/dependency-audit.yml`
- symbols: `.github/workflows/dependency-audit.yml`
- evidence: .github/workflows has only dependency-audit.yml; backend pytest (1258 tests documented in TESTS.md), frontend vitest (261+ tests), ruff linting, and root pipeline tests (221 tests) are not gated in CI. TESTS.md §11 documents pytest baseline: 'cd lexy-app/backend && python3 -m pytest -n auto' → 1258 passed; §4 frontend: npm run test → 261 tests.
- impact: Test failures and lint violations land in main undetected. A broken test suite or ruff violation in a commit can reach production.
- acceptance: All three CI jobs fire on push/PR to main and on schedule; pytest suite runs with -n auto flag; ruff check . reports zero findings; npm run build completes successfully
- validation: ls -la .github/workflows/; grep -n 'pytest' .github/workflows/*.yml; grep -n 'ruff' .github/workflows/*.yml; grep -n 'vitest\|npm.*test' .github/workflows/*.yml

#### tests_ci:tests-01 — Add CI job for frontend: (1) npm ci, (2) npm run lint, (3) npm run build, (4) npm run test. All bloc
- severity **high**, confidence **confirmed**, domain `tests_ci`
- files: `lexy-app/frontend`, `lexy-app/frontend/package.json`
- symbols: `lexy-app/frontend/package.json:11`
- evidence: TESTS.md shows 261–322 frontend vitest tests across 43–44 files. package.json line 11 has `"test": "vitest run"`. No CI job runs it. npm run build is also untested (COMMON_ERRORS.md §3 documents 'tsc --noEmit passes but npm run build fails' — the two suites have different scope).
- impact: Frontend test suite can break silently. Build-time type errors (test-file-exclusive globals like 'global') only surface on `npm run build`, not on `tsc --noEmit`.
- acceptance: CI runs npm run build and npm run test; npm run build completes without TypeScript errors; Vitest suite runs and reports pass/fail; npm run lint passes
- validation: cd /Users/emir/.autoloop/workers/audit-0001/lexy-app/frontend && npm run build 2>&1 | tail -20; cd /Users/emir/.autoloop/workers/audit-0001/lexy-app/frontend && npm run test -- --run 2>&1 | tail -5

#### tests_ci:ci-02 — Move npm audit to the new frontend CI job (see tests-01). Run it AFTER npm ci but BEFORE npm run bui
- severity **high**, confidence **confirmed**, domain `tests_ci`
- files: `.github/workflows/dependency-audit.yml`
- symbols: `.github/workflows/dependency-audit.yml:52`
- evidence: CI npm audit runs with --audit-level=high (line 52), skipping dev dependencies. Neither `npm run build` nor the build process validation is gated. A malicious or broken dev dependency (e.g., a build plugin) is never tested. The frontend can fail to build undetected.
- impact: Build-time vulnerabilities and runtime errors in the build toolchain (Vite, TypeScript, ESLint plugins) go undetected until deployment.
- acceptance: npm audit runs as part of frontend CI; npm run build is tested, build errors fail the job
- validation: grep 'npm audit' .github/workflows/*.yml; grep 'npm run build' .github/workflows/*.yml

#### security_paths:ci-01 — Add a tests.yml workflow with backend pytest -n auto, frontend npm run test, root pytest tests/, and
- severity **high**, confidence **confirmed**, domain `security_paths`
- files: `.github/workflows/dependency-audit.yml`
- symbols: `.github/workflows/`
- evidence: Only .github/workflows/dependency-audit.yml exists (read in full): two jobs, pip-audit and npm audit. No job invokes pytest, ruff, tsc, or a frontend build, despite CLAUDE.md §11 calling ruff+pytest 'part of backend validation, not an optional extra.'
- impact: A PR that fails ruff, breaks any of the backend/frontend/root-pipeline test suites, or breaks the TypeScript build can merge to main with a fully green required-checks state.
- acceptance: tests.yml exists and runs on PRs to main; A PR with a failing test or ruff violation shows a failing required check
- validation: ls -la .github/workflows/

#### tests_ci:db-02 — Add a test asserting pytest-randomly is disabled (or not installed) so deleting the `-p no:randomly`
- severity **high**, confidence **confirmed**, domain `tests_ci`
- files: `pytest.ini`, `lexy-app/backend/pytest.ini`, `docs/COMMON_ERRORS.md`, `docs/TESTS.md`
- symbols: `pytest.ini:addopts`, `lexy-app/backend/pytest.ini:addopts`
- evidence: docs/TESTS.md §1 and docs/COMMON_ERRORS.md §2.2 record that spaCy/thinc registers a `pytest_randomly.random_seeder` entry point whose per-test reseed overflows numpy's 2**32 limit — without the `-p no:randomly` addopts, `python3 -m pytest -n auto` gives 1036 errors vs 745 passed. The block exists in both pytest.ini files but nothing enforces it: any edit or a CI invocation omitting it silently breaks the suite.
- impact: If the addopts block is dropped, ~99% of tests error in setup/teardown and it reads as infrastructure breakage rather than a config regression; a CI check that only greps a pass count can misreport green.
- acceptance: A test asserts pytest-randomly is blocked or absent; Removing the addopts block from pytest.ini makes that test fail; COMMON_ERRORS.md §2.2 warns against removing the block
- validation: grep -n 'addopts.*randomly' pytest.ini lexy-app/backend/pytest.ini; cd lexy-app/backend && python3 -m pytest tests/ -q 2>&1 | tail -1

#### tests_ci:lint-01 — Add CI job: `ruff check . --config ruff.toml` from repo root on push/PR to main. Fail on any finding
- severity **medium**, confidence **confirmed**, domain `tests_ci`
- files: `.github/workflows/dependency-audit.yml`, `ruff.toml`
- symbols: `ruff.toml:12`
- evidence: ruff.toml exists and is configured (select=["E","F"], ignore=["E501"], pinned at 0.14.1). CLAUDE.md §11 says 'run ruff check . from repo root' as part of 'backend validation, not optional extra'. No CI job runs it. COMMON_ERRORS.md §4 documents three traps specific to this repo (E402 deliberate on sibling imports, F401 on availability probes, compileall not validating F401).
- impact: Lint violations go undetected until a developer runs it locally. Two baseline issues: (1) the three traps in COMMON_ERRORS.md can be missed by a --fix run; (2) if ruff version drifts, rule set drifts machine-to-machine.
- acceptance: CI runs ruff check . from repo root; Job fails if any finding is reported; Config pin stays at 0.14.1
- validation: cd /Users/emir/.autoloop/workers/audit-0001 && ruff check . --config ruff.toml 2>&1 | head -20; ruff --version; grep 'select = ' ruff.toml

#### tests_ci:tests-02 — Add CI job: `cd /Users/emir/.autoloop/workers/audit-0001 && python3 -m pytest tests/ -q` (serial, ro
- severity **medium**, confidence **confirmed**, domain `tests_ci`
- files: `pytest.ini`, `tests/runtime/`
- symbols: `pytest.ini:14`
- evidence: Root-level pytest.ini testpaths = backend/tests (line 14), but docs/TESTS.md shows root pipeline suite exists: 'tests/runtime/ (93 tests)' + 'tests/*.py (114 tests covering subtitle-scraper)' = 207 total. SUMMARY.md confirms 'tests/ pytest tests for pipeline modules'. No CI job validates this.
- impact: Root pipeline tests (221 tests documented, covering eligibility, exposure, learning invariants, subtitle processing) run only when a developer runs `pytest tests/` manually. Breakage goes undetected.
- acceptance: CI runs root pipeline pytest suite; All 207–221 tests pass; Job fails if any test fails
- validation: cd /Users/emir/.autoloop/workers/audit-0001 && python3 -m pytest tests/ --collect-only 2>&1 | tail -5; cd /Users/emir/.autoloop/workers/audit-0001 && python3 -m pytest tests/ -q 2>&1 | tail -3

#### security_paths:ing-02 — Rewrite the imports to `from backend.services import ...` / `from backend.services.document_package 
- severity **medium**, confidence **confirmed**, domain `security_paths`
- files: `lexy-app/backend/tests/test_document_package.py`
- symbols: `test_document_package.py:13-43`
- evidence: Line 13 uses `from services import book_import_service` and following lines use bare `from services.document_package import ...` — every other file under lexy-app/backend/tests/ uses `from backend.services import ...`. Bare cwd-independent import requires cwd on sys.path.
- impact: Running the bare `pytest` console script (vs `python3 -m pytest`) from lexy-app/backend silently drops this file's ~107 tests from collection with no error, so a contributor on the wrong entrypoint can believe an untested ingestion-pipeline change is safe.
- acceptance: cd lexy-app/backend && pytest tests/test_document_package.py -q (bare console script) collects and passes all tests; python3 -m pytest tests/test_document_package.py -q also passes
- validation: cd lexy-app/backend && pytest tests/test_document_package.py -q; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -q

#### ingestion_pipeline:ing-04 — Rewrite the file's imports to 'from backend.services import ...' / 'from backend.services.document_p
- severity **medium**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/tests/test_document_package.py`
- symbols: `test_document_package.py:13-43`
- evidence: Line 13 'from services import book_import_service' plus bare 'from services.document_package import ...' through line 43 — the only backend test file not using 'from backend.services import ...'. Requires cwd on sys.path (true only for 'python -m pytest', not the bare 'pytest' console script) — documented in CLAUDE.md §11 / docs/COMMON_ERRORS.md §2, unchanged. Already open as AUDIT_2026-08-03.md ingest-defect-02 — duplicate.
- impact: A contributor who runs bare 'pytest' instead of 'python3 -m pytest' gets a clean-looking collection with this file's 107 tests (all of A2's coverage) silently dropped, and may ship an untested ingestion-pipeline change believing it's covered.
- acceptance: cd lexy-app/backend && pytest tests/test_document_package.py -q (bare console script) collects and passes all 107 tests
- validation: cd lexy-app/backend && pytest tests/test_document_package.py -q; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -q


## Optional improvements (3)
#### repo_structure:arch-04-registry-reconcile-gap — Have the reconcile/taskgen step read autoloop/seed_tasks.json (and tasks.json when present) as an in
- severity **medium**, confidence **confirmed**, domain `repo_structure`
- files: `autoloop/seed_tasks.json`, `docs/AUDIT_2026-08-03.md`
- symbols: `seed_tasks.json:rt-01`, `seed_tasks.json:ext-01`, `test_free_chat_progression.py:300-316`
- evidence: autoloop/seed_tasks.json (the git-tracked task-graph seed) holds exactly 2 tasks: rt-01 (books admin-gate, duplicated across 6+ separate findings/tasks in AUDIT_2026-08-03.md) and ext-01 (phrase extractor drops the reflexive+preposition frame). I confirmed ext-01's underlying defect is still live: test_free_chat_progression.py:300-316 carries a strict xfail matching ext-01's description exactly. None of AUDIT_2026-08-03.md's 6 domain reports reference ext-01 or this xfail anywhere in the 27-task proposal.
- impact: The audit pipeline's own task registry has one real open item (ext-01) invisible to its reconciliation pass, while its sibling (rt-01) is redundantly re-discovered 6+ times — reconcile is checking prior AUDIT_*.md reports for duplicates but not the actual task registry it's meant to reconcile against.
- acceptance: a run of the audit references ext-01's status (open/fixed) explicitly; reconcile.py is extended or documented to read seed_tasks.json/tasks.json as ground truth for existing tasks
- validation: cat autoloop/seed_tasks.json; cd lexy-app/backend && python3 -m pytest tests/test_free_chat_progression.py -k inflected_phrase -q

#### db_migrations:db-05 — Add 'REFERENCES book_documents(doc_id) ON DELETE CASCADE' to book_blocks.doc_id in a new migration, 
- severity **low**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/008_book_reading_mode.py`, `lexy-app/backend/services/book_service.py`
- symbols: `008_book_reading_mode.py:50-67 (book_blocks)`, `008_book_reading_mode.py:36-46 (book_pages)`, `book_service.py:423-434`
- evidence: book_pages.doc_id (line 38) declares 'REFERENCES book_documents(doc_id) ON DELETE CASCADE'; book_blocks.doc_id (line 53) is a bare 'UUID NOT NULL' with no FK, despite being a denormalized copy of the value reachable via page_id. The one current write path (book_service.py:423-434) sources both from the same in-scope variable per document, so no drift occurs today.
- impact: No DB-level guard exists against book_blocks.doc_id drifting from its page's true doc_id; a future write path (e.g. the planned A3 persistence backend) that gets this wrong would silently produce blocks queryable-by-doc_id that don't match their page's document.
- acceptance: book_blocks.doc_id carries an FK to book_documents(doc_id); A migration-time check confirms no existing row violates it before adding the constraint
- validation: grep -n 'doc_id' lexy-app/backend/migrations/versions/008_book_reading_mode.py

#### ingestion_pipeline:ing-09 — Resolve UPLOAD_ROOT lazily inside package_root(), matching PACKAGE_ROOT's call-time read.
- severity **low**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/services/document_package/loader.py`
- symbols: `loader.DEFAULT_PACKAGE_ROOT:25`, `loader.package_root:89-97`
- evidence: DEFAULT_PACKAGE_ROOT = Path(os.getenv('UPLOAD_ROOT','uploads'))/'packages' is computed once at module import (loader.py:25); package_root() (89-97) re-reads os.getenv('PACKAGE_ROOT') fresh on every call. Setting PACKAGE_ROOT after import takes effect immediately; setting UPLOAD_ROOT after import silently does not. Already open as AUDIT_2026-08-03.md ingest-improve-01 — duplicate, unchanged.
- impact: A test or late-binding deployment setting UPLOAD_ROOT expecting document_package.package_root() to follow it (as book_service.py's identically-named _UPLOAD_ROOT does) silently gets the real default 'uploads/packages' instead.
- acceptance: Setting UPLOAD_ROOT via monkeypatch after module import changes package_root()'s result in a new regression test
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k package_root -q


## Human decisions needed (2)
#### repo_structure:arch-05-no-23-task-roadmap-found — Ask the requester which document/artifact is the '23-task roadmap' (a specific commit of ROADMAP.md/
- severity **info**, confidence **confirmed**, domain `repo_structure`
- files: `autoloop/seed_tasks.json`, `docs/ROADMAP.md`, `docs/AUTOLOOP_TODO.md`, `docs/TODO.md`
- evidence: The requested scope asks to 'reconcile findings against the existing 23-task roadmap.' A grep for '23-task'/'23 tasks' across the repo returns nothing. The only machine-readable task registry, autoloop/seed_tasks.json, holds 2 tasks. docs/ROADMAP.md's live plan uses an N1-N6 + historical-P-item scheme with no total of 23; docs/TODO.md numbers items non-sequentially (0a-19, 35, 36...43) and also does not total 23.
- impact: Without knowing which artifact '23-task roadmap' refers to, any stale/duplicate determination made against a substitute (e.g. the prior audit's own 27-task proposal) risks comparing against the wrong baseline and mislabeling items.
- acceptance: the requester identifies the specific artifact, or confirms it doesn't exist in this checkout
- validation: grep -rn '23-task\|23 tasks' .

#### security_paths:proc-01 — When reconciling this report, use the live task registry from the primary checkout (not this worker 
- severity **info**, confidence **confirmed**, domain `security_paths`
- files: `autoloop/seed_tasks.json`, `docs/AUDIT_2026-08-02.md`, `docs/AUDIT_2026-08-03.md`
- evidence: My brief asked me to reconcile against 'the existing 23-task roadmap'. autoloop/seed_tasks.json (the only committed task registry in this checkout) contains exactly 2 tasks (rt-01, ext-01); the two prior audit reports proposed 33 and 27 tasks respectively. No artifact in this checkout holds exactly 23 tasks — the live autoloop task registry state (if any) lives in a gitignored .autoloop/ directory not present in this worker clone.
- impact: Reconciling my findings against a specific '23-task' count is not possible from this read-only checkout; forcing a match would risk hallucinating task IDs that don't exist.
- acceptance: Reconciliation step cites the actual task registry file/count used, not an unverified number
- validation: cat autoloop/seed_tasks.json


## Rejected items (0)
(none)

## Proposed task graph (43 tasks)
To adopt this graph, reply with a `plan` decision containing the tasks below (edit freely — this is a proposal):
```json
[
  {
    "id": "au-001",
    "title": "Add a pre-001 baseline migration creating the content core (video, word_table, sentence, language...",
    "description": "Priority: P1 | severity critical | confidence confirmed | from audit finding tests_ci:db-01\nScope: Add a pre-001 baseline migration creating the content core (video, word_table, sentence, language_table, word_to_sentence, video_category, phrase_blueprint, channel) from the deleted table.sql snapshot, so `alembic upgrade head` completes on an empty DB.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: No new database can be built from scratch \u2014 CI schema validation, new-contributor setup, and fresh deploys all fail at migration 006; the project is un-deployable to a clean DB.\nEvidence: docs/COMMON_ERRORS.md \u00a79 records that a clean DB + `alembic upgrade head` dies at migration 006 (ALTER TABLE video ADD COLUMN channel_id): the chain creates 23 tables and never creates the content core, because there were two schema sources (migrations = app half, the now-deleted table.sql = content half) that cannot be composed.\nExpected files: lexy-app/backend/alembic/versions, lexy-app/backend/alembic.ini, docs/COMMON_ERRORS.md\nAcceptance criteria: createdb + alembic upgrade head succeeds on an empty database; Resulting schema contains all 37 tables including the content core; pg_dump schema-only diff against the production DB shows no missing tables\nValidation: createdb fresh_test; cd lexy-app/backend && DB_NAME=fresh_test alembic upgrade head; psql fresh_test -c '\\dt' | grep -E 'video|word_table|phrase'; git log --oneline -- table.sql | head -1\nParallelizable: no",
    "depends_on": []
  },
  {
    "id": "au-002",
    "title": "Add an INSERT INTO user_video_category ... SELECT ... jsonb_array_elements_text(settings->'liked_...",
    "description": "Priority: P1 | severity high | confidence confirmed | from audit finding db_migrations:db-01\nScope: Add an INSERT INTO user_video_category ... SELECT ... jsonb_array_elements_text(settings->'liked_genres'/'disliked_genres') ON CONFLICT DO NOTHING step before the DELETE, mirroring migration 027's channel-preference backfill pattern.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any DB whose users.settings still holds legacy liked_genres/disliked_genres when this migration runs (fresh install from an old backup, or any environment migrating from pre-014 state) permanently loses that preference data with no backfill path.\nEvidence: Docstring (lines 4-6) claims liked_genres/disliked_genres are 'migrated ... into user_video_category'. upgrade() (21-40) only adds the preference column (23-27), deletes both JSONB keys from users.settings (32-36), and drops video.genre (39-40) \u2014 no INSERT INTO user_video_category exists anywhere in this file or in 013.\nExpected files: lexy-app/backend/migrations/versions/014_drop_video_genre.py\nAcceptance criteria: A DB seeded only with users.settings.liked_genres/disliked_genres (no user_video_category rows) retains those preferences in user_video_category after upgrading through 014; Migration docstring accurately describes what upgrade() performs\nValidation: grep -n 'INSERT INTO user_video_category' lexy-app/backend/migrations/versions/014_drop_video_genre.py lexy-app/backend/migrations/versions/013_video_category_cleanup.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-003",
    "title": "Make downgrade() raise when any reading_selections row has a non-empty anchors array, or add a ro...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding db_migrations:db-03\nScope: Make downgrade() raise when any reading_selections row has a non-empty anchors array, or add a round-trip test proving token_ids survive downgrade+upgrade.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A downgrade-then-upgrade cycle of migration 025 regenerates every token_id, silently orphaning every existing reading_selections.anchors reference \u2014 the selection row survives but its anchor no longer resolves against book_blocks.tokens, with no error surfaced anywhere.\nEvidence: _tokenize (21-27) assigns a fresh uuid.uuid4() per token on every run; downgrade() (64-65) is a bare 'ALTER TABLE book_blocks DROP COLUMN tokens' with no guard. schemas.py:706 confirms reading_selections.anchors keys on this same token_id, which deliberately replaced the old positional token_index for stability across edits.\nExpected files: lexy-app/backend/migrations/versions/025_block_token_ids.py, lexy-app/backend/models/schemas.py\nAcceptance criteria: downgrade() refuses when it would orphan existing anchors, or a regression test proves token_id stability across downgrade+upgrade\nValidation: cat lexy-app/backend/migrations/versions/025_block_token_ids.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-004",
    "title": "Have downgrade() raise (or require an explicit force flag) when any is_system row exists, rather ...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding db_migrations:db-04\nScope: Have downgrade() raise (or require an explicit force flag) when any is_system row exists, rather than silently deleting curated content.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Once the operational seeding script referenced in this migration's own docstring has populated built-in vocabulary lists, downgrading 037 permanently deletes all of that curated content (and its items) with no warning, backup, or confirmation step.\nEvidence: Docstring (39-42) states real system lists are seeded via 'a separate reviewed script' run AFTER this migration. downgrade() (81-89) unconditionally runs 'DELETE FROM word_lists WHERE is_system' (cascading to word_list_items) with no guard or count check.\nExpected files: lexy-app/backend/migrations/versions/037_system_word_lists.py\nAcceptance criteria: downgrade() refuses (or requires explicit confirmation) when is_system rows exist\nValidation: cat lexy-app/backend/migrations/versions/037_system_word_lists.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-005",
    "title": "Add a validator comparing manifest.counts.pages/elements against len(page_dims)/len(elements), an...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding ingestion_pipeline:ing-02\nScope: Add a validator comparing manifest.counts.pages/elements against len(page_dims)/len(elements), and one checking page_dims keys form a contiguous 1..count range.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A worker bug that drops an entire page (its page_dims entry and every element on it) produces zero fatal/warning issues and imports as if the book were complete.\nEvidence: validate_page_indices only fires when an element references a page missing from page_dims; validate_page_dims only iterates the page_dims keys that exist. Neither cross-checks manifest.counts.pages against len(page_dims), page_dims key contiguity, nor counts.elements against len(document.elements) \u2014 grep for 'counts' in document_package/ shows only result.py reads it, for output not validation. Was flagged as ing-02 in AUDIT_2026-08-02.md but dropped from AUDIT_2026-08-03.md's list; re-confirmed still present.\nExpected files: lexy-app/backend/services/document_package/validators.py\nAcceptance criteria: A synthetic package with one page_dims entry removed (and no elements on that page) fails validation with a new stable issue code; A synthetic package whose counts.elements disagrees with len(elements) fails validation\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k Structural -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-006",
    "title": "Add a structural validator flagging bbox['page'] != element['page_index'] with a new stable issue...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding ingestion_pipeline:ing-03\nScope: Add a structural validator flagging bbox['page'] != element['page_index'] with a new stable issue code; fix the doc's \u00a74 example to use matching values.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A corrupted/buggy package where an element's bbox.page disagrees with its page_index passes every validation gate silently; inert today, a live coordinate-mapping trap once A3 or a review UI keys off bbox.page.\nEvidence: _BBOX_KEYS=('x1','y1','x2','y2') never includes 'page'; validate_bboxes:351-356 keys page dims off element.get('page_index') only, never bbox.get('page'). The doc's own \u00a74 worked example (lines 245-246) shows page_index:11 alongside bbox.page:12 in the same element, unreconciled. Still open per AUDIT_2026-08-03.md security_paths:ing-01 \u2014 duplicate, do not spawn a second task.\nExpected files: lexy-app/backend/services/document_package/validators.py, docs/INGESTION_PIPELINE.md\nAcceptance criteria: A synthetic element with bbox.page != page_index produces a new stable validation issue code; INGESTION_PIPELINE.md \u00a74 example has bbox.page == page_index\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k Structural -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-007",
    "title": "Guard downgrade() to refuse when reading_selections rows hold non-empty anchors, or persist/resto...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding security_paths:data-01\nScope: Guard downgrade() to refuse when reading_selections rows hold non-empty anchors, or persist/restore token_ids across the round-trip.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A downgrade-then-upgrade of migration 025 regenerates every token_id, silently orphaning every existing reading_selections.anchors reference with no error surfaced.\nEvidence: _tokenize (21-27) assigns fresh uuid.uuid4() per token every invocation; downgrade() (64-65) is a bare ALTER TABLE book_blocks DROP COLUMN tokens, no guard. schemas.py:706 confirms reading_selections.anchors keys on this same token_id UUID ('replaced token_index for stability across edits'), with no FK constraint.\nExpected files: lexy-app/backend/migrations/versions/025_block_token_ids.py, lexy-app/backend/models/schemas.py, lexy-app/backend/migrations/versions/009_reading_selections.py\nAcceptance criteria: downgrade() raises when orphaning anchors would occur, or a round-trip test proves token_ids survive downgrade+upgrade\nValidation: cat lexy-app/backend/migrations/versions/025_block_token_ids.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-008",
    "title": "Add an idempotent INSERT...SELECT backfill (ON CONFLICT DO NOTHING) into user_video_category insi...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding security_paths:data-02\nScope: Add an idempotent INSERT...SELECT backfill (ON CONFLICT DO NOTHING) into user_video_category inside 014's upgrade(), executed before the JSONB keys are deleted.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: The only copy of each legacy user's liked/disliked genre preferences is deleted with no backfill anywhere in the alembic chain, so genre prefs are lost on any DB seeded before migration 014.\nEvidence: 014's own docstring (line 4-5) claims liked_genres/disliked_genres are migrated from users.settings into user_video_category. Read both upgrade() bodies directly: 014:21-40 only adds a column and deletes the JSONB keys (32-36); 013:37-76 seeds categories/FKs but contains no INSERT INTO user_video_category from settings data anywhere in the chain.\nExpected files: lexy-app/backend/migrations/versions/014_drop_video_genre.py, lexy-app/backend/migrations/versions/013_video_category_cleanup.py\nAcceptance criteria: 014_drop_video_genre.py upgrade() backfills user_video_category from the JSONB keys before deleting them, or the docstring is corrected to state no backfill occurs\nValidation: grep -n 'INSERT INTO user_video_category' lexy-app/backend/migrations/versions/014_drop_video_genre.py lexy-app/backend/migrations/versions/013_video_category_cleanup.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-009",
    "title": "Add a structural validator comparing bbox['page'] (when present) to element['page_index'], flaggi...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding security_paths:ing-01\nScope: Add a structural validator comparing bbox['page'] (when present) to element['page_index'], flagging a mismatch as fatal per the preservation-principle severity rule already used in this file.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A worker bug or corrupted package where a bbox's own 'page' field disagrees with the element's page_index passes every validation gate silently.\nEvidence: _BBOX_KEYS = (\"x1\",\"y1\",\"x2\",\"y2\") (line 21) never includes 'page'; validate_bboxes (351) keys page-dims lookup off element.get('page_index') only. docs/INGESTION_PIPELINE.md:245-246,261 shows the package format explicitly gives bbox its own 'page' field ('bbox carries page explicitly').\nExpected files: lexy-app/backend/services/document_package/validators.py, docs/INGESTION_PIPELINE.md\nAcceptance criteria: Synthetic element with mismatched bbox page vs page_index fails validation with a new issue code\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k Structural -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-010",
    "title": "Add a one-line comment in downgrade() noting the rebuilt JSONB is a best-effort reconstruction, n...",
    "description": "Priority: P1 | severity low | confidence confirmed | from audit finding db_migrations:db-02\nScope: Add a one-line comment in downgrade() noting the rebuilt JSONB is a best-effort reconstruction, not a restore of pre-upgrade values.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A downgrade of 014 produces JSONB preference values reflecting current app state, not the true pre-upgrade values, misleading anyone who treats alembic downgrade as a real rollback.\nEvidence: downgrade() (47-64) rebuilds users.settings.liked_genres/disliked_genres by aggregating current user_video_category rows \u2014 a table upgrade() never wrote to (db-01) and that the running application may have populated long after 014 shipped.\nExpected files: lexy-app/backend/migrations/versions/014_drop_video_genre.py\nAcceptance criteria: downgrade() carries a comment documenting the reconstruction is not a true restore\nValidation: grep -n -B2 'liked_genres' lexy-app/backend/migrations/versions/014_drop_video_genre.py\nParallelizable: yes",
    "depends_on": [
      "au-001"
    ]
  },
  {
    "id": "au-011",
    "title": "Log (via RAISE NOTICE or a row-count check) how many rows the DELETE removed, so an unexpected pr...",
    "description": "Priority: P1 | severity low | confidence confirmed | from audit finding db_migrations:db-06\nScope: Log (via RAISE NOTICE or a row-count check) how many rows the DELETE removed, so an unexpected preference-data loss is visible in migration output.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any user preference row referencing a category outside the 18 seeded YouTube categories (e.g. a stale or renamed category from an earlier app version) is permanently deleted with no visibility into how many rows or which users were affected.\nEvidence: Step 5 (lines 66-69) runs 'DELETE FROM user_video_category WHERE video_category NOT IN (SELECT category FROM video_category)' unconditionally, immediately before adding the FK in step 6 (72-76), with no row count logged.\nExpected files: lexy-app/backend/migrations/versions/013_video_category_cleanup.py\nAcceptance criteria: Migration emits a visible count of deleted orphan rows during upgrade\nValidation: cat lexy-app/backend/migrations/versions/013_video_category_cleanup.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-012",
    "title": "Only emit checksums_verified when the loop found zero fatal issues in this gate, or reword it to ...",
    "description": "Priority: P1 | severity low | confidence confirmed | from audit finding ingestion_pipeline:ing-05\nScope: Only emit checksums_verified when the loop found zero fatal issues in this gate, or reword it to 'checked' rather than 'verified'.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: import_result.json can carry an info entry claiming files were 'verified' in the same run where those files are simultaneously listed under errors as checksum_mismatch, misleading anyone scanning info/warnings without cross-referencing validation.checksums.\nEvidence: loader.py:402-430 loops pkg.files adding checksum_mismatch/checksum_missing_file/checksum_path_escape as FATAL per bad entry; lines 432-437 then unconditionally add an INFO 'checksums_verified: verified N file(s)' regardless of whether any of those same N entries just failed above. Already open as AUDIT_2026-08-03.md ingest-defect-01 \u2014 duplicate, code unchanged.\nExpected files: lexy-app/backend/services/document_package/loader.py\nAcceptance criteria: A package with a checksum_mismatch entry produces no checksums_verified info claiming success\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k Checksum -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-013",
    "title": "Add Depends(require_admin) to both routes, matching delete_book/phrases.seed; add a non-admin-403...",
    "description": "Priority: P2 | severity high | confidence confirmed | from audit finding ingestion_pipeline:ing-01\nScope: Add Depends(require_admin) to both routes, matching delete_book/phrases.seed; add a non-admin-403 regression test.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any authenticated non-admin learner can enumerate every operator-curated ingestion package name and force full checksum/structural validation compute on demand.\nEvidence: books.py:183 list_packages and :191 import_package depend only on Depends(get_current_user); sibling routes in the same file (delete_book/delete_page) and /phrases/seed gate on require_admin. test_document_package.py:987-1034 asserts only 401/403 for unauthenticated, never non-admin-403. Matches AUDIT_2026-08-03.md's already-open books-admin-gate/sec-01/ing-04 (3 duplicate write-ups) \u2014 still unresolved, do not spawn a 4th task.\nExpected files: lexy-app/backend/routers/books.py\nAcceptance criteria: Non-admin authenticated caller gets 403 from GET /books/packages and POST /books/import; Admin behavior unchanged; docs/SECURITY.md lists both routes as admin-gated\nValidation: rg -n 'require_admin' lexy-app/backend/routers/books.py; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k TestImportEndpoint -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-014",
    "title": "Gate both routes with the same is_admin/require_admin pattern already used by delete_document/del...",
    "description": "Priority: P2 | severity high | confidence confirmed | from audit finding repo_structure:arch-02-books-admin-gate\nScope: Gate both routes with the same is_admin/require_admin pattern already used by delete_document/delete_page in this file.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any authenticated non-admin user can enumerate every operator-curated package name and force checksum/structural validation compute via POST /books/import, since packages carry no per-user ownership scope.\nEvidence: books.py:183,189 depend only on Depends(get_current_user); no admin check. The same file's delete_document/delete_page (384,399) thread `is_admin=user.get('is_admin', False)` into book_service, which gates elevated behavior on it (book_service.py:790,809) \u2014 an established admin-aware pattern that list_packages/import_package skip entirely. Re-confirmed by reading both files this session; already tracked as repo_structure:books-admin-gate / security_paths:sec-01 / au-014/au-015 \u2014 do not spawn a second task.\nExpected files: lexy-app/backend/routers/books.py\nAcceptance criteria: non-admin caller gets 403 from both GET /books/packages and POST /books/import; admin behavior unchanged\nValidation: rg -n 'require_admin|is_admin' lexy-app/backend/routers/books.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-015",
    "title": "Add Depends(require_admin) to both routes, matching delete_book/delete_page; add a non-admin-403 ...",
    "description": "Priority: P2 | severity high | confidence confirmed | from audit finding security_paths:sec-01\nScope: Add Depends(require_admin) to both routes, matching delete_book/delete_page; add a non-admin-403 regression test.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any authenticated non-admin user can enumerate every operator-curated book package name and force checksum/structural validation compute via POST /books/import.\nEvidence: books.py:183 and :189 depend only on Depends(get_current_user); no require_admin, unlike delete_book/delete_page in the same file and POST /phrases/seed. Verified directly by reading the file.\nExpected files: lexy-app/backend/routers/books.py\nAcceptance criteria: Non-admin caller gets 403 admin_required from GET /books/packages and POST /books/import; Admin behavior unchanged\nValidation: rg -n 'require_admin' lexy-app/backend/routers/books.py; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-016",
    "title": "Rebuild the Docker context from the repo root (COPY subtitle-scraper/ + data/ alongside backend/)...",
    "description": "Priority: P4 | severity critical | confidence confirmed | from audit finding repo_structure:arch-01-docker-boot-crash\nScope: Rebuild the Docker context from the repo root (COPY subtitle-scraper/ + data/ alongside backend/) or make the phrase_finder import lazy with a clear degraded-mode error; add a build+boot smoke test.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any router importing matcher_service (phrases.py, matcher.py, chat.py) pulls this in at app startup, so `docker compose up` \u2014 README.md's own quickstart \u2014 crashes before uvicorn binds a port.\nEvidence: Dockerfile:28,31 COPY only backend/ and frontend/dist; subtitle-scraper/ and data/ (repo-root siblings) sit outside the lexy-app/ build context. matcher_service.py:35 computes parents[3] of __file__, which resolves to '/' under in-image WORKDIR=/app, giving _SCRAPER_PATH='/subtitle-scraper' (nonexistent); line 43 `import phrase_finder as _pf` runs at module scope. Re-confirmed by reading both files this session; already tracked as repo_structure:docker-boot-crash / au-016 in docs/AUDIT_2026-08-03.md \u2014 do not spawn a second task.\nExpected files: lexy-app/Dockerfile, lexy-app/backend/services/matcher_service.py\nAcceptance criteria: docker compose up boots without crashing; a CI smoke test builds the image and gets a 2xx from a health endpoint\nValidation: cd lexy-app && docker compose build && docker compose up -d && curl -f http://localhost:8000/api/v1/auth/login -X POST -d '{}' -H 'Content-Type: application/json'\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-017",
    "title": "Move the Docker build context to the repo root (COPY subtitle-scraper/ + data/ alongside backend/...",
    "description": "Priority: P4 | severity high | confidence confirmed | from audit finding security_paths:arch-01\nScope: Move the Docker build context to the repo root (COPY subtitle-scraper/ + data/ alongside backend/) and update matcher_service.py's path resolution to match, or document docker-compose as unsupported.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Following README's documented `docker compose up` quickstart crashes the backend at boot with ModuleNotFoundError before uvicorn starts.\nEvidence: Dockerfile COPYs only backend/ and built frontend/dist (WORKDIR=/app), never subtitle-scraper/ or data/ (both at repo root, outside the lexy-app/ build context). matcher_service.py:35 computes Path(__file__).resolve().parents[3]; in-image this resolves to '/', so line 43's module-scope `import phrase_finder` fails at startup. README.md:24 documents `docker compose up` as the setup path.\nExpected files: lexy-app/Dockerfile, lexy-app/backend/services/matcher_service.py, README.md\nAcceptance criteria: docker compose up boots without a startup crash; A smoke test builds the image and gets a 2xx from a health endpoint\nValidation: cd lexy-app && docker compose build && docker compose up -d && curl -f http://localhost:8000/api/v1/auth/login -X POST -d '{}' -H 'Content-Type: application/json'\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-018",
    "title": "Loosen _is_duplicate_candidate to drop or widen the exact-category prefilter (e.g. allow a curate...",
    "description": "Priority: P4 | severity medium | confidence confirmed | from audit finding security_paths:meta-01\nScope: Loosen _is_duplicate_candidate to drop or widen the exact-category prefilter (e.g. allow a curated set of interchangeable categories) before applying the substance-similarity check.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Independent domain agents that phrase the same defect under a different category label (e.g. 'security' vs 'defect' vs 'missing_test') can never merge regardless of textual similarity, so this exact multi-domain run is likely to re-propose duplicate tasks for defects like the books.py admin-gate gap (sec-01 above) across several domain reports.\nEvidence: _is_duplicate_candidate (118-148) returns False immediately on a.category != b.category (line 143) before any substance comparison, and additionally requires file overlap plus Jaccard similarity >= 0.7 (line 73/101-115) over impact+proposed_action tokens only.\nExpected files: autoloop/audit/reconcile.py\nAcceptance criteria: Two synthetic findings describing the same defect under different categories with overlapping affected_files merge into one Finding via reconcile(); A regression test in autoloop/tests/test_audit_reconcile.py locks the cross-category merge\nValidation: python3 -m pytest autoloop/tests/test_audit_reconcile.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-019",
    "title": "Add an integration test in CI that mocks an OpenAI-compatible server response and calls structure...",
    "description": "Priority: P4 | severity medium | confidence confirmed | from audit finding tests_ci:arch-01\nScope: Add an integration test in CI that mocks an OpenAI-compatible server response and calls structured() against it. Use httpx MockTransport or a similar stub to avoid needing a real server. Verify round-trip success and error paths (malformed JSON, missing fields, 5xx errors).\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: The OpenAI-compatible backend could break end-to-end and CI would not catch it. A missing dependency (httpx) or a URL-building regression would only surface when an operator tries to use it.\nEvidence: Two LLM backends exist: AnthropicProvider (default) and OpenAICompatibleProvider (opt-in). docs/SECURITY.md S18 documents S18 \u2014 LOW risk, opt-in. TESTS.md shows 'OpenAI-compatible provider (+30 tests, same file)' with comprehensive coverage of URL forms, error handling, retry logic. HOWEVER: the provider is only tested in unit tests. No integration test covers an actual structured() call against a live or mocked remote server in CI.\nExpected files: lexy-app/backend/services/llm_provider.py\nAcceptance criteria: Mock OpenAI-compatible server responds to a structured() call; Response is correctly parsed and returned as a dict; Error paths (malformed JSON, 5xx, connection failure) are tested\nValidation: grep -n 'OpenAICompatibleProvider' lexy-app/backend/tests/test_llm_provider.py | head -5; grep -n '@pytest.mark.asyncio' lexy-app/backend/tests/test_llm_provider.py | wc -l\nParallelizable: yes",
    "depends_on": [
      "au-020"
    ]
  },
  {
    "id": "au-020",
    "title": "Add a tests.yml workflow with backend pytest -n auto, frontend npm run test, root pytest tests/, ...",
    "description": "Priority: P5 | severity high | confidence confirmed | from audit finding security_paths:ci-01\nScope: Add a tests.yml workflow with backend pytest -n auto, frontend npm run test, root pytest tests/, and ruff check . as required status checks on main.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A PR that fails ruff, breaks any of the backend/frontend/root-pipeline test suites, or breaks the TypeScript build can merge to main with a fully green required-checks state.\nEvidence: Only .github/workflows/dependency-audit.yml exists (read in full): two jobs, pip-audit and npm audit. No job invokes pytest, ruff, tsc, or a frontend build, despite CLAUDE.md \u00a711 calling ruff+pytest 'part of backend validation, not an optional extra.'\nExpected files: .github/workflows/dependency-audit.yml\nAcceptance criteria: tests.yml exists and runs on PRs to main; A PR with a failing test or ruff violation shows a failing required check\nValidation: ls -la .github/workflows/\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-021",
    "title": "Add GitHub Actions jobs to CI: backend pytest (serial + parallel), frontend vitest, ruff check (f...",
    "description": "Priority: P5 | severity high | confidence confirmed | from audit finding tests_ci:ci-01\nScope: Add GitHub Actions jobs to CI: backend pytest (serial + parallel), frontend vitest, ruff check (from repo root), npm run build. All should be blocking.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Test failures and lint violations land in main undetected. A broken test suite or ruff violation in a commit can reach production.\nEvidence: .github/workflows has only dependency-audit.yml; backend pytest (1258 tests documented in TESTS.md), frontend vitest (261+ tests), ruff linting, and root pipeline tests (221 tests) are not gated in CI. TESTS.md \u00a711 documents pytest baseline: 'cd lexy-app/backend && python3 -m pytest -n auto' \u2192 1258 passed; \u00a74 frontend: npm run test \u2192 261 tests.\nExpected files: .github/workflows/dependency-audit.yml\nAcceptance criteria: All three CI jobs fire on push/PR to main and on schedule; pytest suite runs with -n auto flag; ruff check . reports zero findings; npm run build completes successfully\nValidation: ls -la .github/workflows/; grep -n 'pytest' .github/workflows/*.yml; grep -n 'ruff' .github/workflows/*.yml; grep -n 'vitest\\|npm.*test' .github/workflows/*.yml\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-022",
    "title": "Move npm audit to the new frontend CI job (see tests-01). Run it AFTER npm ci but BEFORE npm run ...",
    "description": "Priority: P5 | severity high | confidence confirmed | from audit finding tests_ci:ci-02\nScope: Move npm audit to the new frontend CI job (see tests-01). Run it AFTER npm ci but BEFORE npm run build, so build failures surface. Keep --omit=dev since dev deps are not shipped.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Build-time vulnerabilities and runtime errors in the build toolchain (Vite, TypeScript, ESLint plugins) go undetected until deployment.\nEvidence: CI npm audit runs with --audit-level=high (line 52), skipping dev dependencies. Neither `npm run build` nor the build process validation is gated. A malicious or broken dev dependency (e.g., a build plugin) is never tested. The frontend can fail to build undetected.\nExpected files: .github/workflows/dependency-audit.yml\nAcceptance criteria: npm audit runs as part of frontend CI; npm run build is tested, build errors fail the job\nValidation: grep 'npm audit' .github/workflows/*.yml; grep 'npm run build' .github/workflows/*.yml\nParallelizable: yes",
    "depends_on": [
      "au-020",
      "au-024"
    ]
  },
  {
    "id": "au-023",
    "title": "Add a test asserting pytest-randomly is disabled (or not installed) so deleting the `-p no:random...",
    "description": "Priority: P5 | severity high | confidence confirmed | from audit finding tests_ci:db-02\nScope: Add a test asserting pytest-randomly is disabled (or not installed) so deleting the `-p no:randomly` addopts makes it fail, and add a 'do not remove' entry to docs/COMMON_ERRORS.md \u00a72.2.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: If the addopts block is dropped, ~99% of tests error in setup/teardown and it reads as infrastructure breakage rather than a config regression; a CI check that only greps a pass count can misreport green.\nEvidence: docs/TESTS.md \u00a71 and docs/COMMON_ERRORS.md \u00a72.2 record that spaCy/thinc registers a `pytest_randomly.random_seeder` entry point whose per-test reseed overflows numpy's 2**32 limit \u2014 without the `-p no:randomly` addopts, `python3 -m pytest -n auto` gives 1036 errors vs 745 passed. The block exists in both pytest.ini files but nothing enforces it: any edit or a CI invocation omitting it silently breaks the suite.\nExpected files: pytest.ini, lexy-app/backend/pytest.ini, docs/COMMON_ERRORS.md, docs/TESTS.md\nAcceptance criteria: A test asserts pytest-randomly is blocked or absent; Removing the addopts block from pytest.ini makes that test fail; COMMON_ERRORS.md \u00a72.2 warns against removing the block\nValidation: grep -n 'addopts.*randomly' pytest.ini lexy-app/backend/pytest.ini; cd lexy-app/backend && python3 -m pytest tests/ -q 2>&1 | tail -1\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-024",
    "title": "Add CI job for frontend: (1) npm ci, (2) npm run lint, (3) npm run build, (4) npm run test. All b...",
    "description": "Priority: P5 | severity high | confidence confirmed | from audit finding tests_ci:tests-01\nScope: Add CI job for frontend: (1) npm ci, (2) npm run lint, (3) npm run build, (4) npm run test. All blocking, all in lexy-app/frontend directory. Build-time errors surface in step 3.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Frontend test suite can break silently. Build-time type errors (test-file-exclusive globals like 'global') only surface on `npm run build`, not on `tsc --noEmit`.\nEvidence: TESTS.md shows 261\u2013322 frontend vitest tests across 43\u201344 files. package.json line 11 has `\"test\": \"vitest run\"`. No CI job runs it. npm run build is also untested (COMMON_ERRORS.md \u00a73 documents 'tsc --noEmit passes but npm run build fails' \u2014 the two suites have different scope).\nExpected files: lexy-app/frontend, lexy-app/frontend/package.json\nAcceptance criteria: CI runs npm run build and npm run test; npm run build completes without TypeScript errors; Vitest suite runs and reports pass/fail; npm run lint passes\nValidation: cd /Users/emir/.autoloop/workers/audit-0001/lexy-app/frontend && npm run build 2>&1 | tail -20; cd /Users/emir/.autoloop/workers/audit-0001/lexy-app/frontend && npm run test -- --run 2>&1 | tail -5\nParallelizable: yes",
    "depends_on": [
      "au-020"
    ]
  },
  {
    "id": "au-025",
    "title": "Rewrite the file's imports to 'from backend.services import ...' / 'from backend.services.documen...",
    "description": "Priority: P5 | severity medium | confidence confirmed | from audit finding ingestion_pipeline:ing-04\nScope: Rewrite the file's imports to 'from backend.services import ...' / 'from backend.services.document_package import ...', matching every other backend test file.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A contributor who runs bare 'pytest' instead of 'python3 -m pytest' gets a clean-looking collection with this file's 107 tests (all of A2's coverage) silently dropped, and may ship an untested ingestion-pipeline change believing it's covered.\nEvidence: Line 13 'from services import book_import_service' plus bare 'from services.document_package import ...' through line 43 \u2014 the only backend test file not using 'from backend.services import ...'. Requires cwd on sys.path (true only for 'python -m pytest', not the bare 'pytest' console script) \u2014 documented in CLAUDE.md \u00a711 / docs/COMMON_ERRORS.md \u00a72, unchanged. Already open as AUDIT_2026-08-03.md ingest-defect-02 \u2014 duplicate.\nExpected files: lexy-app/backend/tests/test_document_package.py\nAcceptance criteria: cd lexy-app/backend && pytest tests/test_document_package.py -q (bare console script) collects and passes all 107 tests\nValidation: cd lexy-app/backend && pytest tests/test_document_package.py -q; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-026",
    "title": "Rewrite the imports to `from backend.services import ...` / `from backend.services.document_packa...",
    "description": "Priority: P5 | severity medium | confidence confirmed | from audit finding security_paths:ing-02\nScope: Rewrite the imports to `from backend.services import ...` / `from backend.services.document_package import ...` to match every other test file in the directory.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Running the bare `pytest` console script (vs `python3 -m pytest`) from lexy-app/backend silently drops this file's ~107 tests from collection with no error, so a contributor on the wrong entrypoint can believe an untested ingestion-pipeline change is safe.\nEvidence: Line 13 uses `from services import book_import_service` and following lines use bare `from services.document_package import ...` \u2014 every other file under lexy-app/backend/tests/ uses `from backend.services import ...`. Bare cwd-independent import requires cwd on sys.path.\nExpected files: lexy-app/backend/tests/test_document_package.py\nAcceptance criteria: cd lexy-app/backend && pytest tests/test_document_package.py -q (bare console script) collects and passes all tests; python3 -m pytest tests/test_document_package.py -q also passes\nValidation: cd lexy-app/backend && pytest tests/test_document_package.py -q; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-027",
    "title": "Add CI job: `ruff check . --config ruff.toml` from repo root on push/PR to main. Fail on any find...",
    "description": "Priority: P5 | severity medium | confidence confirmed | from audit finding tests_ci:lint-01\nScope: Add CI job: `ruff check . --config ruff.toml` from repo root on push/PR to main. Fail on any finding. The config pins 0.14.1 and the rule set, so results are reproducible.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Lint violations go undetected until a developer runs it locally. Two baseline issues: (1) the three traps in COMMON_ERRORS.md can be missed by a --fix run; (2) if ruff version drifts, rule set drifts machine-to-machine.\nEvidence: ruff.toml exists and is configured (select=[\"E\",\"F\"], ignore=[\"E501\"], pinned at 0.14.1). CLAUDE.md \u00a711 says 'run ruff check . from repo root' as part of 'backend validation, not optional extra'. No CI job runs it. COMMON_ERRORS.md \u00a74 documents three traps specific to this repo (E402 deliberate on sibling imports, F401 on availability probes, compileall not validating F401).\nExpected files: .github/workflows/dependency-audit.yml, ruff.toml\nAcceptance criteria: CI runs ruff check . from repo root; Job fails if any finding is reported; Config pin stays at 0.14.1\nValidation: cd /Users/emir/.autoloop/workers/audit-0001 && ruff check . --config ruff.toml 2>&1 | head -20; ruff --version; grep 'select = ' ruff.toml\nParallelizable: yes",
    "depends_on": [
      "au-020"
    ]
  },
  {
    "id": "au-028",
    "title": "Add CI job: `cd /Users/emir/.autoloop/workers/audit-0001 && python3 -m pytest tests/ -q` (serial,...",
    "description": "Priority: P5 | severity medium | confidence confirmed | from audit finding tests_ci:tests-02\nScope: Add CI job: `cd /Users/emir/.autoloop/workers/audit-0001 && python3 -m pytest tests/ -q` (serial, root suite is smaller than backend). Block the main CI on it.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Root pipeline tests (221 tests documented, covering eligibility, exposure, learning invariants, subtitle processing) run only when a developer runs `pytest tests/` manually. Breakage goes undetected.\nEvidence: Root-level pytest.ini testpaths = backend/tests (line 14), but docs/TESTS.md shows root pipeline suite exists: 'tests/runtime/ (93 tests)' + 'tests/*.py (114 tests covering subtitle-scraper)' = 207 total. SUMMARY.md confirms 'tests/ pytest tests for pipeline modules'. No CI job validates this.\nExpected files: pytest.ini, tests/runtime/\nAcceptance criteria: CI runs root pipeline pytest suite; All 207\u2013221 tests pass; Job fails if any test fails\nValidation: cd /Users/emir/.autoloop/workers/audit-0001 && python3 -m pytest tests/ --collect-only 2>&1 | tail -5; cd /Users/emir/.autoloop/workers/audit-0001 && python3 -m pytest tests/ -q 2>&1 | tail -3\nParallelizable: yes",
    "depends_on": [
      "au-020"
    ]
  },
  {
    "id": "au-029",
    "title": "Have the reconcile/taskgen step read autoloop/seed_tasks.json (and tasks.json when present) as an...",
    "description": "Priority: P6 | severity medium | confidence confirmed | from audit finding repo_structure:arch-04-registry-reconcile-gap\nScope: Have the reconcile/taskgen step read autoloop/seed_tasks.json (and tasks.json when present) as an input, not just prior AUDIT_*.md reports, so already-tracked-but-unreported tasks like ext-01 are surfaced or explicitly confirmed still-open rather than silently absent.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: The audit pipeline's own task registry has one real open item (ext-01) invisible to its reconciliation pass, while its sibling (rt-01) is redundantly re-discovered 6+ times \u2014 reconcile is checking prior AUDIT_*.md reports for duplicates but not the actual task registry it's meant to reconcile against.\nEvidence: autoloop/seed_tasks.json (the git-tracked task-graph seed) holds exactly 2 tasks: rt-01 (books admin-gate, duplicated across 6+ separate findings/tasks in AUDIT_2026-08-03.md) and ext-01 (phrase extractor drops the reflexive+preposition frame). I confirmed ext-01's underlying defect is still live: test_free_chat_progression.py:300-316 carries a strict xfail matching ext-01's description exactly. None of AUDIT_2026-08-03.md's 6 domain reports reference ext-01 or this xfail anywhere in the 27-task proposal.\nExpected files: autoloop/seed_tasks.json, docs/AUDIT_2026-08-03.md\nAcceptance criteria: a run of the audit references ext-01's status (open/fixed) explicitly; reconcile.py is extended or documented to read seed_tasks.json/tasks.json as ground truth for existing tasks\nValidation: cat autoloop/seed_tasks.json; cd lexy-app/backend && python3 -m pytest tests/test_free_chat_progression.py -k inflected_phrase -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-030",
    "title": "Add 'REFERENCES book_documents(doc_id) ON DELETE CASCADE' to book_blocks.doc_id in a new migratio...",
    "description": "Priority: P6 | severity low | confidence confirmed | from audit finding db_migrations:db-05\nScope: Add 'REFERENCES book_documents(doc_id) ON DELETE CASCADE' to book_blocks.doc_id in a new migration, after verifying no existing row's doc_id disagrees with its page's doc_id.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: No DB-level guard exists against book_blocks.doc_id drifting from its page's true doc_id; a future write path (e.g. the planned A3 persistence backend) that gets this wrong would silently produce blocks queryable-by-doc_id that don't match their page's document.\nEvidence: book_pages.doc_id (line 38) declares 'REFERENCES book_documents(doc_id) ON DELETE CASCADE'; book_blocks.doc_id (line 53) is a bare 'UUID NOT NULL' with no FK, despite being a denormalized copy of the value reachable via page_id. The one current write path (book_service.py:423-434) sources both from the same in-scope variable per document, so no drift occurs today.\nExpected files: lexy-app/backend/migrations/versions/008_book_reading_mode.py, lexy-app/backend/services/book_service.py\nAcceptance criteria: book_blocks.doc_id carries an FK to book_documents(doc_id); A migration-time check confirms no existing row violates it before adding the constraint\nValidation: grep -n 'doc_id' lexy-app/backend/migrations/versions/008_book_reading_mode.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-031",
    "title": "Resolve UPLOAD_ROOT lazily inside package_root(), matching PACKAGE_ROOT's call-time read.",
    "description": "Priority: P6 | severity low | confidence confirmed | from audit finding ingestion_pipeline:ing-09\nScope: Resolve UPLOAD_ROOT lazily inside package_root(), matching PACKAGE_ROOT's call-time read.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A test or late-binding deployment setting UPLOAD_ROOT expecting document_package.package_root() to follow it (as book_service.py's identically-named _UPLOAD_ROOT does) silently gets the real default 'uploads/packages' instead.\nEvidence: DEFAULT_PACKAGE_ROOT = Path(os.getenv('UPLOAD_ROOT','uploads'))/'packages' is computed once at module import (loader.py:25); package_root() (89-97) re-reads os.getenv('PACKAGE_ROOT') fresh on every call. Setting PACKAGE_ROOT after import takes effect immediately; setting UPLOAD_ROOT after import silently does not. Already open as AUDIT_2026-08-03.md ingest-improve-01 \u2014 duplicate, unchanged.\nExpected files: lexy-app/backend/services/document_package/loader.py\nAcceptance criteria: Setting UPLOAD_ROOT via monkeypatch after module import changes package_root()'s result in a new regression test\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k package_root -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-032",
    "title": "Verify whether A2 (document-package import) exists and merged. If merged: update SUMMARY.md paths...",
    "description": "Priority: P7 | severity medium | confidence confirmed | from audit finding tests_ci:doc-01\nScope: Verify whether A2 (document-package import) exists and merged. If merged: update SUMMARY.md paths to match real files. If not merged: remove the entries and keep them in docs/TODO.md or docs/ROADMAP.md only.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: SUMMARY.md is the file-level index used to navigate the codebase (CLAUDE.md \u00a713). Stale or wrong entries send developers to the wrong location and erode trust in the index.\nEvidence: SUMMARY.md line 354 lists 'services/book_import_service.py | Document-package import orchestration (roadmap A2).' and 'services/document_package/' directory under the heading 'Services'. Glob for `services/book_import_service.py` returns 'lexy-app/backend/routers/books.py', NOT a service file. The A2 work may be unmerged or the entry is stale.\nExpected files: docs/SUMMARY.md\nAcceptance criteria: SUMMARY.md lists only files that exist; All path references resolve correctly; No service/component listed that is in a different directory\nValidation: find lexy-app/backend -name 'book_import_service.py' -o -name 'document_package' -type d; find . -path './lexy-app/backend/services/document_package' -type d 2>/dev/null | wc -l; grep -n 'book_import_service\\|document_package' docs/SUMMARY.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-033",
    "title": "Correct line 38 to: 'Alembic migrations (37 files in `migrations/versions/`)'",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding docs_drift:doc-01\nScope: Correct line 38 to: 'Alembic migrations (37 files in `migrations/versions/`)'\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Users following the guide will search for alembic/versions/ and not find it, causing confusion about where migrations are stored.\nEvidence: CLAUDE.md claims 'Alembic migrations (25+ files in `alembic/versions/`)' but actual location is lexy-app/backend/migrations/versions/ (37 files as of current HEAD). Path is inaccurate; migration count is correct.\nExpected files: CLAUDE.md\nAcceptance criteria: CLAUDE.md references correct path migrations/versions/; Migration count reflects actual current state\nValidation: ls -1 lexy-app/backend/migrations/versions/ | wc -l\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-034",
    "title": "Update CLAUDE.md:111 to reference '(line 259)' instead of '(line 178)'",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding docs_drift:doc-02\nScope: Update CLAUDE.md:111 to reference '(line 259)' instead of '(line 178)'\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Users trying to navigate to the referenced line in the source will land on wrong code, disrupting their understanding of the progression flow.\nEvidence: CLAUDE.md:111 claims '`apply_progression` wraps the upsert + promotion + SRS update in a single `async with conn.transaction():` (line 178).' Actual line is 259 in lexy-app/backend/services/progression_service.py:259 where `async with conn.transaction():` appears.\nExpected files: CLAUDE.md\nAcceptance criteria: Line reference points to actual transaction() call\nValidation: grep -n 'async with conn.transaction' lexy-app/backend/services/progression_service.py | head -1\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-035",
    "title": "Update CLAUDE.md:48 to match docs/TESTS.md baseline: '(261 tests across 43 files)'",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding docs_drift:doc-03\nScope: Update CLAUDE.md:48 to match docs/TESTS.md baseline: '(261 tests across 43 files)'\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Developers reviewing test coverage metrics will see inconsistent numbers. The TESTS.md page is the authoritative tracker per CLAUDE.md:296, so CLAUDE.md is stale.\nEvidence: CLAUDE.md:48 claims 'Vitest + jsdom (322 tests across 44 files)' but docs/TESTS.md:100 states 'Current count (2026-07-27): 261 tests across 43 files' as the measured baseline.\nExpected files: CLAUDE.md\nAcceptance criteria: Test count in CLAUDE.md matches current docs/TESTS.md\nValidation: grep -E 'Current count|^\\| File |test.*\\.tsx' lexy-app/frontend/src | wc -l\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-036",
    "title": "Fix the curl example at INGESTION_PIPELINE.md:318 to use package_name instead of package_path.",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding ingestion_pipeline:ing-06\nScope: Fix the curl example at INGESTION_PIPELINE.md:318 to use package_name instead of package_path.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: An operator following the documented walkthrough verbatim gets a 422 missing-required-field error on package_name instead of a successful import.\nEvidence: INGESTION_PIPELINE.md:318's operator walkthrough posts {\"package_path\": \"<document_id>\"}, but PackageImportRequest (schemas.py:1018) declares only a required package_name: str (no alias) and routers/books.py:203 reads body.package_name. Flagged in AUDIT_2026-08-02.md security_paths:doc-02 but dropped from AUDIT_2026-08-03.md; re-verified still present.\nExpected files: docs/INGESTION_PIPELINE.md, lexy-app/backend/models/schemas.py\nAcceptance criteria: The doc's example field name matches PackageImportRequest.package_name\nValidation: grep -n 'package_path\\|package_name' docs/INGESTION_PIPELINE.md lexy-app/backend/models/schemas.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-037",
    "title": "Change '+89 backend' to '+107 backend' at docs/TESTS.md:956.",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding ingestion_pipeline:ing-07\nScope: Change '+89 backend' to '+107 backend' at docs/TESTS.md:956.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A reader trusting the '+89' heading under-counts test_document_package.py's actual coverage by 18 tests in the file CLAUDE.md designates as the coverage index.\nEvidence: Line 956 heads A2's entry '+89 backend / +1 file'; grep -c 'def test_' on test_document_package.py = 96, plus parametrize expansions bring the collected total to 107 \u2014 the figure TESTS.md itself states later (~line 2174) and INGESTION_PIPELINE.md \u00a713 also cites. Already open as AUDIT_2026-08-03.md ingest-doc-01 \u2014 duplicate, unchanged.\nExpected files: docs/TESTS.md\nAcceptance criteria: Both figures for test_document_package.py agree within docs/TESTS.md\nValidation: grep -n '89 backend\\|107 tests' docs/TESTS.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-038",
    "title": "Reword \u00a75 to 'no database mutation' (consistent with \u00a74's own claim that import_result.json is wr...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding ingestion_pipeline:ing-08\nScope: Reword \u00a75 to 'no database mutation' (consistent with \u00a74's own claim that import_result.json is written after every attempt), or have the router pass write_result=False for dry runs.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: An operator relying on the documented 'dry-run writes nothing' guarantee finds the package directory mutated with an import_result.json file after every real dry-run API call \u2014 doc and shipped behavior disagree.\nEvidence: Doc \u00a75 states dry-run 'runs every gate and builds the full persistence plan, then writes nothing.' But import_package's write_result param defaults True (book_import_service.py:123), and routers/books.py:207-211 calls import_package(dry_run=body.dry_run) without passing write_result=False \u2014 confirmed by test_document_package.py:850-859's own test exercising dry_run=True, write_result=True and asserting import_result.json IS written. Only the unused dry_run_package() helper (never called by the router; grep shows zero non-test callers) sets write_result=False.\nExpected files: docs/INGESTION_PIPELINE.md, lexy-app/backend/services/book_import_service.py, lexy-app/backend/routers/books.py\nAcceptance criteria: docs/INGESTION_PIPELINE.md \u00a75 no longer claims a dry run writes nothing to disk when the shipped default does\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k import_result_is_written -q; grep -n 'writes nothing' docs/INGESTION_PIPELINE.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-039",
    "title": "Add one-line entries for rate_limiter.py, srs_backfill_service.py, and srs_cleanup_service.py to ...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding repo_structure:arch-03-summary-missing-services\nScope: Add one-line entries for rate_limiter.py, srs_backfill_service.py, and srs_cleanup_service.py to docs/SUMMARY.md's services table.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: docs/SUMMARY.md is the file CLAUDE.md \u00a712 designates as the mandatory 'where do I look to change X' index, to be updated 'whenever a file is added' \u2014 three real services (rate limiting, two SRS maintenance backfills) are invisible to anyone using it as intended.\nEvidence: Glob of lexy-app/backend/services/*.py returns 36 files; grepping docs/SUMMARY.md for 'rate_limiter', 'srs_backfill_service', 'srs_cleanup_service' returns zero matches, while the other 33 service files each have an entry in the services table (lines 46-82).\nExpected files: docs/SUMMARY.md, lexy-app/backend/services/rate_limiter.py, lexy-app/backend/services/srs_backfill_service.py, lexy-app/backend/services/srs_cleanup_service.py\nAcceptance criteria: all three files have an entry in docs/SUMMARY.md's services table\nValidation: grep -n 'rate_limiter\\|srs_backfill_service\\|srs_cleanup_service' docs/SUMMARY.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-040",
    "title": "Correct docs/TESTS.md line 99 to '44 files' (or recount and update both with a stated verificatio...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding security_paths:doc-01\nScope: Correct docs/TESTS.md line 99 to '44 files' (or recount and update both with a stated verification command); do not edit ROADMAP.md:68, which is already accurate.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Both prior audit reports (AUDIT_2026-08-02.md, AUDIT_2026-08-03.md) mis-attributed this drift to ROADMAP.md being wrong; a task built from their acceptance criteria ('update ROADMAP.md line 68') would edit the file that is already correct and leave the actually-stale TESTS.md:99 untouched.\nEvidence: Direct glob of lexy-app/frontend/src: 35 *.test.tsx (28 components/ + 4 hooks/ + 3 test/) PLUS 9 *.test.ts (7 api/ + 2 hooks/) = 44 files total. docs/ROADMAP.md:68's '44 files' matches exactly; docs/TESTS.md:99's '43 files' is off by one.\nExpected files: docs/TESTS.md, docs/ROADMAP.md, docs/AUDIT_2026-08-02.md, docs/AUDIT_2026-08-03.md\nAcceptance criteria: docs/TESTS.md reflects the actual frontend test file count (44), verified by a glob including both .test.tsx and .test.ts\nValidation: find lexy-app/frontend/src -name '*.test.tsx' -o -name '*.test.ts' | wc -l\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-041",
    "title": "Remove the conftest.py reference from that SUMMARY.md row, or replace it with an accurate descrip...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding security_paths:doc-02\nScope: Remove the conftest.py reference from that SUMMARY.md row, or replace it with an accurate description of root pytest.ini alone.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A contributor following SUMMARY.md's file index to understand root-pipeline test setup is told a file exists and does something it neither does nor exists to do.\nEvidence: SUMMARY.md:348 states 'pytest.ini, conftest.py | Pytest config; conftest adds `src/` to path' for the repo root. A repo-wide glob for conftest.py finds only autoloop/tests/conftest.py, lexy-app/backend/tests/conftest.py, and tests/evaluation/conftest.py \u2014 no conftest.py exists at the repo root at all; root pytest.ini carries no sys.path logic.\nExpected files: docs/SUMMARY.md\nAcceptance criteria: SUMMARY.md no longer claims a root conftest.py exists or adds src/ to path\nValidation: find . -maxdepth 1 -name conftest.py; grep -n 'conftest' docs/SUMMARY.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-042",
    "title": "Fix CLAUDE.md \u00a73's migration path/count, rewrite \u00a78b's status_marked_known sentence to describe a...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding security_paths:doc-03\nScope: Fix CLAUDE.md \u00a73's migration path/count, rewrite \u00a78b's status_marked_known sentence to describe apply_progression(status_override=...) directly, and fix docs/SECURITY.md:546's section reference.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: CLAUDE.md is the file this repo tells every contributor to read first; it contradicts itself on which function writes user_word_knowledge status, and both it and SECURITY.md carry cross-references to sections/paths that no longer exist.\nEvidence: CLAUDE.md \u00a73 says migrations are '25+ files in `alembic/versions/`'; actual path is lexy-app/backend/migrations/versions/ (script_location=migrations in alembic.ini) holding 37 files, no alembic/versions/ dir exists. CLAUDE.md \u00a78b says status_marked_known's flip is 'forced to known by word_service.upsert_word_status', while \u00a710/\u00a712 of the SAME file say that function 'is gone'/'removed'. docs/SECURITY.md:546 cites 'CLAUDE.md \u00a77', but CLAUDE.md's sections are 1,2,3,5,6,8,8b,9,10,11,12,13,14 \u2014 no \u00a74 or \u00a77 exists.\nExpected files: CLAUDE.md, docs/SECURITY.md\nAcceptance criteria: CLAUDE.md no longer mentions upsert_word_status as an active function or 'alembic/versions/'; docs/SECURITY.md:546 resolves to a section that exists\nValidation: grep -n upsert_word_status CLAUDE.md; grep -n 'alembic/versions' CLAUDE.md; grep -n '^## ' CLAUDE.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-043",
    "title": "Add a timestamp and verification method to the claim: 'Pulls transcripts for ~916 YouTube channel...",
    "description": "Priority: P7 | severity info | confidence confirmed | from audit finding docs_drift:doc-04\nScope: Add a timestamp and verification method to the claim: 'Pulls transcripts for ~916 YouTube channels (measured 2026-07-27; see `subtitle-scraper/seed_data/channels.json` for the canonical list).'\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Low. The order of magnitude (~916) is plausible but undated and unverifiable from code alone.\nEvidence: CLAUDE.md:52 claims '~916 YouTube channels' in subtitle-scraper but provides no reference to where this measurement was taken or when. No `git log --grep` or dated comment marks the measurement. Unverifiable without access to the production database.\nExpected files: CLAUDE.md\nAcceptance criteria: Claim includes date and pointer to verification source\nValidation: grep -c 'youtube_channel_id\\|channel_id' subtitle-scraper/seed_data/channels.json || echo 'Count via: jq 'length' seed_data/channels.json'\nParallelizable: yes",
    "depends_on": []
  }
]
```

Not promoted to tasks:
- `repo_structure:arch-05-no-23-task-roadmap-found`: human decision — surfaced to the operator, not a task
- `security_paths:proc-01`: human decision — surfaced to the operator, not a task
