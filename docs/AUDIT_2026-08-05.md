# Repository audit — 2026-08-05

Generated by the autoloop audit executor (deterministic rendering of reconciled subagent findings; raw agent reports preserved at `.autoloop/audit/20260805T012248Z-c1771c`).

- branch: `autoloop/audit-0002` | HEAD: `adb7e2e9e165` | uncommitted files: 0
- revision feedback incorporated: Reconcile the report before approval: merge substantively identical cross-domain findings such as repo_structure:sec-01 with security_paths:sec-01 and security_paths:data-01 with db_migrations:db-02, preserve the strongest combined evidence/impact/acceptance criteria, and ensure the proposed task graph contains one implementation task per underlying defect rather than duplicate work. Also explicitly distinguish genuine current-HEAD defects from roadmap items previously marked completed but absent from this branch, so integration/state drift is not misreported as a newly discovered independent defect.

## Domain coverage — 6/6 domains reported usable output

| domain | usable output | findings |
|---|---|---|
| `docs_drift` | yes | 1 |
| `tests_ci` | yes | 7 |
| `repo_structure` | yes | 8 |
| `security_paths` | yes | 7 |
| `db_migrations` | yes | 5 |
| `ingestion_pipeline` | yes | 3 |

## Validation baseline
- `ruff check .` — **PASS**
  - tail: `All checks passed!`
- `python3 -m pytest autoloop/tests -q -p no:cacheprovider` — **PASS**
  - tail: `[32m[32m[1m1151 passed[0m, [33m1 skipped[0m, [33m1 deselected[0m[32m in 233.89s (0:03:53)[0m[0m`
- `python3 -m pytest tests/ -q -p no:cacheprovider` — **PASS**
  - tail: `[33m[32m368 passed[0m, [33m[1m5 warnings[0m[33m in 8.32s[0m[0m`

## Confirmed defects (2)
#### db_migrations:db-01 — Author a new baseline migration (not a verbatim table.sql replay, which conflicts with 013-022's con
- severity **critical**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/006_video_channel_genre.py`, `lexy-app/backend/migrations/versions/013_video_category_cleanup.py`, `lexy-app/backend/migrations/versions/015_channel_table.py`, `lexy-app/backend/migrations/versions/032_word_frequency.py`, `lexy-app/backend/migrations/versions/036_add_word_norm_for_unicode_autocomplete.py`, `docs/SCHEMA.md`, `docs/COMMON_ERRORS.md`
- symbols: `006_video_channel_genre.py:16-20`, `013_video_category_cleanup.py:upgrade:39-76`, `015_channel_table.py:upgrade:23-58`, `032_word_frequency.py:upgrade:42-70`, `036_add_word_norm_for_unicode_autocomplete.py:upgrade:73-101`
- evidence: Grepped CREATE TABLE across all 37 migration files plus subtitle-scraper for {video, video_category, user_video_category, word_table, sentence, language_table, word_to_sentence, phrase_blueprint}: zero matches — all 8 are only ever ALTERed (006:16, 013:39-76, 015:23-58, 032:42, 036:73,96). docs/SCHEMA.md:3-5 calls migrations/versions the schema's 'source of truth', which is false for these tables. COMMON_ERRORS.md:1074-1097 documents the same wall and that the deleted table.sql (25a194d) is a later snapshot conflicting with 013-022's own constraints if replayed verbatim, so recovering it as-is will not compose.
- impact: `alembic upgrade head` on any empty database fails at revision 006 (`relation "video" does not exist`); no fresh deploy, CI database, or new-contributor setup can build the schema from the migration chain alone.
- acceptance: createdb fresh_test && alembic upgrade head succeeds against an empty database; psql \dt lists all 8 content-core tables including user_video_category; docs/SCHEMA.md's 'source of truth' claim is either true or explicitly caveated
- validation: createdb fresh_test && cd lexy-app/backend && DB_NAME=fresh_test alembic upgrade head; psql fresh_test -c '\dt' | grep -E 'video|word_table|phrase_blueprint|user_video_category'

#### ingestion_pipeline:ing-02 — In the except PackageError branch, also call result.write(...) against the resolved package director
- severity **low**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/services/book_import_service.py`
- symbols: `book_import_service.import_package:136-150`
- evidence: import_package's `except PackageError` branch (book_import_service.py:136-150) returns build_result(...) without calling result.write(...), unlike the two sibling early-return branches at :165-170 and :183-195 which both call `result.write(pkg.package_root)` when write_result is True; the only test covering this path (test_document_package.py:981-986) sidesteps it by passing write_result=False.
- impact: Calling import_package(..., write_result=True) on a package whose directory exists but whose manifest/document JSON is malformed silently produces no import_result.json on disk, breaking the §4 promise that every import attempt is diffable via that file for that one failure class.
- acceptance: A test asserting import_result.json exists on disk after import_package(name, write_result=True) is called against a package with a malformed manifest.json or document.json
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k unreadable -q


## Security / data-loss risks (9)
#### security_paths:paths-02 — Implement real OS-level confinement for the write-capable agent subprocess (container, chroot, or fi
- severity **high**, confidence **confirmed**, domain `security_paths`
- files: `autoloop/audit/agents.py`, `autoloop/implement_executor.py`, `autoloop/escape_detector.py`
- symbols: `ClaudeCliRunner.build_argv:agents.py:104-119`
- evidence: docs/SECURITY.md S24 (open, HIGH). Re-verified on current HEAD: `build_argv` carries only `-p/--model/--output-format/--permission-mode/--allowedTools/--disallowedTools`; `rg -n '\-\-add-dir|sandbox|chroot|container' autoloop/audit/agents.py autoloop/implement_executor.py` still returns no match, matching the doc's own check. Confirming still open on this branch, not resolved — not a newly discovered defect.
- impact: A write-capable agent subprocess that misbehaves (bug or ignored instruction) can write anywhere the orchestrator process can write, including the primary checkout; escape_detector.py only detects after the fact and cannot catch a write reverted before its post-snapshot.
- acceptance: Write-capable agent subprocess cannot create/modify a file outside its assigned worker repo even when instructed to; escape_detector tests continue passing as defense-in-depth
- validation: pytest autoloop/tests/test_m1_hardening.py -k escape_detector -q

#### db_migrations:db-02 — Before deleting the JSONB keys, add an idempotent INSERT INTO user_video_category ... SELECT ... jso
- severity **high**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/014_drop_video_genre.py`, `lexy-app/backend/migrations/versions/013_video_category_cleanup.py`
- symbols: `014_drop_video_genre.py:1-7`, `014_drop_video_genre.py:upgrade:21-40`, `013_video_category_cleanup.py:upgrade:37-76`
- evidence: 014's docstring (lines 4-6) claims liked_genres/disliked_genres are 'migrated ... into user_video_category (best-effort)'; upgrade() (21-40) only adds a `preference` column (23-27) and deletes both JSONB keys from users.settings (32-36) — no INSERT INTO user_video_category exists here or in 013 (37-76, which only seeds/normalizes video_category from a fixed list). Compounds db-01: user_video_category has no CREATE TABLE anywhere in the chain either.
- impact: Any DB whose users.settings still holds liked_genres/disliked_genres when 014 runs (old backup restore, or any pre-014 environment) permanently loses that preference data with no backfill path, contradicting the migration's own docstring.
- acceptance: A DB seeded only with users.settings.liked_genres/disliked_genres has one user_video_category row per legacy genre after upgrading through 014, or the docstring no longer claims a migration happens
- validation: grep -n 'INSERT INTO user_video_category' lexy-app/backend/migrations/versions/014_drop_video_genre.py lexy-app/backend/migrations/versions/013_video_category_cleanup.py

#### tests_ci:pytest-001 — No action required unless pytest-randomly becomes a direct dependency. Ensure COMMON_ERRORS.md docum
- severity **medium**, confidence **confirmed**, domain `tests_ci`
- files: `pytest.ini`, `lexy-app/pytest.ini`
- symbols: `pytest.ini:10–15`, `lexy-app/pytest.ini:1–10`
- evidence: Both pytest.ini files carry `addopts = -p no:randomly` to block pytest-randomly (a plugin that causes seed overflow in thinc). This is correct and load-bearing per TESTS.md. However, the reason is an unmasked seed passed to entry points—a TRANSITIVE dependency issue (pytest-randomly 'arrived transitively; observed as 4.1.0' per TESTS.md line 16). The block is a workaround, not a fix. If pytest-randomly is later added as a direct dependency (or a new upstream adds it), this workaround still holds; if not, it costs nothing.
- impact: A developer who unknowingly installs pytest-randomly via a feature branch or a typo will run tests that error at setup/teardown, but the block in pytest.ini rescues them. This is defensive-good, but the condition is fragile.
- acceptance: COMMON_ERRORS.md includes an entry describing the pytest-randomly seed overflow and the `-p no:randomly` workaround
- validation: grep -n 'randomly\|pytest-randomly' docs/COMMON_ERRORS.md

#### security_paths:paths-01 — Add `Depends(require_admin)` to `GET /books/packages` and `POST /books/import` (or scope PACKAGE_ROO
- severity **medium**, confidence **confirmed**, domain `security_paths`
- files: `lexy-app/backend/routers/books.py`, `lexy-app/backend/services/document_package/persistence.py`, `lexy-app/backend/models/schemas.py`
- symbols: `books.list_packages:182-185`, `books.import_package:188-212`, `persistence.DryRunPersistence.persist:181-207`
- evidence: routers/books.py:183,191 gate both endpoints with only `Depends(get_current_user)` — no `require_admin`, unlike the analogous curated-content endpoint `POST /phrases/seed` which IS admin-gated. Any authenticated user can list every package name under the shared `PACKAGE_ROOT` and POST /books/import (dry_run defaults True) to see source_sha256, page/block counts and validation diagnostics for packages they did not upload.
- impact: Any signed-up user can enumerate and inspect metadata/validation state of every book package staged by the operator or other workflows; once roadmap A3 wires real persistence (already anticipated in code — see PackageImportRequest docstring, PendingSchemaPersistence), the same missing gate lets any user import someone else's staged package into their own book library with no curation step.
- acceptance: A non-admin user gets 403 from GET /books/packages and POST /books/import; Existing admin/dry-run tests still pass
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py tests/test_books_upload.py -q

#### security_paths:paths-03 — Require a per-process token (printed by `main()`, sent as a header) for POST /api/task, as already s
- severity **medium**, confidence **confirmed**, domain `security_paths`
- files: `autoloop/dashboard.py`
- symbols: `Handler.do_POST:911`, `Handler._submit_task:1010-1054`
- evidence: docs/SECURITY.md S28 (open, MEDIUM). Re-verified: `_submit_task` (dashboard.py:1010) still accepts `approved_paths` from any unauthenticated POST to the localhost dashboard with no per-process token, matching the doc's own description; confirming still open, not a new discovery.
- impact: Any local process that can reach 127.0.0.1:<port> (not just the operator's browser tab) can queue a task naming arbitrary approved_paths, which the loop drains and authorizes without asking the operator again.
- acceptance: POST /api/task without the correct token is rejected; Existing dashboard tests updated to send the token
- validation: pytest autoloop/tests/test_dashboard.py -q

#### db_migrations:db-03 — Have downgrade() refuse (or require an explicit force flag) when any is_system row exists, instead o
- severity **medium**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/037_system_word_lists.py`, `scripts/seed_system_lists.py`, `docs/ROADMAP.md`
- symbols: `037_system_word_lists.py:downgrade:81-89`, `scripts/seed_system_lists.py:1-49`
- evidence: downgrade() (line 87) runs an unconditional `DELETE FROM word_lists WHERE is_system` (cascades to word_list_items), justified by the migration's own docstring 'no system list is created here' (line 39) — true only until scripts/seed_system_lists.py --apply runs, a real in-repo idempotent script that creates exactly those is_system rows. docs/ROADMAP.md N5a states this script ran in production (2 lists/9,122 items); that specific count is an operational claim I can't verify from the repo, but the unconditional-DELETE-paired-with-a-real-populating-script hazard is confirmed by reading both files directly, independent of the production number.
- impact: Downgrading 037 on any database where scripts/seed_system_lists.py has been applied silently deletes all curated built-in vocabulary-list content with no guard, warning, or backup step.
- acceptance: downgrade() on a DB with seeded is_system rows refuses or requires explicit confirmation instead of silently deleting them; a regression test seeds an is_system row then asserts downgrade() raises
- validation: cat lexy-app/backend/migrations/versions/037_system_word_lists.py

#### db_migrations:db-04 — Make downgrade() refuse when any reading_selections row has a non-empty anchors array, or derive tok
- severity **medium**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/025_block_token_ids.py`, `lexy-app/backend/models/schemas.py`
- symbols: `025_block_token_ids.py:_tokenize:21-27`, `025_block_token_ids.py:downgrade:64-65`, `schemas.py:ReadingSelectionAnchor:704-707`
- evidence: _tokenize (21-27) assigns a fresh uuid.uuid4() per token on every invocation (not derived from block_id/position); downgrade() (64-65) is a bare `ALTER TABLE book_blocks DROP COLUMN tokens`, no guard. schemas.py:706 confirms ReadingSelectionAnchor.token_id ('replaced token_index for stability across edits') is exactly this UUID, with no FK tying reading_selections.anchors to book_blocks.tokens.
- impact: A downgrade-then-upgrade cycle of migration 025 regenerates every token_id, silently orphaning every existing reading_selections.anchors reference — the selection survives but its anchor no longer resolves, with no error surfaced anywhere.
- acceptance: downgrade() raises when orphaning anchors would occur, or a round-trip test proves token_id stability across downgrade+upgrade
- validation: cat lexy-app/backend/migrations/versions/025_block_token_ids.py

#### security_paths:paths-04 — Back the limiter with Redis (or the DB) before scaling past one worker, per the existing suggested f
- severity **low**, confidence **confirmed**, domain `security_paths`
- files: `lexy-app/backend/services/rate_limiter.py`
- symbols: `_windows:74`, `check_and_record:78`
- evidence: docs/SECURITY.md S12 (open, LOW). Confirmed still true: `_windows` is a plain module-level `defaultdict(deque)` guarded by an in-process `asyncio.Lock()` — no shared store.
- impact: With multiple uvicorn/gunicorn workers each worker has its own counters, so the effective LLM-abuse limit is N x the documented 30/min-400/hour, and resets on restart.
- acceptance: Rate limit is enforced consistently across N worker processes in a multi-worker deployment test
- validation: cd lexy-app/backend && python3 -m pytest tests/test_rate_limiter.py -q

#### db_migrations:db-05 — Emit a visible row count of deleted orphan rows during upgrade (RAISE NOTICE or a logged SELECT COUN
- severity **low**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/013_video_category_cleanup.py`
- symbols: `013_video_category_cleanup.py:upgrade:66-69`
- evidence: Step 5 (66-69) runs `DELETE FROM user_video_category WHERE video_category NOT IN (SELECT category FROM video_category)` unconditionally, immediately before adding the FK in step 6, with no row count logged or returned anywhere in the file.
- impact: Any user preference row referencing a category outside the 18 seeded YouTube categories (stale/renamed data from an earlier app version) is permanently deleted with no visibility into how many rows or which users were affected.
- acceptance: migration emits a visible count of deleted orphan rows during upgrade
- validation: cat lexy-app/backend/migrations/versions/013_video_category_cleanup.py


## Architectural risks (6)
#### repo_structure:arch-01 — Move the Docker build context to the repo root (COPY subtitle-scraper/ and data/ alongside backend/)
- severity **critical**, confidence **confirmed**, domain `repo_structure`
- files: `lexy-app/Dockerfile`, `lexy-app/backend/services/matcher_service.py`
- symbols: `Dockerfile:21-31`, `matcher_service.py:35`, `matcher_service.py:43`
- evidence: Dockerfile:28,31 COPY only backend/ and frontend/dist (WORKDIR=/app); matcher_service.py:35 computes Path(__file__).resolve().parents[3], which under the image resolves to '/', giving a nonexistent _SCRAPER_PATH; line 43 imports phrase_finder at module scope, unconditionally.
- impact: Any router importing matcher_service (phrases.py, matcher.py, chat.py) fails at app startup inside the built image, so the documented docker compose up path never binds a port.
- acceptance: docker compose up boots without crashing; a CI or manual smoke test gets a 2xx from a health endpoint after docker compose up
- validation: cd lexy-app && docker compose build && docker compose up -d && curl -f http://localhost:8000/api/v1/auth/login -X POST -d '{}' -H 'Content-Type: application/json'

#### repo_structure:arch-02 — On load, reconcile execution.worktree_path against a freshly-computed path: keep it if it still exis
- severity **high**, confidence **confirmed**, domain `repo_structure`
- files: `autoloop/orchestrator.py`, `autoloop/worker_env.py`
- symbols: `orchestrator.py:2017-2018,2042-2043,2843,2917`, `worker_env.WorkerRepoManager._path:305-310`
- evidence: A loaded TaskExecution's stored worktree_path is used verbatim to build GitGateway at 4 separate sites; WorkerRepoManager._path only recomputes root_dir/task_id fresh inside .create(). Nothing on the load path reconciles a stale stored path against the current [paths].workers_root.
- impact: An operator who relocates workers_root gets a bare FileNotFoundError on the loop's next dispatch of any in-flight task, with no diagnostic naming the mismatch.
- acceptance: moving workers_root with an in-flight task either resumes at the stored path or parks with an explicit message naming both paths, never a bare FileNotFoundError
- validation: python3 -m pytest autoloop/tests/test_postcommit_flow.py autoloop/tests/test_m1_hardening.py -q

#### repo_structure:arch-03 — Emit a transcript/log event when each agent starts (not only on completion), and surface per-domain 
- severity **high**, confidence **confirmed**, domain `repo_structure`
- files: `autoloop/audit/executor.py`
- symbols: `AuditExecutor._run_agents:521-529`
- evidence: _run_agents does list(pool.map(agent_runner.run, specs)) (line 527) and only writes to raw_dir in the loop starting line 528, after the WHOLE map call returns; no event is emitted when an individual agent starts.
- impact: For a fan-out taking tens of minutes, an operator watching an unattended audit run sees an empty raw/ directory and a silent log, then a burst on completion, unable to tell 'working' from 'wedged' without inspecting process state by hand.
- acceptance: a transcript event exists per agent start; status output during an audit run shows which domain agents have started vs finished
- validation: python3 -m pytest autoloop/tests/test_audit_executor.py -q

#### repo_structure:arch-04 — When N consecutive rounds produce a byte-identical validation summary, stop retrying and park as an 
- severity **medium**, confidence **confirmed**, domain `repo_structure`
- files: `autoloop/orchestrator.py`
- symbols: `consecutive_failures increments:3328,3355`, `policy.check_failure_budget:3333,3362`
- evidence: Grepped orchestrator.py for any comparison of successive validation outputs: none exists — every failure path only increments a plain consecutive_failures counter and checks it against a budget; docs/AUTOLOOP_TODO.md B6 records rt-01 burning 3 of 5 attempts on byte-identical environmental failures.
- impact: A validation failure that cannot be fixed by retrying (missing credentials, unreachable service) still consumes the task's finite attempt budget every round, until the task is wrongly exhausted instead of parked as an environmental blocker.
- acceptance: two consecutive rounds with identical validation output park as an environmental blocker without a third retry
- validation: python3 -m pytest autoloop/tests/test_m1_hardening.py -q

#### repo_structure:meta-01 — Loosen the cross-domain fold gate (treat security/data_loss as compatible categories, lower MERGE_SI
- severity **medium**, confidence **confirmed**, domain `repo_structure`
- files: `autoloop/audit/reconcile.py`, `docs/AUDIT_2026-08-05.md`
- symbols: `reconcile._is_duplicate_candidate:118-148`, `reconcile.MERGE_SIMILARITY:73`, `reconcile._describes_same_defect:101-115`
- evidence: docs/AUDIT_2026-08-05.md:40-56 and :139-146 carry three blocks for one missing-admin-gate defect on the same two books.py routes (repo_structure:sec-01, security_paths:sec-01, ingestion_pipeline:ing-sec-01 → tasks au-011/au-012/au-013); :58-74 carries two blocks for one 014-migration data-loss defect (au-002/au-003/au-007). Fold gate requires exact category equality (reconcile.py:143) plus >=0.7 Jaccard over impact+proposed_action tokens (reconcile.py:101-115); the only passing merge case, autoloop/tests/test_audit_reconcile.py:63-79, feeds byte-identical text rather than independent paraphrase.
- impact: Cross-domain agents paraphrase the same defect differently, so a gate tuned on identical strings under-fires, and the report plus task graph carry duplicate work items for a single underlying defect.
- acceptance: two findings describing the same route and defect in independently-paraphrased prose (not identical strings) fold into one finding via reconcile(); reconcile against a fixture equivalent to the 2026-08-05 raw findings yields one block, not three, for the books.py admin-gate defect; the same fixture yields one block, not two, for the 014-migration data-loss defect
- validation: python3 -m pytest autoloop/tests/test_audit_reconcile.py -q

#### tests_ci:ci-001 — Add a workflow that runs: (1) `cd lexy-app/backend && python3 -m pytest -n auto` (2) `cd lexy-app/fr
- severity **medium**, confidence **probable**, domain `tests_ci`
- files: `.github/workflows/dependency-audit.yml`
- symbols: `.github/workflows/dependency-audit.yml:1–53`
- evidence: .github/workflows/dependency-audit.yml only runs dependency audits (pip-audit + npm audit); there is no workflow that runs the test suites themselves (`pytest`, vitest, evaluation harness) or linting (ruff, tsc).
- impact: Broken code cannot be caught by CI; commits to main are not validated beyond dependency vulnerabilities. A test failure or lint violation can land silently.
- acceptance: CI workflow runs and reports (pass/fail) for at least one test suite on every push/PR to main
- validation: ls -la .github/workflows/ && grep -l 'pytest\|vitest\|ruff' .github/workflows/*.yml || echo 'no test CI found'


## Documentation drift (9)
#### docs_drift:doc_drift-01 — Update CLAUDE.md line 52 from '~916 YouTube channels' to '~190 YouTube channels' (or verify if the 9
- severity **medium**, confidence **confirmed**, domain `docs_drift`
- files: `CLAUDE.md`
- symbols: `CLAUDE.md:52`
- evidence: CLAUDE.md:52 claims 'Pulls transcripts for ~916 YouTube channels' but subtitle-scraper/seed_data/channels.json contains exactly 190 entries (verified via grep '"id":')
- impact: Developers reading CLAUDE.md will have incorrect expectations about the corpus size. The claim is 4.8× higher than the actual seed data.
- acceptance: CLAUDE.md line 52 channel count matches current seed_data/channels.json entry count; If 916 is from an old/live database state, clarify the distinction in the doc
- validation: grep -c '"id":' subtitle-scraper/seed_data/channels.json

#### tests_ci:test-002 — Re-run `pytest autoloop/tests` and update lines 1402/1414 with the current actual count and date, or
- severity **medium**, confidence **confirmed**, domain `tests_ci`
- files: `docs/TESTS.md`
- symbols: `docs/TESTS.md:1402,1414`
- evidence: docs/TESTS.md line 1402 claims autoloop suite is '790 passed, 1 skipped (~2m26s serial)' as of 2026-08-01, yet later admits (lines 1408–1412) this figure is 'now behind' due to additions on 2026-08-02. Line 1414 explicitly disclaims it: 'That 790 is a MEASURED figure from 2026-08-01 and is now behind...Deliberately not replaced with an arithmetic guess.'
- impact: Test count documentation is stale and known-stale; future readers cannot trust numeric claims without re-running the suite.
- acceptance: Either the 790 figure is replaced with a current measurement, or a prominent disclaimer at the start of the autoloop section flags all counts as historical
- validation: cd /repo root && pytest autoloop/tests -q | tail -1

#### repo_structure:doc-01 — Mark B9 in docs/AUTOLOOP_TODO.md as RESOLVED with a pointer to _rebase_execution_if_stale, matching 
- severity **medium**, confidence **confirmed**, domain `repo_structure`
- files: `docs/AUTOLOOP_TODO.md`, `autoloop/orchestrator.py`
- symbols: `AUTOLOOP_TODO.md:172-207 (B9)`, `orchestrator._rebase_execution_if_stale:3451-3453`
- evidence: AUTOLOOP_TODO.md:184 still describes B9 as an open bug with no resolved marker, and line 174 cites it as a live risk; orchestrator.py:3451-3453 already implements the fix it proposes, its own docstring citing 'docs/AUTOLOOP_TODO.md B9'.
- impact: The tracker misrepresents a shipped fix as an open bug, risking a duplicate task scheduled for already-resolved work and misleading anyone triaging AUTOLOOP_TODO.md by priority.
- acceptance: docs/AUTOLOOP_TODO.md B9 carries a resolved marker and cites orchestrator.py:_rebase_execution_if_stale
- validation: grep -n 'B9' docs/AUTOLOOP_TODO.md; rg -n '_rebase_execution_if_stale' autoloop/orchestrator.py

#### repo_structure:doc-03 — Add rows for routers/account.py and routers/errors.py to the routers table, and for services/srs_bac
- severity **medium**, confidence **confirmed**, domain `repo_structure`
- files: `docs/SUMMARY.md`, `lexy-app/backend/main.py`
- symbols: `SUMMARY.md:22-44 (routers table)`, `SUMMARY.md:46-83 (services table)`, `main.py:164-165`, `routers/account.py`, `routers/errors.py`, `services/srs_backfill_service.py`, `services/srs_cleanup_service.py`
- evidence: main.py:164-165 mounts errors_router and account_router (live endpoints: DELETE /api/v1/account, POST /api/v1/errors/client) but SUMMARY.md's router table (lines 22-44) has no row for either. srs_backfill_service.py and srs_cleanup_service.py are real, tested (test_srs_backfill.py, test_srs_cleanup.py), script-driven (scripts/backfill_missing_active_srs.py, scripts/cleanup_orphan_srs_cards.py), documented in docs/MAINTENANCE.md/ROADMAP.md but absent from SUMMARY.md's services table (lines 46-83).
- impact: CLAUDE.md §12 designates SUMMARY.md as the canonical 'where do I look to change X' index; a developer or agent scanning it for account deletion, client-error reporting, or SRS maintenance tooling finds nothing and either misses the existing code or re-implements it.
- acceptance: docs/SUMMARY.md's routers table lists account.py and errors.py; docs/SUMMARY.md's services table lists srs_backfill_service.py and srs_cleanup_service.py
- validation: grep -n 'account_router\|errors_router' docs/SUMMARY.md; grep -n 'srs_backfill_service\|srs_cleanup_service' docs/SUMMARY.md

#### ingestion_pipeline:ing-01 — Either add scripts/eval/extraction_stats.py implementing the doc's spec (unterminated/lowercase-star
- severity **medium**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `docs/INGESTION_PIPELINE.md`, `evaluation/baselines.py`
- symbols: `docs/INGESTION_PIPELINE.md §2 (line 36)`, `docs/INGESTION_PIPELINE.md §14 (lines 769-772)`, `evaluation/baselines.py:11-14`
- evidence: §2 (line 36) and §14 (lines 769-772) of docs/INGESTION_PIPELINE.md, plus evaluation/baselines.py:11-14, all reference `scripts/eval/extraction_stats.py` as the live, re-runnable harness reproducing 43.5%/21.6%/1964; repo-wide Glob/Grep for `extraction_stats.py` finds zero matches anywhere except these doc/comment references.
- impact: The documented 'zero-dependency, works today' regression gate that any pipeline change is supposed to pass before landing cannot actually be run, and evaluation/baselines.py's own comment relies on a script that isn't in the repo.
- acceptance: `python3 scripts/eval/extraction_stats.py files/json/*.json` exists and reproduces the cited figures, or the doc/comment no longer claims a script that isn't present
- validation: test -f scripts/eval/extraction_stats.py; rg -n 'scripts/eval/extraction_stats' docs/INGESTION_PIPELINE.md evaluation/baselines.py

#### tests_ci:test-003 — Update docs/TESTS.md lines 29–32 to say: 'NOT covered by a bare run...`tests/evaluation` — use `pyte
- severity **low**, confidence **confirmed**, domain `tests_ci`
- files: `docs/TESTS.md`, `pytest.ini`
- symbols: `docs/TESTS.md:29–32`, `pytest.ini:28–32`
- evidence: Both files claim that 'a bare `pytest` run' does not cover `lexy-app/backend/tests` or `lexy-app/frontend`, requiring separate commands — and that is correct. However, docs/TESTS.md claims evaluation tests are covered (line 943, 'tests now include...147 tests') but pytest.ini still excludes them from the default surface even after they exist and are documented.
- impact: Documentation accuracy: the cited default behavior in line 29–32 is partially incorrect for evaluation tests.
- acceptance: docs/TESTS.md accurately reflects which suites are/aren't covered by a bare `pytest` run
- validation: grep -n 'NOT covered by a bare run' docs/TESTS.md

#### tests_ci:ruff-001 — Run `ruff check . --show-files` and verify the output includes files under `autoloop/`, or update ru
- severity **low**, confidence **confirmed**, domain `tests_ci`
- files: `ruff.toml`, `docs/SUMMARY.md`
- symbols: `ruff.toml:7–10`, `docs/SUMMARY.md:333`
- evidence: ruff.toml lines 7–10 say the config 'covers the backend, the scraper, the root pipeline modules, scripts, masking and ilp in one pass', and that the rule set was pinned after a 2026-07-27 cleanup ('153 findings -> 0'). However, SUMMARY.md line 333 lists cli.py under autoloop/ as part of the checked files — verify ruff.toml's glob patterns actually reach all these locations.
- impact: Potential scope gap: ruff config claim may not match its actual file coverage; autoloop/ scope is not declared in ruff.toml.
- acceptance: ruff.toml documentation and actual `ruff check` output agree on scope
- validation: ruff check . --show-files | grep -E 'autoloop|cli.py'

#### repo_structure:doc-02 — Delete the stale row at SUMMARY.md:333, keeping the more complete row at 335.
- severity **low**, confidence **confirmed**, domain `repo_structure`
- files: `docs/SUMMARY.md`
- symbols: `SUMMARY.md:333`, `SUMMARY.md:335`
- evidence: Two separate table rows both keyed `prompts.py, config.py, cli.py` exist four lines apart (333 and 335, verified present at current HEAD); the row at 335 is materially newer (mentions blockers_dir, required workers_root, _RESOLUTION_PRECONDITIONS) but the older row at 333 was never removed.
- impact: A reader of the file-level index (CLAUDE.md §12 designates SUMMARY.md the canonical navigation aid) hits two entries for the same three files and cannot tell which is authoritative.
- acceptance: only one `prompts.py, config.py, cli.py` row remains in docs/SUMMARY.md
- validation: grep -n 'prompts.py.*config.py.*cli.py' docs/SUMMARY.md

#### security_paths:paths-07 — Update the three line-number citations in docs/SECURITY.md to match current search_service.py.
- severity **info**, confidence **confirmed**, domain `security_paths`
- files: `docs/SECURITY.md`, `lexy-app/backend/services/search_service.py`
- symbols: `search_service.py:164,205,322`
- evidence: docs/SECURITY.md:521 and its own verification grep (line 100) cite `search_service.py:62,103,216` for the accepted f-string-SQL pattern; the three matching f-string SELECTs are actually at lines 164, 205 and 322 on current HEAD (content itself re-verified safe — only a loop index and a module constant SIMILARITY_THRESHOLD are interpolated, no user data).
- impact: A future reader following the doc's own cited line numbers to re-verify the finding lands on unrelated code, undermining the file's 'every finding carries a verification check' guarantee.
- acceptance: docs/SECURITY.md's cited lines for the search_service f-string pattern match the actual file
- validation: rg -n 'f"(SELECT|INSERT|UPDATE|DELETE)' lexy-app/backend/services/search_service.py


## Missing regression protection (2)
#### tests_ci:test-001 — Add `tests/evaluation` to root `pytest.ini:testpaths` (change line 37 to `testpaths = tests autoloop
- severity **high**, confidence **confirmed**, domain `tests_ci`
- files: `pytest.ini`, `docs/TESTS.md`
- symbols: `pytest.ini:37`, `docs/TESTS.md:943,948`
- evidence: pytest.ini:37 declares `testpaths = tests autoloop/tests` but docs/TESTS.md §943–948 describes 147 evaluation-harness tests in `tests/evaluation/` with six test files; these tests are NOT collected by any bare `pytest` command at the repo root.
- impact: A bare `pytest` run omits ~147 tests despite docs claiming they are part of the suite; CI workflows or devs using the root pytest.ini never run this suite.
- acceptance: Bare `pytest` at repo root now collects tests from tests/evaluation/; Docs claim in TESTS.md remains accurate — 147 tests are now run as documented
- validation: pytest --collect-only | grep 'test_evaluate\|test_annotation\|test_integration' | wc -l

#### tests_ci:test-004 — Remove or correct the 'conftest adds `src/` to path' comment from lexy-app/pytest.ini, or verify it 
- severity **medium**, confidence **confirmed**, domain `tests_ci`
- files: `pytest.ini`, `docs/TESTS.md`
- symbols: `pytest.ini:14–15`, `docs/TESTS.md:37`
- evidence: pytest.ini at root testpaths declares `tests autoloop/tests` but lexy-app/pytest.ini separately declares `testpaths = backend/tests` (line 14). Root conftest.py does not exist; when running a bare `pytest` from the repository root, the root-level ini's `testpaths` is used, not the backend's. This is correct. However, a Python test file under `tests/` cannot import backend modules without the cwd being managed — see lexy-app/pytest.ini comment: 'conftest adds `src/` to path' — the `src/app/` tree that was deleted 2026-07-27.
- impact: Residual dead comment mentioning deleted code path; no functional impact but confuses future readers.
- acceptance: lexy-app/pytest.ini comments accurately reflect current conftest.py behavior
- validation: grep -n 'sys.path\|conftest' lexy-app/pytest.ini


## Optional improvements (1)
#### ingestion_pipeline:ing-03 — Add a docs/TODO.md entry tracking the import-alignment follow-up, or change the file's imports to th
- severity **low**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/tests/test_document_package.py`, `docs/TODO.md`
- symbols: `test_document_package.py:13`
- evidence: test_document_package.py:13 uses a bare `from services import ...` import, the only backend test file with that convention (docs/COMMON_ERRORS.md:320-337); COMMON_ERRORS.md names aligning it to the `backend.services` convention a proposed follow-up task, but docs/TODO.md has no matching entry.
- impact: Anyone running the bare `pytest` console script in lexy-app/backend silently collects 1151 instead of 1258 tests, and the known fix has no tracked owner so it gets rediscovered from scratch.
- acceptance: docs/TODO.md has an entry for test_document_package.py's import convention, or bare `pytest --collect-only` in lexy-app/backend collects 1258 tests
- validation: cd lexy-app/backend && pytest --collect-only -q | tail -5; cd lexy-app/backend && python3 -m pytest --collect-only -q | tail -5


## Human decisions needed (2)
#### security_paths:paths-05 — Product decision needed: set REGISTRATION_CODE by default in production config, or add CAPTCHA/email
- severity **medium**, confidence **confirmed**, domain `security_paths`
- files: `lexy-app/backend/services/auth_service.py`
- symbols: `register_user`
- evidence: docs/SECURITY.md S2 residual. Confirmed on current HEAD: `os.getenv("REGISTRATION_CODE", "")` (auth_service.py:38) is empty by default, so signup stays open unless an operator sets it. Documented deferred-by-design, not a newly discovered gap — noted here to keep it on the task graph, not to reclaim it as new.
- impact: Without REGISTRATION_CODE set, an attacker can script account creation (throttled only 5/hr/IP) to multiply the per-user LLM budget.
- acceptance: A written decision is recorded in docs/SECURITY_ARCHITECTURE_DECISIONS.md and, if closing, implemented
- validation: rg -n 'REGISTRATION_CODE' lexy-app/backend/services/auth_service.py

#### security_paths:paths-06 — Product decision needed: add a `token_version` column checked in decode_token (or short-lived access
- severity **medium**, confidence **confirmed**, domain `security_paths`
- files: `lexy-app/backend/routers/account.py`, `lexy-app/backend/core/security.py`
- symbols: `delete_account:38-63`
- evidence: docs/SECURITY.md S3 residual. Confirmed: `delete_account` (account.py:63) requires `verify_password`, but `rg -n token_version lexy-app/backend/core/security.py` finds nothing — no revocation mechanism exists for the 7-day JWT. Documented deferred-by-design, not newly discovered.
- impact: A leaked bearer token still performs any non-destructive action (read data, change settings, chat) until natural 7-day expiry; only account deletion is blocked by the password re-auth.
- acceptance: A written decision is recorded and, if closing, a revoked token is rejected by decode_token before its natural expiry
- validation: rg -n 'verify_password' lexy-app/backend/routers/account.py


## Rejected items (0)
(none)

## Proposed task graph (29 tasks)
To adopt this graph, reply with a `plan` decision containing the tasks below (edit freely — this is a proposal):
```json
[
  {
    "id": "au-001",
    "title": "Author a new baseline migration (not a verbatim table.sql replay, which conflicts with 013-022's ...",
    "description": "Priority: P1 | severity critical | confidence confirmed | from audit finding db_migrations:db-01\nScope: Author a new baseline migration (not a verbatim table.sql replay, which conflicts with 013-022's constraints) that CREATEs video/video_category/user_video_category/word_table/sentence/language_table/word_to_sentence/phrase_blueprint.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: `alembic upgrade head` on any empty database fails at revision 006 (`relation \"video\" does not exist`); no fresh deploy, CI database, or new-contributor setup can build the schema from the migration chain alone.\nEvidence: Grepped CREATE TABLE across all 37 migration files plus subtitle-scraper for {video, video_category, user_video_category, word_table, sentence, language_table, word_to_sentence, phrase_blueprint}: zero matches \u2014 all 8 are only ever ALTERed (006:16, 013:39-76, 015:23-58, 032:42, 036:73,96). docs/SCHEMA.md:3-5 calls migrations/versions the schema's 'source of truth', which is false for these tables. COMMON_ERRORS.md:1074-1097 documents the same wall and that the deleted table.sql (25a194d) is a later snapshot conflicting with 013-022's own constraints if replayed verbatim, so recovering it as-is will not compose.\nExpected files: lexy-app/backend/migrations/versions/006_video_channel_genre.py, lexy-app/backend/migrations/versions/013_video_category_cleanup.py, lexy-app/backend/migrations/versions/015_channel_table.py, lexy-app/backend/migrations/versions/032_word_frequency.py, lexy-app/backend/migrations/versions/036_add_word_norm_for_unicode_autocomplete.py, docs/SCHEMA.md, docs/COMMON_ERRORS.md\nAcceptance criteria: createdb fresh_test && alembic upgrade head succeeds against an empty database; psql \\dt lists all 8 content-core tables including user_video_category; docs/SCHEMA.md's 'source of truth' claim is either true or explicitly caveated\nValidation: createdb fresh_test && cd lexy-app/backend && DB_NAME=fresh_test alembic upgrade head; psql fresh_test -c '\\dt' | grep -E 'video|word_table|phrase_blueprint|user_video_category'\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-002",
    "title": "Before deleting the JSONB keys, add an idempotent INSERT INTO user_video_category ... SELECT ... ...",
    "description": "Priority: P1 | severity high | confidence confirmed | from audit finding db_migrations:db-02\nScope: Before deleting the JSONB keys, add an idempotent INSERT INTO user_video_category ... SELECT ... jsonb_array_elements_text(settings->'liked_genres'/'disliked_genres') ON CONFLICT DO NOTHING, or correct the docstring to state the data is dropped.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any DB whose users.settings still holds liked_genres/disliked_genres when 014 runs (old backup restore, or any pre-014 environment) permanently loses that preference data with no backfill path, contradicting the migration's own docstring.\nEvidence: 014's docstring (lines 4-6) claims liked_genres/disliked_genres are 'migrated ... into user_video_category (best-effort)'; upgrade() (21-40) only adds a `preference` column (23-27) and deletes both JSONB keys from users.settings (32-36) \u2014 no INSERT INTO user_video_category exists here or in 013 (37-76, which only seeds/normalizes video_category from a fixed list). Compounds db-01: user_video_category has no CREATE TABLE anywhere in the chain either.\nExpected files: lexy-app/backend/migrations/versions/014_drop_video_genre.py, lexy-app/backend/migrations/versions/013_video_category_cleanup.py\nAcceptance criteria: A DB seeded only with users.settings.liked_genres/disliked_genres has one user_video_category row per legacy genre after upgrading through 014, or the docstring no longer claims a migration happens\nValidation: grep -n 'INSERT INTO user_video_category' lexy-app/backend/migrations/versions/014_drop_video_genre.py lexy-app/backend/migrations/versions/013_video_category_cleanup.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-003",
    "title": "Have downgrade() refuse (or require an explicit force flag) when any is_system row exists, instea...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding db_migrations:db-03\nScope: Have downgrade() refuse (or require an explicit force flag) when any is_system row exists, instead of deleting unconditionally.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Downgrading 037 on any database where scripts/seed_system_lists.py has been applied silently deletes all curated built-in vocabulary-list content with no guard, warning, or backup step.\nEvidence: downgrade() (line 87) runs an unconditional `DELETE FROM word_lists WHERE is_system` (cascades to word_list_items), justified by the migration's own docstring 'no system list is created here' (line 39) \u2014 true only until scripts/seed_system_lists.py --apply runs, a real in-repo idempotent script that creates exactly those is_system rows. docs/ROADMAP.md N5a states this script ran in production (2 lists/9,122 items); that specific count is an operational claim I can't verify from the repo, but the unconditional-DELETE-paired-with-a-real-populating-script hazard is confirmed by reading both files directly, independent of the production number.\nExpected files: lexy-app/backend/migrations/versions/037_system_word_lists.py, scripts/seed_system_lists.py, docs/ROADMAP.md\nAcceptance criteria: downgrade() on a DB with seeded is_system rows refuses or requires explicit confirmation instead of silently deleting them; a regression test seeds an is_system row then asserts downgrade() raises\nValidation: cat lexy-app/backend/migrations/versions/037_system_word_lists.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-004",
    "title": "Make downgrade() refuse when any reading_selections row has a non-empty anchors array, or derive ...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding db_migrations:db-04\nScope: Make downgrade() refuse when any reading_selections row has a non-empty anchors array, or derive token_id deterministically (hash of block_id + token index) so re-tokenizing is idempotent across a round-trip.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A downgrade-then-upgrade cycle of migration 025 regenerates every token_id, silently orphaning every existing reading_selections.anchors reference \u2014 the selection survives but its anchor no longer resolves, with no error surfaced anywhere.\nEvidence: _tokenize (21-27) assigns a fresh uuid.uuid4() per token on every invocation (not derived from block_id/position); downgrade() (64-65) is a bare `ALTER TABLE book_blocks DROP COLUMN tokens`, no guard. schemas.py:706 confirms ReadingSelectionAnchor.token_id ('replaced token_index for stability across edits') is exactly this UUID, with no FK tying reading_selections.anchors to book_blocks.tokens.\nExpected files: lexy-app/backend/migrations/versions/025_block_token_ids.py, lexy-app/backend/models/schemas.py\nAcceptance criteria: downgrade() raises when orphaning anchors would occur, or a round-trip test proves token_id stability across downgrade+upgrade\nValidation: cat lexy-app/backend/migrations/versions/025_block_token_ids.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-005",
    "title": "No action required unless pytest-randomly becomes a direct dependency. Ensure COMMON_ERRORS.md do...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding tests_ci:pytest-001\nScope: No action required unless pytest-randomly becomes a direct dependency. Ensure COMMON_ERRORS.md documents this trap (does it?) so future triage knows to check ruff.ini/pytest.ini for `-p no:randomly`.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A developer who unknowingly installs pytest-randomly via a feature branch or a typo will run tests that error at setup/teardown, but the block in pytest.ini rescues them. This is defensive-good, but the condition is fragile.\nEvidence: Both pytest.ini files carry `addopts = -p no:randomly` to block pytest-randomly (a plugin that causes seed overflow in thinc). This is correct and load-bearing per TESTS.md. However, the reason is an unmasked seed passed to entry points\u2014a TRANSITIVE dependency issue (pytest-randomly 'arrived transitively; observed as 4.1.0' per TESTS.md line 16). The block is a workaround, not a fix. If pytest-randomly is later added as a direct dependency (or a new upstream adds it), this workaround still holds; if not, it costs nothing.\nExpected files: pytest.ini, lexy-app/pytest.ini\nAcceptance criteria: COMMON_ERRORS.md includes an entry describing the pytest-randomly seed overflow and the `-p no:randomly` workaround\nValidation: grep -n 'randomly\\|pytest-randomly' docs/COMMON_ERRORS.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-006",
    "title": "Emit a visible row count of deleted orphan rows during upgrade (RAISE NOTICE or a logged SELECT C...",
    "description": "Priority: P1 | severity low | confidence confirmed | from audit finding db_migrations:db-05\nScope: Emit a visible row count of deleted orphan rows during upgrade (RAISE NOTICE or a logged SELECT COUNT(*) before the DELETE).\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any user preference row referencing a category outside the 18 seeded YouTube categories (stale/renamed data from an earlier app version) is permanently deleted with no visibility into how many rows or which users were affected.\nEvidence: Step 5 (66-69) runs `DELETE FROM user_video_category WHERE video_category NOT IN (SELECT category FROM video_category)` unconditionally, immediately before adding the FK in step 6, with no row count logged or returned anywhere in the file.\nExpected files: lexy-app/backend/migrations/versions/013_video_category_cleanup.py\nAcceptance criteria: migration emits a visible count of deleted orphan rows during upgrade\nValidation: cat lexy-app/backend/migrations/versions/013_video_category_cleanup.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-007",
    "title": "In the except PackageError branch, also call result.write(...) against the resolved package direc...",
    "description": "Priority: P1 | severity low | confidence confirmed | from audit finding ingestion_pipeline:ing-02\nScope: In the except PackageError branch, also call result.write(...) against the resolved package directory (guarding for the case where the directory itself doesn't exist) before returning, mirroring the function's other two early-return paths.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Calling import_package(..., write_result=True) on a package whose directory exists but whose manifest/document JSON is malformed silently produces no import_result.json on disk, breaking the \u00a74 promise that every import attempt is diffable via that file for that one failure class.\nEvidence: import_package's `except PackageError` branch (book_import_service.py:136-150) returns build_result(...) without calling result.write(...), unlike the two sibling early-return branches at :165-170 and :183-195 which both call `result.write(pkg.package_root)` when write_result is True; the only test covering this path (test_document_package.py:981-986) sidesteps it by passing write_result=False.\nExpected files: lexy-app/backend/services/book_import_service.py\nAcceptance criteria: A test asserting import_result.json exists on disk after import_package(name, write_result=True) is called against a package with a malformed manifest.json or document.json\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k unreadable -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-008",
    "title": "Implement real OS-level confinement for the write-capable agent subprocess (container, chroot, or...",
    "description": "Priority: P2 | severity high | confidence confirmed | from audit finding security_paths:paths-02\nScope: Implement real OS-level confinement for the write-capable agent subprocess (container, chroot, or filesystem namespace with only the worker repo writable), per the suggested fix already recorded in docs/SECURITY.md S24.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A write-capable agent subprocess that misbehaves (bug or ignored instruction) can write anywhere the orchestrator process can write, including the primary checkout; escape_detector.py only detects after the fact and cannot catch a write reverted before its post-snapshot.\nEvidence: docs/SECURITY.md S24 (open, HIGH). Re-verified on current HEAD: `build_argv` carries only `-p/--model/--output-format/--permission-mode/--allowedTools/--disallowedTools`; `rg -n '\\-\\-add-dir|sandbox|chroot|container' autoloop/audit/agents.py autoloop/implement_executor.py` still returns no match, matching the doc's own check. Confirming still open on this branch, not resolved \u2014 not a newly discovered defect.\nExpected files: autoloop/audit/agents.py, autoloop/implement_executor.py, autoloop/escape_detector.py\nAcceptance criteria: Write-capable agent subprocess cannot create/modify a file outside its assigned worker repo even when instructed to; escape_detector tests continue passing as defense-in-depth\nValidation: pytest autoloop/tests/test_m1_hardening.py -k escape_detector -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-009",
    "title": "Add `Depends(require_admin)` to `GET /books/packages` and `POST /books/import` (or scope PACKAGE_...",
    "description": "Priority: P2 | severity medium | confidence confirmed | from audit finding security_paths:paths-01\nScope: Add `Depends(require_admin)` to `GET /books/packages` and `POST /books/import` (or scope PACKAGE_ROOT contents per-user) before A3 makes import write real rows.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any signed-up user can enumerate and inspect metadata/validation state of every book package staged by the operator or other workflows; once roadmap A3 wires real persistence (already anticipated in code \u2014 see PackageImportRequest docstring, PendingSchemaPersistence), the same missing gate lets any user import someone else's staged package into their own book library with no curation step.\nEvidence: routers/books.py:183,191 gate both endpoints with only `Depends(get_current_user)` \u2014 no `require_admin`, unlike the analogous curated-content endpoint `POST /phrases/seed` which IS admin-gated. Any authenticated user can list every package name under the shared `PACKAGE_ROOT` and POST /books/import (dry_run defaults True) to see source_sha256, page/block counts and validation diagnostics for packages they did not upload.\nExpected files: lexy-app/backend/routers/books.py, lexy-app/backend/services/document_package/persistence.py, lexy-app/backend/models/schemas.py\nAcceptance criteria: A non-admin user gets 403 from GET /books/packages and POST /books/import; Existing admin/dry-run tests still pass\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py tests/test_books_upload.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-010",
    "title": "Require a per-process token (printed by `main()`, sent as a header) for POST /api/task, as alread...",
    "description": "Priority: P2 | severity medium | confidence confirmed | from audit finding security_paths:paths-03\nScope: Require a per-process token (printed by `main()`, sent as a header) for POST /api/task, as already suggested in docs/SECURITY.md S28.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any local process that can reach 127.0.0.1:<port> (not just the operator's browser tab) can queue a task naming arbitrary approved_paths, which the loop drains and authorizes without asking the operator again.\nEvidence: docs/SECURITY.md S28 (open, MEDIUM). Re-verified: `_submit_task` (dashboard.py:1010) still accepts `approved_paths` from any unauthenticated POST to the localhost dashboard with no per-process token, matching the doc's own description; confirming still open, not a new discovery.\nExpected files: autoloop/dashboard.py\nAcceptance criteria: POST /api/task without the correct token is rejected; Existing dashboard tests updated to send the token\nValidation: pytest autoloop/tests/test_dashboard.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-011",
    "title": "Back the limiter with Redis (or the DB) before scaling past one worker, per the existing suggeste...",
    "description": "Priority: P2 | severity low | confidence confirmed | from audit finding security_paths:paths-04\nScope: Back the limiter with Redis (or the DB) before scaling past one worker, per the existing suggested fix.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: With multiple uvicorn/gunicorn workers each worker has its own counters, so the effective LLM-abuse limit is N x the documented 30/min-400/hour, and resets on restart.\nEvidence: docs/SECURITY.md S12 (open, LOW). Confirmed still true: `_windows` is a plain module-level `defaultdict(deque)` guarded by an in-process `asyncio.Lock()` \u2014 no shared store.\nExpected files: lexy-app/backend/services/rate_limiter.py\nAcceptance criteria: Rate limit is enforced consistently across N worker processes in a multi-worker deployment test\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_rate_limiter.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-012",
    "title": "Move the Docker build context to the repo root (COPY subtitle-scraper/ and data/ alongside backen...",
    "description": "Priority: P4 | severity critical | confidence confirmed | from audit finding repo_structure:arch-01\nScope: Move the Docker build context to the repo root (COPY subtitle-scraper/ and data/ alongside backend/) and fix the parents[N] arithmetic for the new WORKDIR, or document Docker as unsupported.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any router importing matcher_service (phrases.py, matcher.py, chat.py) fails at app startup inside the built image, so the documented docker compose up path never binds a port.\nEvidence: Dockerfile:28,31 COPY only backend/ and frontend/dist (WORKDIR=/app); matcher_service.py:35 computes Path(__file__).resolve().parents[3], which under the image resolves to '/', giving a nonexistent _SCRAPER_PATH; line 43 imports phrase_finder at module scope, unconditionally.\nExpected files: lexy-app/Dockerfile, lexy-app/backend/services/matcher_service.py\nAcceptance criteria: docker compose up boots without crashing; a CI or manual smoke test gets a 2xx from a health endpoint after docker compose up\nValidation: cd lexy-app && docker compose build && docker compose up -d && curl -f http://localhost:8000/api/v1/auth/login -X POST -d '{}' -H 'Content-Type: application/json'\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-013",
    "title": "On load, reconcile execution.worktree_path against a freshly-computed path: keep it if it still e...",
    "description": "Priority: P4 | severity high | confidence confirmed | from audit finding repo_structure:arch-02\nScope: On load, reconcile execution.worktree_path against a freshly-computed path: keep it if it still exists, adopt the recomputed one if not, otherwise park naming both paths.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: An operator who relocates workers_root gets a bare FileNotFoundError on the loop's next dispatch of any in-flight task, with no diagnostic naming the mismatch.\nEvidence: A loaded TaskExecution's stored worktree_path is used verbatim to build GitGateway at 4 separate sites; WorkerRepoManager._path only recomputes root_dir/task_id fresh inside .create(). Nothing on the load path reconciles a stale stored path against the current [paths].workers_root.\nExpected files: autoloop/orchestrator.py, autoloop/worker_env.py\nAcceptance criteria: moving workers_root with an in-flight task either resumes at the stored path or parks with an explicit message naming both paths, never a bare FileNotFoundError\nValidation: python3 -m pytest autoloop/tests/test_postcommit_flow.py autoloop/tests/test_m1_hardening.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-014",
    "title": "Emit a transcript/log event when each agent starts (not only on completion), and surface per-doma...",
    "description": "Priority: P4 | severity high | confidence confirmed | from audit finding repo_structure:arch-03\nScope: Emit a transcript/log event when each agent starts (not only on completion), and surface per-domain started/finished state in status output.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: For a fan-out taking tens of minutes, an operator watching an unattended audit run sees an empty raw/ directory and a silent log, then a burst on completion, unable to tell 'working' from 'wedged' without inspecting process state by hand.\nEvidence: _run_agents does list(pool.map(agent_runner.run, specs)) (line 527) and only writes to raw_dir in the loop starting line 528, after the WHOLE map call returns; no event is emitted when an individual agent starts.\nExpected files: autoloop/audit/executor.py\nAcceptance criteria: a transcript event exists per agent start; status output during an audit run shows which domain agents have started vs finished\nValidation: python3 -m pytest autoloop/tests/test_audit_executor.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-015",
    "title": "When N consecutive rounds produce a byte-identical validation summary, stop retrying and park as ...",
    "description": "Priority: P4 | severity medium | confidence confirmed | from audit finding repo_structure:arch-04\nScope: When N consecutive rounds produce a byte-identical validation summary, stop retrying and park as an environmental blocker instead of charging the attempt count.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A validation failure that cannot be fixed by retrying (missing credentials, unreachable service) still consumes the task's finite attempt budget every round, until the task is wrongly exhausted instead of parked as an environmental blocker.\nEvidence: Grepped orchestrator.py for any comparison of successive validation outputs: none exists \u2014 every failure path only increments a plain consecutive_failures counter and checks it against a budget; docs/AUTOLOOP_TODO.md B6 records rt-01 burning 3 of 5 attempts on byte-identical environmental failures.\nExpected files: autoloop/orchestrator.py\nAcceptance criteria: two consecutive rounds with identical validation output park as an environmental blocker without a third retry\nValidation: python3 -m pytest autoloop/tests/test_m1_hardening.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-016",
    "title": "Loosen the cross-domain fold gate (treat security/data_loss as compatible categories, lower MERGE...",
    "description": "Priority: P4 | severity medium | confidence confirmed | from audit finding repo_structure:meta-01\nScope: Loosen the cross-domain fold gate (treat security/data_loss as compatible categories, lower MERGE_SIMILARITY, and/or weight title+location signals) and cover it with a paraphrase-based test.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Cross-domain agents paraphrase the same defect differently, so a gate tuned on identical strings under-fires, and the report plus task graph carry duplicate work items for a single underlying defect.\nEvidence: docs/AUDIT_2026-08-05.md:40-56 and :139-146 carry three blocks for one missing-admin-gate defect on the same two books.py routes (repo_structure:sec-01, security_paths:sec-01, ingestion_pipeline:ing-sec-01 \u2192 tasks au-011/au-012/au-013); :58-74 carries two blocks for one 014-migration data-loss defect (au-002/au-003/au-007). Fold gate requires exact category equality (reconcile.py:143) plus >=0.7 Jaccard over impact+proposed_action tokens (reconcile.py:101-115); the only passing merge case, autoloop/tests/test_audit_reconcile.py:63-79, feeds byte-identical text rather than independent paraphrase.\nExpected files: autoloop/audit/reconcile.py, docs/AUDIT_2026-08-05.md\nAcceptance criteria: two findings describing the same route and defect in independently-paraphrased prose (not identical strings) fold into one finding via reconcile(); reconcile against a fixture equivalent to the 2026-08-05 raw findings yields one block, not three, for the books.py admin-gate defect; the same fixture yields one block, not two, for the 014-migration data-loss defect\nValidation: python3 -m pytest autoloop/tests/test_audit_reconcile.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-017",
    "title": "Add a workflow that runs: (1) `cd lexy-app/backend && python3 -m pytest -n auto` (2) `cd lexy-app...",
    "description": "Priority: P4 | severity medium | confidence probable | from audit finding tests_ci:ci-001\nScope: Add a workflow that runs: (1) `cd lexy-app/backend && python3 -m pytest -n auto` (2) `cd lexy-app/frontend && npx vitest run` (3) `pytest tests/ autoloop/tests tests/evaluation` (4) `ruff check .` (5) `cd lexy-app/frontend && npx tsc --noEmit`.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Broken code cannot be caught by CI; commits to main are not validated beyond dependency vulnerabilities. A test failure or lint violation can land silently.\nEvidence: .github/workflows/dependency-audit.yml only runs dependency audits (pip-audit + npm audit); there is no workflow that runs the test suites themselves (`pytest`, vitest, evaluation harness) or linting (ruff, tsc).\nExpected files: .github/workflows/dependency-audit.yml\nAcceptance criteria: CI workflow runs and reports (pass/fail) for at least one test suite on every push/PR to main\nValidation: ls -la .github/workflows/ && grep -l 'pytest\\|vitest\\|ruff' .github/workflows/*.yml || echo 'no test CI found'\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-018",
    "title": "Add `tests/evaluation` to root `pytest.ini:testpaths` (change line 37 to `testpaths = tests autol...",
    "description": "Priority: P5 | severity high | confidence confirmed | from audit finding tests_ci:test-001\nScope: Add `tests/evaluation` to root `pytest.ini:testpaths` (change line 37 to `testpaths = tests autoloop/tests tests/evaluation`) so bare `pytest` collects all three trees.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A bare `pytest` run omits ~147 tests despite docs claiming they are part of the suite; CI workflows or devs using the root pytest.ini never run this suite.\nEvidence: pytest.ini:37 declares `testpaths = tests autoloop/tests` but docs/TESTS.md \u00a7943\u2013948 describes 147 evaluation-harness tests in `tests/evaluation/` with six test files; these tests are NOT collected by any bare `pytest` command at the repo root.\nExpected files: pytest.ini, docs/TESTS.md\nAcceptance criteria: Bare `pytest` at repo root now collects tests from tests/evaluation/; Docs claim in TESTS.md remains accurate \u2014 147 tests are now run as documented\nValidation: pytest --collect-only | grep 'test_evaluate\\|test_annotation\\|test_integration' | wc -l\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-019",
    "title": "Remove or correct the 'conftest adds `src/` to path' comment from lexy-app/pytest.ini, or verify ...",
    "description": "Priority: P5 | severity medium | confidence confirmed | from audit finding tests_ci:test-004\nScope: Remove or correct the 'conftest adds `src/` to path' comment from lexy-app/pytest.ini, or verify it is still needed and document what it actually adds.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Residual dead comment mentioning deleted code path; no functional impact but confuses future readers.\nEvidence: pytest.ini at root testpaths declares `tests autoloop/tests` but lexy-app/pytest.ini separately declares `testpaths = backend/tests` (line 14). Root conftest.py does not exist; when running a bare `pytest` from the repository root, the root-level ini's `testpaths` is used, not the backend's. This is correct. However, a Python test file under `tests/` cannot import backend modules without the cwd being managed \u2014 see lexy-app/pytest.ini comment: 'conftest adds `src/` to path' \u2014 the `src/app/` tree that was deleted 2026-07-27.\nExpected files: pytest.ini, docs/TESTS.md\nAcceptance criteria: lexy-app/pytest.ini comments accurately reflect current conftest.py behavior\nValidation: grep -n 'sys.path\\|conftest' lexy-app/pytest.ini\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-020",
    "title": "Add a docs/TODO.md entry tracking the import-alignment follow-up, or change the file's imports to...",
    "description": "Priority: P6 | severity low | confidence confirmed | from audit finding ingestion_pipeline:ing-03\nScope: Add a docs/TODO.md entry tracking the import-alignment follow-up, or change the file's imports to the `backend.services` convention used by every other backend test.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Anyone running the bare `pytest` console script in lexy-app/backend silently collects 1151 instead of 1258 tests, and the known fix has no tracked owner so it gets rediscovered from scratch.\nEvidence: test_document_package.py:13 uses a bare `from services import ...` import, the only backend test file with that convention (docs/COMMON_ERRORS.md:320-337); COMMON_ERRORS.md names aligning it to the `backend.services` convention a proposed follow-up task, but docs/TODO.md has no matching entry.\nExpected files: lexy-app/backend/tests/test_document_package.py, docs/TODO.md\nAcceptance criteria: docs/TODO.md has an entry for test_document_package.py's import convention, or bare `pytest --collect-only` in lexy-app/backend collects 1258 tests\nValidation: cd lexy-app/backend && pytest --collect-only -q | tail -5; cd lexy-app/backend && python3 -m pytest --collect-only -q | tail -5\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-021",
    "title": "Update CLAUDE.md line 52 from '~916 YouTube channels' to '~190 YouTube channels' (or verify if th...",
    "description": "Priority: P7 | severity medium | confidence confirmed | from audit finding docs_drift:doc_drift-01\nScope: Update CLAUDE.md line 52 from '~916 YouTube channels' to '~190 YouTube channels' (or verify if the 916 figure is from a different data source or previous state that should be retained).\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Developers reading CLAUDE.md will have incorrect expectations about the corpus size. The claim is 4.8\u00d7 higher than the actual seed data.\nEvidence: CLAUDE.md:52 claims 'Pulls transcripts for ~916 YouTube channels' but subtitle-scraper/seed_data/channels.json contains exactly 190 entries (verified via grep '\"id\":')\nExpected files: CLAUDE.md\nAcceptance criteria: CLAUDE.md line 52 channel count matches current seed_data/channels.json entry count; If 916 is from an old/live database state, clarify the distinction in the doc\nValidation: grep -c '\"id\":' subtitle-scraper/seed_data/channels.json\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-022",
    "title": "Either add scripts/eval/extraction_stats.py implementing the doc's spec (unterminated/lowercase-s...",
    "description": "Priority: P7 | severity medium | confidence confirmed | from audit finding ingestion_pipeline:ing-01\nScope: Either add scripts/eval/extraction_stats.py implementing the doc's spec (unterminated/lowercase-start/bare-number/label-distribution stats over files/json/*_layout.json), or update \u00a72/\u00a714 and the baselines.py comment to name the actual mechanism.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: The documented 'zero-dependency, works today' regression gate that any pipeline change is supposed to pass before landing cannot actually be run, and evaluation/baselines.py's own comment relies on a script that isn't in the repo.\nEvidence: \u00a72 (line 36) and \u00a714 (lines 769-772) of docs/INGESTION_PIPELINE.md, plus evaluation/baselines.py:11-14, all reference `scripts/eval/extraction_stats.py` as the live, re-runnable harness reproducing 43.5%/21.6%/1964; repo-wide Glob/Grep for `extraction_stats.py` finds zero matches anywhere except these doc/comment references.\nExpected files: docs/INGESTION_PIPELINE.md, evaluation/baselines.py\nAcceptance criteria: `python3 scripts/eval/extraction_stats.py files/json/*.json` exists and reproduces the cited figures, or the doc/comment no longer claims a script that isn't present\nValidation: test -f scripts/eval/extraction_stats.py; rg -n 'scripts/eval/extraction_stats' docs/INGESTION_PIPELINE.md evaluation/baselines.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-023",
    "title": "Mark B9 in docs/AUTOLOOP_TODO.md as RESOLVED with a pointer to _rebase_execution_if_stale, matchi...",
    "description": "Priority: P7 | severity medium | confidence confirmed | from audit finding repo_structure:doc-01\nScope: Mark B9 in docs/AUTOLOOP_TODO.md as RESOLVED with a pointer to _rebase_execution_if_stale, matching the convention already used for A2/B4b/B10 in the same file.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: The tracker misrepresents a shipped fix as an open bug, risking a duplicate task scheduled for already-resolved work and misleading anyone triaging AUTOLOOP_TODO.md by priority.\nEvidence: AUTOLOOP_TODO.md:184 still describes B9 as an open bug with no resolved marker, and line 174 cites it as a live risk; orchestrator.py:3451-3453 already implements the fix it proposes, its own docstring citing 'docs/AUTOLOOP_TODO.md B9'.\nExpected files: docs/AUTOLOOP_TODO.md, autoloop/orchestrator.py\nAcceptance criteria: docs/AUTOLOOP_TODO.md B9 carries a resolved marker and cites orchestrator.py:_rebase_execution_if_stale\nValidation: grep -n 'B9' docs/AUTOLOOP_TODO.md; rg -n '_rebase_execution_if_stale' autoloop/orchestrator.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-024",
    "title": "Add rows for routers/account.py and routers/errors.py to the routers table, and for services/srs_...",
    "description": "Priority: P7 | severity medium | confidence confirmed | from audit finding repo_structure:doc-03\nScope: Add rows for routers/account.py and routers/errors.py to the routers table, and for services/srs_backfill_service.py and services/srs_cleanup_service.py to the services table.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: CLAUDE.md \u00a712 designates SUMMARY.md as the canonical 'where do I look to change X' index; a developer or agent scanning it for account deletion, client-error reporting, or SRS maintenance tooling finds nothing and either misses the existing code or re-implements it.\nEvidence: main.py:164-165 mounts errors_router and account_router (live endpoints: DELETE /api/v1/account, POST /api/v1/errors/client) but SUMMARY.md's router table (lines 22-44) has no row for either. srs_backfill_service.py and srs_cleanup_service.py are real, tested (test_srs_backfill.py, test_srs_cleanup.py), script-driven (scripts/backfill_missing_active_srs.py, scripts/cleanup_orphan_srs_cards.py), documented in docs/MAINTENANCE.md/ROADMAP.md but absent from SUMMARY.md's services table (lines 46-83).\nExpected files: docs/SUMMARY.md, lexy-app/backend/main.py\nAcceptance criteria: docs/SUMMARY.md's routers table lists account.py and errors.py; docs/SUMMARY.md's services table lists srs_backfill_service.py and srs_cleanup_service.py\nValidation: grep -n 'account_router\\|errors_router' docs/SUMMARY.md; grep -n 'srs_backfill_service\\|srs_cleanup_service' docs/SUMMARY.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-025",
    "title": "Re-run `pytest autoloop/tests` and update lines 1402/1414 with the current actual count and date,...",
    "description": "Priority: P7 | severity medium | confidence confirmed | from audit finding tests_ci:test-002\nScope: Re-run `pytest autoloop/tests` and update lines 1402/1414 with the current actual count and date, or standardize on 'measured on [date]\u2014run the suite for current numbers' pattern to make staleness explicit upfront.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Test count documentation is stale and known-stale; future readers cannot trust numeric claims without re-running the suite.\nEvidence: docs/TESTS.md line 1402 claims autoloop suite is '790 passed, 1 skipped (~2m26s serial)' as of 2026-08-01, yet later admits (lines 1408\u20131412) this figure is 'now behind' due to additions on 2026-08-02. Line 1414 explicitly disclaims it: 'That 790 is a MEASURED figure from 2026-08-01 and is now behind...Deliberately not replaced with an arithmetic guess.'\nExpected files: docs/TESTS.md\nAcceptance criteria: Either the 790 figure is replaced with a current measurement, or a prominent disclaimer at the start of the autoloop section flags all counts as historical\nValidation: cd /repo root && pytest autoloop/tests -q | tail -1\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-026",
    "title": "Delete the stale row at SUMMARY.md:333, keeping the more complete row at 335.",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding repo_structure:doc-02\nScope: Delete the stale row at SUMMARY.md:333, keeping the more complete row at 335.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A reader of the file-level index (CLAUDE.md \u00a712 designates SUMMARY.md the canonical navigation aid) hits two entries for the same three files and cannot tell which is authoritative.\nEvidence: Two separate table rows both keyed `prompts.py, config.py, cli.py` exist four lines apart (333 and 335, verified present at current HEAD); the row at 335 is materially newer (mentions blockers_dir, required workers_root, _RESOLUTION_PRECONDITIONS) but the older row at 333 was never removed.\nExpected files: docs/SUMMARY.md\nAcceptance criteria: only one `prompts.py, config.py, cli.py` row remains in docs/SUMMARY.md\nValidation: grep -n 'prompts.py.*config.py.*cli.py' docs/SUMMARY.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-027",
    "title": "Run `ruff check . --show-files` and verify the output includes files under `autoloop/`, or update...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding tests_ci:ruff-001\nScope: Run `ruff check . --show-files` and verify the output includes files under `autoloop/`, or update ruff.toml's documentation to clarify what is actually linted.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Potential scope gap: ruff config claim may not match its actual file coverage; autoloop/ scope is not declared in ruff.toml.\nEvidence: ruff.toml lines 7\u201310 say the config 'covers the backend, the scraper, the root pipeline modules, scripts, masking and ilp in one pass', and that the rule set was pinned after a 2026-07-27 cleanup ('153 findings -> 0'). However, SUMMARY.md line 333 lists cli.py under autoloop/ as part of the checked files \u2014 verify ruff.toml's glob patterns actually reach all these locations.\nExpected files: ruff.toml, docs/SUMMARY.md\nAcceptance criteria: ruff.toml documentation and actual `ruff check` output agree on scope\nValidation: ruff check . --show-files | grep -E 'autoloop|cli.py'\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-028",
    "title": "Update docs/TESTS.md lines 29\u201332 to say: 'NOT covered by a bare run...`tests/evaluation` \u2014 use `p...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding tests_ci:test-003\nScope: Update docs/TESTS.md lines 29\u201332 to say: 'NOT covered by a bare run...`tests/evaluation` \u2014 use `pytest tests/evaluation` to run only the evaluation harness.'\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Documentation accuracy: the cited default behavior in line 29\u201332 is partially incorrect for evaluation tests.\nEvidence: Both files claim that 'a bare `pytest` run' does not cover `lexy-app/backend/tests` or `lexy-app/frontend`, requiring separate commands \u2014 and that is correct. However, docs/TESTS.md claims evaluation tests are covered (line 943, 'tests now include...147 tests') but pytest.ini still excludes them from the default surface even after they exist and are documented.\nExpected files: docs/TESTS.md, pytest.ini\nAcceptance criteria: docs/TESTS.md accurately reflects which suites are/aren't covered by a bare `pytest` run\nValidation: grep -n 'NOT covered by a bare run' docs/TESTS.md\nParallelizable: yes",
    "depends_on": [
      "au-018"
    ]
  },
  {
    "id": "au-029",
    "title": "Update the three line-number citations in docs/SECURITY.md to match current search_service.py.",
    "description": "Priority: P7 | severity info | confidence confirmed | from audit finding security_paths:paths-07\nScope: Update the three line-number citations in docs/SECURITY.md to match current search_service.py.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A future reader following the doc's own cited line numbers to re-verify the finding lands on unrelated code, undermining the file's 'every finding carries a verification check' guarantee.\nEvidence: docs/SECURITY.md:521 and its own verification grep (line 100) cite `search_service.py:62,103,216` for the accepted f-string-SQL pattern; the three matching f-string SELECTs are actually at lines 164, 205 and 322 on current HEAD (content itself re-verified safe \u2014 only a loop index and a module constant SIMILARITY_THRESHOLD are interpolated, no user data).\nExpected files: docs/SECURITY.md, lexy-app/backend/services/search_service.py\nAcceptance criteria: docs/SECURITY.md's cited lines for the search_service f-string pattern match the actual file\nValidation: rg -n 'f\"(SELECT|INSERT|UPDATE|DELETE)' lexy-app/backend/services/search_service.py\nParallelizable: yes",
    "depends_on": []
  }
]
```

Not promoted to tasks:
- `security_paths:paths-05`: human decision — surfaced to the operator, not a task
- `security_paths:paths-06`: human decision — surfaced to the operator, not a task
