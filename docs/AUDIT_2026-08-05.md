# Repository audit — 2026-08-05

Generated by the autoloop audit executor (deterministic rendering of reconciled subagent findings; raw agent reports preserved at `.autoloop/audit/20260805T010046Z-c3d90e`).

- branch: `autoloop/audit-0002` | HEAD: `278b93107ac6` | uncommitted files: 0
- requested scope: Audit architecture, tests/CI, security, DB/migrations, documentation drift, ingestion pipeline, and browser-loop reliability, prioritizing unresolved, blocked, quarantined, and repeatedly timing-out roadmap items.

## Domain coverage — 6/6 domains reported usable output

| domain | usable output | findings |
|---|---|---|
| `docs_drift` | yes | 3 |
| `tests_ci` | yes | 6 |
| `repo_structure` | yes | 10 |
| `security_paths` | yes | 7 |
| `db_migrations` | yes | 5 |
| `ingestion_pipeline` | yes | 4 |

## Validation baseline
- `ruff check .` — **PASS**
  - tail: `All checks passed!`
- `python3 -m pytest autoloop/tests -q -p no:cacheprovider` — **PASS**
  - tail: `[32m[32m[1m1151 passed[0m, [33m1 skipped[0m, [33m1 deselected[0m[32m in 241.05s (0:04:01)[0m[0m`
- `python3 -m pytest tests/ -q -p no:cacheprovider` — **PASS**
  - tail: `[33m[32m368 passed[0m, [33m[1m5 warnings[0m[33m in 11.46s[0m[0m`

## Confirmed defects (0)
(none)

## Security / data-loss risks (14)
#### db_migrations:db-01 — Add a baseline migration (run before or as part of 001) that CREATEs the content-core tables (video,
- severity **critical**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/006_video_channel_genre.py`, `docs/COMMON_ERRORS.md`
- symbols: `migrations/versions (001-037, revision chain)`, `006_video_channel_genre.py:16`
- evidence: Grepped `CREATE TABLE.*(video|word_table|sentence|language_table|word_to_sentence|video_category|phrase_blueprint)` across all 37 migration files: zero matches. 006:16 (`ALTER TABLE video ADD COLUMN...`) is the first of many ALTERs against a table no migration ever CREATEs. docs/COMMON_ERRORS.md:1068-1097 documents the same wall and that the content-half schema (`table.sql`) was deleted in `25a194d`.
- impact: `alembic upgrade head` on any empty database dies at revision 006 — no fresh deploy, CI database, or new-contributor setup can build the schema from the migration chain alone.
- acceptance: `createdb fresh_test && alembic upgrade head` succeeds against an empty database; resulting schema contains all 37+ tables including the content core; schema-only diff against a live DB shows no missing tables
- validation: createdb fresh_test && cd lexy-app/backend && DB_NAME=fresh_test alembic upgrade head; psql fresh_test -c '\dt' | grep -E 'video|word_table|phrase_blueprint'

#### repo_structure:sec-01 — Add Depends(require_admin) to both routes, matching delete_book/delete_page and POST /phrases/seed; 
- severity **high**, confidence **confirmed**, domain `repo_structure`
- files: `lexy-app/backend/routers/books.py`
- symbols: `books.list_packages:182-185`, `books.import_package:188-212`, `books.py:384,399`
- evidence: list_packages/import_package depend only on Depends(get_current_user); the same file's delete_document/delete_page (384,399) thread is_admin into book_service, an established gating pattern these two routes skip. Confirmed identically in three earlier 2026-08-03 audit write-ups but still has no entry in docs/SECURITY.md's numbered findings, despite CLAUDE.md §14 requiring one.
- impact: Any authenticated non-admin user can enumerate every operator-curated document package name and force checksum/structural validation compute via POST /books/import.
- acceptance: non-admin caller gets 403 from GET /books/packages and POST /books/import; docs/SECURITY.md lists the finding with the required four fields
- validation: rg -n 'require_admin|is_admin' lexy-app/backend/routers/books.py; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -q

#### security_paths:sec-01 — Add Depends(require_admin) (core/deps.py:54) to both GET /books/packages and POST /books/import, mat
- severity **high**, confidence **confirmed**, domain `security_paths`
- files: `lexy-app/backend/routers/books.py`, `lexy-app/backend/services/book_import_service.py`
- symbols: `routers.books.list_packages:182-185`, `routers.books.import_package:188-212`, `book_import_service.import_package:116-211`
- evidence: Re-read at HEAD 2026-08-05: books.py:183,189 depend only on get_current_user; the same file's delete_book/delete_page (383-384,398-399) thread is_admin, a pattern these two skip. book_import_service.import_package:123 defaults write_result=True and the router (207-211) never passes write_result=False, so result.write(pkg.package_root) (168/193/209) fires on every successful validation.
- impact: Any authenticated non-admin learner can enumerate every operator-curated package name and, for each real one, force a write of import_result.json into that operator-owned directory and burn checksum/structural validation compute, not just read data.
- acceptance: Non-admin caller gets 403 admin_required from both routes; Admin behavior unchanged; A regression test asserts a non-admin cannot trigger import_result.json to be written
- validation: rg -n 'require_admin' lexy-app/backend/routers/books.py; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -q

#### security_paths:data-01 — Add an idempotent INSERT ... SELECT jsonb_array_elements_text(settings->'liked_genres') (ON CONFLICT
- severity **high**, confidence **confirmed**, domain `security_paths`
- files: `lexy-app/backend/migrations/versions/014_drop_video_genre.py`, `lexy-app/backend/migrations/versions/013_video_category_cleanup.py`
- symbols: `014_drop_video_genre.py:upgrade:21-40`, `014_drop_video_genre.py:1-7`, `013_video_category_cleanup.py:upgrade:37-76`
- evidence: Read both files directly: 014's docstring claims liked_genres/disliked_genres are 'migrated ... into user_video_category'; upgrade() only adds a column (23-27) and deletes both JSONB keys (32-36). 013's upgrade (37-76) only seeds/normalizes video_category, never inserts from users.settings. No INSERT INTO user_video_category from settings JSON exists anywhere in the chain.
- impact: Any DB whose users.settings still holds liked_genres/disliked_genres when 014 runs (fresh install from an old backup, or any pre-014 environment) permanently loses that preference data with no backfill path.
- acceptance: A DB seeded only with users.settings liked_genres/disliked_genres retains those preferences in user_video_category after upgrading through 014, or the docstring no longer claims a migration happens
- validation: grep -n 'INSERT INTO user_video_category' lexy-app/backend/migrations/versions/014_drop_video_genre.py lexy-app/backend/migrations/versions/013_video_category_cleanup.py

#### db_migrations:db-02 — Add an idempotent INSERT INTO user_video_category ... SELECT ... jsonb_array_elements_text(...) ON C
- severity **high**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/014_drop_video_genre.py`
- symbols: `014_drop_video_genre.py:upgrade:21-40`, `014_drop_video_genre.py:1-7`
- evidence: Docstring at 014_drop_video_genre.py:4-6 claims liked_genres/disliked_genres are migrated into user_video_category, but upgrade() at 014_drop_video_genre.py:21-40 only adds the preference column, deletes both JSONB keys (32-36) and drops video.genre (39-40); no INSERT INTO user_video_category exists here or in 013_video_category_cleanup.py.
- impact: Any DB whose users.settings still held legacy liked_genres/disliked_genres when this migration ran permanently lost that preference data with no backfill path.
- acceptance: A DB seeded with only users.settings.liked_genres/disliked_genres and no user_video_category rows has one user_video_category row per legacy genre after upgrading through 014; Or: the 014 docstring no longer claims a migration and states the legacy preference data is dropped
- validation: grep -n 'INSERT INTO user_video_category' lexy-app/backend/migrations/versions/014_drop_video_genre.py lexy-app/backend/migrations/versions/013_video_category_cleanup.py

#### tests_ci:security-07 — CLAUDE.md §12: add a sentence: 'Update docs/SECURITY.md when you change auth/upload/settings/subproc
- severity **medium**, confidence **confirmed**, domain `tests_ci`
- files: `docs/SECURITY.md`, `CLAUDE.md`
- symbols: `SECURITY.md`, `CLAUDE.md §12`
- evidence: docs/SECURITY.md opens with 'Read §12 of CLAUDE.md before committing security-relevant code' and lists four trigger files (docs/SUMMARY.md, docs/TESTS.md, docs/SECURITY.md, docs/COMMON_ERRORS.md). However, CLAUDE.md §12 does NOT mention that these four files exist or are mandatory to update on every change. It only says 'never read .env' and 'all state changes go through progression_service'. The cross-reference is broken: SECURITY.md assumes a reader knows which four docs to update; CLAUDE.md does not tell them.
- impact: A developer makes a security-relevant change (e.g. auth middleware), reads CLAUDE.md §12 as instructed, finds no mention of docs/SECURITY.md, and commits without updating it. The living security tracker becomes stale. Audits miss the change.
- acceptance: CLAUDE.md §12 names the four tracker files; New security-related PRs update docs/SECURITY.md before merge
- validation: grep -n 'docs/SECURITY.md\|docs/TESTS.md\|docs/SUMMARY.md' CLAUDE.md | grep -v '§' | wc -l; grep '^##' docs/SECURITY.md | head -3

#### repo_structure:db-01 — Make downgrade() refuse when any reading_selections row holds a non-empty anchors array, or persist/
- severity **medium**, confidence **confirmed**, domain `repo_structure`
- files: `lexy-app/backend/migrations/versions/025_block_token_ids.py`, `lexy-app/backend/models/schemas.py`
- symbols: `025_block_token_ids._tokenize:21-27`, `025_block_token_ids.downgrade:64-65`, `schemas.py:706`
- evidence: _tokenize (21-27) assigns a fresh uuid.uuid4() per token on every invocation; downgrade() (64-65) is a bare `ALTER TABLE book_blocks DROP COLUMN tokens` with no guard, and re-running upgrade() would re-tokenize with new UUIDs. schemas.py:706 confirms reading_selections.anchors keys on this same token_id with no FK.
- impact: A downgrade-then-upgrade cycle of migration 025 regenerates every token_id, silently orphaning every existing reading_selections.anchors reference with no error surfaced anywhere.
- acceptance: downgrade() raises when orphaning anchors would occur, or a round-trip test proves token_id stability across downgrade+upgrade
- validation: cat lexy-app/backend/migrations/versions/025_block_token_ids.py

#### repo_structure:db-02 — Add an idempotent INSERT ... SELECT backfill from the JSONB arrays into user_video_category before d
- severity **medium**, confidence **confirmed**, domain `repo_structure`
- files: `lexy-app/backend/migrations/versions/014_drop_video_genre.py`
- symbols: `014_drop_video_genre.py:1-7`, `014_drop_video_genre.py:upgrade:21-40`
- evidence: The module docstring (lines 4-6) claims liked_genres/disliked_genres are 'migrated ... into user_video_category (best-effort)'; upgrade() (21-40) only adds the preference column and deletes both JSONB keys from users.settings (32-36) — there is no INSERT INTO user_video_category anywhere in this file.
- impact: Any database whose users.settings still holds legacy liked_genres/disliked_genres when this migration runs (fresh install from an old backup, or migrating from pre-014 state) permanently loses that preference data with no backfill path, contradicting the migration's own stated intent.
- acceptance: a DB seeded only with users.settings.liked_genres/disliked_genres retains those preferences in user_video_category after upgrading through 014, or the docstring accurately says it will not
- validation: grep -n 'INSERT INTO user_video_category' lexy-app/backend/migrations/versions/014_drop_video_genre.py

#### security_paths:data-02 — Add a structural validator comparing bbox['page'] (when present) to element['page_index'] and flag a
- severity **medium**, confidence **confirmed**, domain `security_paths`
- files: `lexy-app/backend/services/document_package/validators.py`, `docs/INGESTION_PIPELINE.md`
- symbols: `validators.py:_BBOX_KEYS:22`, `validators.py:validate_bboxes:302-375`
- evidence: Read directly: the module's own docstring (lines 8-11) names 'a bbox.page that disagrees with its element's page_index' as fatal, but _BBOX_KEYS=('x1','y1','x2','y2') never includes 'page', and validate_bboxes keys the page-dims lookup off element.get('page_index') only (357-358) — bbox.get('page') is never read anywhere in the file.
- impact: A worker bug or corrupted package where a bbox's own 'page' field disagrees with the element's page_index passes every validation gate silently, contradicting the module's own stated invariant.
- acceptance: A synthetic element with mismatched bbox.page vs page_index fails validation with a new stable issue code
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k Structural -q

#### security_paths:data-03 — Have downgrade() refuse when it would orphan existing anchors, or add a round-trip test proving toke
- severity **medium**, confidence **confirmed**, domain `security_paths`
- files: `lexy-app/backend/migrations/versions/025_block_token_ids.py`, `lexy-app/backend/models/schemas.py`
- symbols: `025_block_token_ids.py:_tokenize:21-27`, `025_block_token_ids.py:downgrade:64-65`, `schemas.py:ReadingSelectionAnchor.token_id:704-706`
- evidence: Read both directly: _tokenize assigns a fresh uuid.uuid4() per token on every run; downgrade() is a bare 'ALTER TABLE book_blocks DROP COLUMN tokens' with no guard. schemas.py:706 confirms ReadingSelectionAnchor.token_id is that same UUID, commented 'replaced token_index for stability across edits' — the join key reading_selections.anchors depends on, with no FK.
- impact: A downgrade-then-upgrade cycle of migration 025 regenerates every token_id, silently orphaning existing reading_selections.anchors references (the selection row survives but its anchor no longer resolves), with no error surfaced.
- acceptance: downgrade() either preserves token_id stability or explicitly refuses/warns when reading_selections rows with non-empty anchors exist
- validation: cat lexy-app/backend/migrations/versions/025_block_token_ids.py

#### db_migrations:db-03 — Have downgrade() refuse (or require an explicit force flag) when any is_system row exists, rather th
- severity **medium**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/037_system_word_lists.py`, `docs/ROADMAP.md`
- symbols: `037_system_word_lists.py:downgrade:81-89`
- evidence: downgrade() (line 87) runs an unconditional `DELETE FROM word_lists WHERE is_system` (cascading to word_list_items), justified in the migration's own docstring by 'no system list is created here'. docs/ROADMAP.md N5a step 4 records that the follow-up seeding script has since run in production, creating 2 built-in lists / 9,122 items — so the downgrade's no-data assumption is now false.
- impact: Downgrading 037 today silently deletes all curated built-in vocabulary-list content (9,122 items) with no guard, warning, or backup step, contradicting the migration's own rationale for the unconditional DELETE.
- acceptance: downgrade() on a DB with seeded is_system rows refuses or requires explicit confirmation instead of silently deleting them
- validation: cat lexy-app/backend/migrations/versions/037_system_word_lists.py

#### db_migrations:db-04 — Make downgrade() raise when any reading_selections row has a non-empty anchors array, or add a regre
- severity **medium**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/025_block_token_ids.py`, `lexy-app/backend/models/schemas.py`
- symbols: `025_block_token_ids.py:_tokenize:21-27`, `025_block_token_ids.py:downgrade:64-65`, `schemas.py:ReadingSelectionAnchor:704-707`
- evidence: _tokenize (21-27) assigns a fresh `uuid.uuid4()` per token on every invocation; downgrade() (64-65) is a bare `ALTER TABLE book_blocks DROP COLUMN tokens`, no guard. schemas.py:706 confirms reading_selections.anchors keys on this same token_id ('replaced token_index for stability across edits'), with no FK constraint tying them together.
- impact: A downgrade-then-upgrade cycle of migration 025 regenerates every token_id, silently orphaning every existing reading_selections.anchors reference — the selection row survives but its anchor no longer resolves against book_blocks.tokens, with no error surfaced anywhere.
- acceptance: downgrade() refuses when it would orphan existing anchors, or a test proves token_ids survive a downgrade+upgrade cycle
- validation: cat lexy-app/backend/migrations/versions/025_block_token_ids.py

#### ingestion_pipeline:ing-sec-01 — Add Depends(require_admin) to GET /books/packages and POST /books/import; update docs/SECURITY.md's 
- severity **medium**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/routers/books.py`, `lexy-app/backend/core/deps.py`, `docs/SECURITY.md`
- symbols: `routers/books.py:182-212 (list_packages, import_package)`, `core/deps.py:54 (require_admin)`
- evidence: routers/books.py:182-211 gates GET /books/packages and POST /books/import on Depends(get_current_user) only, no require_admin (deps.py:54, pattern used in phrases.py:36 and lemma_corrections.py:64/90/117/150). test_document_package.py:1137-1149 asserts a freshly-registered non-admin user succeeds. docs/SECURITY.md:543 lists admin-gated routes and omits these two.
- impact: Any authenticated non-admin user can enumerate every worker-produced package under the shared, non-user-scoped PACKAGE_ROOT and trigger disk-heavy checksum verification plus an import_result.json write on any of them, unlike equivalent global-catalog endpoints elsewhere in the app.
- acceptance: Non-admin POST /books/import returns 403 admin_required; Non-admin GET /books/packages returns 403 admin_required; docs/SECURITY.md lists both routes under admin-gated routes
- validation: rg -n 'require_admin' lexy-app/backend/routers/books.py; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k TestImportEndpoint -q

#### db_migrations:db-05 — Emit a visible row count of deleted orphan rows during upgrade (RAISE NOTICE or a logged SELECT COUN
- severity **low**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/013_video_category_cleanup.py`
- symbols: `013_video_category_cleanup.py:upgrade:66-69`
- evidence: Step 5 (lines 66-69) runs `DELETE FROM user_video_category WHERE video_category NOT IN (SELECT category FROM video_category)` unconditionally, immediately before adding the FK in step 6, with no row count logged.
- impact: Any user preference row referencing a category outside the 18 seeded YouTube categories (e.g. stale/renamed data from an earlier app version) is permanently deleted with no visibility into how many rows or which users were affected.
- acceptance: migration emits a visible count of deleted orphan rows during upgrade
- validation: cat lexy-app/backend/migrations/versions/013_video_category_cleanup.py


## Architectural risks (8)
#### repo_structure:arch-01 — Move the Docker build context to the repo root (COPY subtitle-scraper/ and data/ alongside backend/)
- severity **critical**, confidence **confirmed**, domain `repo_structure`
- files: `lexy-app/Dockerfile`, `lexy-app/backend/services/matcher_service.py`
- symbols: `Dockerfile:21-31`, `matcher_service.py:35`, `matcher_service.py:43`
- evidence: Dockerfile:28,31 COPY only backend/ and frontend/dist (WORKDIR=/app); matcher_service.py:35 computes Path(__file__).resolve().parents[3], which under the image resolves to '/', giving a nonexistent _SCRAPER_PATH='/subtitle-scraper'; line 43 does `import phrase_finder as _pf` at module scope.
- impact: Any router importing matcher_service (phrases.py, matcher.py, chat.py) pulls this in at app startup, so README's documented `docker compose up` crashes before uvicorn binds a port.
- acceptance: docker compose up boots without crashing; a CI job builds the image and gets a 2xx from a health endpoint
- validation: cd lexy-app && docker compose build && docker compose up -d && curl -f http://localhost:8000/api/v1/auth/login -X POST -d '{}' -H 'Content-Type: application/json'

#### repo_structure:arch-02 — On load, reconcile execution.worktree_path against a freshly-computed path: if the stored path exist
- severity **high**, confidence **confirmed**, domain `repo_structure`
- files: `autoloop/orchestrator.py`, `autoloop/worker_env.py`
- symbols: `orchestrator.py:2012-2018`, `worker_env.WorkerRepoManager._path:305-310`
- evidence: A loaded TaskExecution's stored worktree_path is used verbatim to build the GitGateway at orchestrator.py:2017-2018; WorkerRepoManager._path (worker_env.py:310) computes root_dir/task_id fresh only inside .create(). If [paths].workers_root changes between runs, an existing execution record keeps pointing at the old path and nothing reconciles it — matches docs/AUTOLOOP_TODO.md A3, still unresolved.
- impact: An operator who relocates workers_root gets FileNotFoundError on the loop's next dispatch of any in-flight task, with no diagnostic naming the mismatch; documented workaround is manually archiving the execution record.
- acceptance: moving workers_root with an in-flight task either resumes at the stored path or parks with an explicit message naming both paths, never a bare FileNotFoundError
- validation: python3 -m pytest autoloop/tests/test_postcommit_flow.py autoloop/tests/test_m1_hardening.py -q

#### repo_structure:arch-03 — Emit a transcript event when each agent starts (not only on completion), and surface per-domain stat
- severity **high**, confidence **confirmed**, domain `repo_structure`
- files: `autoloop/audit/executor.py`
- symbols: `AuditExecutor._run_agents:521-545`
- evidence: _run_agents (executor.py:526-527) does `list(pool.map(agent_runner.run, specs))` and only writes to raw_dir in the loop starting at line 528, after the whole map call returns — no per-agent start event is logged anywhere in this file.
- impact: For a fan-out taking tens of minutes, an operator watching an unattended run sees an empty raw/ directory and a silent loop.out, then a burst on completion — unable to distinguish 'working' from 'wedged' without inspecting process state by hand.
- acceptance: `autoloop status` during an audit run shows which domain agents have started vs. finished; a transcript event exists per agent start
- validation: python3 -m pytest autoloop/tests/test_audit_executor.py -q

#### security_paths:arch-01 — Move the Docker build context to the repo root (COPY subtitle-scraper/ + data/ alongside lexy-app/ba
- severity **high**, confidence **confirmed**, domain `security_paths`
- files: `lexy-app/Dockerfile`, `lexy-app/backend/services/matcher_service.py`
- symbols: `Dockerfile:21-31`, `matcher_service.py:35,43`
- evidence: Read both files directly: Dockerfile COPYs only backend/ and frontend/dist (WORKDIR /app), never subtitle-scraper/ or data/ (repo-root siblings). matcher_service.py:35 computes Path(__file__).resolve().parents[3], which under /app/backend/services/matcher_service.py resolves to '/', giving a nonexistent _SCRAPER_PATH; line 43's module-scope 'import phrase_finder' then fails at import.
- impact: Any router importing matcher_service (phrases.py, matcher.py, chat.py) crashes app startup with ModuleNotFoundError, so `docker compose up` (README's documented quickstart) never binds a port; the actual deploy paths (Procfile/entrypoint.sh per docs/ROADMAP.md P2) are unaffected, hence high rather than critical.
- acceptance: docker compose up boots without crashing; A CI or manual smoke test gets a 2xx from a health endpoint after `docker compose up`
- validation: cd lexy-app && docker compose build && docker compose up -d && curl -f http://localhost:8000/api/v1/auth/login -X POST -d '{}' -H 'Content-Type: application/json'

#### tests_ci:arch-08 — Add a pytest plugin or pre-commit hook to reject SELECT...LIMIT without ORDER BY in test files. OR: 
- severity **medium**, confidence **confirmed**, domain `tests_ci`
- files: `docs/COMMON_ERRORS.md`
- symbols: `COMMON_ERRORS.md §2`
- evidence: docs/COMMON_ERRORS.md documents known isolation hazards with shared-catalog queries (§2: xdist flakes, §2d: fresh-database failures on unordered picks). The root cause is identical: SELECT...FROM <shared catalog> LIMIT 1 without deterministic ORDER BY picks different rows on different database states. Marked 'latent bugs' in 7 test files; TESTS.md acknowledges 'any ... FROM <shared catalog> LIMIT 1 without an ORDER BY ... is a latent bug'. No linter or test catches this pattern. A new test written by a developer who hasn't read COMMON_ERRORS.md re-introduces it.
- impact: Flaky tests in CI (xdist) and on fresh databases (new contributor setup). The same architectural anti-pattern is documented but not enforced. Test review burden increases.
- acceptance: New tests cannot use unordered LIMIT 1 on catalog tables; Existing violations are fixed (see xdist-02); Grep `SELECT.*LIMIT 1' test files returns 0 matches on catalog tables without ORDER BY
- validation: rg 'SELECT.*FROM (phrase_table|grammar_rule_table|video).*LIMIT 1' lexy-app/backend/tests | rg -v 'ORDER BY' | wc -l; cd lexy-app/backend && python3 -m pytest -n auto -q 2>&1 | grep -E 'FAILED|FLAKY' | wc -l

#### repo_structure:arch-04 — When N consecutive rounds produce a byte-identical validation summary, stop retrying and park as an 
- severity **medium**, confidence **confirmed**, domain `repo_structure`
- files: `autoloop/orchestrator.py`
- symbols: `Orchestrator post-commit validation / attempt-count path`
- evidence: docs/AUTOLOOP_TODO.md B6 describes rt-01 burning 3 of 5 attempts on byte-identical environmental validation failures (missing DB credentials); grepping orchestrator.py for any 'consecutive identical validation' detection finds none — the only 'byte-identical' hit (line 23) is an unrelated resubmit-content comment.
- impact: A validation failure that cannot be fixed by retrying (missing credentials, unreachable service) still consumes the task's finite attempt budget every round, until the task is wrongly exhausted rather than parked as an environmental blocker.
- acceptance: two consecutive rounds with identical validation output park as `environmental` without a third retry
- validation: python3 -m pytest autoloop/tests/test_m1_hardening.py -q

#### security_paths:arch-02 — Add a baseline migration that creates the content core (video, word_table, sentence, language_table,
- severity **medium**, confidence **confirmed**, domain `security_paths`
- files: `lexy-app/backend/migrations/versions`, `docs/COMMON_ERRORS.md`
- symbols: `docs/COMMON_ERRORS.md:1077-1097`, `lexy-app/backend/migrations/versions/006_video_channel_genre.py`
- evidence: Confirmed no baseline migration exists (glob of migrations/versions/00*.py starts at 001) and no fix has been added since docs/COMMON_ERRORS.md's own §9 write-up. Whether the chain literally dies at 006 is documented there, not independently reproduced this session — that specific claim is probable, not confirmed; 'no baseline migration exists and none added since' is confirmed.
- impact: A brand-new database cannot be built via `alembic upgrade head` alone (workaround: pg_dump schema-only from a live DB), so CI schema validation and new-contributor setup have no self-sufficient path.
- acceptance: createdb + alembic upgrade head succeeds on an empty database and produces all expected tables
- validation: createdb fresh_test; cd lexy-app/backend && DB_NAME=fresh_test alembic upgrade head

#### ingestion_pipeline:ing-test-01 — Align test_document_package.py's imports to the `from backend.services...` convention used by every 
- severity **low**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/tests/test_document_package.py`
- symbols: `tests/test_document_package.py:13-14`
- evidence: test_document_package.py:13-14 imports `from services import book_import_service` / `from services.document_package import contract` (bare), the only backend test file not using `from backend.services import ...`. docs/COMMON_ERRORS.md:320-337 documents this resolves only under `python3 -m pytest` and silently drops 1258→1151 tests with the bare `pytest` console script, no collection error.
- impact: Running the backend suite with plain `pytest` instead of `python3 -m pytest` from lexy-app/backend silently loses all 107 A2 document-package tests with no failure signal, just a lower pass count.
- acceptance: `cd lexy-app/backend && pytest tests/test_document_package.py -q` (bare console script) collects and runs all 107 tests, matching `python3 -m pytest`
- validation: cd lexy-app/backend && pytest tests/test_document_package.py -q; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -q


## Documentation drift (8)
#### tests_ci:doc-03 — Update CLAUDE.md §8b: delete the sentence naming upsert_word_status or change it to 'which was delet
- severity **medium**, confidence **confirmed**, domain `tests_ci`
- files: `CLAUDE.md`, `docs/SUMMARY.md`
- symbols: `CLAUDE.md §8b`, `docs/SECURITY.md`
- evidence: CLAUDE.md §8b states `status_marked_known` 'forced to known by word_service.upsert_word_status (called from the router before apply_progression)'. That function was deleted 2026-05-19 (#2). Router now makes single apply_progression(..., status_override=...) call. CLAUDE.md §8b names this correctly in atomicity rule two paragraphs away, but the obsolete sentence appears first, and COMMON_ERRORS.md notes this same doc-drift was flagged THREE consecutive times before fix on 2026-08-04.
- impact: New developers reading §8b top-down conclude the two-call non-atomic flow is still live. They may re-introduce the bug when refactoring status updates, or waste time hunting a phantom code path.
- acceptance: CLAUDE.md §8b makes no reference to upsert_word_status as live; The atomicity rule appears in the first paragraph; A grep for 'upsert_word_status' in CLAUDE.md returns zero live-code mentions
- validation: grep 'upsert_word_status' CLAUDE.md | grep -v 'Historical\|deleted'; grep -A 3 'status_marked_known' CLAUDE.md | head -10

#### repo_structure:doc-01 — Mark B9 in docs/AUTOLOOP_TODO.md as RESOLVED with a pointer to `_rebase_execution_if_stale`, matchin
- severity **medium**, confidence **confirmed**, domain `repo_structure`
- files: `docs/AUTOLOOP_TODO.md`, `autoloop/orchestrator.py`
- symbols: `AUTOLOOP_TODO.md:184-207 (B9)`, `orchestrator._rebase_execution_if_stale:3451-3525`
- evidence: AUTOLOOP_TODO.md B9 (no resolved marker) describes the base-pinning bug and proposes 're-base the record ... or refuse with a message naming both shas'; orchestrator.py:3451-3525 `_rebase_execution_if_stale` already implements exactly that (quarantine-and-rebase when review_round==0, refuse naming both shas otherwise), its own docstring citing 'docs/AUTOLOOP_TODO.md B9'.
- impact: The tracker misrepresents a shipped fix as an open P1 bug, risking a duplicate task scheduled for already-resolved work and misleading anyone triaging AUTOLOOP_TODO.md by priority.
- acceptance: docs/AUTOLOOP_TODO.md B9 carries a resolved marker and cites orchestrator.py:_rebase_execution_if_stale
- validation: grep -n 'B9' docs/AUTOLOOP_TODO.md; rg -n '_rebase_execution_if_stale' autoloop/orchestrator.py

#### docs_drift:doc-drift-01 — Update CLAUDE.md:111 to reference the correct line numbers: 'line 215' for the function or 'line 259
- severity **low**, confidence **confirmed**, domain `docs_drift`
- files: `CLAUDE.md`
- symbols: `CLAUDE.md:111`, `lexy-app/backend/services/progression_service.py:215-259`
- evidence: CLAUDE.md line 111 cites `apply_progression` at 'line 178' but the function definition begins at line 215 and the transaction starts at line 259 (progression_service.py).
- impact: Stale line number reference makes it harder to locate the code being documented; readers may look at the wrong section.
- acceptance: Line number in CLAUDE.md matches the actual location of apply_progression or its transaction block
- validation: grep -n 'async def apply_progression' lexy-app/backend/services/progression_service.py; grep -n 'async with conn.transaction' lexy-app/backend/services/progression_service.py | head -1

#### docs_drift:doc-drift-02 — Update CLAUDE.md:126 to reference the correct lines: 'lines 566-581' instead of 'line ~333'.
- severity **low**, confidence **confirmed**, domain `docs_drift`
- files: `CLAUDE.md`
- symbols: `CLAUDE.md:126`, `lexy-app/backend/services/progression_service.py:566-581`
- evidence: CLAUDE.md line 126 cites `_update_srs` 'line ~333' for the no-op check on `action="incorrect"`, but the actual check is at lines 566-581 (progression_service.py).
- impact: Stale line number reference (~200 lines off) makes documentation unreliable for code navigation.
- acceptance: Line number in CLAUDE.md matches the location of the incorrect/no-op check in _update_srs
- validation: grep -n 'action == "incorrect"' lexy-app/backend/services/progression_service.py; sed -n '566,581p' lexy-app/backend/services/progression_service.py | grep -c 'no-op if missing'

#### repo_structure:doc-02 — Delete the stale row at SUMMARY.md:333, keeping the more complete row at 335.
- severity **low**, confidence **confirmed**, domain `repo_structure`
- files: `docs/SUMMARY.md`
- symbols: `SUMMARY.md:333`, `SUMMARY.md:335`
- evidence: Two separate table rows both keyed `prompts.py, config.py, cli.py` exist four lines apart (333 and 335); the second is materially newer (mentions blockers_dir, required workers_root, _RESOLUTION_PRECONDITIONS) but the older row at 333 was never removed.
- impact: A reader of the file-level index (CLAUDE.md §12 designates SUMMARY.md as the canonical navigation aid) hits two entries for the same three files and cannot tell which is authoritative.
- acceptance: only one `prompts.py, config.py, cli.py` row remains in docs/SUMMARY.md
- validation: grep -n 'prompts.py.*config.py.*cli.py' docs/SUMMARY.md

#### security_paths:doc-01 — Update the verification-check line numbers in docs/SECURITY.md's Verified-strengths section to match
- severity **low**, confidence **confirmed**, domain `security_paths`
- files: `docs/SECURITY.md`, `lexy-app/backend/services/search_service.py`
- symbols: `docs/SECURITY.md:521`, `search_service.py:164,205,322`
- evidence: docs/SECURITY.md:521 cites the safe f-string SQL (loop-index-only interpolation) at 'search_service.py:62,103,216'; grepping f"(SELECT|INSERT|UPDATE|DELETE) at HEAD finds it at lines 164, 205, 322 instead. The interpolations themselves are still safe (term_idx loop counter only) — only the cited line numbers are stale.
- impact: SECURITY.md's own rule is that a finding without an accurate verification check 'rots into a claim nobody re-tests'; the stale line numbers make the documented grep silently miss the real matches if someone re-runs it literally.
- acceptance: docs/SECURITY.md's cited line numbers for the search_service.py f-string SQL match the current file
- validation: rg -n 'f"(SELECT|INSERT|UPDATE|DELETE)' lexy-app/backend/services/search_service.py

#### ingestion_pipeline:ing-doc-01 — Update CLAUDE.md §3 to read `lexy-app/backend/migrations/versions/` instead of `alembic/versions/`.
- severity **low**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `CLAUDE.md`
- symbols: `CLAUDE.md:38`
- evidence: CLAUDE.md:38 states migrations live in `alembic/versions/`; lexy-app/backend/alembic.ini:2 sets `script_location = migrations`, and the 37 version files actually live at lexy-app/backend/migrations/versions/*.py.
- impact: Anyone (or any agent) trusting the authoritative CLAUDE.md to find migrations searches a nonexistent alembic/versions/ path.
- acceptance: CLAUDE.md's migrations path matches alembic.ini's script_location and the real directory
- validation: grep -n script_location lexy-app/backend/alembic.ini; ls lexy-app/backend/migrations/versions | wc -l

#### docs_drift:doc-drift-03 — Update docs/SUMMARY.md:49 to reference the correct line numbers: 'lines 516-517' instead of 'line 26
- severity **info**, confidence **confirmed**, domain `docs_drift`
- files: `CLAUDE.md`, `docs/SUMMARY.md`
- symbols: `CLAUDE.md:49`, `docs/SUMMARY.md:49`
- evidence: docs/SUMMARY.md line 49 states `_update_srs` 'now skips active-card creation for grammar_rule (line 268)' but the actual skip logic is at lines 516-517, not line 268.
- impact: Incorrect line number makes the referenced code location unreachable; creates inconsistency between documentation files.
- acceptance: Line number in docs/SUMMARY.md correctly points to the grammar_rule skip logic
- validation: grep -n 'if item_type == "grammar_rule" and direction == "active"' lexy-app/backend/services/progression_service.py


## Missing regression protection (4)
#### tests_ci:ci-01 — Add GitHub Actions steps for root `pytest` (hermetic, ~2 min), backend `python3 -m pytest -n auto` a
- severity **high**, confidence **confirmed**, domain `tests_ci`
- files: `.github/workflows/dependency-audit.yml`
- symbols: `backend suite`, `frontend tests`, `root pipeline tests`
- evidence: .github/workflows/dependency-audit.yml runs only `pip-audit` and `npm audit` on push/PR to main; docs/TESTS.md documents THREE required test suites (root pytest, backend pytest -n auto, frontend vitest run) that CI never invokes. Root suite covers pipeline modules; backend covers 1258 tests; frontend covers 322 tests. None validated on PR or main push.
- impact: Regressions in all three suites could merge without detection. A PR touching backend code could break 1258 tests without CI signal. Pipeline changes are untested in CI entirely.
- acceptance: CI workflow runs all three test suites on every PR to main; Any test failure blocks the PR; Validation passes on a real code change before merge
- validation: grep -c 'run: pytest' .github/workflows/*.yml; grep 'pytest.*backend' .github/workflows/dependency-audit.yml

#### tests_ci:ruff-05 — Add GitHub Actions step: `ruff check . --select E,F` (from repo root, fail on any finding). Mirrors 
- severity **medium**, confidence **confirmed**, domain `tests_ci`
- files: `ruff.toml`
- symbols: `ruff.toml:19,28`
- evidence: ruff.toml enables E (all indent/whitespace/blank rules) but disables E501 (line-too-long) with comment '405 violations across existing codebase'. However: (1) root `pytest` does not run ruff, only Python tests; (2) CI never runs ruff at all; (3) a PR could introduce E/F violations without detection. COMMON_ERRORS.md notes 'ruff check . from REPO ROOT' is required; CLAUDE.md §11 says it is 'part of validation' but CI does not enforce it.
- impact: A PR passes all tests and CI but violates repo linting rules, discovered only on a human `ruff check` run. New violations creep in; the 153→0 cleanup of 2026-07-27 is lost incrementally.
- acceptance: CI runs ruff check on every PR; Any E or F violation blocks merge; Baseline is clean (0 findings)
- validation: ruff check . --select E,F 2>&1 | tail -1; grep -c 'ruff' .github/workflows/dependency-audit.yml

#### tests_ci:backend-06 — Root pytest.ini: add `lexy-app/backend/tests` to testpaths, OR document that backend must be run sep
- severity **medium**, confidence **confirmed**, domain `tests_ci`
- files: `lexy-app/backend/pytest.ini`
- symbols: `pytest.ini`, `testpaths`
- evidence: lexy-app/pytest.ini exists with asyncio settings and testpaths=backend/tests. However, a developer (or CI) running `cd lexy-app && pytest` will find lexy-app/pytest.ini but then `python3 -m pytest` from repo root falls through to root pytest.ini. Root pytest.ini testpaths includes autoloop/tests but NOT lexy-app/backend/tests explicitly, so a bare root `pytest` skips the backend suite entirely — despite CLAUDE.md §11 and docs/TESTS.md claiming it is a required part of validation.
- impact: Backend test suite is invisible to root `pytest` runs. Developers validate 'root passes' thinking it includes backend, when it actually only runs pipeline+autoloop. The 1258 backend tests remain unchecked until someone explicitly runs `cd lexy-app && pytest` or `cd lexy-app/backend && pytest -n auto`.
- acceptance: A bare `pytest` from repo root includes backend test count in summary; OR: docs/TESTS.md clearly states backend is never run by bare pytest; CI runs backend suite as its own job
- validation: cd /path/to/repo && python3 -m pytest -q --co 2>&1 | grep -c 'test_'; cd /path/to/repo && python3 -m pytest -q --tb=no 2>&1 | grep -E '(passed|failed)'

#### repo_structure:test-01 — When a real Codex CLI exhaustion is observed, capture its exact stderr/stdout as a fixture and add b
- severity **low**, confidence **confirmed**, domain `repo_structure`
- files: `autoloop/codex/quota.py`
- symbols: `quota.DEFAULT_QUOTA_PATTERNS:31-44`, `quota.is_quota_exhausted:52-69`
- evidence: The module's own docstring (lines 3-8, 17-22) states the exact wording Codex CLI uses for quota exhaustion 'cannot be verified from this repository' and the pattern list is 'a starting point, not a verified list'; no test exercises a real captured CLI transcript, only synthetic strings.
- impact: A real exhaustion message that does not match any of the 12 substrings degrades silently to an ordinary consecutive-failure count instead of parking with a clear diagnosis, until an operator manually adds the pattern after seeing the transcript.
- acceptance: a captured real-world exhaustion transcript exists as a test fixture and is classified correctly by is_quota_exhausted
- validation: python3 -m pytest autoloop/tests -k quota -q


## Optional improvements (1)
#### ingestion_pipeline:ing-improve-01 — Add a `pages_in` count (len(pkg.page_dims) or distinct declared page_index values) to build_result's
- severity **low**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `docs/INGESTION_PIPELINE.md`, `lexy-app/backend/services/document_package/result.py`
- symbols: `result.py:104-113 (build_result counts)`
- evidence: docs/INGESTION_PIPELINE.md §4's import_result.json example carries `counts.pages` (document page total); result.py:104-113's shipped `counts` has no pages/pages_in field, only `pages_written` = distinct page_index values among surviving plan blocks (persistence.py:197-207).
- impact: A page whose every element was skipped produces no signal in import_result.json — pages_written silently omits it and there is no pages_in to compare against, unlike the elements_in/elements_skipped pair that makes element loss auditable.
- acceptance: import_result.json counts include pages_in; a synthetic package where one declared page's elements are all skipped shows pages_in > pages_written, covered by a test
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k pages -q


## Human decisions needed (0)
(none)

## Rejected items (2)
- [tests_ci] findings[1]: category 'correctness' not in ('defect', 'security', 'data_loss', 'architecture', 'doc_drift', 'missing_test', 'improvement', 'human_decision', 'style') — `{"id": "xdist-02", "category": "correctness", "severity": "high", "confidence": "confirmed", "affected_files": ["lexy-ap`
- [tests_ci] findings[3]: category 'correctness' not in ('defect', 'security', 'data_loss', 'architecture', 'doc_drift', 'missing_test', 'improvement', 'human_decision', 'style') — `{"id": "pytest-04", "category": "correctness", "severity": "medium", "confidence": "confirmed", "affected_files": ["pyte`

## Proposed task graph (35 tasks)
To adopt this graph, reply with a `plan` decision containing the tasks below (edit freely — this is a proposal):
```json
[
  {
    "id": "au-001",
    "title": "Add a baseline migration (run before or as part of 001) that CREATEs the content-core tables (vid...",
    "description": "Priority: P1 | severity critical | confidence confirmed | from audit finding db_migrations:db-01\nScope: Add a baseline migration (run before or as part of 001) that CREATEs the content-core tables (video, word_table, sentence, language_table, word_to_sentence, video_category, phrase_blueprint) at their pre-006 shape, recovered from `git show <sha-before-25a194d>:table.sql`.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: `alembic upgrade head` on any empty database dies at revision 006 \u2014 no fresh deploy, CI database, or new-contributor setup can build the schema from the migration chain alone.\nEvidence: Grepped `CREATE TABLE.*(video|word_table|sentence|language_table|word_to_sentence|video_category|phrase_blueprint)` across all 37 migration files: zero matches. 006:16 (`ALTER TABLE video ADD COLUMN...`) is the first of many ALTERs against a table no migration ever CREATEs. docs/COMMON_ERRORS.md:1068-1097 documents the same wall and that the content-half schema (`table.sql`) was deleted in `25a194d`.\nExpected files: lexy-app/backend/migrations/versions/006_video_channel_genre.py, docs/COMMON_ERRORS.md\nAcceptance criteria: `createdb fresh_test && alembic upgrade head` succeeds against an empty database; resulting schema contains all 37+ tables including the content core; schema-only diff against a live DB shows no missing tables\nValidation: createdb fresh_test && cd lexy-app/backend && DB_NAME=fresh_test alembic upgrade head; psql fresh_test -c '\\dt' | grep -E 'video|word_table|phrase_blueprint'\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-002",
    "title": "Add an idempotent INSERT INTO user_video_category ... SELECT ... jsonb_array_elements_text(...) O...",
    "description": "Priority: P1 | severity high | confidence confirmed | from audit finding db_migrations:db-02\nScope: Add an idempotent INSERT INTO user_video_category ... SELECT ... jsonb_array_elements_text(...) ON CONFLICT DO NOTHING before the JSONB keys are deleted, or correct the docstring to state the data is dropped.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any DB whose users.settings still held legacy liked_genres/disliked_genres when this migration ran permanently lost that preference data with no backfill path.\nEvidence: Docstring at 014_drop_video_genre.py:4-6 claims liked_genres/disliked_genres are migrated into user_video_category, but upgrade() at 014_drop_video_genre.py:21-40 only adds the preference column, deletes both JSONB keys (32-36) and drops video.genre (39-40); no INSERT INTO user_video_category exists here or in 013_video_category_cleanup.py.\nExpected files: lexy-app/backend/migrations/versions/014_drop_video_genre.py\nAcceptance criteria: A DB seeded with only users.settings.liked_genres/disliked_genres and no user_video_category rows has one user_video_category row per legacy genre after upgrading through 014; Or: the 014 docstring no longer claims a migration and states the legacy preference data is dropped\nValidation: grep -n 'INSERT INTO user_video_category' lexy-app/backend/migrations/versions/014_drop_video_genre.py lexy-app/backend/migrations/versions/013_video_category_cleanup.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-003",
    "title": "Add an idempotent INSERT ... SELECT jsonb_array_elements_text(settings->'liked_genres') (ON CONFL...",
    "description": "Priority: P1 | severity high | confidence confirmed | from audit finding security_paths:data-01\nScope: Add an idempotent INSERT ... SELECT jsonb_array_elements_text(settings->'liked_genres') (ON CONFLICT DO NOTHING) into user_video_category inside 014's upgrade() before deleting the JSONB keys, or correct the docstring to state no backfill occurs.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any DB whose users.settings still holds liked_genres/disliked_genres when 014 runs (fresh install from an old backup, or any pre-014 environment) permanently loses that preference data with no backfill path.\nEvidence: Read both files directly: 014's docstring claims liked_genres/disliked_genres are 'migrated ... into user_video_category'; upgrade() only adds a column (23-27) and deletes both JSONB keys (32-36). 013's upgrade (37-76) only seeds/normalizes video_category, never inserts from users.settings. No INSERT INTO user_video_category from settings JSON exists anywhere in the chain.\nExpected files: lexy-app/backend/migrations/versions/014_drop_video_genre.py, lexy-app/backend/migrations/versions/013_video_category_cleanup.py\nAcceptance criteria: A DB seeded only with users.settings liked_genres/disliked_genres retains those preferences in user_video_category after upgrading through 014, or the docstring no longer claims a migration happens\nValidation: grep -n 'INSERT INTO user_video_category' lexy-app/backend/migrations/versions/014_drop_video_genre.py lexy-app/backend/migrations/versions/013_video_category_cleanup.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-004",
    "title": "Have downgrade() refuse (or require an explicit force flag) when any is_system row exists, rather...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding db_migrations:db-03\nScope: Have downgrade() refuse (or require an explicit force flag) when any is_system row exists, rather than deleting unconditionally.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Downgrading 037 today silently deletes all curated built-in vocabulary-list content (9,122 items) with no guard, warning, or backup step, contradicting the migration's own rationale for the unconditional DELETE.\nEvidence: downgrade() (line 87) runs an unconditional `DELETE FROM word_lists WHERE is_system` (cascading to word_list_items), justified in the migration's own docstring by 'no system list is created here'. docs/ROADMAP.md N5a step 4 records that the follow-up seeding script has since run in production, creating 2 built-in lists / 9,122 items \u2014 so the downgrade's no-data assumption is now false.\nExpected files: lexy-app/backend/migrations/versions/037_system_word_lists.py, docs/ROADMAP.md\nAcceptance criteria: downgrade() on a DB with seeded is_system rows refuses or requires explicit confirmation instead of silently deleting them\nValidation: cat lexy-app/backend/migrations/versions/037_system_word_lists.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-005",
    "title": "Make downgrade() raise when any reading_selections row has a non-empty anchors array, or add a re...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding db_migrations:db-04\nScope: Make downgrade() raise when any reading_selections row has a non-empty anchors array, or add a regression test proving token_id stability across a downgrade+upgrade round-trip.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A downgrade-then-upgrade cycle of migration 025 regenerates every token_id, silently orphaning every existing reading_selections.anchors reference \u2014 the selection row survives but its anchor no longer resolves against book_blocks.tokens, with no error surfaced anywhere.\nEvidence: _tokenize (21-27) assigns a fresh `uuid.uuid4()` per token on every invocation; downgrade() (64-65) is a bare `ALTER TABLE book_blocks DROP COLUMN tokens`, no guard. schemas.py:706 confirms reading_selections.anchors keys on this same token_id ('replaced token_index for stability across edits'), with no FK constraint tying them together.\nExpected files: lexy-app/backend/migrations/versions/025_block_token_ids.py, lexy-app/backend/models/schemas.py\nAcceptance criteria: downgrade() refuses when it would orphan existing anchors, or a test proves token_ids survive a downgrade+upgrade cycle\nValidation: cat lexy-app/backend/migrations/versions/025_block_token_ids.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-006",
    "title": "Make downgrade() refuse when any reading_selections row holds a non-empty anchors array, or persi...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding repo_structure:db-01\nScope: Make downgrade() refuse when any reading_selections row holds a non-empty anchors array, or persist/restore token_ids across the round-trip.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A downgrade-then-upgrade cycle of migration 025 regenerates every token_id, silently orphaning every existing reading_selections.anchors reference with no error surfaced anywhere.\nEvidence: _tokenize (21-27) assigns a fresh uuid.uuid4() per token on every invocation; downgrade() (64-65) is a bare `ALTER TABLE book_blocks DROP COLUMN tokens` with no guard, and re-running upgrade() would re-tokenize with new UUIDs. schemas.py:706 confirms reading_selections.anchors keys on this same token_id with no FK.\nExpected files: lexy-app/backend/migrations/versions/025_block_token_ids.py, lexy-app/backend/models/schemas.py\nAcceptance criteria: downgrade() raises when orphaning anchors would occur, or a round-trip test proves token_id stability across downgrade+upgrade\nValidation: cat lexy-app/backend/migrations/versions/025_block_token_ids.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-007",
    "title": "Add an idempotent INSERT ... SELECT backfill from the JSONB arrays into user_video_category befor...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding repo_structure:db-02\nScope: Add an idempotent INSERT ... SELECT backfill from the JSONB arrays into user_video_category before deleting the keys, or correct the docstring to state no backfill occurs.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any database whose users.settings still holds legacy liked_genres/disliked_genres when this migration runs (fresh install from an old backup, or migrating from pre-014 state) permanently loses that preference data with no backfill path, contradicting the migration's own stated intent.\nEvidence: The module docstring (lines 4-6) claims liked_genres/disliked_genres are 'migrated ... into user_video_category (best-effort)'; upgrade() (21-40) only adds the preference column and deletes both JSONB keys from users.settings (32-36) \u2014 there is no INSERT INTO user_video_category anywhere in this file.\nExpected files: lexy-app/backend/migrations/versions/014_drop_video_genre.py\nAcceptance criteria: a DB seeded only with users.settings.liked_genres/disliked_genres retains those preferences in user_video_category after upgrading through 014, or the docstring accurately says it will not\nValidation: grep -n 'INSERT INTO user_video_category' lexy-app/backend/migrations/versions/014_drop_video_genre.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-008",
    "title": "Add a structural validator comparing bbox['page'] (when present) to element['page_index'] and fla...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding security_paths:data-02\nScope: Add a structural validator comparing bbox['page'] (when present) to element['page_index'] and flag a mismatch as fatal, matching the docstring's claim.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A worker bug or corrupted package where a bbox's own 'page' field disagrees with the element's page_index passes every validation gate silently, contradicting the module's own stated invariant.\nEvidence: Read directly: the module's own docstring (lines 8-11) names 'a bbox.page that disagrees with its element's page_index' as fatal, but _BBOX_KEYS=('x1','y1','x2','y2') never includes 'page', and validate_bboxes keys the page-dims lookup off element.get('page_index') only (357-358) \u2014 bbox.get('page') is never read anywhere in the file.\nExpected files: lexy-app/backend/services/document_package/validators.py, docs/INGESTION_PIPELINE.md\nAcceptance criteria: A synthetic element with mismatched bbox.page vs page_index fails validation with a new stable issue code\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k Structural -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-009",
    "title": "Have downgrade() refuse when it would orphan existing anchors, or add a round-trip test proving t...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding security_paths:data-03\nScope: Have downgrade() refuse when it would orphan existing anchors, or add a round-trip test proving token_id stability is not expected/needed across downgrade+upgrade and document that explicitly.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A downgrade-then-upgrade cycle of migration 025 regenerates every token_id, silently orphaning existing reading_selections.anchors references (the selection row survives but its anchor no longer resolves), with no error surfaced.\nEvidence: Read both directly: _tokenize assigns a fresh uuid.uuid4() per token on every run; downgrade() is a bare 'ALTER TABLE book_blocks DROP COLUMN tokens' with no guard. schemas.py:706 confirms ReadingSelectionAnchor.token_id is that same UUID, commented 'replaced token_index for stability across edits' \u2014 the join key reading_selections.anchors depends on, with no FK.\nExpected files: lexy-app/backend/migrations/versions/025_block_token_ids.py, lexy-app/backend/models/schemas.py\nAcceptance criteria: downgrade() either preserves token_id stability or explicitly refuses/warns when reading_selections rows with non-empty anchors exist\nValidation: cat lexy-app/backend/migrations/versions/025_block_token_ids.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-010",
    "title": "Emit a visible row count of deleted orphan rows during upgrade (RAISE NOTICE or a logged SELECT C...",
    "description": "Priority: P1 | severity low | confidence confirmed | from audit finding db_migrations:db-05\nScope: Emit a visible row count of deleted orphan rows during upgrade (RAISE NOTICE or a logged SELECT COUNT(*) before the DELETE).\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any user preference row referencing a category outside the 18 seeded YouTube categories (e.g. stale/renamed data from an earlier app version) is permanently deleted with no visibility into how many rows or which users were affected.\nEvidence: Step 5 (lines 66-69) runs `DELETE FROM user_video_category WHERE video_category NOT IN (SELECT category FROM video_category)` unconditionally, immediately before adding the FK in step 6, with no row count logged.\nExpected files: lexy-app/backend/migrations/versions/013_video_category_cleanup.py\nAcceptance criteria: migration emits a visible count of deleted orphan rows during upgrade\nValidation: cat lexy-app/backend/migrations/versions/013_video_category_cleanup.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-011",
    "title": "Add Depends(require_admin) to both routes, matching delete_book/delete_page and POST /phrases/see...",
    "description": "Priority: P2 | severity high | confidence confirmed | from audit finding repo_structure:sec-01\nScope: Add Depends(require_admin) to both routes, matching delete_book/delete_page and POST /phrases/seed; add a non-admin-403 regression test; record the finding in docs/SECURITY.md.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any authenticated non-admin user can enumerate every operator-curated document package name and force checksum/structural validation compute via POST /books/import.\nEvidence: list_packages/import_package depend only on Depends(get_current_user); the same file's delete_document/delete_page (384,399) thread is_admin into book_service, an established gating pattern these two routes skip. Confirmed identically in three earlier 2026-08-03 audit write-ups but still has no entry in docs/SECURITY.md's numbered findings, despite CLAUDE.md \u00a714 requiring one.\nExpected files: lexy-app/backend/routers/books.py\nAcceptance criteria: non-admin caller gets 403 from GET /books/packages and POST /books/import; docs/SECURITY.md lists the finding with the required four fields\nValidation: rg -n 'require_admin|is_admin' lexy-app/backend/routers/books.py; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-012",
    "title": "Add Depends(require_admin) (core/deps.py:54) to both GET /books/packages and POST /books/import, ...",
    "description": "Priority: P2 | severity high | confidence confirmed | from audit finding security_paths:sec-01\nScope: Add Depends(require_admin) (core/deps.py:54) to both GET /books/packages and POST /books/import, matching the file's existing owner-or-admin pattern.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any authenticated non-admin learner can enumerate every operator-curated package name and, for each real one, force a write of import_result.json into that operator-owned directory and burn checksum/structural validation compute, not just read data.\nEvidence: Re-read at HEAD 2026-08-05: books.py:183,189 depend only on get_current_user; the same file's delete_book/delete_page (383-384,398-399) thread is_admin, a pattern these two skip. book_import_service.import_package:123 defaults write_result=True and the router (207-211) never passes write_result=False, so result.write(pkg.package_root) (168/193/209) fires on every successful validation.\nExpected files: lexy-app/backend/routers/books.py, lexy-app/backend/services/book_import_service.py\nAcceptance criteria: Non-admin caller gets 403 admin_required from both routes; Admin behavior unchanged; A regression test asserts a non-admin cannot trigger import_result.json to be written\nValidation: rg -n 'require_admin' lexy-app/backend/routers/books.py; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-013",
    "title": "Add Depends(require_admin) to GET /books/packages and POST /books/import; update docs/SECURITY.md...",
    "description": "Priority: P2 | severity medium | confidence confirmed | from audit finding ingestion_pipeline:ing-sec-01\nScope: Add Depends(require_admin) to GET /books/packages and POST /books/import; update docs/SECURITY.md's admin-gated route list and the TestImportEndpoint auth expectations accordingly.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any authenticated non-admin user can enumerate every worker-produced package under the shared, non-user-scoped PACKAGE_ROOT and trigger disk-heavy checksum verification plus an import_result.json write on any of them, unlike equivalent global-catalog endpoints elsewhere in the app.\nEvidence: routers/books.py:182-211 gates GET /books/packages and POST /books/import on Depends(get_current_user) only, no require_admin (deps.py:54, pattern used in phrases.py:36 and lemma_corrections.py:64/90/117/150). test_document_package.py:1137-1149 asserts a freshly-registered non-admin user succeeds. docs/SECURITY.md:543 lists admin-gated routes and omits these two.\nExpected files: lexy-app/backend/routers/books.py, lexy-app/backend/core/deps.py, docs/SECURITY.md\nAcceptance criteria: Non-admin POST /books/import returns 403 admin_required; Non-admin GET /books/packages returns 403 admin_required; docs/SECURITY.md lists both routes under admin-gated routes\nValidation: rg -n 'require_admin' lexy-app/backend/routers/books.py; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k TestImportEndpoint -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-014",
    "title": "CLAUDE.md \u00a712: add a sentence: 'Update docs/SECURITY.md when you change auth/upload/settings/subp...",
    "description": "Priority: P2 | severity medium | confidence confirmed | from audit finding tests_ci:security-07\nScope: CLAUDE.md \u00a712: add a sentence: 'Update docs/SECURITY.md when you change auth/upload/settings/subprocess paths (see that file's preamble for the four required fields). Also update docs/SUMMARY.md, docs/TESTS.md, docs/COMMON_ERRORS.md if the change affects their domains.'\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A developer makes a security-relevant change (e.g. auth middleware), reads CLAUDE.md \u00a712 as instructed, finds no mention of docs/SECURITY.md, and commits without updating it. The living security tracker becomes stale. Audits miss the change.\nEvidence: docs/SECURITY.md opens with 'Read \u00a712 of CLAUDE.md before committing security-relevant code' and lists four trigger files (docs/SUMMARY.md, docs/TESTS.md, docs/SECURITY.md, docs/COMMON_ERRORS.md). However, CLAUDE.md \u00a712 does NOT mention that these four files exist or are mandatory to update on every change. It only says 'never read .env' and 'all state changes go through progression_service'. The cross-reference is broken: SECURITY.md assumes a reader knows which four docs to update; CLAUDE.md does not tell them.\nExpected files: docs/SECURITY.md, CLAUDE.md\nAcceptance criteria: CLAUDE.md \u00a712 names the four tracker files; New security-related PRs update docs/SECURITY.md before merge\nValidation: grep -n 'docs/SECURITY.md\\|docs/TESTS.md\\|docs/SUMMARY.md' CLAUDE.md | grep -v '\u00a7' | wc -l; grep '^##' docs/SECURITY.md | head -3\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-015",
    "title": "Move the Docker build context to the repo root (COPY subtitle-scraper/ and data/ alongside backen...",
    "description": "Priority: P4 | severity critical | confidence confirmed | from audit finding repo_structure:arch-01\nScope: Move the Docker build context to the repo root (COPY subtitle-scraper/ and data/ alongside backend/) and add a CI smoke test that builds the image and curls a health endpoint.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any router importing matcher_service (phrases.py, matcher.py, chat.py) pulls this in at app startup, so README's documented `docker compose up` crashes before uvicorn binds a port.\nEvidence: Dockerfile:28,31 COPY only backend/ and frontend/dist (WORKDIR=/app); matcher_service.py:35 computes Path(__file__).resolve().parents[3], which under the image resolves to '/', giving a nonexistent _SCRAPER_PATH='/subtitle-scraper'; line 43 does `import phrase_finder as _pf` at module scope.\nExpected files: lexy-app/Dockerfile, lexy-app/backend/services/matcher_service.py\nAcceptance criteria: docker compose up boots without crashing; a CI job builds the image and gets a 2xx from a health endpoint\nValidation: cd lexy-app && docker compose build && docker compose up -d && curl -f http://localhost:8000/api/v1/auth/login -X POST -d '{}' -H 'Content-Type: application/json'\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-016",
    "title": "On load, reconcile execution.worktree_path against a freshly-computed path: if the stored path ex...",
    "description": "Priority: P4 | severity high | confidence confirmed | from audit finding repo_structure:arch-02\nScope: On load, reconcile execution.worktree_path against a freshly-computed path: if the stored path exists, keep it; if not and the recomputed one does, adopt it explicitly; otherwise park with a message naming both paths.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: An operator who relocates workers_root gets FileNotFoundError on the loop's next dispatch of any in-flight task, with no diagnostic naming the mismatch; documented workaround is manually archiving the execution record.\nEvidence: A loaded TaskExecution's stored worktree_path is used verbatim to build the GitGateway at orchestrator.py:2017-2018; WorkerRepoManager._path (worker_env.py:310) computes root_dir/task_id fresh only inside .create(). If [paths].workers_root changes between runs, an existing execution record keeps pointing at the old path and nothing reconciles it \u2014 matches docs/AUTOLOOP_TODO.md A3, still unresolved.\nExpected files: autoloop/orchestrator.py, autoloop/worker_env.py\nAcceptance criteria: moving workers_root with an in-flight task either resumes at the stored path or parks with an explicit message naming both paths, never a bare FileNotFoundError\nValidation: python3 -m pytest autoloop/tests/test_postcommit_flow.py autoloop/tests/test_m1_hardening.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-017",
    "title": "Emit a transcript event when each agent starts (not only on completion), and surface per-domain s...",
    "description": "Priority: P4 | severity high | confidence confirmed | from audit finding repo_structure:arch-03\nScope: Emit a transcript event when each agent starts (not only on completion), and surface per-domain state in `status`.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: For a fan-out taking tens of minutes, an operator watching an unattended run sees an empty raw/ directory and a silent loop.out, then a burst on completion \u2014 unable to distinguish 'working' from 'wedged' without inspecting process state by hand.\nEvidence: _run_agents (executor.py:526-527) does `list(pool.map(agent_runner.run, specs))` and only writes to raw_dir in the loop starting at line 528, after the whole map call returns \u2014 no per-agent start event is logged anywhere in this file.\nExpected files: autoloop/audit/executor.py\nAcceptance criteria: `autoloop status` during an audit run shows which domain agents have started vs. finished; a transcript event exists per agent start\nValidation: python3 -m pytest autoloop/tests/test_audit_executor.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-018",
    "title": "Move the Docker build context to the repo root (COPY subtitle-scraper/ + data/ alongside lexy-app...",
    "description": "Priority: P4 | severity high | confidence confirmed | from audit finding security_paths:arch-01\nScope: Move the Docker build context to the repo root (COPY subtitle-scraper/ + data/ alongside lexy-app/backend/) and fix the WORKDIR-relative parents[N] arithmetic, or document that the Dockerfile is not the supported deploy path.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any router importing matcher_service (phrases.py, matcher.py, chat.py) crashes app startup with ModuleNotFoundError, so `docker compose up` (README's documented quickstart) never binds a port; the actual deploy paths (Procfile/entrypoint.sh per docs/ROADMAP.md P2) are unaffected, hence high rather than critical.\nEvidence: Read both files directly: Dockerfile COPYs only backend/ and frontend/dist (WORKDIR /app), never subtitle-scraper/ or data/ (repo-root siblings). matcher_service.py:35 computes Path(__file__).resolve().parents[3], which under /app/backend/services/matcher_service.py resolves to '/', giving a nonexistent _SCRAPER_PATH; line 43's module-scope 'import phrase_finder' then fails at import.\nExpected files: lexy-app/Dockerfile, lexy-app/backend/services/matcher_service.py\nAcceptance criteria: docker compose up boots without crashing; A CI or manual smoke test gets a 2xx from a health endpoint after `docker compose up`\nValidation: cd lexy-app && docker compose build && docker compose up -d && curl -f http://localhost:8000/api/v1/auth/login -X POST -d '{}' -H 'Content-Type: application/json'\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-019",
    "title": "When N consecutive rounds produce a byte-identical validation summary, stop retrying and park as ...",
    "description": "Priority: P4 | severity medium | confidence confirmed | from audit finding repo_structure:arch-04\nScope: When N consecutive rounds produce a byte-identical validation summary, stop retrying and park as an environmental blocker instead of charging the task's attempt count.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A validation failure that cannot be fixed by retrying (missing credentials, unreachable service) still consumes the task's finite attempt budget every round, until the task is wrongly exhausted rather than parked as an environmental blocker.\nEvidence: docs/AUTOLOOP_TODO.md B6 describes rt-01 burning 3 of 5 attempts on byte-identical environmental validation failures (missing DB credentials); grepping orchestrator.py for any 'consecutive identical validation' detection finds none \u2014 the only 'byte-identical' hit (line 23) is an unrelated resubmit-content comment.\nExpected files: autoloop/orchestrator.py\nAcceptance criteria: two consecutive rounds with identical validation output park as `environmental` without a third retry\nValidation: python3 -m pytest autoloop/tests/test_m1_hardening.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-020",
    "title": "Add a baseline migration that creates the content core (video, word_table, sentence, language_tab...",
    "description": "Priority: P4 | severity medium | confidence confirmed | from audit finding security_paths:arch-02\nScope: Add a baseline migration that creates the content core (video, word_table, sentence, language_table, etc.) so the chain is self-sufficient from an empty database.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A brand-new database cannot be built via `alembic upgrade head` alone (workaround: pg_dump schema-only from a live DB), so CI schema validation and new-contributor setup have no self-sufficient path.\nEvidence: Confirmed no baseline migration exists (glob of migrations/versions/00*.py starts at 001) and no fix has been added since docs/COMMON_ERRORS.md's own \u00a79 write-up. Whether the chain literally dies at 006 is documented there, not independently reproduced this session \u2014 that specific claim is probable, not confirmed; 'no baseline migration exists and none added since' is confirmed.\nExpected files: lexy-app/backend/migrations/versions, docs/COMMON_ERRORS.md\nAcceptance criteria: createdb + alembic upgrade head succeeds on an empty database and produces all expected tables\nValidation: createdb fresh_test; cd lexy-app/backend && DB_NAME=fresh_test alembic upgrade head\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-021",
    "title": "Add a pytest plugin or pre-commit hook to reject SELECT...LIMIT without ORDER BY in test files. O...",
    "description": "Priority: P4 | severity medium | confidence confirmed | from audit finding tests_ci:arch-08\nScope: Add a pytest plugin or pre-commit hook to reject SELECT...LIMIT without ORDER BY in test files. OR: create a macro `pick_one_ordered(pool, table, pk_col)` and enforce its use in code review. Document in TESTS.md.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Flaky tests in CI (xdist) and on fresh databases (new contributor setup). The same architectural anti-pattern is documented but not enforced. Test review burden increases.\nEvidence: docs/COMMON_ERRORS.md documents known isolation hazards with shared-catalog queries (\u00a72: xdist flakes, \u00a72d: fresh-database failures on unordered picks). The root cause is identical: SELECT...FROM <shared catalog> LIMIT 1 without deterministic ORDER BY picks different rows on different database states. Marked 'latent bugs' in 7 test files; TESTS.md acknowledges 'any ... FROM <shared catalog> LIMIT 1 without an ORDER BY ... is a latent bug'. No linter or test catches this pattern. A new test written by a developer who hasn't read COMMON_ERRORS.md re-introduces it.\nExpected files: docs/COMMON_ERRORS.md\nAcceptance criteria: New tests cannot use unordered LIMIT 1 on catalog tables; Existing violations are fixed (see xdist-02); Grep `SELECT.*LIMIT 1' test files returns 0 matches on catalog tables without ORDER BY\nValidation: rg 'SELECT.*FROM (phrase_table|grammar_rule_table|video).*LIMIT 1' lexy-app/backend/tests | rg -v 'ORDER BY' | wc -l; cd lexy-app/backend && python3 -m pytest -n auto -q 2>&1 | grep -E 'FAILED|FLAKY' | wc -l\nParallelizable: yes\nNote: unresolved dependency references from the audit: ['xdist-02']",
    "depends_on": []
  },
  {
    "id": "au-022",
    "title": "Align test_document_package.py's imports to the `from backend.services...` convention used by eve...",
    "description": "Priority: P4 | severity low | confidence confirmed | from audit finding ingestion_pipeline:ing-test-01\nScope: Align test_document_package.py's imports to the `from backend.services...` convention used by every other backend test file, removing the cwd-on-sys.path dependency.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Running the backend suite with plain `pytest` instead of `python3 -m pytest` from lexy-app/backend silently loses all 107 A2 document-package tests with no failure signal, just a lower pass count.\nEvidence: test_document_package.py:13-14 imports `from services import book_import_service` / `from services.document_package import contract` (bare), the only backend test file not using `from backend.services import ...`. docs/COMMON_ERRORS.md:320-337 documents this resolves only under `python3 -m pytest` and silently drops 1258\u21921151 tests with the bare `pytest` console script, no collection error.\nExpected files: lexy-app/backend/tests/test_document_package.py\nAcceptance criteria: `cd lexy-app/backend && pytest tests/test_document_package.py -q` (bare console script) collects and runs all 107 tests, matching `python3 -m pytest`\nValidation: cd lexy-app/backend && pytest tests/test_document_package.py -q; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-023",
    "title": "Add GitHub Actions steps for root `pytest` (hermetic, ~2 min), backend `python3 -m pytest -n auto...",
    "description": "Priority: P5 | severity high | confidence confirmed | from audit finding tests_ci:ci-01\nScope: Add GitHub Actions steps for root `pytest` (hermetic, ~2 min), backend `python3 -m pytest -n auto` against test DB (async, ~60s), frontend `npm run test --run` (vitest, ~30s). Skip frontend if only backend code changed.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Regressions in all three suites could merge without detection. A PR touching backend code could break 1258 tests without CI signal. Pipeline changes are untested in CI entirely.\nEvidence: .github/workflows/dependency-audit.yml runs only `pip-audit` and `npm audit` on push/PR to main; docs/TESTS.md documents THREE required test suites (root pytest, backend pytest -n auto, frontend vitest run) that CI never invokes. Root suite covers pipeline modules; backend covers 1258 tests; frontend covers 322 tests. None validated on PR or main push.\nExpected files: .github/workflows/dependency-audit.yml\nAcceptance criteria: CI workflow runs all three test suites on every PR to main; Any test failure blocks the PR; Validation passes on a real code change before merge\nValidation: grep -c 'run: pytest' .github/workflows/*.yml; grep 'pytest.*backend' .github/workflows/dependency-audit.yml\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-024",
    "title": "Root pytest.ini: add `lexy-app/backend/tests` to testpaths, OR document that backend must be run ...",
    "description": "Priority: P5 | severity medium | confidence confirmed | from audit finding tests_ci:backend-06\nScope: Root pytest.ini: add `lexy-app/backend/tests` to testpaths, OR document that backend must be run separately and add an alias `pytest-all` that runs all three. Clarify in TESTS.md: 'bare pytest covers root + autoloop only; backend needs separate run'.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Backend test suite is invisible to root `pytest` runs. Developers validate 'root passes' thinking it includes backend, when it actually only runs pipeline+autoloop. The 1258 backend tests remain unchecked until someone explicitly runs `cd lexy-app && pytest` or `cd lexy-app/backend && pytest -n auto`.\nEvidence: lexy-app/pytest.ini exists with asyncio settings and testpaths=backend/tests. However, a developer (or CI) running `cd lexy-app && pytest` will find lexy-app/pytest.ini but then `python3 -m pytest` from repo root falls through to root pytest.ini. Root pytest.ini testpaths includes autoloop/tests but NOT lexy-app/backend/tests explicitly, so a bare root `pytest` skips the backend suite entirely \u2014 despite CLAUDE.md \u00a711 and docs/TESTS.md claiming it is a required part of validation.\nExpected files: lexy-app/backend/pytest.ini\nAcceptance criteria: A bare `pytest` from repo root includes backend test count in summary; OR: docs/TESTS.md clearly states backend is never run by bare pytest; CI runs backend suite as its own job\nValidation: cd /path/to/repo && python3 -m pytest -q --co 2>&1 | grep -c 'test_'; cd /path/to/repo && python3 -m pytest -q --tb=no 2>&1 | grep -E '(passed|failed)'\nParallelizable: yes",
    "depends_on": [
      "au-023"
    ]
  },
  {
    "id": "au-025",
    "title": "Add GitHub Actions step: `ruff check . --select E,F` (from repo root, fail on any finding). Mirro...",
    "description": "Priority: P5 | severity medium | confidence confirmed | from audit finding tests_ci:ruff-05\nScope: Add GitHub Actions step: `ruff check . --select E,F` (from repo root, fail on any finding). Mirrors the documented local validation command.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A PR passes all tests and CI but violates repo linting rules, discovered only on a human `ruff check` run. New violations creep in; the 153\u21920 cleanup of 2026-07-27 is lost incrementally.\nEvidence: ruff.toml enables E (all indent/whitespace/blank rules) but disables E501 (line-too-long) with comment '405 violations across existing codebase'. However: (1) root `pytest` does not run ruff, only Python tests; (2) CI never runs ruff at all; (3) a PR could introduce E/F violations without detection. COMMON_ERRORS.md notes 'ruff check . from REPO ROOT' is required; CLAUDE.md \u00a711 says it is 'part of validation' but CI does not enforce it.\nExpected files: ruff.toml\nAcceptance criteria: CI runs ruff check on every PR; Any E or F violation blocks merge; Baseline is clean (0 findings)\nValidation: ruff check . --select E,F 2>&1 | tail -1; grep -c 'ruff' .github/workflows/dependency-audit.yml\nParallelizable: yes",
    "depends_on": [
      "au-023"
    ]
  },
  {
    "id": "au-026",
    "title": "When a real Codex CLI exhaustion is observed, capture its exact stderr/stdout as a fixture and ad...",
    "description": "Priority: P5 | severity low | confidence confirmed | from audit finding repo_structure:test-01\nScope: When a real Codex CLI exhaustion is observed, capture its exact stderr/stdout as a fixture and add both the pattern and a regression test asserting is_quota_exhausted classifies it correctly.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A real exhaustion message that does not match any of the 12 substrings degrades silently to an ordinary consecutive-failure count instead of parking with a clear diagnosis, until an operator manually adds the pattern after seeing the transcript.\nEvidence: The module's own docstring (lines 3-8, 17-22) states the exact wording Codex CLI uses for quota exhaustion 'cannot be verified from this repository' and the pattern list is 'a starting point, not a verified list'; no test exercises a real captured CLI transcript, only synthetic strings.\nExpected files: autoloop/codex/quota.py\nAcceptance criteria: a captured real-world exhaustion transcript exists as a test fixture and is classified correctly by is_quota_exhausted\nValidation: python3 -m pytest autoloop/tests -k quota -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-027",
    "title": "Add a `pages_in` count (len(pkg.page_dims) or distinct declared page_index values) to build_resul...",
    "description": "Priority: P6 | severity low | confidence confirmed | from audit finding ingestion_pipeline:ing-improve-01\nScope: Add a `pages_in` count (len(pkg.page_dims) or distinct declared page_index values) to build_result's counts dict alongside pages_written.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A page whose every element was skipped produces no signal in import_result.json \u2014 pages_written silently omits it and there is no pages_in to compare against, unlike the elements_in/elements_skipped pair that makes element loss auditable.\nEvidence: docs/INGESTION_PIPELINE.md \u00a74's import_result.json example carries `counts.pages` (document page total); result.py:104-113's shipped `counts` has no pages/pages_in field, only `pages_written` = distinct page_index values among surviving plan blocks (persistence.py:197-207).\nExpected files: docs/INGESTION_PIPELINE.md, lexy-app/backend/services/document_package/result.py\nAcceptance criteria: import_result.json counts include pages_in; a synthetic package where one declared page's elements are all skipped shows pages_in > pages_written, covered by a test\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k pages -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-028",
    "title": "Mark B9 in docs/AUTOLOOP_TODO.md as RESOLVED with a pointer to `_rebase_execution_if_stale`, matc...",
    "description": "Priority: P7 | severity medium | confidence confirmed | from audit finding repo_structure:doc-01\nScope: Mark B9 in docs/AUTOLOOP_TODO.md as RESOLVED with a pointer to `_rebase_execution_if_stale`, matching the convention used for A2/B4b/B10 in the same file.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: The tracker misrepresents a shipped fix as an open P1 bug, risking a duplicate task scheduled for already-resolved work and misleading anyone triaging AUTOLOOP_TODO.md by priority.\nEvidence: AUTOLOOP_TODO.md B9 (no resolved marker) describes the base-pinning bug and proposes 're-base the record ... or refuse with a message naming both shas'; orchestrator.py:3451-3525 `_rebase_execution_if_stale` already implements exactly that (quarantine-and-rebase when review_round==0, refuse naming both shas otherwise), its own docstring citing 'docs/AUTOLOOP_TODO.md B9'.\nExpected files: docs/AUTOLOOP_TODO.md, autoloop/orchestrator.py\nAcceptance criteria: docs/AUTOLOOP_TODO.md B9 carries a resolved marker and cites orchestrator.py:_rebase_execution_if_stale\nValidation: grep -n 'B9' docs/AUTOLOOP_TODO.md; rg -n '_rebase_execution_if_stale' autoloop/orchestrator.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-029",
    "title": "Update CLAUDE.md \u00a78b: delete the sentence naming upsert_word_status or change it to 'which was de...",
    "description": "Priority: P7 | severity medium | confidence confirmed | from audit finding tests_ci:doc-03\nScope: Update CLAUDE.md \u00a78b: delete the sentence naming upsert_word_status or change it to 'which was deleted 2026-05-19'. Cross-reference the atomicity rule immediately above instead.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: New developers reading \u00a78b top-down conclude the two-call non-atomic flow is still live. They may re-introduce the bug when refactoring status updates, or waste time hunting a phantom code path.\nEvidence: CLAUDE.md \u00a78b states `status_marked_known` 'forced to known by word_service.upsert_word_status (called from the router before apply_progression)'. That function was deleted 2026-05-19 (#2). Router now makes single apply_progression(..., status_override=...) call. CLAUDE.md \u00a78b names this correctly in atomicity rule two paragraphs away, but the obsolete sentence appears first, and COMMON_ERRORS.md notes this same doc-drift was flagged THREE consecutive times before fix on 2026-08-04.\nExpected files: CLAUDE.md, docs/SUMMARY.md\nAcceptance criteria: CLAUDE.md \u00a78b makes no reference to upsert_word_status as live; The atomicity rule appears in the first paragraph; A grep for 'upsert_word_status' in CLAUDE.md returns zero live-code mentions\nValidation: grep 'upsert_word_status' CLAUDE.md | grep -v 'Historical\\|deleted'; grep -A 3 'status_marked_known' CLAUDE.md | head -10\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-030",
    "title": "Update CLAUDE.md:111 to reference the correct line numbers: 'line 215' for the function or 'line ...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding docs_drift:doc-drift-01\nScope: Update CLAUDE.md:111 to reference the correct line numbers: 'line 215' for the function or 'line 259' for the transaction.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Stale line number reference makes it harder to locate the code being documented; readers may look at the wrong section.\nEvidence: CLAUDE.md line 111 cites `apply_progression` at 'line 178' but the function definition begins at line 215 and the transaction starts at line 259 (progression_service.py).\nExpected files: CLAUDE.md\nAcceptance criteria: Line number in CLAUDE.md matches the actual location of apply_progression or its transaction block\nValidation: grep -n 'async def apply_progression' lexy-app/backend/services/progression_service.py; grep -n 'async with conn.transaction' lexy-app/backend/services/progression_service.py | head -1\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-031",
    "title": "Update CLAUDE.md:126 to reference the correct lines: 'lines 566-581' instead of 'line ~333'.",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding docs_drift:doc-drift-02\nScope: Update CLAUDE.md:126 to reference the correct lines: 'lines 566-581' instead of 'line ~333'.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Stale line number reference (~200 lines off) makes documentation unreliable for code navigation.\nEvidence: CLAUDE.md line 126 cites `_update_srs` 'line ~333' for the no-op check on `action=\"incorrect\"`, but the actual check is at lines 566-581 (progression_service.py).\nExpected files: CLAUDE.md\nAcceptance criteria: Line number in CLAUDE.md matches the location of the incorrect/no-op check in _update_srs\nValidation: grep -n 'action == \"incorrect\"' lexy-app/backend/services/progression_service.py; sed -n '566,581p' lexy-app/backend/services/progression_service.py | grep -c 'no-op if missing'\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-032",
    "title": "Update CLAUDE.md \u00a73 to read `lexy-app/backend/migrations/versions/` instead of `alembic/versions/`.",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding ingestion_pipeline:ing-doc-01\nScope: Update CLAUDE.md \u00a73 to read `lexy-app/backend/migrations/versions/` instead of `alembic/versions/`.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Anyone (or any agent) trusting the authoritative CLAUDE.md to find migrations searches a nonexistent alembic/versions/ path.\nEvidence: CLAUDE.md:38 states migrations live in `alembic/versions/`; lexy-app/backend/alembic.ini:2 sets `script_location = migrations`, and the 37 version files actually live at lexy-app/backend/migrations/versions/*.py.\nExpected files: CLAUDE.md\nAcceptance criteria: CLAUDE.md's migrations path matches alembic.ini's script_location and the real directory\nValidation: grep -n script_location lexy-app/backend/alembic.ini; ls lexy-app/backend/migrations/versions | wc -l\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-033",
    "title": "Delete the stale row at SUMMARY.md:333, keeping the more complete row at 335.",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding repo_structure:doc-02\nScope: Delete the stale row at SUMMARY.md:333, keeping the more complete row at 335.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A reader of the file-level index (CLAUDE.md \u00a712 designates SUMMARY.md as the canonical navigation aid) hits two entries for the same three files and cannot tell which is authoritative.\nEvidence: Two separate table rows both keyed `prompts.py, config.py, cli.py` exist four lines apart (333 and 335); the second is materially newer (mentions blockers_dir, required workers_root, _RESOLUTION_PRECONDITIONS) but the older row at 333 was never removed.\nExpected files: docs/SUMMARY.md\nAcceptance criteria: only one `prompts.py, config.py, cli.py` row remains in docs/SUMMARY.md\nValidation: grep -n 'prompts.py.*config.py.*cli.py' docs/SUMMARY.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-034",
    "title": "Update the verification-check line numbers in docs/SECURITY.md's Verified-strengths section to ma...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding security_paths:doc-01\nScope: Update the verification-check line numbers in docs/SECURITY.md's Verified-strengths section to match current search_service.py, or switch the check to a content-based grep instead of hardcoded line numbers.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: SECURITY.md's own rule is that a finding without an accurate verification check 'rots into a claim nobody re-tests'; the stale line numbers make the documented grep silently miss the real matches if someone re-runs it literally.\nEvidence: docs/SECURITY.md:521 cites the safe f-string SQL (loop-index-only interpolation) at 'search_service.py:62,103,216'; grepping f\"(SELECT|INSERT|UPDATE|DELETE) at HEAD finds it at lines 164, 205, 322 instead. The interpolations themselves are still safe (term_idx loop counter only) \u2014 only the cited line numbers are stale.\nExpected files: docs/SECURITY.md, lexy-app/backend/services/search_service.py\nAcceptance criteria: docs/SECURITY.md's cited line numbers for the search_service.py f-string SQL match the current file\nValidation: rg -n 'f\"(SELECT|INSERT|UPDATE|DELETE)' lexy-app/backend/services/search_service.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-035",
    "title": "Update docs/SUMMARY.md:49 to reference the correct line numbers: 'lines 516-517' instead of 'line...",
    "description": "Priority: P7 | severity info | confidence confirmed | from audit finding docs_drift:doc-drift-03\nScope: Update docs/SUMMARY.md:49 to reference the correct line numbers: 'lines 516-517' instead of 'line 268'.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Incorrect line number makes the referenced code location unreachable; creates inconsistency between documentation files.\nEvidence: docs/SUMMARY.md line 49 states `_update_srs` 'now skips active-card creation for grammar_rule (line 268)' but the actual skip logic is at lines 516-517, not line 268.\nExpected files: CLAUDE.md, docs/SUMMARY.md\nAcceptance criteria: Line number in docs/SUMMARY.md correctly points to the grammar_rule skip logic\nValidation: grep -n 'if item_type == \"grammar_rule\" and direction == \"active\"' lexy-app/backend/services/progression_service.py\nParallelizable: yes",
    "depends_on": [
      "au-031"
    ]
  }
]
```
