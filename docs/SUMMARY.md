# SUMMARY.md

File-level index of the codebase. **"Where do I look to change X?"** — answered here. Update when files are added, removed, or change responsibility.

Conventions: each entry is `path — purpose. Touchpoints.` Touchpoints list adjacent files you usually edit together.

---

## Backend — `lexy-app/backend/`

### Entry + infra
| Path | Purpose |
|---|---|
| `main.py` | FastAPI app. Lifespan: pool init, phrase + grammar seeds, resume pending content requests. Mounts all routers. CORS env-driven via `CORS_ORIGINS` (`_parse_cors_origins`). |
| `database.py` | asyncpg pool create/close/get. Single global pool. TLS via `DB_SSL_MODE` (S4: `disable`/`require`/`verify-ca`/`verify-full`): `_resolve_ssl` for the asyncpg pool, `resolve_sslmode` (libpq string, omit-on-unset) reused by Alembic + the scraper. |
| `core/deps.py` | `get_current_user` JWT verification dependency. |
| `core/security.py` | password hashing (bcrypt), `encode_token` / `decode_token`. |
| `models/schemas.py` | All Pydantic request/response models. ~600 lines, one file. |
| `alembic.ini`, `migrations/env.py` | Alembic config. `env.py` builds the URL from `DB_*` env + appends `?sslmode=` via `database.resolve_sslmode` (S4). |
| `migrations/versions/0XX_*.py` | 37 migrations (head `037`), append-only. Schema lives here. |

### Routers (HTTP surface) — `lexy-app/backend/routers/`
| Path | Endpoints | Calls |
|---|---|---|
| `auth.py` | `/auth/register`, `/auth/login` | `auth_service` |
| `words.py` | `/words/by-text`, `/words/knowledge`, `/words/{type}/{id}/status`, `/words/word/{id}/transcript-click` | `word_service`, `progression_service`, `usage_events_service` |
| `srs.py` | `/srs/due`, `/srs/review/{card_id}` | `review_service` |
| `chat.py` | `/chat/sessions`, `/chat/sessions/{id}/messages`, `/chat/guided-sessions`, `/chat/guided-sessions/{id}/messages`, `/complete` | `chat_service`, `guided_chat_service`, `llm_service`, `progression_service` |
| `books.py` | `/books/upload`, `/books`, `/books/{id}`, `/books/{id}/pages`, `/books/{id}/pages/{n}`, `/books/{id}/pages/{n}/image`, `/blocks/{id}` PATCH, `/blocks/{id}/llm-repair`, `/pages/{n}/batch-llm-repair` | `book_service`, `book_llm_service` |
| `reading.py` | `/books/{id}/pages/{n}/word-statuses`, `/books/{id}/selections`, `/books/{id}/pages/{n}/selections`, `/reading/selections/due`, `/reading/selections/{id}` PATCH/DELETE, `/reading/selections/{id}/review`, `/reading/translate`, `/reading/explain` | `reading_service`, `reading_llm_service`, `progression_service`, `book_service` |
| `phrases.py` | `/phrases/match`, `/phrases/seed` | `matcher_service`, `phrase_service` |
| `matcher.py` | `/sentences/match` (shared with phrases) | `matcher_service` |
| `insights.py` | `/insights/cards`, `/insights/prep`, `/insights/prep/generate-examples` | `insights_service`, `llm_service` |
| `analytics.py` | `/analytics/unknown-frequent`, `/learning-frequent`, `/recently-failed`, `/most-interacted` | `usage_events_service` |
| `recommendations.py` | `/recommendations/sentences`, `/videos`, `/items` | `recommendation_service` |
| `playlists.py` | `/playlists/generate` | `playlist_service` |
| `settings.py` | `/settings/preferences` GET/PUT | `settings_service` |
| `reminders.py` | `/reminders/summary` | `reminder_service` |
| `search.py` | `/search`, `/suggest`, `/word-forms`, `/languages`, `/categories`, `/video-sentences` (legacy public, no auth) | `search_service` |
| `videos.py` | `/videos/{id}/reading-stats` | `reading_stats_service` |
| `content_requests.py` | `/content-requests` POST/GET. Keeps the S7 `content_id` validation (normalize to a canonical YouTube id at the API boundary) and the subprocess spawn of `subtitle-scraper/pipeline.py --requests-only`. The SQL moved to `content_request_service` (arch-05); the router still decides *whether* to spawn, from the returned `status`. | `content_request_service` + subprocess |
| `notifications.py` | `/notifications/stream` (SSE). Owns the `_yield_unseen` generator and its per-row mark-after-yield ordering (disconnect leaves un-yielded rows unseen for re-delivery) — that sequencing is deliberately **not** in the service, since it's about when the consumer resumes, not about the SQL. LISTEN/NOTIFY refactor deferred (TODO #4b). | `notification_service` |
| `word_lists.py` | `/word-lists` POST/GET, `/word-lists/{id}` GET/DELETE, `/word-lists/{id}/export` (text/plain), `/word-lists/{id}/mark-unknown-learning`. `GET /word-lists/{id}` takes optional `limit` (1–1000) and `offset` (≥0), which window **`entries` only** — `total` and `counts` stay whole-list on every page, so the status badges and the mark-learning count keep describing the list rather than the window. Omitting both returns the pre-pagination response unchanged. All routes user-scoped, 404 (not 403) on someone else's id — ownership resolves before the params are read, so paging cannot widen access. | `word_list_service`, `progression_service` |
| `lemma_corrections.py` | `POST /lemma-corrections` (auth + throttle, flag → candidate) + `GET /admin/lemma-corrections` (`require_admin`, queue) + `POST /admin/lemma-corrections/{id}/{accept,reject}` (`require_admin`; accept transactionally upserts `lemma_override`) + `…/{id}/adjudicate` (`require_admin`, read-only dry-run LLM proposal via injected `get_lemma_adjudicator` → 503 if none) — #39 3A/3B/3C. | `lemma_correction_service` |

### Services (business logic) — `lexy-app/backend/services/`
| Path | Owns |
|---|---|
| `progression_service.py` | **Single source of truth** for knowledge-state changes. `_RULES` dict maps event → ProgressionDelta. `apply_progression` is transactional (line 178). `_update_srs` runs SM-2 and now skips active-card creation for grammar_rule (line 268). |
| `review_service.py` | Real SRS implementation. `get_due_cards` joins per-type display table. `submit_answer` maps to `progression_service`. |
| `word_seed_service.py` | German catalog backfill from `data/final_result.txt` column 0 (N5a step 1). Article-strips, keeps clean single tokens, inserts only surfaces absent under `text_norm.normalize_key` with `pos=''` (a real POS would fork rows — `pos` is in the unique key). Never updates existing rows, never touches `phrase_table`. Driven by `scripts/backfill_word_catalog.py`. `inserted` comes from `INSERT ... RETURNING`, so a pre-existing row is never counted as written. |
| `text_norm.py` | `normalize_key` (NFC + strip + `lower()`) and `index_by_key` — the Unicode comparison key for catalog lookups. Exists because the DB is C-collation, where Postgres's `lower()`/`ILIKE` fold ASCII only, so `lower(col) = ANY(python_lowered)` never matched an umlaut-initial word. Uses `lower()` **not** `casefold()`: casefold maps ß→ss and would merge `schließen`/`schliessen` into one ambiguous key. |
| `catalog_resolver.py` | **Shared** surface → catalog row resolution, used by `word_list_service` and `reading_service`. Folds case in Python via `text_norm.normalize_key` (SQL `lower()`/`ILIKE` are ASCII-only under this DB's C collation). Shape-based precedence: multi-token → phrase first, single-token → word first. Phrases match `canonical`, **never** `surface_form` (773 German rows differ). Never first-matches: several candidates → `ambiguous` with `item_id=None`. `resolve_surfaces` (batch) + `resolve_one` (single). |
| `gloss_seed_service.py` | Pre-seeds curated English glosses from `data/words_4000_old.txt` into `llm_cache` under the sentinel model `curated:words_4000_old` (TODO #43 step 2). Words only; skips multi-entry cells, multi-sense and >4-word translations. Makes **no LLM calls**. Driven by `scripts/seed_gloss_cache.py` (dry-run default, `--apply` to write, `RETURNING`-based inserted count). |
| `system_list_seed_service.py` | Seeds the two built-in system lists from `data/final_result.txt` (col 0 → *Top German Words*, col 1 → *German Verb & Phrase Patterns*) via `catalog_resolver`, the same rule user lists use. **Keeps ambiguous and unresolved surfaces** with `item_id=NULL` rather than dropping or first-matching them. Idempotent by constraint: `ON CONFLICT (name) WHERE is_system` for the list, `ON CONFLICT (list_id, lower(surface))` for items — append-only. No LLM calls. Driven by `scripts/seed_system_lists.py` (dry-run default, `--apply`, `RETURNING` counts). |
| `WordListsPage.tsx` | Vocabulary lists UI. Splits the index into a **Built-in lists** section (`is_system`) and *Your lists*: built-ins carry a “Built-in” badge, show no Delete control, and keep Open / export / mark-unknown-learning. `SYSTEM_DISPLAY_NAME` overrides a stored list name for display only — the backend name is the seeding idempotency key. Detail entries are **fetched** a page at a time — `ENTRY_CHUNK` (200) is sent as the backend's `limit`, and “Show more” requests `offset=<entries loaded>` and appends. The footer denominator is `detail.total` (whole-list) while the numerator is `entries.length` (loaded), which is what makes it read “Showing 200 of 5,035” instead of “200 of 200”. Counts, export and mark-learning read the whole-list `total`/`counts`, which every page carries. `showDetail()` is the single funnel for *replacing* the open list and resets loading/error state; `handleShowMore` is the one deliberate exception because it *extends* rather than replaces. Late responses are guarded both ways: a page for a list the user navigated away from is dropped, and a late failure cannot overwrite the new list's error state. |
| `word_list_service.py` | User vocabulary lists (upload/paste → resolve → export). Resolves against **both `word_table` and `phrase_table`** (the latter seeded from `data/final_result.txt`), so a pasted blueprint binds to the same phrase row chat/SRS use. Precedence is shape-based: multi-token → phrase first, single-token → word first; several matches within the preferred type mean `ambiguous` and the fallback is not consulted. Five entry states: known/learning/unknown/**unresolved** / **ambiguous** (never auto-bound). `mark_unknown_as_learning` goes through `progression_service` with the resolved `item_type`; it never writes `user_word_knowledge` or `srs_cards`. Since migration 037 reads are `(user_id = $2 OR is_system)` so **built-in system lists** (owner-less, `is_system=true`) are visible to everyone; writes stay user-only, `delete_list` refuses a system list via its unchanged `user_id` filter, and `mark_unknown_as_learning` suppresses its late-binding UPDATE on shared rows while still applying per-user progression. `mark_unknown_as_learning` is **capped at MAX_LIST_WORDS (500) per call** — each entry costs its own `apply_progression` transaction, so an uncapped call on a 5,035-item built-in list would be thousands of sequential round-trips and could add ~9,600 SRS cards in one click, with no undo. The response carries `remaining`/`capped`; because the call is idempotent, capping degrades into chunking (call again). `get_list` takes optional `limit`/`offset` (also exposed as query params on `GET /word-lists/{id}`) that page **`entries` only** — `total` and `counts` stay whole-list on every page. Omitting both reproduces the pre-pagination response exactly. It shrinks the response, not the query: `_load_entries` still resolves the whole list because counts need it. |
| `word_service.py` | `resolve_word_ids` (Unicode-correct case-insensitive match via `text_norm.normalize_key` — **not** `ILIKE`, which folds ASCII only under this DB's C collation), `lookup_word_by_text` (resolves then enriches; ambiguous on POS, never first-matched), `learn_word_anyway` (**resolves before inserting** so an existing `pos=''`/scraper row is reused instead of forked — `ON CONFLICT (word, language, pos)` cannot see a row under a different `pos`), `get_user_knowledge`. (`upsert_word_status` was deleted 2026-05-19 — `progression_service.apply_progression(..., status_override=...)` is now the single writer.) |
| `chat_service.py` | session/message CRUD + `match_learning_words` (free-chat matching against the user's vocab — words **and** phrases, via `matcher_service.match_sentence_with_ids` for the phrase half). `match_learning_words` folds case in **Python** via `text_norm.normalize_key`, inverting the join (fetch the user's tracked non-known words, filter in Python) — SQL `LOWER()` folds ASCII only under this DB's C collation, so umlaut words earned no free-chat credit. The phrase half matches on spaCy-returned `phrase_id`s and never folds. |
| `guided_chat_service.py` | `get_next_target` (priority: due active → learning without active → random; considers words AND phrases at every tier), `update_progress` (event mapping). |
| `llm_provider.py` | **The only place an LLM client is constructed.** Two backends behind `LLM_PROVIDER`: `AnthropicProvider` (default) and `OpenAICompatibleProvider` (POSTs `{LLM_BASE_URL}/chat/completions` via `httpx`, `response_format=json_schema`, one retry falling back to `json_object`). Any host — built for a GPU desktop over Tailscale, not localhost. `LLMProviderError` carries provider/model/base-url and never the API key (`_redact` scrubs URL userinfo too). `LLMProvider` Protocol — `structured(system, messages, schema, max_tokens) -> dict` + `model_id`. `AnthropicProvider` maps a JSON Schema (`title`/`description` + body) onto a forced single-tool call; `split_schema` strips those two keys so the wire bytes match the pre-seam tool dicts. `LLMProviderError` replaces the old bare `StopIteration`. **No image/multimodal support yet** — both backends pass `messages` through verbatim, so adding it means a neutral image content-block normalizer here (the two wire formats diverge). See `docs/INGESTION_PIPELINE.md` §8. |
| `llm_service.py` | All Claude Haiku calls. tool_use for structured outputs. Cached via `llm_cache_service`. Has `MOCK_LLM=true` mode. `translate_item_gloss` reads the **curated** cache key (model `curated:words_4000_old`) before the model-specific one, so seeded human glosses survive an `LLM_MODEL` switch while genuine LLM output stays model-scoped. It never *writes* curated rows. |
| `llm_cache_service.py` | SHA256(prompt_key+model+params) → `llm_cache` table. TTL or permanent. |
| `book_service.py` | PDF upload, docling+masking ingestion, page/block CRUD, sentence_count, user_text_override. |
| `book_llm_service.py` | Per-block LLM OCR repair. Used by `/blocks/{id}/llm-repair` and batch. |
| `reading_service.py` | Selection CRUD, word-status lookup for page highlighting, `find_catalog_item` (selection → word/phrase id), `record_review` (own SRS schedule), `get_due_selections`. `find_catalog_item` delegates to `catalog_resolver` — Unicode-correct, matches phrase `canonical` (was `surface_form`), and **fails safe on ambiguity** instead of silently first-matching. `get_word_statuses_for_page` folds in Python and keys results with plain `.lower()` to match the frontend's JS `toLowerCase()` lookup (deliberately not NFC — the frontend has no `.normalize()`). |
| `reading_llm_service.py` | `translate_sentence`, `explain_in_context`. Permanent cache. |
| `reading_stats_service.py` | Lemma coverage stats for books/videos. |
| `insights_service.py` | `get_insight_cards` (frequent_unknowns, recent_mistakes), `get_prep_data` (translation + grammar + examples + linked rules). |
| `recommendation_service.py` | Pure scoring functions (score_sentence, score_video, channel_category_multiplier) + DB orchestration. Mixed item types go through `enrich_by_type` (dispatches to per-type enrichers for word/phrase/grammar_rule). `enrich_items` remains the word-only backend. |
| `prioritization_service.py` | `get_prioritized_items` — combines is_due (×4), mistake_recency (×3 decay), freq_rank (×2 linear), is_learning (×1). |
| `usage_events_service.py` | `record_event` (analytics fire-and-forget) + `record_transcript_click_event` (atomic dedup-aware insert) + 4 aggregations. Insight filters include `'transcript'` context. |
| `matcher_service.py` | Async wrapper around `subtitle-scraper/phrase_finder.py`. Imports the scraper via `sys.path.insert` (post-#3, no more `os.chdir`). `match_sentence` runs sync spaCy in a thread pool. Parses per-language (#39 slice 2): German via phrase_finder's resident model, others via `nlp_service`'s cache (`_model_for`, lock-guarded); `match_sentence_with_ids` loads `lemma_override` and threads it into the extractor so chat canonicals are corrected. |
| `phrase_service.py` | `seed_from_blueprint_map`, `enrich_phrases`, phrase_type inference from blueprint. |
| `lemma_correction_service.py` | `create_candidate` (INSERT…ON CONFLICT…report_count+1) + `list_candidates`; `accept_candidate` (transactional: upsert `lemma_override`, flip `accepted`) + `reject_candidate`; `adjudicate_candidate_dry_run` (read-only, injected adjudicator → advisory proposal) + `build_adjudication_input`/`format_adjudication_prompt` + domain exceptions (#39 3A/3B/3C). Accept is the only path from a user signal to `lemma_override`. |
| `grammar_service.py` | `seed_rules` (curated DE list), `get_rules_for_phrase_type`, `get_rules_for_lemma`. |
| `playlist_service.py` | Video playlist generation from target words. Two optimizers behind one signature: `greedy_cover` (default, no deps) and `ilp_cover` (opt-in `algorithm="ilp"`, PuLP/CBC, optimal under `max_videos`). ILP solves **maximum coverage under the cap**, not minimum set cover, so it degrades to a partial playlist instead of going infeasible. `pulp` imports lazily — missing solver → 503, greedy unaffected. No frontend selector yet: API-only. |
| `nlp_service.py` | spaCy wrapper utilities. |
| `auth_service.py` | register, login. |
| `settings_service.py` | get/update preferences from `users.settings` JSONB. Default values centralised here. |
| `reminder_service.py` | Learning reminder summary. |
| `content_request_service.py` | Every statement against `content_request` (arch-05 extraction from the router). `create_or_reset` — INSERT … ON CONFLICT `(user_id, request_type, content_id)`; resets **only** a `failed` row back to `pending` (clearing `error`), leaves `pending`/`done` untouched, returns the whole row (`RETURNING *`). `list_for_user` (newest first) and `count_pending` (global, feeds the startup resume in `main.py`'s lifespan). Deliberately does **not** spawn the scraper: it returns the row, the router reads `status` and decides. `content_id` is expected pre-validated by the router's S7 normalizer — the API is the table's only writer. |
| `notification_service.py` | Read side of the `notification` table: `fetch_unseen` (oldest first, returns asyncpg `Record`s so `payload` reaches the caller as-is) and `mark_seen` (single row). Delivery *ordering* is not here — `routers/notifications.py` interleaves these two around its yields to get re-delivery on disconnect. Write side lives in `subtitle-scraper/pipeline.py` (`_notify_user`), a separate process. |
| `search_service.py` | Full-text video/subtitle search. Corpus `search` resolves the query to `word_id`s / `blueprint_id`s in Python via `text_norm.normalize_key`, then matches `= ANY(...)` — SQL `ILIKE`/`~*` fold ASCII only under this DB's C collation. `_resolve_blueprint_ids` also `re.escape`s the term (the old word-boundary predicate interpolated raw user input into a regex — SECURITY.md S19). No debug `print()` in the request path (seven were removed 2026-07-28). **Autocomplete is fixed too** (migration 036): `_suggest_words` matches a `normalize_key`-folded prefix against the generated `word_table.word_norm` column via `ix_word_table_lang_word_norm` (EXPLAIN-verified index scan), and `_suggest_phrases` matches `phrase_blueprint.lookup_key_norm` with the word-boundary check done in Python — Postgres's `\m`/`\M` are ctype-dependent and never match a non-ASCII token under the C locale. Both escape user input (`_escape_like_prefix`, `_escape_regex`). |

### Tests — `lexy-app/backend/tests/`
See `docs/TESTS.md` for full list, coverage status, and known failures.

---

## Frontend — `lexy-app/frontend/`

### Top level
| Path | Purpose |
|---|---|
| `src/App.tsx` | Router + Layout. Outlet context provides token/prefs/actions to every page. |
| `src/main.tsx` | Vite entry. |
| `src/auth.ts` | `localStorage` token/email helpers. |
| `vite.config.ts` | Dev proxy `/api → :8000`. Vitest + jsdom config. |
| `package.json` | React 19, RR 7, Vite 8, TS 5.9, Vitest. |

### Pages (routed) — `src/App.tsx` declares
| Route | Component | What it shows |
|---|---|---|
| `/` | `HomePage` | Search + PlayerView + Free/Guided chat |
| `/for-you` | `ForYouPage` → `RecommendationsPanel` | Items, videos, sentences + InsightsSection |
| `/playlist` | `PlaylistPage` | Build playlists from target words |
| `/books` | `BooksPage` → `BookLibraryPage` / `BookReaderPage` | Upload, list, read PDFs |
| `/review` | `ReviewPage` → `SRSReviewPage` | SRS due cards. Passive = English-gloss prompt + reveal + self-grade; active = typed German + LLM evaluation via `/srs/review/{id}/produce`. |
| `/add-content` | `AddContentPage` → `ContentRequestPage` | Channel/video requests |
| `/settings` | `SettingsPage` → `SettingsPanel` | Prefs, colours, reps, channels, dark mode |

### Components — `src/components/`
Grouped by feature:

**Search / Player flow**
- `SearchBar.tsx` — multi-term search input
- `PlayerView.tsx` — main result player with tabs
- `YoutubeEmbed.tsx` — iframe with seekTo/pause/play
- `PlayerControls.tsx` — prev/next/replay
- `SubtitleDisplay.tsx` — clickable highlighted text
- `TranscriptPanel.tsx` — sentence list with statuses (has its own test)

**Word interaction**
- `WordStatusPicker.tsx` — modal lookup + status switcher. Offers "Learn this anyway" when the surface isn't in the catalog (W2 / Hole 1); shows a candidate chooser when `/by-text` returns `status='ambiguous'` (W3 / Hole 2).
- `TurnFeedbackChip.tsx` — eval chip in chat

**Chat**
- `ChatWindow.tsx`, `MessageInput.tsx` — generic message UI
- `FreeChatPage.tsx`, `GuidedChatPage.tsx` — session containers
- `TargetCard.tsx`, `SessionSummaryCard.tsx` — guided chat target + summary
- `PrepView.tsx` — pre-guided-chat translation + examples + templates + grammar

**Books / reading**
- `BookLibraryPage.tsx` — list, upload, sort, delete
- `BookReaderPage.tsx` — page view, word highlighting, selection panel, translate row
- `SelectionPanel.tsx` — save custom learning unit from selected tokens
- `SelectionReviewPanel.tsx` — review saved selections per-book. Global session loop lives at `/reading-review` (`ReadingReviewPage.tsx`, shipped #5).
- `ReadingStatsPanel.tsx` — coverage stats

**Recommendations / insights**
- `RecommendationsPanel.tsx` — For You container
- `RecommendationCards.tsx` — item/video/sentence cards
- `InsightsSection.tsx` — frequent_unknowns + recent_mistakes
- `GrammarRulePanel.tsx` — grammar rule detail + add to study

**Other**
- `PlaylistPanel.tsx`, `LoginForm.tsx`, `NotificationToast.tsx`, `ReminderBanner.tsx`, `FollowedChannelsSection.tsx`, `SettingsPanel.tsx`, `ContentRequestPage.tsx`, `SRSReviewPage.tsx`, `icons.tsx`
- `WordListsPage.tsx` — vocabulary lists: paste or upload a `.txt`, see per-word known/learning/unknown/unresolved/ambiguous, mark unknown as learning, download. Route `/lists`, nav entry "Lists".

### Hooks — `src/hooks/`
| Hook | Purpose |
|---|---|
| `useSearch.ts` | Query state + pagination. Takes `language` as a parameter (defaults to `'de'`); caller threads `recLanguage`. |
| `usePreferences.ts` | Fetch + update prefs. Exposes `error`; preserves last-known prefs on failure instead of snapping back to defaults. |
| `useChat.ts`, `useGuidedChat.ts` | Free + guided chat lifecycles. |
| `usePlayerSentences.ts` | Sentence parsing, navigation. |
| `useWordStatus.ts` | Lookup + mark + W3 ambiguous-candidate flow. `recordTranscriptClick` surfaces failures via `console.warn`; backend dedups per `(user, item, sentence_id, UTC day)`. |
| `useWordColors.ts` | Page-level word-status fetch for highlighting. |
| `useNotifications.ts` | SSE consumer. |
| `useReminders.ts`, `useReadingStats.ts`, `useRecommendations.ts`, `useInsights.ts` | Feature-specific data fetchers. |

### API clients — `src/api/`
One file per domain, thin fetch wrappers with `assertOk`:
`auth.ts`, `books.ts`, `chat.ts`, `contentRequests.ts`, `insights.ts`, `playlists.ts`, `reading.ts`, `recommendations.ts`, `reminders.ts`, `search.ts`, `settings.ts`, `srs.ts`, `suggest.ts`, `wordLists.ts`, `words.ts`.

### Types + config + utils
- `src/types/index.ts` — every shared interface (~416 lines, single file).
- `src/config/wordColors.ts` — known/learning/unknown colour scheme.
- `src/utils/sentenceUtils.tsx` — highlightText, renderClickableText, normaliseSentences.
- `src/utils/progressUtils.ts` — formatDueDate, progressDots, PASSIVE_MAX, ACTIVE_MAX.
- `src/utils/recommendationUtils.ts` — formatDuration etc.

---

## Pipeline (root) — in-memory utilities, NOT mounted on FastAPI

Source files at repo root. Used by `subtitle-scraper/` and ad-hoc data prep. **This is the only pipeline tree** — the `src/app/` repackaging that used to shadow it was deleted 2026-07-27 (TODO #17). Tests live in `tests/runtime/`.

| Path | Owns |
|---|---|
| `pipeline.py` | `GermanSubtitlePipeline` orchestrator, `PipelineConfig`, `I1Match`, `parse_srt`. End-to-end SRT → i+1 matches. |
| `subtitle_cleaner.py` | `SubtitleTextCleaner`, `SubtitleFragment` — HTML/formatting removal. |
| `subtitle_merger.py` | `SubtitleMerger`, `MergedSubtitleWindow` — fragment merging, hyphenation. |
| `subtitle_segmenter.py` | `SubtitleSegmenter`, `CandidateUtterance` — sentence segmentation via spaCy. |
| ~~`subtitle_ingestion.py`~~ | **Does not exist.** SRT parsing lives in `pipeline.py` as `parse_srt` (row above). The name came from the deleted `src/app/subtitles/ingestion.py`; an earlier draft of this index listed it as if it were a root module. |
| `utterance_quality_filter.py` | `UtteranceQualityEvaluator` — length, noise, proper-noun filters. |
| `utterance_unit_extractor.py` | `UtteranceUnitExtractor` — spaCy-driven unit extraction. |
| `learning_units.py` | `LearningUnit`, `LearningUnitType` enum, lemmatisation. |
| `user_knowledge.py` | `UserKnowledgeStore`, `KnowledgeState` enum, `find_sole_unknown`, `KnowledgeFilterPolicy`. |
| `word_knowledge.py` | `WordKnowledgeStore` — lemma vocab tracking. |
| `exposure_counter.py` | `QualifiedExposureCounter`, `CountingPolicy` (ALLOW_ALL/DEDUPLICATE_UTTERANCE/DEDUPLICATE_SESSION/DIMINISHING_RETURNS). |
| `exposure_service.py` | `ExposureService` — counter + knowledge store coordination. |
| `pipeline_diagnostics.py` | `PipelineRunDiagnostics` profiling/timing. |
| `onboarding.py` | `OnboardingFlow` — tier-based vocab seeding. |
| `eligibility.py` | `EligibilityFilter` — i+1 readiness. |
| `validate_tier_lemmas.py` | One-shot B1 word list validator. |

### Tests — `tests/`
Pytest tests for pipeline modules. Mostly hermetic (no DB). 207 tests:
`tests/runtime/` (93) covers the root pipeline modules above; `tests/*.py`
(114) covers `subtitle-scraper/`. There is no longer a `src/app/` tree or
tests targeting one — see TODO #17.

---

## Subtitle scraper — `subtitle-scraper/`

| Path | Owns |
|---|---|
| `pipeline.py` | yt-dlp scraper orchestrator. `load_channels` (DB), `populate`, `insert_phrases`, `load_lemma_overrides` (#39 — reads `lemma_override` → patches spaCy lemma errors at phrase-generation time), `_notify_user`, `_mark_request`. Run modes: full, `--requests-only`. |
| `transcript_fetcher.py` | yt-dlp wrapper. JSON3 + WebVTT parsing. Cache under `transcript_cache/`. |
| `phrase_finder.py` | Phrase extraction dispatched by language via `extract_phrases(doc, lang)` / `_LANGUAGE_EXTRACTORS`. German (`extract_german_logic`) loads `data/final_result.txt` (path resolved from `__file__`, no chdir) + spaCy at import. Spanish (`extract_spanish_logic`, #36 slices 1+2) covers finite reflexives, an allowlisted verb+prep set, and clitic-attached reflexive infinitives, with the same output dict shape. `matcher_service.match_sentence` wraps the str in `nlp(...)` before calling the German extractor. |
| `seed_channels.py` | Bootstrap upsert of `seed_data/channels.json` → `channel` table. Idempotent. |
| `backfill_video_channels.py`, `backfill_channel_names.py`, `backfill_categories.py` | One-off backfills. |
| `channel_finder.py` | YouTube subscription / CSV channel discovery. Prints IDs to stdout. |
| `db_ssl.py` | S4 — `sslmode_from_env()` / `connect_kwargs()` resolve `DB_SSL_MODE` to a libpq `sslmode` (reject `prefer`/`allow`; omit on unset/disable). All five `psycopg2.connect()` sites pass `**connect_kwargs()`. Standalone duplicate of `database.resolve_sslmode` (scraper can't import the backend). |
| `language_config.py` | #18 — single source for per-language config (`LANGUAGES` dict: spacy_model, transcript_codes, has_morphology, phrase_extractor) + helpers + derived `MODEL_MAP`/`TRANSCRIPT_CODES`/`NO_MORPH_LANGS`. `pipeline.py` re-exports these as `LANG_*`. Add a language by editing here only. Plain Python (no YAML dep); order is load-bearing for transcript auto-detect. |
| `seed_data/channels.json` | Bundled bootstrap seed for the `channel` table. Read by `seed_channels.py`. |
| `debug_transcript.py` | Single-video transcript debug. |
| `profile_pipeline.py`, `profile_full_pipeline.py` | Profiling. |
| `top-1000-most-subscribed-youtube-channels-in-germany.csv` | Input for `channel_finder.py` CSV mode. |
| `transcript_cache/` | Cached transcripts (gitignored). |

---

## PDF text extraction — `pdf_text_extraction/`

| Path | Owns |
|---|---|
| `runner.py` | `PipelineRunner` orchestrator. `run_document`, `run_batch`. |
| `config.py` | All stage dataclasses: PipelineConfig, DoclingConfig, TATRConfig, MaskingConfig, TextAssemblyConfig, TwoPassConfig, DatabaseConfig. |
| `components/layout_extractor.py` | Docling wrapper. |
| `components/two_pass_extractor.py` | Invisible-text detection (4 rules: blank, char density, word coverage, ghost colour). Thresholds TODO #31. |
| `components/region_masker.py` | White-rectangle masking with IOU merge. |
| `components/text_assembler.py` | Hierarchical text assembly + context stitching. |
| `components/artifact_filter.py` | NER + relevance filtering, ligature fixing. |
| `components/node_scorer.py`, `evidence_gatherer.py` | Scoring helpers. |
| `components/media_cropper.py`, `visualizer.py` | Figure/table cropping + diagnostic images. |
| `table_detectors/{docling,tatr,hybrid}_detector.py` | 3 table detectors. |
| `outputs/{writer,media_json_writer,db_ingester}.py` | Output writers. |
| `models/dto.py`, `models/scored_node.py` | DTOs. |

## Older / alternate PDF pipeline — `masking/`, `postprocessing/`

- `masking/latest_ingest.py` — unified pipeline alternative to `pdf_text_extraction/`.
- `masking/simple_pdf_processor.py` — lightweight OCR.
- `masking/visualize_docling_full.py` — debug visualisation.
- `masking/mask_tables.py` — table detection + masking standalone.
- `postprocessing/script.py` — batch text → `word_occurrences` ingest. Calls `phrase_finder`.
- `postprocessing/db_config.py` — DB credentials (gitignored).

---

## Data + scripts

| Path | Purpose |
|---|---|
| `data/final_result.txt` | **Central vocabulary source, and load-bearing.** 5,156-row TSV: 4,075 headwords + 950 phrase blueprints. Seeds `phrase_table` at backend startup via `main.py` → `matcher_service.get_blueprint_map()` → `phrase_service.seed_from_blueprint_map()`, and read at import by `subtitle-scraper/phrase_finder.py`. Editing it changes the live phrase catalog. |
| `data/PROVENANCE.md` | **Read before using anything in `data/`.** Per-file provenance, licence basis, measured content, and which file is preferred vs redundant. Exists because the directory previously had no attribution and an audit had to infer each file from its byte structure. |
| `data/words_4000_old.txt` | **Metadata enrichment over `final_result.txt`'s headwords, despite the name** — 11-column TSV, 4,095 entries with translations (100%), examples (100%), POS (99%), conjugations (71%), ordered frequency-descending. Joins 1:1 on the headword. `words_4000.txt` is strictly its column 0 and is redundant. |
| `data/b1_unparsed.txt`, `b1_parsed.txt` | B1 word list — unparsed is the entry set (2,840, 815 more than parsed); parsed is a strict subset adding gender + valency. Keep both. |
| `data/known_words.txt`, `verbs.txt` | Reference lists; `known_words.txt` is personal study history and is read by `tests/test_free_chat_progression.py`. |
| `files/book_pdfs/` | 29 German B1 books (PDFs). |
| `files/json/`, `files/masked/`, `files/text/` | PDF pipeline intermediates. |
| `ilp/optimal_set_finder.py` | PuLP ILP — pick minimum books covering target vocab. Standalone CLI, reads word-list files + its own DB config; outputs in same dir. **Still standalone and unchanged** — the playlist feature below re-implements the technique rather than importing this module. |
| `scripts/*.py` | One-off data fixers (verb finder, b1 word finder, known words fixer, etc.). Mostly legacy. |
| ~~`youtube_category_test/main.py`~~ | **Deleted 2026-07-27.** yt-dlp category proof-of-concept, superseded by production code: `subtitle-scraper/pipeline.py` reads `info["categories"]` on every ingest, `backfill_categories.py` fills older rows, and the result feeds `video_category` / `user_video_category`. |

---

## Autoloop — `autoloop/` (Fable ↔ ChatGPT orchestration)

Infrastructure for the autonomous engineering loop — see `docs/AUTOLOOP.md`.
ChatGPT is driven through a real browser (Playwright over CDP against a
dedicated, pre-logged-in Chrome profile), **never the OpenAI API**. Runtime
state in `.autoloop/` (gitignored). **Autoloop v1 (2026-07-30):**
produce-then-review (below) is the ONLY commit path — `cli.py`'s
`_build_orchestrator` always constructs the full collaborator set
(`WorkerRepoManager`, `TaskExecutionStore`, `IntentStore`, a provisioned
`Publisher`); the older authorize-then-produce/change-manifest path was
retired, not fixed (`docs/SECURITY.md` S21). There is now a real
write-capable executor (`implement_executor.py`) alongside the audit — both
run through the same path.

**Worker-isolation hardening (2026-07-31, `docs/AUTOLOOP.md` §4e,
`docs/SECURITY.md` S23-S26):** an adversarial review of the M2 worker
isolation (below) found it was git-configuration isolation only —
filesystem location and task-scope authorization were unaddressed. Closed:
worker repos now live at a required, externally-validated
`config.workers_root` instead of inside the checkout; a task's write scope
(`Task.approved_paths`) is fixed before the writer starts and can never be
widened by anything the executor reports; a failed round's residue is
quarantined rather than silently reused (since wrk-01, 2026-08-18, only
for preparations that did NOT pass the resumed-worker reuse gate — a valid
recorded worker is resumed as it stands, residue included, and the
residual-dirty refusal at post-commit review is what still keeps
unreported content out of a candidate); the attempt-count ceiling
persists before the executor runs, not after a commit. Opened, honestly: a
new filesystem-snapshot escape DETECTOR (`escape_detector.py`) catches an
agent writing outside its worker repo, but detection is not an OS-level
sandbox — recorded as open finding S24, on purpose.

| Path | Purpose |
|---|---|
| `orchestrator.py` | Persisted state machine: ready → (**delivering**) → submitting → awaiting → executing (+ submission_unconfirmed / **submission_rejected** / needs_user / stopped / failed). **Chunked packet delivery (2026-08-14, pkt-01):** `delivering` is entered only when a review packet's patch is too large for one chat message; it deposits the patch as numbered parts, confirming each by READBACK from persisted history (mounting the virtualized message tail first, when the provider offers it), and reaches `submitting` only once every part is confirmed — `_step_submitting` re-checks that itself and raises rather than requesting a verdict on a partial patch. Any part that does not land, or a provider that does not declare `supports_chunked_delivery`, falls back to the omission notice via `_fall_back_to_omission`, which re-stamps all three holders of `report_sha256` (request, `postcommit.packet_sha256`, `TaskExecution.presented_report_sha256`) and disowns the parts that already landed. **Transport recovery (2026-07-31):** a send whose acceptance is merely unobserved still licenses nothing; a send the browser *disproves* routes to `submission_rejected`, which reconciles for confirmation and, on confirmed absence, permits exactly ONE same-chat resend of the same request id. A second confirmed rejection — or a `ConversationUnusableError` — may rotate once (`policy.max_conversation_rotations`, `browser.project_url` required, no derivation) via submit-then-capture-then-**reconcile**: the new URL binds nothing until the conversation itself confirms it holds the request. Every request carries its own authoritative `conversation_url` + `conversation_epoch`, so a late reply in an abandoned chat can never authorize anything. **False ambiguity (2026-08-16):** before parking `submission_ambiguous`, `_resolve_or_park_ambiguous` asks the by-content search (`find_conversation_with`, which mounts the virtualized tail) whether the request is actually there — the 2026-08-05 park on `alr-af11e1b3-0006`, whose request was present and already answered. Proof of presence in this request's OWN conversation (`_same_conversation`, compared by `/c/<id>` because the search's `urljoin` drops the project prefix) resumes into `awaiting` and sends nothing; absence, a hit in a different chat, a refusal to conclude, or no project configured all park exactly as before, saying which. Presence is proved and acted on; absence is never inferred and never acted on, and `run --resubmit` stays the only thing that repeats a send. Failure routing, budgets, review-integrity enforcement. `_dispatch_executor` ALWAYS routes to `_dispatch_task_postcommit` (the produce-then-review commit path) — audit included, via `_resolve_audit_task`'s synthetic per-run `Task` (a stable unit id distinct from the protocol pseudo-id `"audit"`, so a `revise` round resumes the same worker repo instead of forking a new one). The legacy manifest-based authorize-then-produce branch and `_dispatch_git` are REMOVED (S21); a stray `commit`/`commit_and_push`/unbound `push` directive is refused via the ordinary `policy_denied` corrective-reprompt machinery (`legacy_git_path_retired`), never routed to an executor. On success the commit is created immediately (hooks enabled) and gated on structural + re-run-validation checks, never a prior chat approval; ANY failure parks in `needs_user` with the commit left exactly where it is (nothing here can roll it back). **Publisher (Autoloop M2, wired 2026-07-30):** `publisher`/`publisher_url_snapshot` constructor params — `_dispatch_task_push` imports the candidate into the dedicated `Publisher` repo and publishes from there, refusing BEFORE publishing if the main checkout's live `remote.origin.url` no longer matches the provisioned snapshot (naming `reprovision-publisher --confirm`) — see `publisher.py`. **Blockers (2026-07-31):** every `_to_needs_user` park is now classified `kind="task_fatal"` or `kind="loop_fatal"` (default `loop_fatal`, fail-closed — see `docs/AUTOLOOP.md` §9c) and, when `blocker_store` is configured (always true in production), persisted as a `blockers.Blocker`. **Worker-isolation hardening (2026-07-31, `docs/SECURITY.md` S24/S25):** for a non-audit write-capable dispatch, `_prepare_write_capable_worker` requires the primary checkout clean (`primary_checkout_dirty`, loop_fatal) and the worker repo free of residue from a failed prior attempt (quarantines + recreates otherwise), then re-checks `Task.approved_paths` for symlink traversal (`approved_path_symlink_traversal`, task_fatal); `_execute_with_escape_detection` brackets the executor call with `escape_detector`'s before/after checkout snapshot (`checkout_escape_detected`, loop_fatal); `attempt_count` increments and persists before the executor is ever called, not in `_finish_postcommit` anymore. See `docs/AUTOLOOP.md` §4e for the full design. **Path scope is ADVISORY at both gates (2026-08-05, operator decision — `docs/SECURITY.md` S25 amendment):** the pre-commit check (`outcome.changed_paths` vs `effective_approved_paths`) and the post-commit one in `_verify_committed` (`commit_range_paths` vs `execution.allowed_paths`) both still run the same `tasks.unauthorized_paths` matcher on the same inputs, but now RECORD onto `TaskExecution.out_of_scope_paths` and let the round reach review instead of parking (`changed_paths_outside_approved` is retired as a park code; both had to change together, since relaxing only the first moves the park downstream). Recorded from the comparison, never from an agent claim — `allowed_paths` is still never widened. NOT relaxed: an empty `approved_paths` still refuses dispatch (`approved_paths_missing`), escape detection is still loop_fatal, and every other post-commit refusal stands. **Fault stop (auto-04, 2026-08-16):** an exhausted POLICY-denial budget no longer parks — `_handle_policy_denial` calls the new `_to_fault_stop`, which ends the run in `stopped` with `stop_kind="fault"` while still recording the same `loop_fatal` blocker. A park asks a human a question, and there is none: only the reviewer that just spent the budget could produce an acceptable directive, which is why this was the last park a retired `ask_user` could still reach (by being re-answered). The two sibling sites spending the same `state.policy_denials` counter — `_dispatch_plan`'s rejected plan and `_handle_review_mismatch` — deliberately still park, each with a comment saying why (an operator-owned roadmap; a repository that may have moved under the loop). `_dispatch`'s `stop` branch now classifies itself `"contract"`, so no reader has to infer "clean" from the phase. **A throttled account vs an unattachable browser (brw-11, 2026-08-17):** `_handle_rate_limited` classifies before it waits and before it concludes the limit still holds (`_classify_rate_limit_state`). A throttle modal on a page the loop can drive behaves exactly as before — no restart, no client drop, no `consecutive_failures` — and is asked FIRST, so a genuine throttle is never answered by the restart that deepens it. No modal plus a real click that LANDS (`chatgpt.composer_interactive`) resumes without waiting, still counting the occurrence so a page that keeps clearing cannot become an immediate-retry loop. Zero attachable page targets (`playwright_session.attachable_page_targets`) is NOT a rate limit — the operator closed the window, Chrome is alive, `/json/version` answers and there is no page to be throttled — so `_recover_unattachable_browser` drops the client, restarts the profile ONCE, re-probes once, and otherwise parks `browser_unattachable` naming the BROWSER. That restart spends no budget at all: `rate_limit_backoffs` bounds waiting on the SERVER, and this makes no request. It is bounded per EPISODE instead (`_rate_limit_browser_restarted`), and an episode ends where the throttle streak does — on a step that COMPLETES, cleared unconditionally in `run`'s `else` branch rather than inside the `rate_limit_backoffs` reset, since state 3 never increments that counter and a guard nested there would stay true for the rest of the process. **The restart boundary (loop-02, 2026-08-18):** `_self_upgrade_due` + the `SELF_UPGRADE` outcome. When `auto_merge` has recorded that a merge changed `autoloop/`, `run()` ends the round — after the terminal-phase check (a parked loop reports its park) and before the step budget — at `ready` with no `pending_request`, i.e. immediately before `_step_ready` prepares the next request: nothing sent, nothing awaited, no agent writing (that is the `executing` phase, synchronously), the payload already durable in `state.outbox`. Every other phase is mid-round by construction and is refused, and `pending_request` is checked separately because it outlives its own phase. This class reads the record and never writes it and never execs; the replacement is `cli._self_upgrade_at_boundary`'s. The boundary is opt-in per construction (`self_upgrade_enabled`, default OFF, set only by `cli._build_orchestrator`): the record lives under `state_dir`, and `smoke-browser` builds its own orchestrator in the boundary shape (`ready`, no pending request) while reporting PASS only for a clean contract stop, so an unrelated pending upgrade would fail a diagnostic that is diagnosing something else. A send that never appears is the conversation, not the browser (brw-12, 2026-08-18):** `_handle_conversation_unusable` now carries the error's `code` into the transcript and the `RotationRecord` reason, so a rotation forced by a vanished submission (`submission_never_appeared`, raised by the transport's mounted-tail proof during `awaiting`) stays distinguishable from a chat that would not load — and, like every shape of that error, it bypasses the restart machinery and never charges `consecutive_failures`. — EXCEPT when the dispatch already passed the wrk-01 resumed-worker reuse gate, 2026-08-18: `reused_recorded_worker=True` skips only the residue quarantine, because a valid recorded worker's uncommitted residue is the interrupted round's own resumable work; `_verify_committed`'s residual-dirty refusal still keeps unreported content out of any candidate), then re-checks `Task.approved_paths` for symlink traversal (`approved_path_symlink_traversal`, task_fatal); `_execute_with_escape_detection` brackets the executor call with `escape_detector`'s before/after checkout snapshot (`checkout_escape_detected`, loop_fatal); `attempt_count` increments and persists before the executor is ever called, not in `_finish_postcommit` anymore. See `docs/AUTOLOOP.md` §4e for the full design. **Path scope is ADVISORY at both gates (2026-08-05, operator decision — `docs/SECURITY.md` S25 amendment):** the pre-commit check (`outcome.changed_paths` vs `effective_approved_paths`) and the post-commit one in `_verify_committed` (`commit_range_paths` vs `execution.allowed_paths`) both still run the same `tasks.unauthorized_paths` matcher on the same inputs, but now RECORD onto `TaskExecution.out_of_scope_paths` and let the round reach review instead of parking (`changed_paths_outside_approved` is retired as a park code; both had to change together, since relaxing only the first moves the park downstream). Recorded from the comparison, never from an agent claim — `allowed_paths` is still never widened. NOT relaxed: an empty `approved_paths` still refuses dispatch (`approved_paths_missing`), escape detection is still loop_fatal, and every other post-commit refusal stands. **Fault stop (auto-04, 2026-08-16):** an exhausted POLICY-denial budget no longer parks — `_handle_policy_denial` calls the new `_to_fault_stop`, which ends the run in `stopped` with `stop_kind="fault"` while still recording the same `loop_fatal` blocker. A park asks a human a question, and there is none: only the reviewer that just spent the budget could produce an acceptable directive, which is why this was the last park a retired `ask_user` could still reach (by being re-answered). The two sibling sites spending the same `state.policy_denials` counter — `_dispatch_plan`'s rejected plan and `_handle_review_mismatch` — deliberately still park, each with a comment saying why (an operator-owned roadmap; a repository that may have moved under the loop). `_dispatch`'s `stop` branch now classifies itself `"contract"`, so no reader has to infer "clean" from the phase. **A throttled account vs an unattachable browser (brw-11, 2026-08-17):** `_handle_rate_limited` classifies before it waits and before it concludes the limit still holds (`_classify_rate_limit_state`). A throttle modal on a page the loop can drive behaves exactly as before — no restart, no client drop, no `consecutive_failures` — and is asked FIRST, so a genuine throttle is never answered by the restart that deepens it. No modal plus a real click that LANDS (`chatgpt.composer_interactive`) resumes without waiting, still counting the occurrence so a page that keeps clearing cannot become an immediate-retry loop. Zero attachable page targets (`playwright_session.attachable_page_targets`) is NOT a rate limit — the operator closed the window, Chrome is alive, `/json/version` answers and there is no page to be throttled — so `_recover_unattachable_browser` drops the client, restarts the profile ONCE, re-probes once, and otherwise parks `browser_unattachable` naming the BROWSER. That restart spends no budget at all: `rate_limit_backoffs` bounds waiting on the SERVER, and this makes no request. It is bounded per EPISODE instead (`_rate_limit_browser_restarted`), and an episode ends where the throttle streak does — on a step that COMPLETES, cleared unconditionally in `run`'s `else` branch rather than inside the `rate_limit_backoffs` reset, since state 3 never increments that counter and a guard nested there would stay true for the rest of the process. |
| `tasks.py`, `seed_tasks.json` | Task registry / graph: stable slug ids, dependencies, derived ready/blocked (never stored), `next_ready()`, cycle detection, atomic `tasks.json` persistence. ChatGPT authorizes work by task id only. `seed_tasks.json` (git-tracked) seeds a fresh registry with `rt-01` (admin-gate the books-import endpoints) whenever `tasks.json` doesn't exist yet — read-only, consulted by `cli._load_tasks`/`next-task`/continuous mode's selection policy. **Blockers (2026-07-31):** `block(task_id, reason)`/`unblock(task_id)` quarantine/restore a task via a new `status="blocked"` + `blocked_reason` field, surfaced as `TaskState.BLOCKED_BY_OPERATOR` (distinct from the dependency-derived `BLOCKED`) so `ready_tasks()`/`next_ready()` skip it with no change to either method. **`Task.approved_paths`, added 2026-07-31** (`docs/SECURITY.md` S25): the EXACT repo-relative paths a task's write-capable dispatch may touch — validated on the way into `add_many` (`_validate_approved_path`: no globs, no `..`, not absolute/home-relative, `TaskGraphError` code `bad_approved_path`/`duplicate_approved_path`), defaulted to `()` for backward compatibility with an old `tasks.json`. `seed_tasks.json`'s `rt-01` now declares its three real paths (`lexy-app/backend/routers/books.py`, `lexy-app/backend/tests/test_books_import_admin.py`, `docs/SECURITY.md`); `_seed_registry` also now correctly threads `validation`/`validation_cwd` through (a pre-existing gap — they were declared in the JSON but silently dropped before this pass). **`TRACKER_PATHS` / `effective_approved_paths`, added 2026-08-01:** the four `CLAUDE.md`-mandated trackers (`docs/SUMMARY.md`, `docs/TESTS.md`, `docs/SECURITY.md`, `docs/COMMON_ERRORS.md`) are IMPLICITLY approved for every SCOPED task, because updating them is a condition of doing the work — a task that adds a file and does not name `SUMMARY.md` cannot be completed while obeying the repo's own rules. Combined in exactly one place so the dispatch seed, the every-dispatch re-sync, the PRE-commit gate and the POST-commit ownership check cannot drift. An UNSCOPED task stays unscoped (finding #2 unchanged). See `docs/AUTOLOOP.md` §4f-bis. **`TaskState.RETIRED` + `Task.superseded_by`, added 2026-08-16 (task-retire-01):** the THIRD meaning `status="blocked"` was carrying — superseded work, which resolves for nobody, as against `BLOCKED` (a dependency will resolve it) and `BLOCKED_BY_OPERATOR` (a human will). `retire(task_id, superseded_by=(), reason="")` sets `status="retired"` and records the successor id(s); it accepts a pending, in-progress or quarantined task and refuses only a completed one, and it DELETES NOTHING — `blocked_reason` survives untouched, and there is no un-retire. `_validate_superseded_by` checks shape only (valid slug, no self-reference, no repeats): a successor need not exist, since brw-06 was split into brw-07 + brw-08 before either was planned, and nothing schedules off the field. `_RETIREMENTS` + `_migrate_retirements` re-file the six pre-state retirements (brw-02, brw-04, brw-05, brw-06, sub-01, dash-01) when `from_dict` loads them — a code-resident data migration because `tasks.json` is loop state outside this repository — guarded on BOTH the stored status and a marker still present in the reason, so it is idempotent and cannot sweep up `audit-0003`, the one genuine failure. `python -m autoloop retire` is the operator route and the fallback for a reason that no longer matches. A retirement is written ONCE: a second `retire` that would add, replace, reorder or reword anything raises `task_already_retired`, and an exact repeat is a no-op — omitting `--superseded-by` on a repeat used to assign `()` over the recorded chain, making the command the thing that deleted the record. `_persisted_superseded_by` runs the same validator over the field as read back off `tasks.json`, since `from_dict` bypasses `add_many` and is the only gate a stored row passes; a malformed chain is `StateCorruptError`, not a bare string loading as one successor per character. `block` refuses a retired task too — it ends in a bare `status = "blocked"` that would otherwise silently un-retire the row. **Operator mutators, added 2026-08-16 (inbox-02):** `set_approved_paths`, `set_depends_on`, `operator_block`, `operator_unblock` join `set_priority`/`set_description` as the registry side of `inbox.py`'s mutation vocabulary. All three content mutators (description, approved_paths, depends_on) route through `_refuse_immutable`, which allows only `pending`/`blocked` — an `in_progress` task's fields are what a live dispatch is being judged against (a new dependency makes it fail BOTH `mark_completed` and `release`; a swapped scope makes its own writes read as unauthorized), and `completed`/`retired` are records, not queue. Guarded on the STORED status, never `state_of()`, which reports BLOCKED for an in-progress task with an incomplete dependency and would fall silent on exactly the stranded case. Validation is shared with creation: `_validate_approved_paths` and `_validate_depends_on` were extracted from `add_many`'s inline loops (the duplicate-path and known-dependency rules lived nowhere else) and both now also check SHAPE, so a bare string no longer splits per character. `set_depends_on` runs `_check_acyclic` against a `replace()`d candidate, so a refused cycle never touches the registry. `operator_block`/`operator_unblock` are a separate pair from `block`/`unblock`: `block`'s own caller (`cli._handle_parked_task`) quarantines a task that is by definition in-progress and fail-closes to loop_fatal on refusal, so the strand guard cannot live there. `operator_block` refuses an already-blocked task and records the hold's provenance in the new **`Task.hold_origin`** field (`HOLD_ORIGIN_OPERATOR`); `operator_unblock` releases ONLY a task carrying that origin, so an inbox request cannot clear a `task_fatal` quarantine with its blocker still open (that stays `python -m autoloop answer`). Provenance is a stored value and deliberately NOT the `blocked_reason` text: that field is unconstrained free text loop-raised quarantines write too, so the first cut's `blocked_reason.startswith(OPERATOR_HOLD_PREFIX)` test let a genuine quarantine whose reason merely began with those characters be released from the inbox. `block()` clears `hold_origin` unconditionally (whatever the reason says, and so an idempotent re-block cannot inherit a marker), `unblock()` clears it on release, `operator_block` is the only writer and sets it AFTER the delegate returns (so a refused hold stamps nothing), and `from_dict` coerces a missing/`null` value to `""` — an unmarked stored row reads as a loop quarantine, the safe direction. `OPERATOR_HOLD_PREFIX` survives as PROSE on the reason for whoever reads the row and decides nothing. See `docs/SECURITY.md` S30. **`TRACKER_PATHS` stays a fixed constant (2026-08-16, port-02):** it gained a `trackers` PARAMETER on `effective_approved_paths(approved, trackers=TRACKER_PATHS)`, fed in production only by `Orchestrator._tracker_paths()`, which returns the constant — no config read. A `[repo].tracker_paths` setting was written and withdrawn in review the same day: the config lives under the gitignored state dir, so an edit to it is not a reviewed diff, and the "documentation only" suffix blocklist offered as its bound cannot hold (`.env`, `.gitignore`, `Makefile`, `Dockerfile`, extensionless scripts all pass it, and the set is open-ended). `validate_tracker_paths` / `_NON_TRACKER_SUFFIXES` were deleted rather than left caller-less. Portability comes from the constant itself: `autoloop/` is vendored into the repo it operates on, so editing it in a target repo is a commit in that repo's reviewed history. An unscoped task still gains nothing for any tracker list, including an empty one. See `docs/SECURITY.md` S31 and `docs/AUTOLOOP.md` §4f-bis. **The task file is now written by two processes, and coordinated (2026-08-16, dash-04):** `task_file_mutex` is a SHORT-LIVED mutex (a `flock` on `tasks.json.lock`, re-entrant per thread, `TaskStoreBusy` on timeout) held for the milliseconds of one load/mutate/save — deliberately not `LoopLock`, which is held for the whole run and so cannot serialise an operator edit against a live loop. `TaskStore.save`/`archive` and the new `TaskStore.apply_priority` (the dashboard's immediate write, returning the task READ BACK from disk) all take it. `save` also reconciles: it adopts the on-disk `priority` for every task the caller has not itself re-prioritised (`TaskRegistry.priority_overrides`, populated by `set_priority` and cleared on save), so a loop holding a registry in memory for a whole round cannot write a mid-round operator edit back out, while a drained inbox request still wins. Reconciliation fails OPEN on an unreadable file. `MutationLedger` + `mutation_ledger_for` record each immediate write as a `before -> after` content-digest hop in a file beside `workers_root` — outside the checkout, like the inbox and the PAUSE flag — and `attested_priority_edit` (`MutationLedger.completed_chain` + `priority_only_change`, which re-reads the bytes) is what lets the escape detector tell that edit from an agent writing into the state dir. **The attestation is a CHAIN, not reachability (tightened 2026-08-16 in review):** each write appends TWO records — `record_intent` before the file write (so an unwritable ledger leaves the task file untouched) and `record_complete` after it (the only phase that is ever an edge) — and `completed_chain(start, since=watermark, tasks_path=…)` returns the sequence of states the window's completed mutations walked through, or `None` if they do not chain unbroken. `attested_priority_edit` demands that chain's TERMINAL state be exactly what the detector observed, which is what closes two holes reachability left open: a legitimate round trip (3 → 2 → 3) used to leave its intermediate digest usable forever, and a record written before a write that then FAILED used to be an edge to a state that never existed on disk. `canonical_task_path` binds a record to one task file (a shared ledger must not attest another checkout's edits) and `TaskStore.priority_edit_watermark` / `capture_priority_window` bind it to one detection window — the latter reading the baseline bytes and the watermark under a single mutex hold, since taken apart they can straddle an in-flight edit and park a benign one. See `docs/SECURITY.md` S32 and `docs/AUTOLOOP.md` §4f-ter. **`Task.decomposition` + `set_decomposition` (plan-01, 2026-08-18):** where an approved decomposition becomes durable. Text, not a step list — it is agent instructions in the same category as `description`, and a machine-actionable list here would be a second way to split a task (that stays `split-01`'s). Written only from the dispatch path, in the same save as `mark_in_progress`, so a task is never in progress against an unrecorded plan; `policy._check_decomposition` reads it to authorize a later `implement`, and `implement_executor._agent_prompt` shows it to the agent. REPLACES on a reshape, refuses blank (empty means "nothing approved yet", the state dispatch is refused from) and refuses `completed`/`retired`. Deliberately NOT behind `_refuse_immutable`: an `in_progress` task must stay reshapeable, because a task under review is in progress by definition and the write happens before that round's executor starts, judged against by nothing. A `tasks.json` written before the field existed loads with `""`. |
| `blockers.py` | **New 2026-07-31.** Persisted operator-facing `Blocker` records (`id`, `task_id`, `kind`, `code`, `question`, `detail`, `phase`, `created_at`, `resolved_at`, `answer`) — one JSON file per blocker under `state_dir/blockers/`, atomic temp-file + `os.replace` writes, a corrupt record RAISES (`StateCorruptError`) rather than reading as absent. `BlockerStore.next_id(task_id)` derives `blk-<task_id>-<NNN>` by scanning the directory (no separate counter to drift). `resolve(id, answer)` is one-way — refuses an unknown or already-resolved id. See `docs/AUTOLOOP.md` §9c. |
| `conversation.py` | `LLMConversation` abstract interface + provider registry. Two built in since 2026-08-01: **`codex_cli`** (the default reviewer — one `codex exec` process per turn) and `browser_chatgpt` (Playwright over CDP). A further adapter is one class + `register_provider`. Two capabilities are OPTIONAL and probed with `getattr`, so an adapter implementing only the protocol stays valid: send observation (`SubmitResult.REJECTED`), rotation (`retarget`/`current_url`), and `idempotent_submit` (a failed send left nothing behind, so a retry cannot double-post). |
| `codex/conversation.py`, `codex/quota.py` | **Added 2026-08-01.** The Codex CLI reviewer. A subprocess returns an exit code, so none of the browser transport's ambiguity machinery is reachable: `submit` runs the CLI and stashes the reply (CONFIRMED or REJECTED, **never** UNCONFIRMED), `await_response` returns the stash, and `reconcile` is authoritative because the transport is synchronous. Declares `idempotent_submit`; omits `retarget`/`current_url`, so rotation is structurally unreachable rather than disabled. Runs with `cwd` **outside the checkout** — the prompt is self-contained, so the reviewer needs no filesystem access, and that containment holds without depending on a sandbox flag's name. `quota.py` is a pure `(returncode, stdout, stderr) -> bool` classifier with configurable patterns: the real exhaustion wording could not be verified here, so every non-zero exit logs its return code and a bounded stderr tail, making the first real exhaustion a config edit rather than an investigation. |
| `context.py` | Automatic CONTEXT block per request: review-integrity stamp (request_id/timestamp/head_sha/base_sha/report_sha256) + previous decision/task, roadmap, `in_flight` counts (tasks in progress, how many hold an unpublished candidate — the numbers `contract.NEXT_WORK_PREFERENCE` reads), git summary, changed files, validation summary. Two line labels are protocol, not formatting, and each is exported as a constant and pinned by test to the clause that names it: `IN_FLIGHT_LABEL` for the finish-before-start rule, and **`ROADMAP_LABEL` (added 2026-08-15)** for `contract.AUDIT_VS_READY_PREFERENCE`, which reads the ready and priority-1 counts `tasks.TaskRegistry.summary()` renders onto that line. **Task briefs (2026-08-18, `plan-01`):** two further sections make a request answerable on its own, because `implement` now REQUIRES a decomposition and `revise` may reuse the stored one — neither is authorable from an id and a title. `NEXT_READY_LABEL` renders the READY task `TaskRegistry.next_ready()` would pick with its FULL description (verbatim, uncapped — the reviewer cannot open the repo, so a truncated description is one it would silently plan around) and its `effective_approved_paths`, including the always-allowed trackers, or an explicit "cannot be dispatched until it is scoped" when the scope is empty. `IN_REVIEW_LABEL` renders the task under review with its stored `Task.decomposition` verbatim, so a revise can reuse or replace a plan it can actually see; it carries no description (CONTEXT is not chunked, and a review request already holds the diff), and it is skipped when the registry does not know the task (an audit unit) or when it is the same task `next_ready` already rendered. Both are appended STRICTLY after every stamp line — they embed text this package did not author, so ordering keeps a first-match stamp read correct while `contract.verify_review` does the actual refusing (`docs/SECURITY.md` S33). |
| `packet.py` | The post-commit review packet, rendered ONLY from immutable git objects in `task_base_sha..candidate_sha` (commit list, changed paths with mode/type from both trees, diff stat, full patch) plus one loudly-labelled section carrying the executor's own claims. `task_id`/`branch`/`base_sha`/`candidate_sha` are stamped as literal lines *inside* the hashed body, which is what stops an approval of candidate A being replayed onto candidate B. **Chunked delivery (2026-08-14, pkt-01):** the packet always inlines the WHOLE patch — that string is what `context.report_sha256` covers — and a patch too large for one chat message is DELIVERED as numbered parts instead of being omitted. `plan_chunked_delivery` splits it at `DIFF_INCLUDE_MAX_CHARS` (30,000 — the per-message budget, unchanged and still pinned to the single measured 40,056-character failure), mints part ids that deliberately do NOT contain the request id (every provider answers "did this land?" with a substring search), and returns the abridged verdict message plus the omission notice to fall back to. Refuses — falling back to omission — if the stored patch is not inside the payload, a part id would collide, or the patch needs more than `DIFF_MAX_PARTS` (6). See `docs/AUTOLOOP.md` §5d. **`_format_out_of_scope` (2026-08-05)** is the control that replaced the path-scope park: an `OUT-OF-SCOPE PATHS (n)` section computed HERE from `commit_range_paths` vs `execution.allowed_paths` — not from `TaskExecution.out_of_scope_paths`, whose pre-commit half comes from the executor's report — labelled in its own text as advisory (the paths did NOT stop the commit) and placed above the diff, which is OMITTED (never truncated) over `DIFF_INCLUDE_MAX_CHARS` (30,000, sized from the one measured send failure at 40,056). Since 2026-08-15 the section renders ONLY when a path is actually out of scope — an always-present `(none)` section trains a reviewer to skim past the packet where it matters; the "was it dropped in a refactor?" question it used to answer is answered by `test_the_packet_names_every_out_of_scope_path` instead. See `docs/SECURITY.md` S25's amendment. **`_format_assumptions` (auto-04, 2026-08-16)** appends `TaskExecution.assumptions` INSIDE the executor-report section (not beside the git-read ones, where an unread claim would borrow their authority), labelled a claim again because that list is what a hurried reviewer reads on its own. Not first-time visibility — `report_details` is the whole agent transcript — but the label plus the accumulation, since `report_details` is replaced every round while the record's list is unioned. Owns BOTH size bounds, since the record is deliberately unbounded: `ASSUMPTIONS_MAX_CHARS` (4,000, ~13% of `DIFF_INCLUDE_MAX_CHARS` so the unread section can never crowd out the diff) renders the newest entries that fit and states how many it withheld, and `ASSUMPTION_MAX_CHARS_EACH` (500) shortens one over-long line — appending a marker that names the execution record rather than a bare ellipsis — because entries render newest-first and stop at the first that does not fit, so a single pasted-reasoning line would otherwise render nothing but a withheld count and hide every real disclosure behind it. The record itself stays complete either way. Rendered only when non-empty — deliberately unlike the empty report, which announces itself: "no assumption lines" is not a claim the executor made, and a "none recorded" line would invite reading it as a certification that the task was unambiguous. |
| `browser/chatgpt.py` | `BrowserChatGPT` (the browser LLMConversation): `attach` / `submit` / `reconcile` / `await_response`. **Optimistic rendering is never treated as submission** — confirmation needs an assistant turn or a reconciliation reload; ambiguity returns UNCONFIRMED and never auto-retries. Realistic composer input (focus + keyboard clear + `insert_text` + content verification + Send-enabled wait), no navigation while awaiting, per-stage bounded timeouts, structured secret-free diagnostics. **2026-07-31:** a fourth result, `REJECTED`, when the network observation *disproves* acceptance (persisted history still outranks it, and a 2xx alone still never confirms); `ConversationUnusableError` for the narrow "this chat loaded and is wedged" case that authorizes a rotation — a page that never reached the conversation stays an ordinary `BrowserError`; and `retarget`/`current_url` so a rotation can move the client to a replacement chat. **2026-08-16:** `find_conversation_with` (identify a chat by the request it contains, for when the address bar lags) mounts the virtualized message tail before concluding — ChatGPT paints a window, not a history — and confirms the page it read is the one it asked for. Absence needs TWO proofs: the session reports the list is at its END (the position `scroll_to_end` now returns), and the mounted window then stops changing. Neither alone is sound — the node count is consistent with a whole conversation sliding past a constant-size window, an unchanged window is equally consistent with a gesture that never moved, and being at the end says nothing while an answer is still streaming into it. When it cannot establish all of that, or cannot prove which page it read, it raises `ConversationSearchInconclusive` rather than guessing "absent". **2026-08-18 (brw-12):** `await_response` runs those same two absence proofs (`_rule_out_missing_submission`) from BOTH surfaces the wedged-conversation fault wears — a response-START timeout expiring cleanly, and the incident's actual shape, a mid-await DOM read dying with the lost-session label itself (`_classify_awaiting_read_failure` catches the `SessionLostError`, re-probes through the same session — a fresh read succeeding is the attachability proof — and only then rules). A submission this loop made that is provably NOT in the conversation — tail mounted to its demonstrable end, page attachable and un-throttled at the moment of conclusion — raises `ConversationUnusableError(code="submission_never_appeared")`, the classification the 2026-08-17 incident lacked (a locator timeout on the loop's own sent message read as a lost session and bought ten minutes of 45-second Chrome restarts against a fault no restart could fix). Anything short of that proof keeps its old route: a probe that cannot read the page, a sighted request, or an absence that cannot settle re-raises the original `SessionLostError` onto the restart path (a clean timeout with unproven absence falls back to the ordinary `stage="start"` timeout); a throttle or logout discovered at the conclusion still routes as itself. |
| `browser/playwright_session.py` | The only Playwright code. Lazy import; connects over CDP; never launches a browser or touches login. Also implements the optional **send-observation** capability — a passive `page.on("response")`/`on("requestfailed")` listener over the conversation-send endpoint. It records a status and a path; it issues nothing. **Every call into Playwright is guarded POSITIONALLY, not by type (2026-08-15):** `rewrite_error` turns a driver-channel failure into a plain `Exception`, so `except playwright.sync_api.Error` missed "Connection closed while reading from the driver" and it killed the process instead of routing through `BrowserError`. `connect` and `_call` now convert anything that is not an `AutoloopError` into `SessionLostError`, keeping the original exception's type name in the message. `describe_cdp_endpoint` adds the evidence a park needs to be actionable — endpoint, port, whether `/json/version` answers, and the pids of non-helper Chromes on the profile and on the port — measured at fault time, because `_handle_browser_failure` restarts Chrome before anything is recorded. That evidence is LOCAL (probes dial 127.0.0.1, `ps` lists this machine), so an endpoint this machine cannot speak for — a non-loopback host, or no usable port — reports all four fields `unknown` and points at that host or at the url, never at the local dedicated profile. **`attachable_page_targets(cdp_url)` (2026-08-17, brw-11)** answers the question `/json/version` cannot: how many `type: "page"` targets the endpoint actually has. A closed browser WINDOW leaves Chrome running and that endpoint answering with a valid `webSocketDebuggerUrl` while `/json/list` returns `[]` — Playwright cannot attach at all, and every check built on `/json/version` calls it healthy. **Zero and unmeasurable are different answers**: 0 (the endpoint answered and named no page) is what lets `orchestrator._classify_rate_limit_state` tell a throttled account from an unusable browser and restart for the second; anything unreachable or unparseable is None, because an endpoint answering nothing is the ordinary `BrowserError` path, which diagnoses and restarts itself. Unlike the probes above, this one describes the endpoint it is GIVEN rather than this machine, so a remote CDP url is asked about itself. |
| `browser/observation.py` | **Added 2026-07-31.** The one signal the DOM cannot carry: whether the browser's own send request succeeded. `SendObservation` (path + status + coarse failure — no headers, cookies, bodies or query strings, so a diagnostics dump cannot leak credentials), a narrow send-endpoint allowlist, and `classify_submission`, which folds one Send click's observations into accepted / rejected / **unknown**. Missing or self-contradictory evidence always degrades to unknown: correlation is temporal (bounded by our own click in a single-actor conversation), not by request id, because reading bodies is off-limits. |
| `browser/chrome_restart.py` | **Added 2026-08-16 (brw-07), wired in by brw-08.** The loop's restart path — `python3 -m autoloop.browser.chrome_restart`, the value `config.example.toml` ships for `browser.restart_command` and what `cli._repair_browser` / `orchestrator._attempt_browser_restart` launch after a session loss. Replaced `scripts/restart_autoloop_chrome.sh` (retired; a `.sh` cannot be validated at all, since the validation runner allows only ruff/pytest/python/npm/npx/tsc — which is how a recovery command that reported success while restarting nothing shipped). Stops EVERY process carrying `--user-data-dir=<profile>` (the shell version stopped one pid and relaunched into a survivor that still owned the debug port), polls until nothing holds the port (a kill is not proof it was freed), then launches and requires `webSocketDebuggerUrl` from `/json/version` before reporting success. **The safety bound:** the profile path is matched EXACTLY and the binary name never is — the operator's everyday Chrome runs from the same binary under a different profile. Every listing, signal, probe and launch goes through `ChromeOps`, a REQUIRED argument of `restart()`, so no test can inherit a live default; `restart()` never raises for an ordinary failure (a second exception on top of a browser fault turns recovery into a crash) and its bounds total ≈123s, under the callers' 180s. `playwright_session.describe_cdp_endpoint` reuses its probes and `matches_profile`. **The transition:** the live `.autoloop/config.toml` is not in this repository, so a config still naming the retired script keeps LOADING unchanged — `load_config` refuses nothing, and every command except a browser restart works on an unmigrated deployment. The compatibility boundary is `scripts/restart_autoloop_chrome.sh` itself, kept as a failing tombstone: it restarts nothing, prints the `restart_command = [...]` line to paste on stderr and exits 1 (both callers surface `result.stderr` only on a non-zero exit), instead of the bash exit 127 that deleting it would have produced mid-fault. `git rm` and a load-time refusal both wait for a later cleanup, once live configs are migrated. |
| `browser/session.py`, `browser/selectors.py` | Mockable session protocol; every DOM selector in one dataclass (UI-drift fix point). Send observation, rotation (`retarget`/`current_url`) and `scroll_to_end` are OPTIONAL capabilities probed with `getattr`, so the in-memory fakes and any future adapter stay valid — absent them, behaviour is exactly what it was before 2026-07-31. **`scroll_to_end(selector) -> bool | None` (position added 2026-08-16)**: it paints more of a virtualized list AND reports where it got to — True (the scroll container is at its end, a list too short to scroll included), False (more below), None (unmeasurable). The return value is the point: an unchanged window cannot tell "fully mounted" from "the gesture missed", so an adapter answering None keeps its sightings and can never establish ABSENCE (`BrowserChatGPT._mount_message_tail`). |
| `config_writer.py` | **Added 2026-07-31.** Surgical, atomic rewrite of `[browser].conversation_url` after a rotation, so the next session does not walk back into the chat the loop just escaped. Rewrites one line and copies every other byte (the config is hand-maintained and full of comments); temp-file + `os.replace`; and `assert_untracked` **refuses** a git-tracked path, fail-closed if git cannot be consulted — a loop that can quietly edit tracked files while recovering from a browser fault is worse than a failed heal. |
| `contract.py` | Response contract **v3** (audit/plan/implement/revise/commit/push/commit_and_push/stop — plus `ask_user`, **retired 2026-08-06**: still in the enum and still parsed, never advertised by `CONTRACT_INSTRUCTIONS`, so a reply from a conversation holding the old instructions reaches `policy.retired_decision_verdict`'s `legacy_ask_user_retired` denial instead of a `unknown_decision` contract violation that spends the parse-retry budget without saying the decision is gone. `ACTIVE_DECISIONS` = `Decision` − `RETIRED_DECISIONS`, derived by subtraction so a new member is active unless explicitly retired; `question` is likewise kept as an accepted top-level key for that one legacy shape only — optional there, rejected as `unexpected_field` on every active decision, and deliberately undocumented in the instructions); task-id work authorization; `reviewed` stamp on git approvals; **required non-empty `commit.paths`**) + strict single-envelope parser (one fenced block, or a rendered/plain object with an optional language label; a second object or trailing text is rejected, never resolved by position) + `verify_review` — coded rejects, never guesses. `TaskSpec.approved_paths` (added 2026-07-31, type/shape-validated only — a list of non-empty strings) lets a `plan` declare a task's write-scope up front; NOT required at the protocol level (would be a breaking wire change to every existing `plan`; `PROTOCOL_VERSION` stays 3) — the real, fail-closed enforcement is downstream in `tasks.py`/`orchestrator.py` instead (see `docs/SECURITY.md` S25). `CONTRACT_INSTRUCTIONS` = the response format plus TWO advisory clauses, each read by the reviewer and enforced by nothing: `NEXT_WORK_PREFERENCE` (finish work already in flight before starting fresh work) and **`AUDIT_VS_READY_PREFERENCE` (added 2026-08-15)** — while the roadmap has READY tasks, `implement` one of them rather than ordering a fresh `audit`, since an audit ADDS findings. It names `implement` alone because that is the one directive the protocol defines for a READY task (`revise` targets already-started work and is phase-gated). `audit` stays correct when nothing is ready, when every ready task is blocked on something *outside* the roadmap — an unmodelled blocker such as an upstream release or an operator decision, never an unmet declared dependency, which would make the task BLOCKED rather than READY — or when the operator asks. Deliberately NOT a `policy.py` refusal (that layer authorizes actions, and a denial would park the loop instead of redirecting it) and deliberately never absolute (an empty queue is exactly when continuous mode needs an audit to find new work). Each clause carries its own character budget test, because the instructions are re-sent every turn. **`Decomposition` + the optional `decomposition` key (plan-01, 2026-08-18):** the plan an `implement` authorizes — approach, expected files, ordered steps — parsed for SHAPE here and REQUIRED by `policy._check_decomposition`, the same layering `TaskSpec.approved_paths` uses and for the same two reasons (requiring it on the wire would be a breaking change, and a missing plan deserves a policy denial rather than a parse-retry). Accepted on `implement`/`revise` only, forbidden elsewhere. A one-step plan is a first-class answer at every layer, including `render()`, which is the ONE text `Task.decomposition` stores and the implementing agent is shown. The steps stay PROSE and nothing dispatches per step: splitting a task is `split-01`'s atomic mechanism, and a machine-actionable step list here would be a second one. The clause cost 140 characters of instructions and was paid for by compressing 168 out of existing prose, so the budget ceiling did not move. |
| `lock.py` | Single-instance lock per state dir: atomic create, pid/host/start/run-id recorded, live-vs-stale distinction, fail-closed, `unlock`-only recovery (refuses live locks), run-id-guarded release. **`exec_handoff` (2026-08-18, loop-02):** the one way a live lock is ever taken over, and it is a handoff rather than a steal. When the loop replaces its own interpreter to run code it just merged, `os.execv` preserves the pid and runs no `finally`, so the lock file is never released — one live pid holds one lock file across the whole replacement, with no instant another process could take it. The successor image would otherwise fail closed on its OWN pid, so `mark_exec_handoff` writes a marker naming this host, pid and run immediately before the exec, and `acquire` adopts only a lock that carries it, names this pid twice (owner + marker), names the run the lock itself records, and has not been adopted already — adoption clears the marker, so it is one-shot. A live lock without a marker, or one whose marker names another pid, host or run, is refused exactly as before. **The marker alone is not evidence, and that is the point of the token** (`EXEC_HANDOFF_TOKEN_ENV = AUTOLOOP_EXEC_HANDOFF_TOKEN`): host, run id and pid are all readable or reproducible from outside, so a stale-but-valid-looking marker plus a reused pid would otherwise be a complete handoff. `mark_exec_handoff` mints 32 random bytes per arming, puts them in the environment FIRST and then in the lock file (so a marker never lands on disk without an inheritable token behind it), and adoption requires the two to match — which only the image `os.execv` produced can manage, since nothing else inherits this process's environment. Adoption consumes the environment token as well as the marker; a malformed one (non-`str`, empty, non-ASCII — `secrets.compare_digest` raises on the last) is a refusal rather than a `TypeError` inside `acquire`, which in production is the successor's first act after the exec. `clear_exec_handoff` disarms BOTH halves when `execv` itself is refused, the environment one unconditionally and first, since it is what every later subprocess would otherwise inherit. Both rewrites are temp-file + `os.replace`, never unlink-then-recreate: an unlink would open precisely the window the continuity claim denies. `started_at` survives adoption (the lock really has been held since then), which also keeps the predates-boot check honest. |
| `lock.py` | Single-instance lock per state dir: atomic create, pid/host/start/run-id recorded, live-vs-stale distinction, fail-closed, `unlock`-only recovery (refuses live locks), run-id-guarded release. |
| `health.py` | The read-only, lock-free verdict `autoloop health` prints and a scheduler acts on (`needs_attention` / exit code). Signals chosen for false-alarm rate: the LOCK (boot-aware `LoopLock.is_live`, never a process-name match), transcript-tail age (never `state.json` mtime), a live subagent as proof of work (suppresses the silence alarm, evaluated first). **Silence is awake minutes, not wall-clock (2026-08-18, hlth-01):** `machine_sleep_in_window` subtracts everything NOT provably awake from the transcript age before the `DEFAULT_SILENCE_MINUTES` threshold is applied. `lock.boot_time_epoch` is the shared boot boundary (kernel wake history describes one boot; time before it is an off machine, not silence), then `sysctl kern.sleeptime`/`kern.waketime` fill in sleep on darwin (the same sysctl-timeval evidence family `lock.boot_time_epoch` reads, deliberately never a monotonic clock: macOS stops it during sleep) and CLOCK_BOOTTIME−CLOCK_MONOTONIC on linux. The darwin sysctls carry only the LAST sleep→wake pair, so a window predating that sleep credits only the tail since the wake as awake — unprovable time is discounted with the sleep, and a stuck verdict is only ever built on proven awake silence (a real hang still surfaces: its proven-awake tail keeps growing). Unavailable wake history — unreadable boot time included — FAILS TOWARD QUIET on that one axis, with the reason in the detail: a false "stuck" (observed 2026-08-05 — 224 minutes of laptop sleep reported as a hung loop) trains the operator to ignore the monitor, while a missed detection is retried minutes later. Do not raise the threshold to accommodate sleep — that only slows the alert for a genuinely hung loop. |
| `manifest.py` | **RETIRED 2026-07-30, kept for its own unit tests only** (docs/SECURITY.md S21/S22) — no production caller. Was: task-owned change manifests, content-hash snapshots before/after each executor run; `verify_commit` refused pre-existing or untouched paths. Two kinds: `executor` (provenance-bound — this half's own tests, and its only callers, are gone) and `adopted` (content-bound by SHA-256, immutable-tree commit via `commit_adopted` — sound, still tested directly, but its only caller, `orchestrator.py`'s `_dispatch_git`, is also gone). See `docs/AUTOLOOP.md` §4. |
| `doctor.py` | Non-destructive preflight: config, state dir, lock, git identity, branch policy, **worker isolation** (throwaway probe repo via the real `WorkerRepoManager`), **controlled hooks directories**, **publisher configuration**, **publisher URL drift** (§4d), CDP, playwright, provider, conversation URL, **active conversation + rotations used/cap + project-URL shape** (2026-07-31 — after a rotation the state, not the config, says which chat is live, and that is the one an operator needs to open), live login/selector check. Never submits. |
| `audit/` (compaction, 2026-08-01) | **Report size is bounded at the source, never by truncation.** `findings.py` adds structural bounds on the free-text fields (evidence 700, impact 400, action 300, whole-finding 2200) derived from measuring a real report — 28 blocks, median 1,464 chars, one at **21,022**, i.e. 32% of all finding bytes in a single item that had prose written into a field specified as "cite locations". A finding that exceeds a bound is neither accepted nor rejected: it is held as `OversizedFinding` with its item dict intact, and the executor runs **exactly one** reshape round asking that agent to re-express its own words. Still oversized, agent failed, or **fewer findings returned than were sent** → `AuditError` park, with the originals preserved verbatim at `oversized_findings.json`. The count-back is what catches the quietest loss: an agent replying `{"findings": []}` would otherwise look like success. `reconcile.py` dedupes by **merging, not discarding** — the previous rule kept the higher-quality instance and threw the other away, losing impacts and acceptance criteria nothing else carried. A fold requires location overlap **and** a substance signal (high Jaccard overlap of impact + proposed_action, excluding evidence): one finding becomes one task, so folding two distinct defects corrupts task identity, and preserving both texts inside it does not repair that. The threshold errs hard toward leaving findings apart — an un-merged duplicate is a visible extra block, a wrongly-merged pair is a silently lost defect. When a fold does fire it unions every unique evidence reference, impact, acceptance criterion, symbol and command, takes the stronger severity/confidence and the **cautious** parallelism answer. `report.py` renders the proposed task graph once, as JSON — the Markdown table carried the identical fields. |
| `audit/` | Phase-3 audit executor: `findings` (strict agent contract), `agents` (read-only headless `claude -p` runner), `reconcile` (dedupe/classify/reject speculation+style), `taskgen` (`au-NNN` proposal graph), `markdown` (Markdown-only gate, one dated report), `report`, `executor`. |
| `audit/executor.py` | **2026-08-19 (port-03), the domain charters can be shipped by the repository under audit.** `parse_charter_domains` / `render_charter_file` / `load_charter_domains` read `[repo].audit_charters_file` (default `docs/audit_charters.toml`) from the root of the checkout THIS call is rooted at — the audit's own worker repo in production — and `DEFAULT_DOMAINS` stays the fallback, unchanged, for a repository that ships none. Resolved per `execute()` call and cached nowhere, so one process auditing two repositories uses each one's charters. Format is one `[[domain]]` table per domain in wave order with exactly `slug`/`title`/`charter`/`model`; the round trip is exact (nothing normalized), `model` is refused outside `haiku`/`sonnet`/`""` so a repo-side file cannot escalate to the lead's tier, and a slug is restricted to `[A-Za-z0-9_-]` because it names the raw-output file. A file that exists but does not parse returns `ExecutionOutcome(status="error")` BEFORE the run dir or any agent exists — never a fallback to the built-ins, which describe the language-learning app and would file a report that reads as complete about the wrong codebase. Raised as an outcome rather than an `AuditError` because `Orchestrator.run`'s handler chain does not catch that type and the process would end on a traceback the reviewer never sees. |
| `config.py`, `config.example.toml` | **2026-08-19 (port-03):** `[repo].audit_charters_file` joins `env_example_file` / `audit_report_glob` under the same rule — repository-relative, no padding, no `..`, no globs, exact `""` as the opt-out — and `DEFAULT_AUDIT_CHARTERS_FILE` is the fourth `[repo]` default whose value is what used to be unavoidable. Says WHERE the repository states something about itself and grants nothing: read-only confinement stays argv-level in `audit/agents.py` and the always-approved trackers stay `tasks.TRACKER_PATHS` (S31). `cli._build_executor` passes it to `AuditExecutor(charters_file=…)`; a config that omits it behaves exactly as before the key existed. |
| `policy.py` | Deterministic safety layer: directive authorization (git gating + task-graph reference checks), git command whitelist (force push structurally impossible), iteration/failure/parse/denial budgets. **`_check_decomposition` (plan-01, 2026-08-18)** is the gate that makes "every task is decomposed and the decomposition approved before any code is written" real: an `implement` or `revise` must carry `contract.Decomposition`, or the task must already hold one approved earlier, or it is denied `decomposition_missing`. `revise` is covered deliberately: it names a task the same way `implement` does and `_check_task_reference` admits one on a task never implemented at all, so exempting it left a route to starting a task with no plan. It still costs no per-round tax — the ordinary revise-after-implement finds the stored plan and passes carrying nothing, and only revise-before-any-plan is refused. It sits HERE rather than in a round of its own (the loop already exchanges `implement` before any agent runs, so the approval is free; a dedicated plan round would add one to every task, a 30-100% tax on the one-to-three rounds tasks actually take), and HERE rather than in the parser (a missing plan is a well-formed directive that is not authorized, and a denial re-prompts with the whole rule under the denial budget instead of spending the much smaller parse-retry budget). Because `_step_executing` authorizes before `_dispatch`, the refusal lands before `mark_in_progress` and before `_open_attempt` — a plan produces no commit, so it must not spend the budget that bounds commit attempts. |
| `git_gateway.py` | The only git runner — argv subprocess (no shell), policy-validated per call, explicit-refspec push. **The legacy `commit()` method is REMOVED** (2026-07-30, S21: it ran plain `git commit`, gated only on manifest provenance — a pre-commit hook could rewrite approved bytes after the check ran). `commit_adopted` (immutable-tree: `write-tree` → verify → `commit-tree` → `update-ref` CAS, refuses when any commit hook is active) is the primitive that closed the same hole correctly, and is kept — sound, but no production caller anymore either (S22). **Produce-then-review path** (the only live commit path): `commit_and_capture` commits with hooks ENABLED and reads the resulting sha honestly from `rev-parse HEAD` (never predicted), leaving review to `range_diff`/`commit_range_paths` against the immutable commit that already exists; `push_exact` publishes only one already-resolved `<sha>:<dest_ref>` refspec (F2/F5-hardened); `worktree_add`/`worktree_remove`/`worktree_prune`/`branch_exists` support `worktree.py`'s `WorktreeManager`, not what production uses (see `worker_env.py`). `fetch_object(source_path, want_sha)` pulls one already-existing object by literal 40-hex id from a local path (policy-whitelisted with F2-style checks); `__init__`'s optional `env: dict | None = None` lets a gateway run under a scrubbed environment instead of silently inheriting the caller's ambient config — see `worker_env.py`. `object_exists(oid)` (rel-01, 2026-08-15) is the fail-closed existence probe: True/False from `cat-file -e`'s exit code only, raising on anything else, because `read_commit` dies identically for a missing object, a corrupt one and a policy refusal — `cli._candidate_is_retired` needs the distinction before it may write a record off. |
| `auto_merge.py` | **Added 2026-08-14.** The last hop: a completed task's published side branch is merged into the base the loop builds against, and the base is PUSHED (an unpushed merge is the same invisibility one level down). Exists because B10 marks a task completed when its candidate is confirmed on its own branch — publication, not integration — and on 2026-08-06 seven completed tasks sat unmerged, including fixes for failures still occurring. Triggered from `orchestrator._auto_merge_after_completion`, the LAST statement of `_dispatch_task_push` (deliberately after `self._store.save(state)`: the gate reads the phase from `state.json` on disk, and any earlier call would read the stale `executing` and defer forever). Gated on `cli._merge_window_blockers` — **called, not reimplemented**, because a drifted second copy is how thirteen tasks get parked at once. Gate shut / remote base moved / dirty checkout → **deferred** to `MergeDeferralStore` (`<state_dir>/merge-deferrals/`, one file per task, idempotent upsert like `BlockerStore.record`) and retried at the top of the next completion — never parked, since the push already landed. Conflicts read `conflicted_paths()` BEFORE `merge --abort` (the abort clears them), then re-verify head + tree; never force, never auto-resolve. A merge exit of 0 is not evidence: head must have moved, must contain the candidate, must still contain the old base, tree clean — only then `push_exact`. Remote divergence is checked before anything mutates the checkout, because the whitelist has no `reset` to unwind with. **A deferral PINS its execution record:** the retry is drained from that record, so `orchestrator._reconcile_published_execution` refuses to retire one while a deferral is outstanding — archiving it would make the next drain skip the task, and skipping clears the deferral. Flag `policy.auto_merge_enabled`, default OFF. Does NOT sweep branches published before it existed — that is `merge_sweep.py`, which calls `AutoMerger.attempt` per branch rather than reimplementing any of this. **A merge can change the code doing the merging (2026-08-18, loop-02):** when a verified merge touches any path under `autoloop/` (tests included — the claim is "any file"), `_note_loop_code_merge` writes ONE `PendingUpgrade` to `.autoloop/pending_upgrade.json` (`UpgradeStore`) naming the base sha the process would have to be running. That is the module's whole contribution: it does not preflight and it does not replace anything — this code runs mid-round, and the decision belongs to `orchestrator.run`'s boundary and `cli._self_upgrade_at_boundary`. Recorded off the VERIFIED merge, before the push, because the push publishes the base and does not decide what code is in this checkout; the `already_merged` branch records nothing, since the invocation that merged it already did. Under the module's existing failure discipline — a git that will not diff or a state dir that will not take the write logs `self_upgrade_error` and offers no restart, because a raise here would report a real, pushed merge as `failed`. A docs-only merge is silent. See `docs/AUTOLOOP.md` §3f-quater. |
| `auto_merge.py` | **2026-08-19 (docs-01), the one auto-resolved conflict shape.** `_merge`'s conflict branch now offers the conflicts to `_resolve_note_conflicts` before aborting: if every conflicted path is one of the two change-note trackers AND `note_merge.resolve_note_append` accepts all of them, the combined text is written, staged and committed as the merge, logged `auto_merge_notes_resolved`. Every refusal is logged `auto_merge_notes_refused` with its reason and falls through to exactly the abort this path always did — nothing else about conflict handling changed. |
| `note_merge.py` | **Added 2026-08-19 (docs-01).** Pure, git-free resolver for the ONE conflict shape the loop may settle without a human: two branches each appending their own change-note lines to the append-only change-note section that `docs/SUMMARY.md` and `docs/TESTS.md` end with — the one opened by each tracker's `CHANGE-NOTES` comment, which is why prose in these two files refers to that comment by name and never writes it out in full: the resolver requires the marker to appear EXACTLY ONCE per tracker, so a second copy of it anywhere in the file silently switches this whole path off. Takes the three index stages plus git's own half-merged working file; keeps git's merge of everything ABOVE the marker verbatim and rebuilds only the tail, so a conflict in the prose (markers before the marker), an edited/deleted/reordered existing note line (the base section is no longer a literal prefix of a side), a path outside the two trackers, or an appended heading all return `None` and leave the conflict standing. Replaced `merge=union`, which could not be scoped to a region and therefore disabled conflict detection for the whole file. |
| `git_gateway.py` | **2026-08-19 (docs-01):** three thin primitives for the resolver above — `merge_stage_blob(path, stage)` (`cat-file blob :<stage>:<path>`, so the sides come from the index rather than from parsing conflict markers), `add_paths` (`git add -- <paths>`, no index verification: the rest of the index is git's own merge result) and `commit_staged` (`git commit -m`, which records the second parent itself while `MERGE_HEAD` exists). No policy-whitelist change was needed for any of them. |
| `merge_sweep.py` | **Added 2026-08-15 (merge-03).** The other half of integration: branches nobody is going to report. `auto_merge.py` reacts to a completion it SEES; a branch published before it existed — or by a process that died first — has no event left to react to. On 2026-08-06 seven completed tasks were published and unmerged at once (auto-08, auto-12, brw-01, brw-07, inbox-09, rt-10, rt-11) with the base still at d2d4d6b; a hand-written `git ls-remote` loop was what noticed, and two of the seven fixed failures that were still happening. `BacklogSweeper.sweep()` enumerates every COMPLETED task whose execution record carries a candidate, drops the ones already in the base, confirms publication against the remote (`cli._candidate_publication`, so an `ls-remote` equal to the candidate is the only evidence), and hands what is left to `AutoMerger.attempt` — the gate, the dirty check, the moved-base check, the four-part merge verification, the conflict abort and the push are all CALLED, not reimplemented. Three things it adds: **enumeration**; **order** (oldest publication first — `published_at`, with a record that has none sorting ahead of every record that has one, since the field only exists from 2026-08-15, tie-broken on the candidate's committer date — because attempting a branch before the one it builds on manufactures conflicts that do not really exist); and **stopping** (the first branch that does not land — conflict, unverified merge, or deferral — halts the whole sweep, leaves the rest untouched and NAMES them, because a half-swept backlog with one branch aborted mid-way is harder to reason about than a clean stop). Merged-ness is `merge-base --is-ancestor` and NEVER the task status or a branch-name match — those are exactly what made the backlog invisible. The gate is checked ONCE, before the first merge, so a shut window defers the whole sweep instead of writing one deferral per branch. **No state of its own**: the work-list is re-derived from ancestry every run, so a sweep that stopped halfway just re-enumerates what is left. **"Could not look" is never reported as "nothing to merge":** a completed task has three answers, not two — in the base, outstanding, or unjudgeable — and all four unjudgeable states go into `SweepResult.unresolved`, are named (`merge_sweep_unresolved`), are never attempted, and make `is_clear` false so `merge-backlog` exits 1 and the startup hook prints. **Any one of them also withholds the WHOLE invocation** (`HELD`, `merge_sweep_held`): this module deliberately supports a later branch being cut from an earlier one, so merging a candidate the remote DOES confirm can carry an unconfirmed ancestor into the base transitively — publish A, cut B from A, lose A's ref, and merging B alone makes A an ancestor of HEAD anyway. Excluding only the descendants of an unresolved task needs the ancestry of a commit the sweep may be unable to name or resolve at all, so the invariant is the coarse one: nothing has been mutated at that point, so a hold costs a delay and never a bad base — at the accepted price that one stale unjudgeable task blocks every sweep until an operator acts. Startup still only reports and the loop starts normally. Those states: the remote does not confirm the branch (deleted, force-moved, or unreachable — `_candidate_publication` deliberately cannot tell them apart); the execution record cannot be READ; there is no live record and no archived one shows the work already landed; the record names no candidate. An offline run — or one whose only completed task had a torn record — exiting 0 would be the 2026-08-06 invisibility rebuilt inside the tool written to end it. **Retired records are read for one question only:** `retire_execution` archives on publication being CONFIRMED, not merged, so `executions/archive/<task_id>-*.json` is checked for a sha already ancestral to HEAD (silent if so, unresolved if not) — never merged FROM, since `AutoMerger.attempt` reads the live record. **Only the NEWEST retirement answers**, because `archive` keeps one file per retirement (release → retry → the `published-<sha>` retirement that completed the task) and those generations describe different commits, so "any archived copy names an ancestor" clears a task on the strength of a superseded attempt while its completing publication sits unmerged. Generation comes from the archive FILENAME — `retire_execution` appends a fixed-width `YYYYMMDDTHHMMSSZ` instant to every label, and while whole labels do not order across differing reasons, that trailing component does. One archived copy needs no ordering and is judged directly (which keeps pre-stamp records answerable); from two upwards an unstamped label is unresolved rather than guessed at, and a same-second tie requires all the tied copies. Superseded generations are NOT required to have landed — a released attempt's candidate is usually abandoned — or every retried task would be unresolved forever. The glob matches by filename PREFIX and task ids may contain `-` (`rt-1-*.json` matches `rt-1-b-published-<stamp>.json`), so each copy is checked against the `task_id` it carries and dropped only when it proves it belongs to another task; an unreadable or owner-less copy is kept, since "cannot tell" must not read as "not mine". Ancestry alone decides, exactly as on the live path: requiring the archived record's own `published_sha` would be a stricter rule with a date on it (the field only exists from 2026-08-15), leaving every legacy archive permanently unresolved with its candidate demonstrably merged. That is the one deliberate difference from `merge-window`, which ignores the archive because it asks a different question ("could moving the base strand this?" — a retired record cannot). **Publication is re-confirmed per branch immediately before its own merge:** the candidate's key is evicted from the shared `seen` cache first (`_reconfirm`), so branch N is merged on an `ls-remote` taken after branch N-1 landed rather than on enumeration-time evidence, and a ref deleted or force-moved mid-sweep yields `UNCONFIRMED` — that branch and every branch after it are left alone (`merge_sweep_publication_changed`). **A stop is not automatically a restoration:** two `AutoMerger.attempt` outcomes leave the base MOVED — a merge that ran and then failed verification (deliberately not undone; `reset` is off the git whitelist) and one that verified and whose PUSH was then refused, which returns `auto_merge.DEFERRED`, the same slug a shut gate and a dirty checkout produce over a checkout nothing touched. So the claim is never read off the slug: `_probe` observes HEAD and `status --porcelain` immediately before each attempt and again the moment one does not land, and `SweepResult.is_reconciled` is that comparison (an unreadable probe is not a match — "could not look" is not "nothing moved"). Runs at startup (`cli._sweep_backlog_on_startup`, once per process inside the loop lock) and on demand (`python -m autoloop merge-backlog`, which also takes the lock — unlike `merge-window` it moves the head). Behind the same `policy.auto_merge_enabled` flag, default OFF, and every failure swallows to the transcript: a sweep must never stop a run from starting — with exactly one exception, an UNRECONCILED checkout, where `_cmd_run` returns 1 without entering `_run_locked`, since dispatching roadmap work onto a head nobody verified (or pushing work stacked on a merge the remote has never seen) is the very thing stopping the sweep exists to prevent, and there is no policy-legal automatic undo. |
| `worktask.py` | Produce-then-review bookkeeping (Autoloop M1 pass 1): `TaskExecution` (per-task branch/worktree/base-sha/candidate-sha/review-round record), `CommitIntent` (durable pre-commit marker), `TaskExecutionStore`/`IntentStore` (crash-safe JSON, corrupt records raise rather than reading as absent), `reconcile_after_crash` — classifies a post-crash branch tip as `NO_COMMIT`/`RECOVERABLE`/`AMBIGUOUS` from parent linkage + changed-path subset alone (never identity or commit message, which a human commit in the same worktree could coincidentally match — F8). **Record RETIREMENT (rel-01, 2026-08-15):** `TaskExecutionStore.archive(task_id, label)` MOVES a record into `executions/archive/<task_id>-<label>.json` — never `clear()`, since its `candidate_sha` names a commit that may still be recoverable — and `retire_execution(task_id, store, worker_repos, reason=…)` retires BOTH halves of an execution, record and worker repo, under ONE label so they cannot drift apart. The `archive/` subdirectory is load-bearing: `cli._merge_window_blockers` and `dashboard.merge_states` both glob `executions/*.json`, which does not recurse, so filing a record one level down is what actually takes it out of the merge-window gate. Record first, worker second — a left-behind record is silent (it holds the window shut), a left-behind worker is loud (the next `create()` refuses the directory). Called by `cli._cmd_release` and by `orchestrator._reconcile_published_execution` — the latter first checks the auto-merge deferral queue and PINS a record an undrained retry still reads from, since `AutoMerger.attempt` loads the candidate back off the live file and a skipped drain clears the deferral with it. `TaskExecution` also gained `published_sha`/`published_at`, written by `_dispatch_task_push` from the remote's own answer, never from anything the record claims. **`TaskExecution.assumptions` + `accumulate_assumptions` (auto-04, 2026-08-16):** the durable half of retiring `ask_user` — the readings the executor CHOSE where the task did not say, since an ambiguous task can no longer stop the run to ask one. Accumulated across rounds (union, first-seen order, blanks dropped) rather than replaced, because a round-2 executor assuming nothing must not erase what round 1 assumed and shipped into the range being authorized — and because `report_details`, where the same lines also appear, IS replaced every round; persisted UNSORTED (unlike `allowed_paths`/`out_of_scope_paths` — order is which round chose what) and loaded with a `.get` default, so a record written before the field existed reads as "none recorded". The record is COMPLETE — every line the executor wrote, at full length — and the bounds live where the constraint actually is, at render time (`packet.ASSUMPTIONS_MAX_CHARS` for the section, `packet.ASSUMPTION_MAX_CHARS_EACH` for one line). The per-round caps this shipped with (`MAX_ASSUMPTIONS_PER_ROUND` 20 / `MAX_ASSUMPTION_CHARS` 500) were REMOVED in review the same day: they bounded a chat message by editing a durable record, and since `report_details` is replaced every round, a line dropped there was unrecoverable as soon as the next round committed — the exact cross-round persistence this field exists to provide. `policy.max_review_rounds` defaults to unlimited, so the packet is where ~200 KB of accumulated prose has to be cut (its oversize fallback drops the DIFF, not this section), and truncating the RECORD instead would delete evidence out of the file crash-recovery adoption reads. CLAIMS throughout: like `report_summary`, nothing computes with them. **Two attempt budgets + the attempt ledger (budget-01, 2026-08-17):** `attempt_count` was one counter paying for two unrelated things — a task's own unproductive churn AND rounds destroyed by a provider 429, a stall-killed agent or a process that died mid-round — so six tasks (brw-09, exec-01, port-01, brw-11, dash-04, hlth-01) hit `MAX_TASK_ATTEMPTS` on rounds no reviewer had seen and were repaired by hand. `attempt_count` now keeps only the task's own work (validation failures, structural refusals, rounds that reached the reviewer); `fault_attempt_count` takes the rest under its own ceiling (`MAX_TASK_FAULT_ATTEMPTS`, park code `fault_attempt_ceiling`), so BOTH kinds of churn stay bounded and neither spends the other. `attempt_ledger` is one preformatted `"<ordinal>|<budget>|<reason>"` string per dispatch (`format_attempt`/`split_attempt`) — a string rather than a nested dataclass because `load` does `TaskExecution(**data)` and would hand back dicts, and unsorted for the same reason `assumptions` is. Budgets come in two groups: OPEN (`ATTEMPT_PENDING` = charged to `attempt_count`, `ATTEMPT_PENDING_FAULT` = charged to `fault_attempt_count`) and SETTLED (`ATTEMPT_TASK`/`ATTEMPT_FAULT`). An OPEN label is written at dispatch and replaced at the round's exit, so an entry still reading either one can ONLY mean the process did not survive that round; that is what lets restart reconciliation move interrupted rounds onto the fault budget without ever being able to relabel a finished task failure. There are TWO open labels rather than one because a single field cannot answer both "which counter is this charged to" and "has the round finished" — writing a fault-forced redo straight to `ATTEMPT_FAULT` at dispatch (the first cut of this) erased the second answer, so a redo that reached the reviewer looked like a round with nothing in flight and the NEXT session-ending fault silently charged its redo to `attempt_count`. `reason` is a bare outcome slug, or `"<origin>><outcome>"` for a redo (`compose_reason`/`attempt_outcome`), and every reader keys on the outcome. `pending_fault_code` is the one cross-session marker: set when a session-ending fault destroyed a review the task had already earned, consumed once per dispatch — and RE-ARMED from the same origin when that dispatch is itself taken by the environment without reaching a review (`orchestrator._settle_attempt` rule 4), because the review is still lost and the round after it is still recovering it. Re-arming is why a chain of interruptions cannot fall back onto `attempt_count` at its second link, and each carried-forward dispatch still pays a `fault_attempt_count` charge, so the chain ends at `MAX_TASK_FAULT_ATTEMPTS` rather than being a standing exemption; it clears for good the moment a round reaches a reviewer or fails on the task's own merits. |
| `environment.py` | Snapshot/verify pair (Autoloop M1 pass 1) for what a commit or push actually does that isn't the commit itself: active hooks, `core.hooksPath`, every `url.*.insteadOf` rule, and every remote's url/pushurl/push-refspec. `verify_unchanged` catches drift mid-task (e.g. a dependency postinstall script installing a hook) that pre-commit checks alone would miss. |
| `worktree.py` | Per-task worktree lifecycle: `WorktreeManager.create/remove/list_worktrees`. `task_id` doubles as a path component and a git ref component, so it's validated against `[A-Za-z0-9][A-Za-z0-9._-]*` plus an explicit `..` check before touching either. Relies on git's own refusal to check the same branch out into two worktrees at once as a fail-closed concurrency property, rather than reimplementing it. **Not what production `_build_orchestrator` constructs** (it always uses `worker_env.py`'s `WorkerRepoManager` instead, which does not share `.git` with the main checkout); still directly tested (`test_postcommit_flow.py`, `test_postcommit_primitives.py`) and still a supported `Orchestrator(worktrees=...)` mode. |
| `validation.py` | Shared `SAFE_VALIDATION_BINARIES` allowlist + `run_validation_commands` runner, used by both `audit/executor.py` (pre-commit repo-health summary) and the orchestrator's post-commit re-validation (pass 2a) — one definition so the two call sites can't drift on what's safe to launch. **Owns the validation subprocess's ENVIRONMENT too (2026-07-31):** never inherits `os.environ`, always passes `strip_validation_vars(os.environ)` and overlays a `ValidationEnv` when one is configured, then redacts its values out of the returned summary (which becomes `state.last_validation` → state.json, transcript, blockers, review packet). Note `AuditExecutor._run_validation` is a SEPARATE runner sharing only the binary allowlist — it deliberately gets no credentials. **Owns the pytest FLAGS a validation run needs too (val-01, 2026-08-06):** `effective_validation_commands` adds `-n auto` (xdist, one worker per core) and `-p no:cacheprovider` to every configured pytest command before running it — skipping a command that selects the `isolated` marker (it must keep its own process) or that already states a worker count, never touching non-pytest commands, and never changing which tests a command selects. Applied here rather than in the config template because nothing re-reads the template: a `.autoloop/config.toml` copied before a flag existed, a task's declared `validation`, and an `execution.validation_commands` record persisted by an earlier session all reach a run through this one function. Pinned by `autoloop/tests/test_validation_parallelism.py`. |
| `validation.py` | **Owns WHICH TESTS a per-commit run executes too (val-02, 2026-08-20).** `select_validation_commands(commands, changed_paths, repo_root, mode, full_reason)` narrows each configured pytest command to the test files reachable from the commit's own changed paths, and returns a `TestSelection` whose `evidence()` is appended to the validation summary — so `state.last_validation`, and with it the CONTEXT block the reviewer reads, states what was selected, from which changed inputs, by what method, why the subset suffices, and whether the run was subset or full. Selection is IMPORT-GRAPH REACHABILITY, deliberately not filename similarity: `build_import_graph` parses every `.py` file with `ast` and reverses the edges, so a change to `publisher.py` selects `test_v1_smoke.py`, which imports it and which auto-01's f06454b5 (2026-08-06) broke while updating only the four test files it touched. Plus two edge kinds no import statement states — ancestor `__init__.py` (importing `a.b` executes `a/__init__.py`) and `conftest.py` (pytest applies one to its whole directory tree). What the graph cannot read is never assumed away: a file using a dynamic import, a TEST spawning an interpreter, or one that fails to parse is `opaque` and sits on EVERY change's frontier; the interpreter signal is test-only, because half this package names `python3` for a living and treating those as opaque drags in everything importing them. Everything else widens to the full list — a non-Python changed path, a path absent from the tree, no changed paths, a checkout over `_GRAPH_MAX_FILES`, a pytest argv with an unrecognised flag or a node-id/glob target (left exactly as configured), and a reachability result of zero test files. Non-pytest commands (`ruff`) are never touched. Three ways to force the whole suite, all deterministic and all OPERATOR edits — a `plan` directive can set none of them, since `contract._TASK_SPEC_KEYS` carries no `validation` key: `[audit] test_selection = "full"`, a task that declares its own `validation`, and a declared `validation_cwd` (paths would not line up). A reviewer who wants a full run has to ask for one; no directive field grants it. Wired at ONE call site — `orchestrator._run_post_commit_validation`, fed `commit_range_paths` from `_verify_committed`, never the executor's own `changed_paths`. The PRE-commit run in `implement_executor.py` is untouched and still runs the full configured list every round. Pinned by `autoloop/tests/test_test_selection.py`. |
| `scripts/seed_validation_db.py` | **Added 2026-08-01** (not under `autoloop/` — it seeds the APP's tables). Deterministic, idempotent synthetic corpus for a validation database: two videos, five sentences, five words + `word_to_sentence` links, all with fixed ids in a reserved `900_000+` range so a second run inserts nothing. Exists because a schema-only database makes ~55 backend tests SKIP themselves ("run the subtitle pipeline first") — and a task graded by a suite that silently opted out of every content-touching test is not validated. Rows are chosen to satisfy specific guards: a lemma with two surface forms in one video, a word whose lemma differs from its surface, a non-ASCII surface, a `word_id` colliding with a real `phrase_id`. **Nothing is copied from any real database**, and it refuses to run against the DB name `.env.example` declares (reusing `validation_env.repo_declared_db_name`). `--verify` re-checks each guard without writing. Deliberately does NOT seed `sich freuen auf` — see its `PHRASE` comment and `docs/AUTOLOOP.md` §4g. |
| `inbox.py` | **Added 2026-08-01.** Operator task inbox: `add-task` submits a request to a directory BESIDE `workers_root` (outside the checkout, so the escape detector never sees it and no lock is needed), and the loop drains it BETWEEN steps, validating through `TaskRegistry.add_many` and writing `tasks.json` itself — the loop stays the only registry writer, with one exception since 2026-08-16 (dash-04): a task's `priority`, which the dashboard writes immediately under the fine-grained mutex. `KIND_PRIORITY` remains drained as before, for a request written into the directory by hand, and takes precedence over the stored value when it is. Exists because editing `.autoloop/tasks.json` directly trips the escape detector (it snapshots ignored paths too) and races the orchestrator's own save; and because `tasks.json` holds `approved_paths`, that snapshot coverage is load-bearing, not a bug to work around. Malformed requests refused at submit; unparseable files moved to `inbox/rejected/`; a request the registry refuses is reported and dropped so one typo cannot stop a run. **Mutation vocabulary, added 2026-08-16 (inbox-02):** `task` + `priority` became `task` + six kinds — `priority`, `description`, `approved_paths`, `depends_on`, `block`, `unblock` — driven off one `MUTATION_PAYLOAD` table that also IS the per-kind "carries only its own field" rule, so a new kind cannot forget it. The allowed-field check is PER KIND in both directions — `_check_mutation` bounds a mutation to `{kind, id, <payload>}` and `_check_creation` bounds a `task` to `CREATION_FIELDS` (`ALLOWED_FIELDS` is now just the union of the two, referenced by `dashboard.TASK_REQUEST_FIELDS` and validated against by nothing). One global set could not express it: `reason` is legal on a `block` and meaningless on a `task`, and checking the union let `{"kind": "task", …, "reason": …}` submit cleanly and then be dropped on merge — the silent ignore the per-kind rule exists to prevent. Two authorities split by question, ONE implementation each: shape is `check_request_shape` (present? right JSON type? does this kind carry it?), content is the registry's, answered by the same validators creation calls — so refusals reach the operator in one authority's words per question. `check_request_shape` runs at BOTH gates, `TaskInbox.submit` AND `apply_requests`, because hand-writing the JSON file is the only operator route to the five new kinds and such a file never passes through `submit`; on merge an `InboxError` becomes that request's `refused` line and the batch continues, so the never-raises promise is unchanged. `apply_requests` applies in submission order in a SINGLE pass — last write wins, and a mutation queued before its target exists is refused rather than deferred — and its middle bucket stays ONE bucket in second position because both drain callers unpack three values positionally and gate `task_store.save()` on `if added or <middle>`. `retire` is deliberately absent: it is written-once with no reverse, which is the unblockable state `block`/`unblock` are shaped to avoid. See `docs/AUTOLOOP.md` §4f-ter and `docs/SECURITY.md` S30. |
| `dashboard.py` | **Added 2026-07-31** — read-only live tracker on localhost (`python -m autoloop.dashboard --repo <checkout> --port 8787`). Exists because the audit fans out subagents whose output lands only when each FINISHES, so from outside a working loop and a wedged loop look identical. Reads `.autoloop/` state, the newest committed `docs/AUDIT_*.md` (the app backlog lives there, not in the registry), and `ps` — `live_agents()` parses `Your domain: <x>.` out of the process table, the one thing not already on disk. **It never writes to the observed checkout**, and every `git` call goes through `--no-optional-locks`: plain `git status` refreshes and rewrites `.git/index`, so a 2s poll was silently dirtying the repo it observes — and a dirty checkout makes the escape detector refuse the next write-capable task. **Visual encoding reworked 2026-08-01 under the dataviz method:** stages are an ordered progression, so exactly one hue marks the stage running now (categorical slot 1), finished stages recede to secondary ink, and `blocked` is the ONLY state allowed a status colour because it is the only genuine health verdict — the previous version painted `active` with status-good green, the "status colour for a non-status series" anti-pattern. Mark hexes are validated by `validate_palette.js` against the node surface, not the page (light `#2a78d6,#d03b3b` on `#f4f3f0`, dark `#3987e5,#d03b3b` on `#2a2a27`, both ALL PASS; the dark critical WARN at 3.0:1 is relieved by icon+label AND the stage table). **Stale-process banner (2026-08-01):** `PAGE` is baked in at import, so a dashboard started before an edit serves the old HTML for as long as it lives — indistinguishable from a feature that never shipped. The payload carries the import-time source hash and the file's current one; when they differ the page says so. Also: legend, table-view twin, keyboard focus parity with hover, `data-theme` toggle beating the OS setting both ways, proportional figures on stat tiles, and no re-render when the payload is unchanged. **No longer strictly read-only (2026-08-01 priority editing, 2026-08-02 task authoring):** two POST endpoints write to the `TaskInbox` and to nothing else — `/api/priority` (an id + an integer) and `/api/task` (id, title, description, priority and the exact `approved_paths` the operator typed). The inbox lives OUTSIDE the checkout, which is what keeps a submit safe at any instant: a write into `.autoloop/` mid-run is exactly what the escape detector reports, and it would park the loop loop-fatal. Nothing about a path is inferred, no wildcard is representable, and there is deliberately NO path validator here — `TaskRegistry.add_many` stays the single authority on merge, and the endpoint accepts a NARROWER field set than the inbox does (no `validation`/`depends_on`, which are not on the form). The creation form is static markup with its listener bound once, because `render()` rebuilds sections on a 2s poll and would otherwise erase half-typed text and double-submit. Queued requests print their `approved_paths` as text, since the loop merges them without asking again — see `docs/SECURITY.md` S28 for the blast radius, stated rather than implied. **Merged vs unmerged (2026-08-06):** `completed` means PUBLISHED, not integrated, and the gap is where finished work goes missing — seven tasks read done on 2026-08-06 while none of their code was in HEAD, two of them fixing failures that were still happening. `merge_report` shows three states per completed task (merged / published-but-not-merged / not published at all) plus `unknown`, with the not-merged count as a stat tile above the fold. Merged-ness is `is_ancestor` — `git merge-base --is-ancestor <branch sha> HEAD`, exit 0/1 — and NEVER the task status, the execution record or a name match, which are what made this invisible. Two consequences worth knowing: an exit of 128 means the object is absent from the local database, which the observed checkout usually is for a side branch (workers commit in isolated repos), and since every ancestor of local HEAD is present, absent is a definite *not merged* — except in a shallow clone, which is probed and reported unknown; and an unreachable remote or unreadable repo renders `unknown`, never not-merged, so a network hiccup cannot invent an alarming state. The execution record's `candidate_sha` is used as POSITIVE evidence only (that commit is in HEAD → merged even if origin no longer carries the branch); it can never produce a negative. Read-only and lock-free like the rest: `ls-remote` cached 60s per repo, ancestry verdicts cached permanently per `(repo, sha, head)` since they cannot change, `unknown` never cached. **Live progress for the running task (2026-08-14):** `worker_progress(state)` reports insertions/deletions/files-touched and elapsed-since-dispatch for the unit currently executing, so a 25-minute round is legible as "has written 400 lines in 12 minutes" rather than just "an agent is running" (merge-01 was killed by the timeout twice on 2026-08-06 having written 591 insertions across 16 files and then 532 across 15 — that it was working at all was only discoverable afterwards, by hand, in the quarantined worker repo). Read from GIT IN THE WORKER — `git diff --numstat <task_base_sha> --` (so the figure spans committed rounds AND the uncommitted tree; `HEAD` is the fallback and the payload says which was used) plus a capped, symlink-safe scan of `git ls-files -z --others`, because new files are where most of an agent's output lands. Elapsed comes from `state.current_task.started_at`, the dispatch stamp, and only when it names the same task (`TaskExecution` carries no timestamp of its own). **The executor's `report_summary`/`report_details` are never consulted and must never become a fallback.** Three honesty rules hold it up: nothing executing (`state.task_execution` cleared at publish) renders NO figures rather than the last round's; an unreadable worker — or either of the two git reads failing — renders `null`/"unknown" rather than 0, since 0 means "nothing written yet", the alarming state, and inventing it from a directory we could not read would be a lie in the dangerous direction; and a capped scan is marked `partial` and displayed with `≥`. Read-only and lock-free like everything else here (`_run_checked` — the failure-distinguishing sibling of `_run` — injects `--no-optional-locks`), with a 2s subprocess timeout so the page never blocks on git; the figures are excluded from the page's re-render signature and drawn before the unchanged-payload guard, so a ticking clock does not rebuild the DOM every poll. **Roadmap grouped by state (2026-08-15, dash-03):** one flat list of 20+ tasks answers none of the three questions an operator has — what is moving, what needs me, what is next — so `task_groups()` splits it into five counted groups in triage order: In progress / Needs a human / Ready / Blocked / Done. **`TaskRegistry.state_of()` is the ONLY source of that state**, which is a correctness rule and not a style choice: `pending` on disk covers BOTH Ready and Blocked (the difference is derived from dependencies and never stored) and a stored `status="blocked"` means QUARANTINED (`TaskState.BLOCKED_BY_OPERATOR`), so a page reading the status string would be wrong in both directions at once — sending an operator to `autoloop answer` for a task merely queued behind another while one that really needs them reads as queued. Constructing the registry is a pure read (`from_dict` + lookups); no mutating method is reachable and no lock is taken. Per group: In progress carries the execution record's `candidate_sha` + `review_round` (the loop's own bookkeeping, never an agent's claim, and "no candidate committed yet" when it has neither); Needs a human carries `blocked_reason`; Ready is `next_ready()`'s own `(priority, id)` order — repeated rather than simulated, because simulating it means COMPLETING tasks, and pinned against the real `next_ready()`/`mark_completed()` loop by test; Blocked NAMES the incomplete dependencies (blocked without saying by what is not actionable); Done is a collapsed `<details>` with its count in the summary, in STATIC markup so expanding it survives the 2s poll, carrying no priority input (the Save binding is scoped to `#roadmap`) and saying in as many words that done means the reviewed candidate landed on the task's OWN side branch (`docs/AUTOLOOP_TODO.md` B10) — published, NOT merged, which is the merge panel's question. Empty groups are hidden EXCEPT Needs a human, which renders and says `none`: silence and "nothing needs you" must not look identical. The hide rule lives in the payload (`hidden`), not the template, so it is assertable. An empty `groups` list means the graph could not be READ (a roadmap with no tasks still returns five zeroed groups) and the page says which, since "no tasks" and "unreadable" call for opposite reactions. `_executions()` is the shared read of `.autoloop/executions/*.json` for this panel and the merge panel — `glob`, never `rglob`, because a retired record is filed one level down in `archive/` precisely to drop out of it. **Retired group (2026-08-16, task-retire-01):** a sixth group between Blocked and Done, for `TaskState.RETIRED` — work that is neither waiting on a dependency nor waiting on a human, because it already shipped (or stopped) under another id. Six of the seven `blocked` rows on 2026-08-14 were this, which is what made the blocked count worthless as a call to action. Its rows name the SUCCESSOR (`_retired_detail`: `superseded by brw-07, brw-08`, falling back to `blocked_reason` for a task that went stale rather than being replaced, and to a stated "no successor recorded" rather than a blank cell), and `superseded_by` rides on every group's task rows so a consumer of the JSON can follow the chain. Collapsed like Done and for the same reason — it only grows and nobody has to act on it — which is why each disclosure is now looked up by GROUP KEY: `groups.find(g => g.collapsed)` returns the first, and with two collapsed groups it would have quietly filled the Done box with retirements. `collect()` also overlays the registry's derived state onto the flat `roadmap` rows, because the migration in `tasks._migrate_retirements` runs on LOAD while only the loop writes `tasks.json` — without it the app-task panel would mark brw-02 "blocked" two panels below the Retired group listing it. **Audit-report location configurable (2026-08-16, port-02):** `app_tasks(repo, limit, report_glob)` takes the glob instead of hardcoding `docs/AUDIT_*.md`; `collect` supplies `_audit_report_glob(repo)`, which reads `[repo].audit_report_glob` out of the RAW `config.toml` via the new `_config_toml` helper (`_inbox_dir` now shares it) rather than through `load_config`, which is strict and would raise on a checkout that has never been configured — this page must render against any checkout. An absolute, traversing or empty glob yields no rows instead of raising, because `Path.glob` raises outright on an absolute pattern and a read-only tracker has no business reading outside the checkout it was pointed at. **Priority edits are APPLIED, not queued (2026-08-16, dash-04):** `/api/priority` no longer writes an inbox request — it calls `TaskStore.apply_priority` on `.autoloop/tasks.json` and answers with the value READ BACK from the file, which is what proves persistence (echoing the posted number would report success identically whether or not anything landed). Queued was the previous design and it failed the operator: the value became true only on the loop's next drain while the page kept re-rendering the old number from `tasks.json`, so a save that worked was indistinguishable from one that did not. It takes NO `LoopLock` (held for the whole run) — the fine-grained `tasks.task_file_mutex` instead, which the loop's own saves take too — writes nothing else in the state dir, and is bounded to `PRIORITY_REQUEST_FIELDS = {id, priority}` with anything else REFUSED rather than dropped; it cannot create the registry. `_tasks_file`/`_state_dir`/`_task_store` resolve the file through `[paths].state_dir` so the page reads and writes the same path, and the ledger the write is attested into is resolved by `tasks.mutation_ledger_for` from `[paths].workers_root` — the same helper the loop uses. Creation stays queued to the inbox, because a new task carries authorization. See `docs/SECURITY.md` S32. **Throughput summary at the TOP of the page (2026-08-16, dash-05):** `roadmap_stats()` answers the three questions the flat list did not — how much is done, how much is moving, is the queue converging — in a `<section id="summary">` that renders above `#tiles`, the pipeline, the merge panel and the roadmap. Counting it by hand took a script (2026-08-06: 66 tasks, 17 completed, 23 in progress, 18 pending, 8 blocked). It is fed the `groups` payload `collect()` already built, so every TASK-STATE figure comes from `TaskRegistry.state_of()` BY CONSTRUCTION rather than by test — a second `TaskRegistry.from_dict` here could drift from the list rendered under it. `STAT_BUCKETS` is ONE COUNT PER `TaskState`, keyed by `TaskState.value` and pinned to a `GROUPS` key, in `TaskRegistry.summary()`'s own vocabulary — completed / in progress / ready / blocked / quarantined / retired — so the page and the review packet cannot report two roadmaps, and `stats["line"]` is that sentence minus only summary()'s priority-1 breakdown and `next ready:` tail. **Revised 2026-08-16 after review:** the first version rolled READY ∪ BLOCKED into `pending` (their split is derived and never stored, so the stored FILE FORMAT really does conflate them) and spent the freed name on BLOCKED_BY_OPERATOR — which put `blocked` = quarantine at the top of the page and `Blocked` = waiting-on-a-dependency in the Roadmap panel below it, two states under one word with opposite calls to action, which is the exact confusion `TaskState` was split up to end. Labels ride on the payload (`stats["tiles"]`, each row carrying its state, its `GROUPS` key and its label) so the template spells no state itself; the quarantine takes the Roadmap group's own "needs a human" and `blocked` only ever WIDENS to "blocked on a dependency". `by_state` is gone with the roll-up it existed to compensate for: two dicts of the same numbers under different names is how the page came to have two words for one state. **The in-progress breakdown is the part that carries information:** `in_progress_rows()` splits it into published to its side branch (B10 should have completed it, so a non-zero count means something is not retiring) / holding a candidate that is NOT published / no candidate at all (a timeout, a refusal, an abandoned round), plus `unknown`. Twelve tasks held unpublished candidates on 2026-08-06, each pinning a `task_base_sha` and so each a `task_base_behind_head` park waiting to happen — the failure that stopped the loop twice on 2026-08-04, and a flat "23 in progress" says none of it. The candidate comes from the execution record and publication is decided against the remote the record NAMES (`intended_remote`, defaulted to the polled `origin` — most records carry none, and reading absence as "some other remote" would make the whole breakdown read unknown against live data); a branch at a different sha is not-published with BOTH shas in the detail; **an unreachable remote is `unknown`, never not-published**, while "no candidate" is decided from the record alone and so survives a dead network. **Convergence and the open-work splits:** completed against still-open, with retired tasks excluded from the denominator on BOTH sides (counting a deliberately dropped task as outstanding understates progress, counting it as done overstates it) and the denominator printed beside the percentage so the exclusion is visible; then the open work by priority band and by area (`_area_of` = the id prefix — inbox, auto, rt, port, dash), because 31 open reads differently once eleven are p2 and once eight turn out to be one over-split family. `branch_for()` was extracted from `merge_states` and is now shared, so the merge panel and this one cannot name different branches for the same task. Read-only and lock-free like the rest: it reuses the ONE cached `ls-remote` (`ref_shas`) and the one `_executions()` read, adds no network call, and does arithmetic on values already in hand. An empty `groups` renders `readable: false` and says the graph could not be read — a summary is the one panel where a fabricated zero would be believed. **Full descriptions, expandable (2026-08-18, dash-10):** the roadmap panel sent `id`, `title` and `priority` and nothing else, so the one question an operator has about a queued task — what does it actually say — could only be answered by opening `.autoloop/tasks.json` by hand, and that is the file the whole roadmap is steered from. `_grouped` now carries `description` (WHOLE, never sliced — a cut description reads exactly like a task that really is that short), `chars`, `ordinal` and `waits_on` on every row, and each live group renders as a `<details>` docket: ordinal in the left rail, id, serif title, priority chip, "waits on" chip, char count, and the description in a `pre` with `white-space:pre-wrap` (they are hard-wrapped plain text with ALL-CAPS heads, so a reflow would destroy shape that carries meaning). **The ordinal is information, not decoration:** it comes from one `ordinals` map built from `_ready_order`, i.e. the ready set in `next_ready()`'s own `(priority, id)` order, so everything that cannot be picked — in progress, quarantined, dependency-blocked, retired, done — gets `None` and renders a dash; a number beside an unpickable row would be a claim about dispatch order the loop does not honour. `waits_on` comes from the extracted `_waiting_on`, which `_blocked_detail` also formats from (chip and sentence cannot disagree), and is populated for the BLOCKED group only — `state_of` notes that a retirement usually still declares the dependencies it was planned with, so filling it in everywhere would hang a "waits on" chip on a task that waits on nobody. **Descriptions are untrusted text going into HTML** and pass through the page's `esc()` at every interpolation; the tests execute those helpers under node rather than grepping for `esc(`, via the `PURE_ROADMAP_START`/`END` markers around the pure row region. A static search box filters on ids, titles AND description text (half of what an operator looks for is only in the description) and re-renders through the same `renderRoadmap` a poll uses; open rows are restored from `RMOPEN`, because a successful priority save clears `LASTJSON` to force a rebuild and would otherwise snap shut the row being read. The panel's colours are `--rm-*` tokens declared on bare `:root`, under the guarded dark media query and under `:root[data-theme="dark"]`, with the priority BAND as its own ramp and never a status role — a p0 is urgent, not broken. `/api/priority` and its handler are untouched; the input moved into the opened body and nothing about the write path changed. Cost, stated rather than discovered: descriptions are the largest thing in `tasks.json`, so `/api/state` and the page's re-render signature both grow with the roadmap on a 2s poll. |
| `dashboard.py` | **2026-08-20 (roadmap-01), has a completed task's work ever landed?** `shipped_report` / `shipped_states` / `mentions_task_id` / `commit_subjects` / `resolve_commit` answer ONE question per COMPLETED task — is there a commit whose SUBJECT names the task id, and is that commit an ancestor of the base head — and answer nothing else: no source is parsed, no capability is judged, and nothing is written (no registry, no execution record, no merge, no ref). It exists because on 2026-08-17 four completed tasks held every merge sweep with no record naming the work they shipped, and all four were resolved by hand with exactly this query (audit-0001 at 07b659b, dash-02 at dd28dfa, pkt-02 at 95b77a1, pkt-03 as four "pkt-03, part N" commits at 0fcc1c6). Four states, and the last two are deliberately different things: `shipped`, `not-in-base`, `unknown` (the search ran and no subject names the id — NOT "not shipped", since the work may have landed under a subject that never named it), and `unverified` (the search failed, or git could not resolve a matching commit). `is_ancestor` is reused unchanged, so ancestry is still `merge-base --is-ancestor` and nothing else, and every match is retained with its own `in-base`/`not-in-base`/`unverified` classification — one indeterminate commit never erases a definite ancestor found on another. Matching is whole-token against the id alphabet from `tasks._ID_RE`, so `pkt-03` does not match `pkt-030`, `x-pkt-03` or `pkt-03.5`. The search is `git log --all` rather than a walk of the base head, because a commit that is NOT an ancestor cannot be found by walking the head it is not an ancestor of; a commit on no ref at all is the stated blind spot and reads as `unknown`. Read-only through `_run_status` (`--no-optional-locks`) like the rest of the module, and deliberately NOT in `collect()`'s payload — it walks every commit on every ref, and that panel polls every 2s. See `docs/AUTOLOOP.md` §3f-quinquies. |
| `cli.py` | **2026-08-20 (roadmap-01):** `shipped-report` is the operator-facing half of the above — read-only, lock-free, safe beside a live loop, and it never acts on its own output (nothing is retired, completed, reopened, merged or re-prioritised by it). `--repo` selects the checkout whose commits are searched and `--base` the head every match is judged against (default `HEAD`); a base that will not resolve reports NOTHING rather than judging rows against a head nobody could read. `_format_shipped` prints each matching commit with its own verdict — pkt-03's four part-commits are four lines of evidence under one row — and prints the "NO MENTION is not evidence" caveat rather than implying it. Exit 0 means every completed task got an answer, and "no commit subject names this id" IS one; exit 1 means at least one row could not be judged, because "I could not look" must not read the same as "I looked". |
| `validation_env.py` | **Added 2026-07-31** (`docs/SECURITY.md` S27, `docs/AUTOLOOP.md` §4g). The validation-environment boundary: dedicated TEST database credentials reach the post-writer validation subprocess and nothing else. `VALIDATION_ENV_ALLOWLIST` is exactly `DB_HOST`/`DB_PORT`/`DB_NAME`/`DB_USER`/`DB_PASSWORD`/`SECRET_KEY` (the brief said `JWT_SECRET_KEY`; this repo reads `SECRET_KEY` — `core/security.py:11` — verified by a clean-clone collection, and no alias is accepted). `validate_validation_env_path` refuses a relative path, a missing/non-regular file, a symlink (before `resolve()`, so a link into the real `.env` is caught as a link), a file not owned by the running user, any group/world permission bit, and any location inside the checkout / state dir / `workers_root` / either publisher path. `parse_validation_env` refuses unknown keys (so it can never become a channel for `ANTHROPIC_API_KEY`), duplicates, malformed lines incl. an `export ` prefix, empty values, missing keys, and secrets under 8 chars (closing redaction-by-length at load time instead). `repo_declared_db_name` reads `.env.example`'s `DB_NAME` — the one exact production marker this repo defines, refused as a `DB_NAME`; deliberately NO host refusal (`localhost` is where a legitimate test DB lives) and no name heuristics. `ValidationEnv` is not a dataclass on purpose (a generated `__repr__` would print the values into the first assertion diff); `.apply()` builds the validation subprocess env, `.redact()` scrubs summaries, `.describe()` yields names only. `strip_validation_vars()` is the removal side, used by `ClaudeCliRunner.run` (BOTH tool sets) and `worker_env()`. Loaded once in `cli._build_orchestrator`, handed only to `ImplementExecutor` and `Orchestrator._run_post_commit_validation`; `doctor` reports the identical checks. **Production marker configurable (2026-08-16, port-02):** `repo_declared_db_name(repo_root, example_file, db_key)` and `load_validation_env(..., env_example_file, env_example_db_key)` take where to look, defaulted to this repo's `.env.example` / `DB_NAME` (`DEFAULT_ENV_EXAMPLE_FILE` / `DEFAULT_ENV_EXAMPLE_DB_KEY`, pinned equal to `config`'s copies by `test_config_repo_section.py` — no back-import, since `config` imports `tasks`). WHERE to look, never WHAT to refuse: the forbidden name is still read out of the repository, so a config cannot forbid a name the repo does not declare. `cli._load_validation_env` AND `doctor.run_doctor` both pass `config.repo.*` — defaulting either would let doctor come back clean on a config a real run refuses. An empty `env_example_file` returns `""`, i.e. no refusal, the same as an absent file — and that opt-out is the EXACT empty string: `config._load_repo_section` refuses a whitespace-only or padded value rather than passing it through, since this function reads any blank marker as "no declaration" and would silently switch the refusal off. The six-name `VALIDATION_ENV_ALLOWLIST` is untouched and stays deliberately non-configurable. |
| `worker_env.py` | Worker-side isolation (Autoloop M2), **wired into production 2026-07-30**: `worker_env()` builds the scrubbed subprocess environment (`GIT_CONFIG_NOSYSTEM=1` — NOT `GIT_CONFIG_SYSTEM=/dev/null`, which does not suppress Apple's compiled-in system gitconfig — plus `GIT_CONFIG_GLOBAL=/dev/null`, `GIT_TERMINAL_PROMPT=0`, SSH/askpass vars removed). `WorkerRepoManager` creates one isolated, no-remote repo per task (`git init` + a one-time local-path `git fetch`, never `git worktree add`, which would share the main checkout's `.git`) — `cli._build_orchestrator` always constructs one at `config.workers_root`/`config.worker_hooks_dir` (2026-07-31: NOT `config.workers_dir`, the old nested-inside-the-checkout default; see `validate_workers_root` below) and it takes precedence over `worktree.py`'s `WorktreeManager` in `_dispatch_task_postcommit`. `verify_worker_isolation(git, expected_hooks_dir)` reports remote/pushurl/insteadOf/followTags/mirror/credential-helper/hook violations — `git` must be built with `env=worker_env()` (`WorkerRepo.gateway(policy)`) or it reflects the CALLING process's ambient config instead; `doctor` runs this against a throwaway probe repo. **`validate_workers_root(workers_root, repo_root, state_dir) -> list[str]`, added 2026-07-31** (Autoloop worker-isolation hardening, `docs/SECURITY.md` S23/S25): refuses a `workers_root` that is unset, relative, or nested beneath the checkout, its `.git` (including — verified against a linked worktree, where `.git` is a POINTER FILE — the real gitdir it resolves to), the state dir, or either publisher path; called from both `cli._build_orchestrator` (refuses execution) and `doctor.run_doctor` (a `fail` check). **`WorkerRepoManager.quarantine(task_id, label) -> Path`, added 2026-07-31:** MOVES (never deletes) a dirty worker repo to a sibling `quarantine/<task_id>-<label>` directory when `Orchestrator._prepare_write_capable_worker` finds residue from a failed prior attempt, so a later round never silently commits content that failed its own validation — since wrk-01 (2026-08-18) NOT applied to a dispatch that passed the resumed-worker reuse gate, whose residue is the interrupted round's own resumable work (`reused_recorded_worker=True` skips only this check; `_verify_committed`'s residual-dirty refusal still guards the candidate). **`worker_repo_is_reusable(path, branch) -> bool`, added 2026-08-18 (wrk-01):** the read-only probe behind resumed-dispatch worker REUSE — True only when `path` exists, is itself the top level of a git repository, and has exactly `branch` checked out; `_dispatch_task_postcommit` asks it BEFORE stale-base reconciliation (`_rebase_execution_if_stale`, whose nothing-reviewed-yet re-base is skipped when it passes, so a recorded base merely behind head never quarantines-and-rebuilds a reusable worker and the record keeps its stale base) and reuses the recorded worker as-is when it passes — carrying the decision into `_prepare_write_capable_worker` so uncommitted residue there (the interrupted round's own partial work) is neither quarantined nor recreated away — and otherwise falls back to the unchanged `create()` path (missing dir recreated at the recorded base/branch; non-git or wrong-branch path hits `create()`'s "already exists" refusal — no repair, no deletion, record never rewritten). See `docs/AUTOLOOP.md` §4c/§4e and step 1b of the §4b sequence. |
| `escape_detector.py` | **Added 2026-07-31** (Autoloop worker-isolation hardening, `docs/SECURITY.md` S24, OPEN — detection, not prevention). `enumerate_checkout_paths(git)` — every tracked (regardless of working-tree state), untracked-non-ignored, and ignored path, via `git ls-files` (never `git status`, which omits an unchanged tracked file and collapses an ignored directory to one entry). `snapshot_checkout(repo_root, paths) -> CheckoutSnapshot` — content sha256 / symlink target / executable bit per path, read off the filesystem directly (never through git's object store, which would reflect the INDEX for tracked files, not necessarily the working tree). `diff_snapshots(before, after, exempt=None) -> list[str]` — creation/deletion/content/symlink-target/executable-bit violations, paths only, never contents. **`exempt(path, prior, current)` (added 2026-08-16, dash-04)** is a caller-supplied predicate, so this module still knows no special paths of its own and a caller that passes nothing gets exactly the pre-existing behaviour; the only production caller is `orchestrator._operator_priority_exemption`, for an operator's immediate `tasks.json` priority edit, and the predicate it supplies must prove BOTH an unbroken chain of COMPLETED mutation records — in a ledger outside the checkout, for that exact task file, since the watermark captured before the agent started, terminating at exactly the observed after-state — AND that the bytes differ in nothing but `priority` (`tasks.MutationLedger.completed_chain` / `priority_only_change`). An agent editing that file still parks the loop, including one that reproduces a state an earlier legitimate edit passed through or merely announced — see `docs/SECURITY.md` S32. **`is_derived_bytecode(rel, *sides) -> bool` (added 2026-08-16, esc-01)** is the ONE class that diff reports nothing for: a `.pyc` directly inside a `__pycache__/` directory, carrying a tag an interpreter actually emits (`cpython-<digits>[t]`, or this runtime's own `sys.implementation.cache_tag`), whose sibling `.py` is present as a REGULAR FILE in every snapshot side the cache entry exists on (`sides` are those sides, not a union of key sets) — derived from a source that stays fully in scope and hashed, after three false loop-fatal parks caused by out-of-band imports (a dashboard restart, a `health --json` poll) recompiling caches mid-round. Still violations: a `.pyc` outside `__pycache__` (legacy layout, importable with no source), a `.pyo` anywhere (no supported CPython emits one — PEP 488 made it `.opt-N.pyc` — so it is authored, not compiled), a `.pyc` wearing a tag no interpreter emits (`mod.attacker.pyc` — the family shape is load-bearing rather than this runtime's tag, since the incidents' writer was a different process than the loop), an orphan cache entry, a cache entry whose sibling `.py` is a symlink (watched as a target string, never hashed) or vanishes mid-window, and any symlink/directory at a cache path; `.so`/`.pyd` are never exempt. Reaches `diff_worker_tree` too, since that delegates here. `find_symlink_traversal(repo_root, paths) -> list[str]` — for `Task.approved_paths`: refuses a path whose ancestor component (or the leaf itself) already exists on disk as a symlink, since a plain relative-looking string can still write through to outside the repository. Wired into `orchestrator.py`'s `_execute_with_escape_detection` (brackets the write-capable `TaskExecutor.execute()` call, non-audit only) and `_prepare_write_capable_worker`. Does NOT inspect the checkout's own `.git/` internals — scoped to the three `git status` categories over the working tree. See `docs/AUTOLOOP.md` §4e. |
| `publisher.py` | Autoloop M2, **wired into production 2026-07-30**: the only path through which a candidate commit is published. `provision_publisher_repo` idempotently creates a dedicated BARE repo (`state_dir/publisher.git`) with its own empty controlled hooks dir; the remote url is a **provision-time snapshot** (`publisher_url.json`, §4d) — the FIRST call copies it from the main checkout and persists it, every later call re-asserts the PERSISTED value (never a fresh read), so an operator changing `origin` is never picked up silently. `Publisher.import_candidate` fetches a candidate by literal 40-hex sha from a worker's local repo path and verifies it via `read_commit` (never a separate `cat-file -t`, which the policy whitelist would deny); `Publisher.publish` reuses `GitGateway.push_exact` unmodified, passed `expected_url=<snapshot>`. `reprovision_publisher(state_dir, source_git, remote, confirm=True)` is the ONLY function that updates the snapshot — no default makes it callable by accident, and nothing directive-reachable calls it; `python -m autoloop reprovision-publisher --confirm` is its sole caller. `redact_url` strips embedded userinfo before any url reaches `status`/`doctor`/a parked question. `Orchestrator`'s `publisher`/`publisher_url_snapshot` constructor params route `_dispatch_task_push` through it — `cli._build_orchestrator` always constructs and passes both. See `docs/AUTOLOOP.md` §4c/§4d. |
| `state.py`, `transcript.py` | Atomic crash-safe JSON state (schema **v3**: adds `task_execution`, the serialised `TaskExecution` for whichever task is running the produce-then-review commit path — a mismatched version refuses to load rather than migrating), stamped requests; append-only JSONL audit log. |
| `state.py`, `transcript.py` | **2026-07-31, deliberately NOT a schema bump:** `PendingRequest` gains `conversation_url`/`conversation_epoch`/`resends_used`/`last_send_outcome` and `LoopState` gains `conversation_epoch`/`rotations`/`last_rotation` — all defaulted, so an on-disk v3 session keeps loading. The one field that could be misread is handled explicitly rather than by default: an unbound request is adopted onto the loop's URL only while `rotations == 0` (before any rotation the global URL *is* every request's URL) and raises afterwards instead of guessing. |
| `state.py`, `transcript.py` | **2026-08-14, also not a schema bump (pkt-01):** `Phase.DELIVERING`, `LoopState.outbox_diff` (the patch inlined in `outbox`, carried so `_step_ready` can plan a chunked delivery without re-reading git — checked against the payload before use, so a stale one is dropped rather than acted on) and `PendingRequest.delivery` (a `ChunkedDelivery`: the ordered parts, a per-part cursor persisted after each confirmed readback, and the omission payload to fall back to). Both fields default to `None`, which is exactly right for a session whose payload was sent as one message; a phase value is additive and `Phase(...)` still rejects anything unknown. |
| `state.py`, `transcript.py` | **2026-08-16, also not a schema bump (auto-04):** `LoopState.stop_kind` + `stop_blocker_id`, because `stopped` now means two things — the reviewer's own `stop` (`"contract"`) and a fault the loop ended itself on (`"fault"`, `orchestrator._to_fault_stop`). Unlike the additions above, the default (`""`) is deliberately NEITHER real value: a pre-existing session recorded no classification, so every reader gates on the POSITIVE value it wants (`cli._cmd_smoke_browser` PASSes only on `"contract"`; `cli._is_fault_stop` halts continuous mode only on `"fault"`), which puts the ambiguous case on the harmless side of each check. |
| `transcript.py` | **2026-08-20 (prof-01), additive and not a schema bump:** the transcript now answers "how long did that take?" without a second metrics store. `Stopwatch` measures ONE operation on a monotonic clock and `stamp()` folds the result into the `data` dict of the event that operation already emits, under the single key `DURATION_KEY = "duration_seconds"` — no new event types (68 is already a lot), no new file. `stop()` is TOTAL: a clock that raises, a non-finite reading and a BACKWARDS reading all yield `None`, which `stamp` writes as an ABSENT key rather than a wrong number, and the first `stop()` latches the outcome so a re-stamped payload never grows. The same module holds the pure READ side used by `cli._cmd_profile`: `read_records` (tolerant per LINE, because the file is being appended to while it is read and the last line can be a partial write), `STAGES` (prepare / submit / review_wait / execute, each naming its measured event AND its historical `request_id`-paired event pair), `profile_stages`, `summarize` (count/median/mean/nearest-rank p90/total), `build_profile` (the read reduced to a payload-free `TranscriptProfile`) and `render_profile`, which takes that aggregate and never a record. |
| `transcript.py` | **2026-08-20 (prof-01), the two sources are never mixed.** `MEASURED` is the operation's own elapsed time; `GAP_DERIVED` is the difference between two `ts` values, which is the INTERVAL — it contains every fault, retry and wait inside the window, at the one-second resolution `state.utcnow_iso` writes. They are summarized as INDEPENDENT sample sets and rendered as separate rows, and independent is meant literally: a record that carries a measured duration still contributes its timestamp window to the same stage's gap set, because for `submit` and `execute` the gap is a strictly WIDER window that contains the measured operation rather than a competing estimate of it. So `measured.count` reads as "rounds that recorded their own elapsed time" and `gap.count` as "rounds whose window could be paired", the difference between the two rows is what the window swallowed, and the one arithmetic that would be wrong — adding them — is printed as a warning instead of prevented by dropping samples. `review_wait` has no measured form at all, by nature rather than by omission — the stage IS the wait — and says so instead of showing a fabricated number. |
| `orchestrator.py` | **2026-08-20 (prof-01):** three measured durations and one missing identifier. `_timing_clock` (class attribute, `time.monotonic`) + `_stopwatch()` are the whole timing path; `_step_ready` measures packet CONSTRUCTION onto `request_prepared`, `_step_submitting` measures the SEND at its call site — `client.submit(...)`, frozen the instant the transport returns, deliberately excluding both the attach, the controlled reload and the duplicate check ahead of it and the verdict persistence and reconciliation branches after it, all of which stay inside the `request_prepared` → `request_submitted` gap where the profiler already says the window is wider than the work — onto `request_submitted`, and `_dispatch_task_postcommit` measures the executor call onto `executed`. That last stopwatch starts BEFORE the `worker_repos`/`is_audit` branch, not inside an arm: production always takes `_execute_with_escape_detection`, so a stopwatch in the `else` would measure only what tests reach (see `docs/COMMON_ERRORS.md`). `executed` also now carries `request_id`, read from `state.last_response` at the emit site and omitted when there is none — before this it carried none, so `directive` → `executed` matched nothing and the loop's most expensive stage had no timing of any kind. Nothing branches on a duration and no early return stamps one, so a stage that did not complete records nothing rather than a partial number. |
| `cli.py` | **2026-08-20 (prof-01):** `profile` joins the read-only, lock-free command tuple beside `status`/`tasks`/`next-task`/`doctor`. `_cmd_profile` opens exactly one file (`config.transcript_file`, or the `--transcript FILE` override for an archived transcript — a rotated file or a copy taken off another machine), prints per-stage source/count/median/mean/p90/total, and touches no state, no registry and no repository — safe while the loop runs, which is when the question comes up. It prints AGGREGATES only: the transcript holds whole review packets and whole reviewer replies, so the read is reduced by `build_profile` first and `render_profile` receives floats and static stage labels — never a record, not merely never printing one (`docs/SECURITY.md` S36). |
| `executor.py` | `TaskExecutor` seam (`execute(directive, task)` → `ExecutionOutcome`); `NullExecutor` reports honestly today. `ExecutionOutcome` carries five produce-then-review fields (`task_branch`/`task_base_sha`/`candidate_sha`/`changed_paths`/`post_commit_validation`), all defaulted so every prior caller is unaffected. |
| `implement_executor.py` | **Added 2026-07-31** — the write-capable `implement`/`revise` counterpart to `audit/`: `ImplementExecutor` runs ONE `Edit`/`Write`-capable `claude -p` subagent (`implement_agent_runner`, built on `audit.agents.ClaudeCliRunner` — its tool set is now a constructor param, defaulting to the audit's read-only pair) against the task's own isolated worker repo, derives `changed_paths` from the worker repo's real `git status --porcelain -z -uall` (never the agent's claim — `GitGateway.dirty_entries_all`/`dirty_paths_all`, new), then re-runs validation. No `.autoloop/` writes, no Markdown report. `cli._build_executor`'s new `_DispatchingExecutor` routes `implement`/`revise`-of-a-real-task here and `audit`/`revise("audit")` to `AuditExecutor` — `orchestrator.py` is unchanged. See `docs/AUTOLOOP.md` §7b. **Bounded by SILENCE since 2026-08-14 (`stall-01`):** `implement_agent_runner` takes a `policy=` and builds a `stall.WorkerTreeProbe` over the task's worker repo, so the write-capable agent is supervised rather than run under a wall-clock timeout — see `stall.py` below. A failed run of ANY kind now also reports what it left behind (`_partial_work` reads the numbers from the worker repo's own git state), and `changed_paths` is populated on the error outcome too; `orchestrator._dispatch_task_postcommit` returns on `status != "ok"` long before the commit path, so nothing there can commit partial work. **Ambiguity is resolved, not escalated (auto-04, 2026-08-16):** `ask_user` is retired, so `_agent_prompt` now carries `_SMALLEST_REVERSIBLE_READING` — take the narrowest, most easily undone interpretation, and write one `ASSUMPTION:` line per choice. `_extract_assumptions` collects those lines from the agent's own output VERBATIM — nothing dropped or shortened, since this feeds a durable record whose whole job is surviving the round that replaces `report_details`; bounding happens at render time in `packet.py` — anchored at line start AND refusing a `>`/`-`/`*` prefix, so neither prose *about* the convention nor an agent quoting the instruction it was given (`> ASSUMPTION: <what you assumed...>`) can be harvested as a disclosure nobody made, and rides them out on the SUCCESS outcome only — a failed round is never committed, so its assumptions would describe code the reviewer never sees. Unlike `changed_paths` this IS the agent's word, safely: it is only ever shown to a reviewer, never computed with. **The approved plan reaches the agent (plan-01, 2026-08-18):** when the task carries a `decomposition`, `_agent_prompt` appends it under `_DECOMPOSITION_HEADER` — labelled as agreed with the reviewer BEFORE any code was written, to be worked in order and not widened, with "say so in your report" as the route for a plan that turns out wrong. A task with none gets no heading rather than an empty one. Still one dispatch: nothing here runs per step. |
| `stall.py` | **Added 2026-08-14 (`stall-01`).** Progress-based stall detection, replacing `audit.agent_timeout_seconds` — an elapsed bound that never once caught a hung agent and destroyed six agents' in-progress work over 2026-08-05/06 (up to 631 insertions at a time; the losses are listed in the module docstring). Elapsed time cannot tell a large task from a wedged one. `supervise(handle, probe, policy)` bounds the LACK of progress instead: while the worker repository keeps changing the agent runs, and it is killed only after `stall_seconds` with NO filesystem change (default 1800), or at the absolute backstop `ceiling_seconds` (default 14400), which should effectively never fire and reports itself as a finding when it does. `WorkerTreeProbe` is the observation — `git status --porcelain -z -uall` through the policy-validated `GitGateway` plus per-path `st_mtime_ns`/`st_size`, never anything the agent reports about itself and deliberately not a raw filesystem walk (the probe's own `git status` refreshes `.git/index`, so a walk including `.git` would see churn every tick and the detector would never fire; the per-path stat is what catches repeated Edits to ONE file, which leave the path set identical). Every ambiguous case errs toward letting the agent finish: a probe that cannot observe never stall-kills, and a missing sample is "no evidence of silence", not silence. `StallReport.describe()` is the reviewer-facing account — silence, elapsed, files changed, lines written, and whether validation had started, because a stall with substantial partial work and one with none call for opposite responses. `supervise` is pure over an injected handle/clock/sleep; the real path is `spawn_supervised` (Popen with `start_new_session=True`, output to temp FILES — an undrained pipe would be a hang manufactured by the hang detector, and a temp file outside the repo cannot register as progress) plus `ProcessGroupHandle`, which signals the whole group so orphaned children cannot keep writing after the kill. |
| `prompts.py`, `config.py`, `cli.py` | Strict `PromptTemplate` library (+ `audit_kickoff`, `smoke_test`, `postcommit_review`); strict TOML config (`[executor]`, `[audit]` sections; new path properties `workers_dir`/`worker_hooks_dir`/`executions_dir`/`intents_dir`/`seed_tasks_file`/`continuous_fingerprint_file`); `run [--continuous] / status / tasks / next-task / doctor / smoke-browser / pause / resume / unlock / reset / reprovision-publisher` CLI with locking on mutating commands. `_build_orchestrator` always constructs the full produce-then-review collaborator set (see the intro above). `run --continuous` loops the phase machine, resuming a saved non-terminal phase and otherwise auto-selecting a ready task or running one audit per repository fingerprint change (`cli.repo_fingerprint`), sleeping — with ZERO Claude/ChatGPT calls — when neither applies. **`_reconcile_retired_blockers`, added 2026-08-16 (task-retire-01):** closes the open `blockers.Blocker` records of every RETIRED task via `archive_stale` (a machine reason, never a forged `answer`), because the blocker list is read independently of the registry by `start`, `health.check` and the heartbeat — so retiring a quarantined task used to leave the loop stopped on a question about superseded work. Called from `_cmd_retire_task`, from `_cmd_start`'s preflight and at the top of every `_run_continuous` iteration (out-of-process readers, so waiting for exhaustion would leave `health` alarming while the loop works). **Scoped by KIND as an allowlist** — only `task_fatal` quarantines, whose question really is about the one retired task. Every `loop_fatal` record survives regardless of the task id it names, because a loop-wide park (`checkout_escape_detected`, `primary_checkout_dirty`, a worker/publisher environment failure) routinely names whatever was in flight, and closing it on a retirement would manufacture resolution of the safety condition and let `start` proceed; an unrecognised/empty `kind` reads as loop_fatal, matching the fail-closed default in `_to_needs_user`. `(loop)` blockers are never swept either. **Operator-question wording (auto-05, 2026-08-16):** the `clarification` template and `run --answer` (help text + phase refusal) describe the question they carry as the LOOP's own — `state.question`, from a `_to_needs_user` park — not a reviewer's, since the decision that raised one is retired and cannot reach a park (`docs/AUTOLOOP.md` §9c). Wording only; preconditions, payload fields and dispatch untouched. |
| `tests/` | 805 hermetic tests — no network, no playwright, no live claude CLI (see `docs/TESTS.md`). The figure is the previously documented 793 plus the 12 the loop-02 token hardening added (hand-counted, not a re-audit of the total — the aggregate has trailed the collected count before). `test_self_upgrade.py` additionally guarantees, with an autouse fixture, that no test can replace the pytest process via `os.execv`, and with a second one that no test leaves an `AUTOLOOP_EXEC_HANDOFF_TOKEN` behind for the next. |
| `prompts.py`, `config.py`, `cli.py` | Strict `PromptTemplate` library (+ `audit_kickoff`, `smoke_test`, `postcommit_review`); strict TOML config (`[executor]`, `[audit]` sections; path properties `workers_dir`/`worker_hooks_dir`/`executions_dir`/`intents_dir`/`blockers_dir`/`seed_tasks_file`/`continuous_fingerprint_file`, plus the **required, absolute** `[paths].workers_root`, 2026-07-31 — see below); `run [--continuous] / status / tasks / next-task / blockers / answer / doctor / smoke-browser / pause / resume / unlock / reset / reprovision-publisher` CLI with locking on mutating commands (`blockers` is read-only/no-lock; `answer` locks). `_build_orchestrator` always constructs the full produce-then-review collaborator set, including a `BlockerStore` (see the intro above), and now REFUSES (`ConfigError`) before doing so if `worker_env.validate_workers_root` reports `config.workers_root` unsafe. `run --continuous` loops the phase machine, resuming a saved non-terminal phase and otherwise auto-selecting a ready task or running one audit per repository fingerprint change (`cli.repo_fingerprint`), sleeping — with ZERO Claude/ChatGPT calls — when neither applies. **Blockers (2026-07-31):** a `task_fatal` `needs_user` park (`_handle_parked_task`) quarantines just that task and clears the session so the loop continues with other READY tasks; a `loop_fatal` park still stops the loop. Exhaustion (no ready task, unchanged fingerprint, >=1 open blocker) prints every open blocker and exits 0 instead of sleeping forever. **`_RESOLUTION_PRECONDITIONS` (worker-isolation hardening, 2026-07-31; +3 same-day follow-up keys):** `answer` re-checks an environmental blocker's condition rather than trusting the operator's text — `git_failure_budget_exhausted` (renamed from the dead `git_failure_budget`), `worker_environment_drift` (now a dedicated `verify_worker_isolation`-based recheck, not the browser/login probes it used to share with `login_expired`), `worker_isolation_violation` (reuses the same recheck), `push_refused_protected` (now actually emitted — see `orchestrator.py` below), `primary_checkout_dirty` (dedicated `_precondition_checkout_clean`, re-runs `GitGateway.is_dirty()`), `checkout_escape_detected` (its OWN unconditional-refusal precondition, `_precondition_checkout_escape_detected` — NOT `is_dirty()`-based: the escape detector's snapshot covers untracked+ignored paths `is_dirty()` cannot see, so a checkout-cleanliness recheck would clear on an escape that touched only an ignored path, e.g. `.autoloop/state.json` itself — found in a second round of review and fixed same-day), `publisher_url_drift`, `push_refused`, `login_expired`, `submission_ambiguous`. Verified exhaustive against every code `orchestrator.py` actually emits by an AST walk (catches a stale key), plus a curated reverse list (catches a missing one — added same-day; the AST walk alone cannot) — both in `test_m1_hardening.py`. **`[audit]` timeout retirement (2026-08-14, `stall-01`):** `agent_timeout_seconds` split into `agent_stall_seconds` (write-capable silence window, 1800), `agent_ceiling_seconds` (absolute backstop for both paths, 14400) and `audit_agent_timeout_seconds` (the old elapsed meaning, kept ONLY for read-only audit agents, which change no files and so have no progress to observe). A config still naming the retired key LOADS and is handled explicitly rather than ignored: `_migrate_retired_timeout` consumes it (so it never reaches `AuditConfig`), migrates its value onto `audit_agent_timeout_seconds` — the one replacement that keeps its exact meaning — and returns an operator-facing message in `AutoloopConfig.migration_notices`; an explicit `audit_agent_timeout_seconds` takes precedence, and a junk value is refused like any live setting. `load_config` stays PURE (no stream, no "already warned" global), so the notice text is order-independent; `cli.emit_migration_notices` — called from the thin `cli.load_config` wrapper every command reads its config through — prints it to **stderr** (never stdout, which `status`/`tasks`/`next-task` pipe) at most once per process, since `run --continuous` reloads config every round. A stall window at or above the ceiling is refused too: it would read as configured while being unreachable. **Fault stops (auto-04, 2026-08-16):** `_is_fault_stop` / `_report_fault_stop` — `stopped` is now two terminals, so `run --continuous` halts (exit 2, printing the recorded blocker route) on `stop_kind="fault"` instead of treating it as the clean boundary that would kick a fresh session into the same wall, plain `run` exits 2 on one rather than reporting a completed round, and `smoke-browser` PASSes only on `stop_kind == "contract"` — without that gate the first denied reply (the smoke policy allows zero denials) would end in `stopped` and be announced as a healthy round-trip. All three read the POSITIVE value, so a legacy `""` is a clean boundary and an unclassified future fault is a failure. **New `[repo]` section (2026-08-16, port-02):** `RepoConfig` — `env_example_file`, `env_example_db_key`, `audit_report_glob` — what the loop cannot infer about the TARGET repository, each previously a constant naming THIS repo. Every default is the exact constant it replaced, so a config omitting the section behaves identically to the code before it existed (pinned by `test_config_repo_section.py`'s first test, which is the premise of the whole change). **NOTHING here grants authority** — each setting says WHERE to read something the repository itself states, and the one repo-specific constant that *does* decide what an agent may write (the always-approved `tasks.TRACKER_PATHS`) is deliberately NOT a key: a `tracker_paths` setting was written and withdrawn in review, since this file is gitignored and a suffix blocklist cannot make an implicit grant documentation-only (`docs/SECURITY.md` S31). A config still naming it loads and is handled explicitly by `_migrate_retired_tracker_paths` — consumed before `_check_keys`, value DROPPED, operator notice appended to `AutoloopConfig.migration_notices` alongside the retired-timeout one. Strict like every other section otherwise: `_check_keys` refuses an unknown key, `_repo_relative` refuses an absolute/`..`/padded path for both path settings and glob metacharacters for the non-glob one, a non-table `repo = "x"` draws a `ConfigError` naming the section rather than `dict()`'s raw conversion error, the disable is the EXACT empty string for both path settings (a whitespace-only value is refused, not stripped — `repo_declared_db_name` reads a blank marker as "no declaration" and would turn the application-database refusal off while reading as configured), and `env_example_db_key` must be a plain variable name — a key with a space or an `=` could never match a parsed line, so it would silently read as "no marker declared" rather than refusing. **`_clear_fault_budget_on_answer` (budget-01, 2026-08-17):** answering a `fault_attempt_ceiling` blocker resets `TaskExecution.fault_attempt_count` to 0 and nothing else — without it the answer unblocks the task and the next dispatch parks on the identical wall, which is the mechanical hand-repair this task removed. Narrow on purpose: only that code (answering `attempt_count_ceiling` refills NOTHING, since that budget bounds the task's own failing work), only that counter (`candidate_sha`, `review_round`, `last_revise_feedback` and every `attempt_ledger` entry are preserved, so the history of what was spent survives the fresh allowance), and best-effort (an unreadable record must not turn a successful `answer` into a failure). **Running the code it just merged (loop-02, 2026-08-18):** `_self_upgrade_at_boundary` is the ONE place the loop replaces its own process, reached only when `Orchestrator.run` returns `SELF_UPGRADE` inside `_run_continuous`. It preflights the merged tree in a SUBPROCESS first (`_preflight_import` — `python -c "import autoloop, autoloop.policy, …"` in the merged checkout, since this process already holds the old modules and a reload would produce exactly the half-swapped state `execv` avoids), refuses when the merged checkout is not the tree this process imported from, flips the record to `execed` BEFORE the call so a merge that imports and then fails at runtime cannot loop, arms the lock's `exec_handoff` (which mints a one-use token into this process's environment — the exec is what carries it to the successor, so nothing may spawn a child between the arming and the call, and the preflight therefore runs before it), and only then `os.execv(sys.executable, [sys.executable, "-m", "autoloop", *sys.argv[1:]])` — the documented launch shape rebuilt, because `argv[0]` under `-m` is `__main__.py` and re-running that as a script breaks its relative imports. Every carry-on path SETTLES the record (`_settle_upgrade`, clearing it if it cannot be written), because a record left `pending` makes every subsequent round hand the process back — including the path where `os.execv` itself raises, which settles `exec_failed` after disarming the handoff (review round 2, 2026-08-18): `execed` means "a successor is running", so leaving it there would have the next iteration's confirmation report a replacement that never happened. `_confirm_self_upgrade` retires the one-shot after one full `_run_continuous` iteration under the new code — checked at the TOP of the second iteration, since every branch below ends with its own `continue`. Plain `run` reports the pending upgrade and exits 0 without replacing anything: its argv carries flags that are not safe to re-run. New config path property `pending_upgrade_file` (`.autoloop/pending_upgrade.json`). See `docs/AUTOLOOP.md` §3f-quater. |
| `tests/` | 810 hermetic tests — no network, no playwright, no live claude CLI (see `docs/TESTS.md`). |
| `prompts.py`, `config.py`, `cli.py` | Strict `PromptTemplate` library (+ `audit_kickoff`, `smoke_test`, `postcommit_review`); strict TOML config (`[executor]`, `[audit]` sections; path properties `workers_dir`/`worker_hooks_dir`/`executions_dir`/`intents_dir`/`blockers_dir`/`seed_tasks_file`/`continuous_fingerprint_file`, plus the **required, absolute** `[paths].workers_root`, 2026-07-31 — see below); `run [--continuous] / status / tasks / next-task / blockers / answer / doctor / smoke-browser / pause / resume / unlock / reset / reprovision-publisher` CLI with locking on mutating commands (`blockers` is read-only/no-lock; `answer` locks). `_build_orchestrator` always constructs the full produce-then-review collaborator set, including a `BlockerStore` (see the intro above), and now REFUSES (`ConfigError`) before doing so if `worker_env.validate_workers_root` reports `config.workers_root` unsafe. `run --continuous` loops the phase machine, resuming a saved non-terminal phase and otherwise auto-selecting a ready task or running one audit per repository fingerprint change (`cli.repo_fingerprint`), sleeping — with ZERO Claude/ChatGPT calls — when neither applies. **Blockers (2026-07-31):** a `task_fatal` `needs_user` park (`_handle_parked_task`) quarantines just that task and clears the session so the loop continues with other READY tasks; a `loop_fatal` park still stops the loop. Exhaustion (no ready task, unchanged fingerprint, >=1 open blocker) prints every open blocker and exits 0 instead of sleeping forever. **`_RESOLUTION_PRECONDITIONS` (worker-isolation hardening, 2026-07-31; +3 same-day follow-up keys):** `answer` re-checks an environmental blocker's condition rather than trusting the operator's text — `git_failure_budget_exhausted` (renamed from the dead `git_failure_budget`), `worker_environment_drift` (now a dedicated `verify_worker_isolation`-based recheck, not the browser/login probes it used to share with `login_expired`), `worker_isolation_violation` (reuses the same recheck), `push_refused_protected` (now actually emitted — see `orchestrator.py` below), `primary_checkout_dirty` (dedicated `_precondition_checkout_clean`, re-runs `GitGateway.is_dirty()`), `checkout_escape_detected` (its OWN unconditional-refusal precondition, `_precondition_checkout_escape_detected` — NOT `is_dirty()`-based: the escape detector's snapshot covers untracked+ignored paths `is_dirty()` cannot see, so a checkout-cleanliness recheck would clear on an escape that touched only an ignored path, e.g. `.autoloop/state.json` itself — found in a second round of review and fixed same-day), `publisher_url_drift`, `push_refused`, `login_expired`, `submission_ambiguous`. Verified exhaustive against every code `orchestrator.py` actually emits by an AST walk (catches a stale key), plus a curated reverse list (catches a missing one — added same-day; the AST walk alone cannot) — both in `test_m1_hardening.py`. **`[audit]` timeout retirement (2026-08-14, `stall-01`):** `agent_timeout_seconds` split into `agent_stall_seconds` (write-capable silence window, 1800), `agent_ceiling_seconds` (absolute backstop for both paths, 14400) and `audit_agent_timeout_seconds` (the old elapsed meaning, kept ONLY for read-only audit agents, which change no files and so have no progress to observe). A config still naming the retired key LOADS and is handled explicitly rather than ignored: `_migrate_retired_timeout` consumes it (so it never reaches `AuditConfig`), migrates its value onto `audit_agent_timeout_seconds` — the one replacement that keeps its exact meaning — and returns an operator-facing message in `AutoloopConfig.migration_notices`; an explicit `audit_agent_timeout_seconds` takes precedence, and a junk value is refused like any live setting. `load_config` stays PURE (no stream, no "already warned" global), so the notice text is order-independent; `cli.emit_migration_notices` — called from the thin `cli.load_config` wrapper every command reads its config through — prints it to **stderr** (never stdout, which `status`/`tasks`/`next-task` pipe) at most once per process, since `run --continuous` reloads config every round. A stall window at or above the ceiling is refused too: it would read as configured while being unreachable. **Fault stops (auto-04, 2026-08-16):** `_is_fault_stop` / `_report_fault_stop` — `stopped` is now two terminals, so `run --continuous` halts (exit 2, printing the recorded blocker route) on `stop_kind="fault"` instead of treating it as the clean boundary that would kick a fresh session into the same wall, plain `run` exits 2 on one rather than reporting a completed round, and `smoke-browser` PASSes only on `stop_kind == "contract"` — without that gate the first denied reply (the smoke policy allows zero denials) would end in `stopped` and be announced as a healthy round-trip. All three read the POSITIVE value, so a legacy `""` is a clean boundary and an unclassified future fault is a failure. **New `[repo]` section (2026-08-16, port-02):** `RepoConfig` — `env_example_file`, `env_example_db_key`, `audit_report_glob` — what the loop cannot infer about the TARGET repository, each previously a constant naming THIS repo. Every default is the exact constant it replaced, so a config omitting the section behaves identically to the code before it existed (pinned by `test_config_repo_section.py`'s first test, which is the premise of the whole change). **NOTHING here grants authority** — each setting says WHERE to read something the repository itself states, and the one repo-specific constant that *does* decide what an agent may write (the always-approved `tasks.TRACKER_PATHS`) is deliberately NOT a key: a `tracker_paths` setting was written and withdrawn in review, since this file is gitignored and a suffix blocklist cannot make an implicit grant documentation-only (`docs/SECURITY.md` S31). A config still naming it loads and is handled explicitly by `_migrate_retired_tracker_paths` — consumed before `_check_keys`, value DROPPED, operator notice appended to `AutoloopConfig.migration_notices` alongside the retired-timeout one. Strict like every other section otherwise: `_check_keys` refuses an unknown key, `_repo_relative` refuses an absolute/`..`/padded path for both path settings and glob metacharacters for the non-glob one, a non-table `repo = "x"` draws a `ConfigError` naming the section rather than `dict()`'s raw conversion error, the disable is the EXACT empty string for both path settings (a whitespace-only value is refused, not stripped — `repo_declared_db_name` reads a blank marker as "no declaration" and would turn the application-database refusal off while reading as configured), and `env_example_db_key` must be a plain variable name — a key with a space or an `=` could never match a parsed line, so it would silently read as "no marker declared" rather than refusing. **`_clear_fault_budget_on_answer` (budget-01, 2026-08-17):** answering a `fault_attempt_ceiling` blocker resets `TaskExecution.fault_attempt_count` to 0 and nothing else — without it the answer unblocks the task and the next dispatch parks on the identical wall, which is the mechanical hand-repair this task removed. Narrow on purpose: only that code (answering `attempt_count_ceiling` refills NOTHING, since that budget bounds the task's own failing work), only that counter (`candidate_sha`, `review_round`, `last_revise_feedback` and every `attempt_ledger` entry are preserved, so the history of what was spent survives the fresh allowance), and best-effort (an unreadable record must not turn a successful `answer` into a failure). |

---

## Config + entry

| Path | Purpose |
|---|---|
| `Procfile` | `web: uvicorn backend.main:app` |
| `.github/workflows/dependency-audit.yml` | Dependency CI (S15). `pip-audit` (backend) + `npm audit` (frontend) on push/PR to main + weekly cron. |
| `.github/workflows/tests.yml` | Test CI (rt-10). `ruff check .`, `pytest tests/`, `pytest autoloop/tests` (+ the `isolated` marker run) on push/PR to main. The backend suite is **not** gated — blocked by hb-01; see the CI section of `docs/TESTS.md`. |
| `.env.example` | Env schema. Real `.env` is secrets — never read it. |
| `.gitignore` | |
| `.gitattributes` | **New 2026-08-19 (docs-01), and deliberately RULE-FREE.** It declares no merge attribute at all; it exists to record why `merge=union` on the two change-note trackers was tried, measured and removed the same day. Git cannot scope a merge attribute to a REGION, and union never reports a conflict, so the attribute meant "these files can no longer conflict" rather than "combine the appended notes" — it swallowed a genuine prose conflict — and it resolves per LINE, so it duplicated a grown 19,410-character row instead of merging the two additions. Combining notes moved to `autoloop/note_merge.py` + `auto_merge.AutoMerger._merge`. Pinned by `autoloop/tests/test_docs_merge.py` (exact emptiness, plus both union failures demonstrated against real git). |
| `requirements.txt` | Delegates to backend's requirements. |
| `pytest.ini` | Root pytest config. `testpaths = tests autoloop/tests` (a bare `pytest` covers both trees); `addopts = -p no:randomly -m "not isolated"` — the plugin block works around a spaCy/thinc seed conflict, the marker keeps the flaky-in-company test out of every shared run, and there is deliberately no `-n` here: parallelism belongs on the individual validation command (`autoloop/config.example.toml`) so it cannot silently reach the dedicated `-m isolated` run. There is no root `conftest.py`. |
| `README.md` | Repo-root readme. |
| `CLAUDE.md` | This project's master guide (read first). |
| `docs/TODO.md` | Bugs + tasks, ordered by blocking dependency. |
| `docs/AUTOLOOP_TODO.md` | Open work on the loop itself — the live-progress gap during agent fan-out, the unexplained changeset-publish refusal, execution records stranded by a `workers_root` move, and the self-hosting rollback design. App tasks stay in `docs/TODO.md`; security findings stay in `docs/SECURITY.md`, referenced by id. |
| `docs/WORKFLOW_AUDIT.md` | Full word-learning trace with numbered holes. |
| `services/book_import_service.py` | **Document-package import orchestration (roadmap A2).** `verify → validate → dry-run → persist`, no DB mutation before every gate passes. `list_packages`, `validate_package`, `import_package`, `dry_run_package`. Scope is narrow by design: no reconstruction (A4), segmentation (A5), AI review (A8) or worker invocation (A10). |
| `services/document_package/` | The A2 internals. `contract.py` (versions, element enum, coordinate space, 1-based page-numbering base — no logic), `issues.py` (fatal/warning/info collector), `loader.py` (discovery, checksums, **path-containment chokepoint**), `validators.py` (individually testable structural validators), `coordinates.py` (the single Docling↔fitz flip), `persistence.py` (`PersistenceBackend` seam + dry-run + the A3 placeholder), `result.py` (`import_result.json`). Imports nothing from `nlp_histo`/Docling/Torch — asserted by a test. |
| `evaluation/` | **Document-ingestion evaluation harness (roadmap A1).** No LLM, no `nlp_histo` import, no Docling/torch. `normalize.py` (the single shared normalizer every offset depends on), `annotation.py` (gold format + round-trip), `metrics.py` (Stage 1 fidelity + Stage 2 boundary/reader), `model.py` (`PredictedDocument` + the `DocumentSource` adapter seam), `adapters.py`, `segmentation.py` (spaCy `de` baseline), `runner.py`, `report.py`, `compare.py`, `baselines.py` (frozen constants), `cli.py`. Run: `python -m evaluation run --label X`. |
| `benchmark/documents/` | Gold corpus for the harness — original German text, three files per document (`.blocks.json` input, `.gold.txt` expected units, `.gold.json` sidecar). See `benchmark/README.md`. The real graded readers are commercial and stay in gitignored `files/`. |
| `docs/INGESTION_PIPELINE.md` | Design for the document-ingestion boundary: offline `nlp-histo` worker → versioned document package → import → reconstruction → `book_sentences` → sentence-by-sentence reader. Package schemas, coordinate conventions, AI-review op contract, evaluation plan, task breakdown. ROADMAP **N7** / TODO **#44**. |
| `docs/TESTS.md` | Test inventory, coverage gaps, known failures. |
| `docs/COMMON_ERRORS.md` | Symptom-first log of errors actually hit here (tooling, tests, build, lint, research). Grep it by the error text before debugging. |
| `docs/SUMMARY.md` | This file. |
| `docs/audit_charters.toml` | The per-domain briefs this repository's read-only audit agents are given — one `[[domain]]` table each, in wave order, carrying `slug`/`title`/`charter`/`model`. Read read-only, once per run, from the root of the checkout being audited (`[repo].audit_charters_file`); shipped here since port-03 so the knowledge travels with the code it describes. `DEFAULT_DOMAINS` in `autoloop/audit/executor.py` holds the same six domains as the fallback for a repository that ships no file, and `autoloop/tests/test_audit_charters.py` requires the two to stay equal. A file that exists but does not parse — or is a directory, or cannot be read — aborts the audit rather than falling back. |

---

## How to find things — common questions

| Question | Open |
|---|---|
| Where is the state machine? | `lexy-app/backend/services/progression_service.py` (`_RULES`) |
| Where does a transcript click go? | `routers/words.py:record_transcript_click` → `usage_events_service.record_transcript_click_event` (atomic dedup via unique partial index); only on a new insert does it then call `progression_service.apply_progression("transcript_clicked")`. |
| Where do SRS cards come from? | `progression_service._update_srs` — created on `passive_srs/active_srs="create"` or first correct event |
| Where is the LLM called? | `services/llm_service.py` — all calls go through here, cached via `llm_cache_service` |
| Where is the polymorphic `(item_id, item_type)` join? | `review_service.get_due_cards` is the canonical example; `usage_events_service` analytics too |
| Where do book pages render? | `frontend/src/components/BookReaderPage.tsx`, blocks via `book_service.get_page_detail` |
| Where do free-chat words match against vocab? | `chat_service.match_learning_words` (words via direct SQL; phrases via `matcher_service.match_sentence_with_ids`). |
| Where are reading selections wired into main progression? | `routers/reading.py:156` calls `find_catalog_item` then `apply_progression("status_marked_learning")` |
| Where is `users.settings` JSONB read/written? | `services/settings_service.py` — defaults live here |
| Where do notifications get written? | `subtitle-scraper/pipeline.py:_notify_user` for success (`video_done`, `channel_done`); `_mark_request` emits `request_failed` whenever `status='failed'`. |
| Where is auth enforced? | `core/deps.py:get_current_user` — every router uses it as a Depends |
| What seeds run on startup? | `main.py` lifespan: `phrase_service.seed_from_blueprint_map`, `grammar_service.seed_rules`, resume pending content_requests |
| Where is the ChatGPT engineering loop? | `autoloop/orchestrator.py` (state machine); full doc in `docs/AUTOLOOP.md` |

---

## Change notes — append ONE new line at the END of this file

Where a task records what it changed when there is no natural new row for it
above. **This section stays last in the file, and a note is appended at the
very end of it.**

Five rules. They are not style — they are the precondition the loop's own merge
path checks before it will combine two branches' notes
(`auto_merge.AutoMerger._merge` → `autoloop/note_merge.py`; the shape is pinned
by `autoloop/tests/test_docs_merge.py`, the reasoning is in `CLAUDE.md` §12):

1. **Add a line. Never grow a line.** Do not append your note into an existing
   table row or paragraph. The resolver only combines whole lines added AFTER
   everything that was already here; a grown line is an edit, and the merge
   stops. That habit is how one row above reached 19,410 characters.
2. **One note, one line.** Keep it to roughly a sentence (~400 characters). If
   it needs more, add a second line, or put the detail in the section above
   that owns the file and leave a pointer here.
3. **Never edit, delete or reorder a line someone else wrote.** The resolver
   refuses the whole merge if you do, and that refusal is the point: a
   rewritten claim needs a human to look at it.
4. **Order is arrival order, not chronology.** A merge concatenates one
   branch's appended lines then the other's. Read the `Date` and `Task`
   columns, never the position.
5. **Call the marker `CHANGE-NOTES`; never write the comment out again.** The
   section below opens with an HTML comment the resolver finds by that text,
   and it requires the marker to appear EXACTLY ONCE in this file. A second
   copy — the easy way to quote it while documenting how any of this works —
   makes the resolver decline every parallel merge, silently. That is the
   original failure with an extra step, and it is how docs-01 itself first
   shipped.

Adding a genuinely new table row anywhere above is fine and always was — that
is also "a new line". This section exists for the notes that have no row.

Nothing may follow the last note line: this section is the end of the file, so
that the next task's append lands at the end of the ledger rather than inside
whatever came after it.

<!-- CHANGE-NOTES: append below, one line per note, at the END of the file. Never edit a line above. -->

| Date | Task | Note |
|---|---|---|
| 2026-08-19 | docs-01 | This append-only section is where a task's note lands when it has no natural new row above; the four `state.py`, `transcript.py` rows are one dated note per line — that split is the template. |
| 2026-08-19 | docs-01 | Two branches' appends here are combined by `autoloop/note_merge.py` from inside `auto_merge.AutoMerger._merge`; `merge=union` was tried and removed the same day because git cannot scope a merge attribute to a region, so it disabled conflict detection for the whole file. |
| 2026-08-19 | docs-01 | The marker comment must appear EXACTLY ONCE per tracker or `resolve_note_append` refuses every merge of that file, so prose here names it `CHANGE-NOTES` instead of quoting it — a second copy in the `note_merge.py` row above shipped with this task and switched auto-resolution off for `SUMMARY.md` itself. |
| 2026-08-19 | port-03 | The audit's domain charters are now shipped by the repository under audit (`[repo].audit_charters_file`, default `docs/audit_charters.toml`) and THIS repository ships that file, so if §2's two-backend layer table changes, edit `docs/audit_charters.toml` in the same commit or the next audit briefs its agents on a layout that no longer exists; `DEFAULT_DOMAINS` survives only as the fallback for a repository that ships nothing, pinned equal to the file by `test_audit_charters.py`. |
| 2026-08-19 | port-03 | The audit prompt's opening sentence is now chosen by charter SOURCE — `BUILT_IN_FRAMING` (naming the language-learning app, preserved word for word) only on the fallback path, `REPO_SUPPLIED_FRAMING` (naming no project) whenever the domains came from a file, because telling an agent auditing a Go service that it is reading a German language-learning app contradicts its own charter in the sentence before it arrives. |
| 2026-08-19 | port-03 | Charter-file absence now means `FileNotFoundError` from `stat()` and nothing else: a directory, a non-regular entry or an unreadable path at `docs/audit_charters.toml` is refused by name rather than read as "ships no charters", because `is_file()` answered False for all of them and quietly selected the built-ins inside a checkout that meant to ship its own. |
| 2026-08-20 | val-02 | Per-commit test selection reaches the reviewer through the validation SUMMARY (`state.last_validation` → the CONTEXT block's `validation:` line), not through `packet.py` — the packet renders only git-read sections plus the executor's one labelled claim, and a selection decision is neither. A run with no pytest command configured appends nothing, which is why a ruff-only deployment's summary is byte-identical to what it was. |
| 2026-08-20 | val-02 | The pre-commit validation run (`implement_executor.py:762`) is deliberately NOT narrowed by val-02 — it was outside the approved file list — so each round still pays for the full configured suite once, in the executor, and the narrowed run is the post-commit re-run whose result the reviewer is shown. Narrowing that second site is the remaining half of the operator's 2026-08-06 request. |
| 2026-08-20 | base-02 | A reviewed, unpublished candidate whose base fell behind the head is no longer parked on `task_base_behind_head`: `orchestrator._carry_reviewed_candidate_past` MERGES the head into the task branch (`git_gateway.merge_foreign_commit` / `MergeAttempt`) so every reviewed commit keeps its exact sha. Re-basing is still refused, a genuine conflict still parks, and the round-0 rebase and the published-candidate reconciliation are untouched. |
| 2026-08-20 | base-02 | Only `task_base_sha` moves when a candidate is carried past a moved head — to the new head, because `range_diff` / `commit_range_paths` are direct tree diffs and a base left behind would attribute mainline's work to the task. |
| 2026-08-20 | base-02 | `_dispatch_task_push`'s descendancy check gained `_candidate_is_on_task_line`, which accepts a candidate the branch tip carries alongside the base, since a carried-forward candidate is the merge's parent rather than its descendant. |
| 2026-08-20 | base-02 | `cli._merge_window_blockers` and the `dashboard.py` in-flight notes still say a moved head strands such a task — now conservative rather than accurate, and worth a follow-up. |
| 2026-08-19 | scope-04 | A later round may DELETE a path already in its own `TaskExecution.out_of_scope_paths` and nothing else changes — `tasks.authorized_cleanup_paths` gates it on exact match against that loop-written record, `implement_executor._remove_recorded_file` performs the one unlink, and `approved_paths`/`allowed_paths` gain nothing; an EDIT to the same recorded path stays exactly as unauthorized as before. |
| 2026-08-19 | scope-04 | The write-capable agent has no delete tool at all (`WRITE_ALLOWED_TOOLS` is Read/Grep/Glob/Edit/Write, `Bash` disallowed), which is why "remove the residue you added" used to produce a committed zero-byte file: it now writes `REMOVE-OUT-OF-SCOPE: <path>` and the executor unlinks, before the `git status` read so a cleanup-only round has something to commit and before validation so the suite grades the tree that is committed. |
| 2026-08-19 | dash-13 | `dashboard.merge_report` now carries `groups` beside `rows`/`counts`: the merged-into-the-branch panel rendered 61 rows flat on 2026-08-18 while `counts` already said {merged: 57, unpublished: 3, unmerged: 1, unknown: 0}, so the four rows needing a human sat under 57 that did not and the counts were computed and then thrown away as a display axis. |
| 2026-08-19 | dash-13 | `MERGE_GROUPS` is the roadmap panel's `GROUPS`/`COLLAPSED` mechanism applied one section up — an ordered `(state, label, collapsed)` tuple, triage order (`unmerged`, `unpublished`, `unknown`, `merged`), with `merged` the only collapsed group — and `MERGE_STATES` is now documented as the vocabulary rather than a display order. |
| 2026-08-19 | dash-13 | Merge grouping is display-only and deliberately carries NO `hidden` flag: the roadmap has one because it has a hide policy and this panel's rule is that it has none, so every group renders with its count including `Unknown (0)` — an unknown row is a branch the sweep could not judge, the state most worth seeing and the easiest to hide by accident. `merge_states`, the row fields, the `counts` dict and the flat `rows` list are untouched; a state no group claims lands in `unknown` rather than vanishing. |
| 2026-08-19 | dash-13 | The collapsed group is a `<details id="mgmergedbox">` in STATIC markup, looked up by KEY (`groups.find(g => g.key === "merged")`, never "the group carrying the collapsed flag"), and the new `renderMerge` writes only its summary text and its rows — never a disclosure state — so an operator who opens it keeps it open across the 2s poll. `#mergehead` stays above the groups because it names the branch "merged" is relative to. |
| 2026-08-19 | scope-04 | `TaskExecution.removed_out_of_scope_paths` exists because the packet structurally cannot show a cleanup — every path section is computed from `commit_range_paths`, a tree-to-tree diff, so a file created in round 1 and deleted in round 2 is absent from the reviewed range entirely; `out_of_scope_paths` is never pruned when a path lands there, and the two sets overlap on purpose. |
| 2026-08-19 | scope-04 | Still open after this task: a file INSIDE `approved_paths` cannot be deleted either, and `REMOVE-OUT-OF-SCOPE:` refuses it as never recorded — the missing capability there is the agent's tool set, not its authorization (roadmap-01's `autoloop/tests/test_obsolete.py` was that case, and today it needs an operator). |
| 2026-08-20 | dash-14 | `dashboard.dependency_graph` + the page's `renderDeps` draw the `depends_on` relation as a second hand-built SVG (one directed edge per pair, dependency → dependent, layered so a task is drawn after everything it depends on), because the roadmap is a flat list grouped by state and "port-01 is blocked and six tasks are waiting behind it" was a fact an operator could only get by reading `tasks.json` by hand — measured 2026-08-19: 132 tasks, 150 edges, 41 tasks with a dependency. |
| 2026-08-20 | dash-14 | That graph is built from the RAW `tasks.json` rows and never from a `TaskRegistry`: `from_dict` runs `_check_acyclic`, so a cyclic file raises and `task_groups` returns `[]`, and a registry-derived graph would show nothing in exactly the case a cycle is worth showing. |
| 2026-08-20 | dash-14 | What `from_dict` really tolerates and `state_of` then rejects is a DANGLING dependency (`.get` vs `self._tasks[dep]`), drawn here as an `unknown` node rather than a missing edge. Node states still come from the `groups` payload (`state_of`) whenever the registry loads at all, so the graph and the Roadmap panel cannot disagree. |
| 2026-08-20 | dash-14 | Cycle safety is Tarjan SCCs + longest-path layering over the CONDENSATION (`_dep_sccs`, iterative), so a cyclic input terminates as readily as an acyclic one: members of a cycle share a column, everything downstream still lands after them, no edge is dropped to tidy the picture, and each cycle is reported with a concrete path. |
| 2026-08-20 | dash-14 | Only the connected subgraph is drawn — the isolated tasks (91 of 132) are counted out loud, `omitted + shown == total`. Display only: nothing writes, and `depends_on` / `next_ready()` are untouched. |
| 2026-08-20 | dash-14 | The panel owns its own refresh gate (`updateDeps` + `depsBusy` in the `DEPGRAPH` region) rather than riding the page-wide `LASTJSON` guard, which fires on the whole payload: the dependency graph changes when a task is added, merged or retired, while the payload around it moves every 2s, so this panel was rebuilt dozens of times an hour for a graph unchanged by one edge. |
| 2026-08-20 | dash-14 | `updateDeps` redraws only when its own `depgraph` slice changed, HOLDS a changed payload while the operator has focus, hover or a text selection inside `#deppanel`, and delivers it on the next tick after the gesture ends — so a tick costs no hovered node, no focus ring, no selection in the edge table. |
| 2026-08-20 | dash-14 | `render` calls `updateDeps` ABOVE the page-wide guard and without `force` (that flag is also raised by a click on a PIPELINE node); the node picker forces a redraw of `DEPDRAWN`, the graph already on screen, so selecting a node cannot slip a held graph in under the cursor. |
| 2026-08-20 | dash-14 | A held payload is now dropped by ANY non-forced payload, not only by the draw that is it: a graph held mid-gesture goes obsolete the moment a poll reports something else (the change was retired, or a further one landed), and `settle` was then rendering that obsolete graph over the current one when the gesture ended. `HELD` is cleared at the top of `updateDeps` and re-held two lines down if the operator is still holding the panel; a forced draw is the deliberate exception, since it redraws what is already on screen. |
| 2026-08-20 | val-02 | `validation.select_validation_commands` is phase-agnostic by construction — command list, changed repo-relative paths, repo root, no notion of a commit — so adopting it pre-commit needs no change to it: `implement_executor` already holds `sorted(git.dirty_paths_all())` and `git.repo_root` at the line it calls `run_validation_commands`, and the only missing piece is threading `config.audit.test_selection` from `cli._build_executor`. Both files are outside val-02's approved paths, which is why this round stopped at the post-commit site. |
| 2026-08-20 | val-02 | `validation.PRECOMMIT_EVIDENCE` is the sentence that keeps a narrowed packet from reading as less evidence than the round before it — the subset is recorded as ADDED to the executor's still-full pre-commit run, not as a replacement — and it is a claim about a module this task did not touch, so it is pinned by `test_the_pre_commit_run_is_not_narrowed_today`; narrowing that call site fails that test, which is the reminder to rewrite the sentence in the same change. |
| 2026-08-20 | val-02 | `_retarget_pytest` now returns a STATUS (`_RETARGET_UNCHANGED`/`NARROWED`/`SKIP`/`BLOCKED`) rather than a command plus a note, because `UNCHANGED` and `BLOCKED` both hand back the configured argv and mean opposite things: review round 3 found the caller keeping a `BLOCKED` command while still reporting `widened=False`, so `evidence()` claimed each configured pytest command ran only the selected files about a command that had run the whole tree. A `BLOCKED` command now widens the WHOLE selection back to the configured list. |
| 2026-08-20 | val-02 | `SKIP` is deliberately NOT collapsed into `BLOCKED`: "no selected test lives under `other/`" is a reachability answer (every changed path resolved, the closure was computed, nothing landed there), so that command is dropped and named in `TestSelection.skipped` while the rest of the run still narrows — collapsing the two would widen every round in a repository with more than one test tree. Blocked commands are collected across the whole loop and checked after it, so the FULL-SUITE reason names every offender rather than the first. |
| 2026-08-20 | val-02 | `build_import_graph` resolves an import NAME against `_import_roots(rel)` — the repo root PLUS every non-package ancestor directory of the importing file — not against full dotted paths alone, because review round 4 found the repo-root-only lookup dropping this repository's own edges. |
| 2026-08-20 | val-02 | The edges it dropped: `autoloop/tests/` has no `__init__.py`, so pytest's prepend mode puts it on `sys.path` and its modules import each other by bare name (`from test_implement_executor import ...`), which resolved to nothing — a change to an imported test/helper module then selected the changed file and omitted the untouched modules that execute it. `subtitle-scraper/`'s `sys.path.insert` siblings (CLAUDE.md §10) were dropped the same way. |
| 2026-08-20 | val-02 | Package directories are excluded from `_import_roots` because Python 3 has no implicit relative imports — a bare `import publisher` inside `autoloop/` reaches a top-level module, never the neighbour, and the neighbour's real `from .publisher import ...` is already resolved by `_imported_modules`. When one name still matches under more than one root, or two files claim one dotted name under the same root, EVERY candidate gets an edge: the per-root index is `dict[str, set[str]]` rather than the single-valued `by_module` it replaced, whose last-write-wins collision was a second silent edge drop. |
| 2026-08-20 | val-02 | `_import_roots` takes EVERY non-package ancestor, which is deliberately wider than Python's own rule (stop at the first non-package ancestor — the directory pytest's prepend mode inserts). Stopping there is not enough here: the backend suite runs with a cwd of `lexy-app/backend`, so its tests' bare `from services import ...` resolves under a directory that is not their prepend root, and the narrow rule would drop those edges — the same silent drop the change exists to fix. The extra aliases in deep trees can only ADD edges, and the docstring owns the over-approximation rather than citing the pytest rule it does not implement. |
| 2026-08-20 | val-02 | A bound on the saving, recorded so the summary's counts are not read as a defect: `autoloop/tests/conftest.py` imports `autoloop.orchestrator`, and a conftest is treated as imported by every file beneath it, so a change to almost any `autoloop/` module selects the whole `autoloop/tests` tree. That is sound rather than a widening bug — every test in that directory really does execute that conftest — and what such a round saves is the other configured commands (the root `tests/` suite, the `isolated` re-run) being SKIPped as unreachable. Modelling function-scoped conftest imports would be the unsound way to shrink it. |
| 2026-08-20 | dash-16 | `dashboard.dependency_graph` now returns FOUR laid-out graphs under `views`, one per position of the page's two display filters (`DEP_FILTERS` = completed + retired, keyed by `dep_view_key` → `c1r1`/`c1r0`/`c0r1`/`c0r0`), with the payload's top level still being the unfiltered `views[DEP_DEFAULT_VIEW]` so every reader that predates the controls — dash-14's tests included — reads the same graph as before. Measured 2026-08-20: 22 of the 71 drawn nodes are completed and 6 retired, i.e. 39% of the drawing could never hold anything up again. |
| 2026-08-20 | dash-16 | Each view is filtered BEFORE it is laid out (`_dep_view`): every edge incident to a hidden node goes with it, and SCCs, cycle paths, layers and rows are recomputed over what remains — so hiding leaves no empty column and no dependent stranded at a layer number derived from a node that is no longer drawn. A cycle whose members are partly hidden is still detected and reported over the survivors, and a cycle that existed only through a hidden node is gone with its edges rather than reported over a relation the drawing does not show. |
| 2026-08-20 | dash-16 | HIDDEN IS BOOKED APART FROM OMITTED, because they are different reasons a task is not on screen: `omitted` stays dash-14's count of tasks in no relation at all (the same number in all four views) and `hidden` is what the controls took out, so `shown + hidden + omitted == total` holds everywhere and dash-14's `omitted + shown == total` still holds exactly in the default view. `hidden_by_state` splits the count per control and `filterable` is what each control governs drawn-or-not, which is the figure its label carries. |
| 2026-08-20 | dash-16 | A live task whose only relations were to hidden nodes is KEPT and drawn alone, listed in `drawn_alone` and named on the page — dropping it would delete live work from the operator's view for the sole reason that what it was waiting for is finished, which is the one failure of this filter nobody would notice. The `unknown` phantom list is read off the drawn NODES rather than off the surviving edges for the same reason. |
| 2026-08-20 | dash-16 | The controls are two static-markup checkboxes, both `checked`: the panel opens on the whole graph, and `renderDeps` READS `.checked` and never writes it, which is the entire persistence mechanism (dash-12's rule — the element a poll must not rebuild). `depsBusy` deliberately excludes a focused filter box from the interaction guard, exactly as it excludes an open `<details>`: a clicked checkbox keeps focus, so counting it would freeze the graph from the first toggle until the operator clicked the background. A toggle redraws `DEPDRAWN` through the panel's own gate — never `renderDeps` directly, never `render`. |
| 2026-08-20 | roadmap-01 | `shipped-report` claims exactly one thing per COMPLETED task: a commit whose SUBJECT names the task id exists, and it is (or is not) an ancestor of the base head. It parses no source and judges no capability — the previous attempt at that question spent five review rounds closing one false-positive shape of Python after another, and ancestry is decidable where "does this implement that" is not. |
| 2026-08-20 | roadmap-01 | A subject mention is the thing to TEST, never the answer, and absence of one is `unknown` rather than "not shipped" — the work may have landed under a subject that never named the id, so reading absence as evidence is the fail-open direction this report exists to avoid. |
| 2026-08-20 | roadmap-01 | `unverified` is a third state beside `shipped`/`not-in-base`, covering both a search that failed and a match git could not resolve. It is never rounded down to a negative, and one indeterminate commit does not erase a definite ancestor found on another — a row with both is `shipped`, with both still listed. |
| 2026-08-20 | roadmap-01 | Every matching commit is retained: pkt-03 shipped as four "pkt-03, part N" commits, and one aggregate verdict over four lines of evidence is checkable where a single collapsed answer is not. |
| 2026-08-20 | roadmap-01 | Whole-token matching uses `tasks._ID_RE`'s own alphabet as the boundary, so `pkt-03` misses `pkt-030`, `x-pkt-03` and `pkt-03.5`. Documented cost: `.` is a legal id character, so a subject ending "…pkt-03." also misses — conservative in the safe direction, where widening the class would make a neighbouring id match. |
| 2026-08-20 | roadmap-01 | The search is `git log --all`, not a walk of the base head: a commit that is not an ancestor cannot be found by walking the head it is not an ancestor of. Blind spot stated rather than hidden — a commit on no ref at all is invisible and reads as `unknown`. |
| 2026-08-20 | roadmap-01 | The report never acts: no `tasks.json` write, no execution record, no merge, no ref, no priority or status, and nothing retired or reopened on the strength of it. It is also deliberately absent from `dashboard.collect()`'s payload, which polls every 2s while this walks every commit on every ref. |
| 2026-08-20 | prof-01 | Nothing retrofits a duration onto the 7,203 records written before this: `transcript.jsonl` is append-only history, so `profile` DEGRADES those to gap-derived and labels them, and a stage with neither source prints `n/a` with the reason rather than a blank or a zero. Read the docs as saying the field is new, never that old records gained one. |
| 2026-08-20 | prof-01 | `implement_executor.py` was in the approved file list and its SOURCE is deliberately unchanged: there is no in-scope channel for a sub-duration (`ExecutionOutcome` lives in `executor.py`, out of scope; `summary` becomes the commit message; `validation` is injected into every later packet), and the orchestrator's stopwatch around `self._executor.execute(...)` already measures the round's execution cost end to end. Only `tests/test_implement_executor.py` grew — two tests proving the real executor's outcome is identical whether its timing succeeds or its clock raises. |
| 2026-08-20 | prof-01 | The agent's own `AgentResult.duration_seconds` (measured since the audit executor existed, in `audit/agents.py`) is still discarded by `ImplementExecutor`, so `executed.duration_seconds` is agent + validation + git reads as one number and cannot be split. Splitting it needs an additive field on `ExecutionOutcome`; a follow-up, not a defect in this measurement. |
| 2026-08-20 | prof-01 | `submit`'s measured window is the send call alone, so the attach, the controlled reload, the duplicate check and — on a CHUNKED round — the whole `delivering` phase (N part sends, each a browser round trip) all sit inside the `request_prepared` → `request_submitted` gap and in no measured column. That is the one case where the gap holds real work rather than only faults. Said in the stage's own printed note and in AUTOLOOP.md §3d-bis rather than solved with a fifth stage, which would be absent from most transcripts. |
| 2026-08-21 | prof-01 | Every measured window is FROZEN at the operation's own last line (`watch.stop()`) and stamped onto the event later, because `stamp()` stops a running watch — so a window left open until the record was written measured the loop's bookkeeping between the two (verdict persistence, the request-id lookup, payload construction) and printed it as work. Review round 3 found all three sites doing exactly that. Frozen after `build_prompt`, on the transport's return, and the instant either executor arm returns. |
| 2026-08-21 | prof-01 | `measured n=0` no longer claims the transcript is legacy. "every record here predates measured durations" is now printed only when NO record in the file carries a usable duration (`TranscriptProfile.measured_anywhere`, computed over every record, not only the ones the stage table names); a stage with no samples in a transcript that has them elsewhere says only "this stage has no measured samples", which is the true statement about a round whose timing failed or a stage that has not run yet. |
| 2026-08-21 | prof-01 | The renderer's payload-freedom is now structural rather than asserted: `build_profile` reduces a `TranscriptRead` to a `TranscriptProfile` (counts, one flag, per-stage `Stats`) and `render_profile(path, profile)` receives only that. Before this it took the whole read, so S36's "the renderer receives only floats and static labels" described what the function USED, not what it HELD — the packet bodies were one attribute away from stdout. |
| 2026-08-21 | blk-01 | `cli._reconcile_unblocked_tasks` is `_reconcile_retired_blockers` run the other way: a `blocked` task that no OPEN blocker names is returned to the queue (`TaskRegistry.blocker_derived_blocked` + `BlockerStore.open_task_ids`), from `answer`, `archive-blocker`, `start`'s preflight and the top of every `run --continuous` iteration. It never writes a blocker record and never reaches an operator hold (`hold_origin`); `answer`'s unblock is now gated on the answered blocker being the task's LAST open one. |
| 2026-08-21 | blk-01 | `archive-blocker` now takes the `LoopLock` around the whole command (`_cmd_archive_blocker` + `_archive_blocker_locked`), because closing the last record naming a quarantined task must requeue it in the same operation and that writes `.autoloop/tasks.json` — which also sits inside the tree `escape_detector.enumerate_checkout_paths` snapshots (ignored paths included), so owning the lock is what proves no round is in flight. A live or stale lock refuses the ARCHIVAL too, rather than leaving a task blocked with no open blocker; the earlier archive-then-read-the-lock shape was both a half-transition and a check-then-act race. |
| 2026-08-21 | blk-01 | Both closing commands now FAIL CLOSED through `cli._requeue_after_close`: a handled task-load / reconciliation / task-save failure writes the record straight back open (`_reopen_blocker` — a plain `save` of the pre-close snapshot, never a `reopen()` verb, since nothing may un-answer an operator's answer), exits non-zero, and prints neither "resolved" nor "archived". The tolerant `_sweep_unblocked_tasks` it replaces caught the same faults and left the close standing with a note that the loop's next start would repair it, which is the invariant restated as a promise about another process. |
| 2026-08-21 | blk-01 | `answer` performs NO targeted `registry.unblock` of its own any more — the release is `_reconcile_unblocked_tasks`', so one command makes one `tasks.json` write under one rule for who may be released (the targeted call accepted any `blocked` task, operator holds included). Its "ready again" / "stays blocked" / `task_not_blocked` lines are derived from what the sweep did, and the last of those is asked through the new non-mutating `TaskRegistry.unblock_obstacle`, since asking by calling `unblock` would answer it by releasing the hold. |
| 2026-08-21 | blk-01 | Scope of that guarantee, recorded rather than implied: SYNCHRONOUS command failures only — a process killed between the two writes still leaves the split state, and the startup sweeps (`start`, `_run_locked`, `_run_continuous`) stay deliberately tolerant because they have no blocker of their own to put back. `_reconcile_unblocked_tasks` cannot raise once its `TaskStore.save` has returned (the transcript append after it degrades to a warning), which is what makes reopening provably safe. |
| 2026-08-21 | blk-01 | `_requeue_after_close` returns a THREE-valued outcome rather than a bool, because the two failures want opposite sentences: `_REQUEUE_REOPENED` is "nothing changed, run it again", while `_REQUEUE_CLOSE_STANDS` (the restore write itself failed) is "the record remains CLOSED on disk and nothing was requeued". Reporting the second as "was NOT resolved" would be the false line `_cmd_answer`'s own "the message has to be true" rule exists to prevent — and an operator who believed it would answer a blocker `resolve` then refuses a second time. |
