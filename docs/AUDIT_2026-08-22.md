# Repository audit — 2026-08-22

Generated by the autoloop audit executor (deterministic rendering of reconciled subagent findings; raw agent reports preserved at `.autoloop/audit/20260822T073514Z-716556`).

- branch: `autoloop/audit-0001` | HEAD: `b28dc15bad58` | uncommitted files: 0
- requested scope: Scope only brief-01: locate the canonical change-note line-length limit, the test that reads it, the executor brief-generation path, and the exact files needed for a drift-proof implementation and validation.

## Domain coverage — 6/6 domains reported usable output

| domain | usable output | findings |
|---|---|---|
| `docs_drift` | yes | 1 |
| `tests_ci` | yes | 2 |
| `repo_structure` | yes | 1 |
| `security_paths` | yes | 1 |
| `db_migrations` | yes | 4 |
| `ingestion_pipeline` | yes | 3 |

## Validation baseline
- `ruff check .` — **PASS**
  - tail: `All checks passed!`
- `python3 -m pytest autoloop/tests -q -p no:cacheprovider` — **FAIL (rc=1)**
  - tail: `[31m[31m[1m1 failed[0m, [32m2458 passed[0m, [33m1 skipped[0m, [33m1 deselected[0m[31m in 518.08s (0:08:38)[0m[0m`
- `python3 -m pytest tests/ -q -p no:cacheprovider` — **PASS**
  - tail: `[33m[32m368 passed[0m, [33m[1m5 warnings[0m[33m in 11.19s[0m[0m`

## Confirmed defects (2)
#### ingestion_pipeline:ing-02 — Align test_document_package.py's imports with the `from backend.services...` convention used by ever
- severity **medium**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/tests/test_document_package.py`, `docs/COMMON_ERRORS.md`
- symbols: `test_document_package.py:13`
- evidence: test_document_package.py:13 `from services import book_import_service` is the only backend test file not using `from backend.services import ...` (contrast test_progression.py:23); COMMON_ERRORS.md:496-513 documents this open trap: `pytest` (console script) silently collects 1151 instead of 1258 tests, dropping all 107 A2 tests, while `python3 -m pytest` collects them because cwd lands on sys.path. No conftest.py sys.path fix exists (checked lexy-app/backend/tests/conftest.py).
- impact: Any invocation of the backend suite that isn't exactly `python3 -m pytest` (a CI runner, an IDE test button, a future contributor typing `pytest`) silently drops A2's 107-test file with only a swallow-able collection error, not a visible failure.
- acceptance: test_document_package.py collects successfully under both `pytest` and `python3 -m pytest` from lexy-app/backend; docs/COMMON_ERRORS.md entry marked resolved with date
- validation: cd lexy-app/backend && pytest tests/test_document_package.py -q; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -q

#### ingestion_pipeline:ing-01 — Guard validate_profile with isinstance(profile, str) before the ACCEPTED_PROFILES membership test, e
- severity **medium**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/services/document_package/loader.py`
- symbols: `LoadedPackage.profile:73-76`, `validate_profile:512-535`
- evidence: loader.py:75-76 returns the raw manifest value with no type check; loader.py:528 tests frozenset membership with no isinstance guard, unlike validators.py:91 which short-circuits on non-str before the membership test. tests/test_document_package.py covers only a valid string profile and a missing profile — no non-string case.
- impact: A manifest with a non-string extractor.profile (e.g. a JSON list, from an untrusted package per loader.py's own docstring) raises an unhandled TypeError in validate_package; nothing in book_import_service or main.py catches it, so POST /api/v1/books/import returns a raw 500 instead of the documented 200-with-rejected diagnostic.
- acceptance: A package whose manifest.extractor.profile is a list or dict is rejected via a ValidationIssue, not an exception; POST /api/v1/books/import against such a package returns 200 with status=rejected
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k profile


## Security / data-loss risks (0)
(none)

## Architectural risks (2)
#### db_migrations:db-02 — Either revert to adding the guard via a new migration/runner-level check instead of editing 025 in p
- severity **medium**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/025_block_token_ids.py`
- symbols: `025_block_token_ids.py:1-25`, `025_block_token_ids.py:downgrade:140-174`
- evidence: Docstring lines 20-24 self-documents 'the 2026-08-05 edit that added the downgrade guard touched only downgrade()' on a migration whose Create Date is 2026-03-29 — an in-place edit to an already-applied migration.
- impact: CLAUDE.md §12's 'Migrations are append-only — never edit an existing migration once it's been run anywhere' has no stated exception; this precedent risks silent divergence between environments that ran 025 before vs after the edit and weakens the guarantee the rule exists to give.
- acceptance: CLAUDE.md explicitly states whether downgrade()-only edits to applied migrations are permitted; no future migration's already-applied logic is edited in place without following that stated rule
- validation: git log --follow -p -- lexy-app/backend/migrations/versions/025_block_token_ids.py

#### repo_structure:arch-01 — Export the cap from autoloop/note_merge.py as a public constant that test_docs_merge.py imports inst
- severity **low**, confidence **confirmed**, domain `repo_structure`
- files: `autoloop/tests/test_docs_merge.py`, `autoloop/note_merge.py`, `CLAUDE.md`, `autoloop/validation.py`
- symbols: `test_docs_merge.py:73 MAX_NOTE_LINE`, `test_docs_merge.py:885-895 test_every_change_note_line_is_short_enough_to_merge_by_line`, `note_merge.py:116-141 _added_lines (deliberately no cap)`, `validation.py:1055-1183 select_validation_commands`
- evidence: MAX_NOTE_LINE=700 is defined and read only at test_docs_merge.py:73/885-895 (repo-wide grep for `700` finds no other definition). note_merge.py:125-128 references that test by dotted-name string, not an import. CLAUDE.md §12 states 'ONE NEW LINE' with no numeric bound. implement_executor.py:320-350's `_agent_prompt` (the task brief a coding agent receives) never restates tracker rules — it only points at 'CLAUDE.md' by name — so CLAUDE.md is the sole place a task-writing agent could see a number, and it has none.
- impact: The one number that decides whether a change note is compliant lives in a single test-local constant with no importable link to note_merge.py's docstring or to CLAUDE.md's instruction text; a future rename of the test or change to the cap has nothing forcing the three to stay consistent, and the agent writing notes is never shown a concrete target length.
- acceptance: autoloop/note_merge.py exposes the numeric change-note line cap as a named module constant; autoloop/tests/test_docs_merge.py imports that constant rather than defining its own MAX_NOTE_LINE; CLAUDE.md §12 states the same concrete number; test_docs_merge.py's existing assertions and behavior are unchanged
- validation: python3 -m pytest autoloop/tests/test_docs_merge.py -q; grep -rn 'MAX_NOTE_LINE\|NOTE_LINE' autoloop/note_merge.py autoloop/tests/test_docs_merge.py CLAUDE.md


## Documentation drift (3)
#### docs_drift:doc_drift_01 — Add to CLAUDE.md §12 after 'ONE NEW LINE': 'Each line must not exceed 700 characters — this is a gua
- severity **medium**, confidence **confirmed**, domain `docs_drift`
- files: `CLAUDE.md`, `docs/audit_charters.toml`, `autoloop/audit/executor.py`
- symbols: `CLAUDE.md:314`, `test_docs_merge.py:73`, `test_docs_merge.py:885-895`
- evidence: autoloop/tests/test_docs_merge.py:73 defines MAX_NOTE_LINE=700; test_docs_merge.py:885-895 enforces this on every note in SUMMARY.md and TESTS.md. But CLAUDE.md:314 (the 'Record a change note as ONE NEW LINE' section) documents the shape rules without mentioning the 700-char limit. A task reading CLAUDE.md won't know notes must stay ≤700 chars.
- impact: Agents and users creating change notes per CLAUDE.md guidance may exceed 700 characters, violating the documented test constraint that exists to prevent documentation bloat.
- acceptance: CLAUDE.md mentions 700-character limit for change notes; test_docs_merge.py passes without modification
- validation: grep -n '700' CLAUDE.md; python3 -m pytest autoloop/tests/test_docs_merge.py::test_every_change_note_line_is_short_enough_to_merge_by_line -xvs

#### tests_ci:brief-01 — Extract MAX_NOTE_LINE = 700 from test_docs_merge.py into a shared constant in autoloop/note_merge.py
- severity **medium**, confidence **confirmed**, domain `tests_ci`
- files: `autoloop/tests/test_docs_merge.py`, `autoloop/note_merge.py`, `autoloop/audit/executor.py`, `CLAUDE.md`
- symbols: `test_docs_merge.py:73`, `test_docs_merge.py:885-895`, `note_merge.py:124-128`, `executor.py:497-568`, `CLAUDE.md:314-323`
- evidence: test_docs_merge.py:73 defines MAX_NOTE_LINE = 700 as the canonical limit for change-note lines in docs/SUMMARY.md and docs/TESTS.md. test_docs_merge.py:885-895 validates this limit, but only in the test suite. The resolver (note_merge.py:124-128) explicitly does NOT enforce the limit ('Deliberately NO length cap'), leaving the validation purely in test coverage. Meanwhile, executor.py:497-568's _agent_prompt briefs audit agents on change-note rules but cannot reference the limit because it is not in a shared, readable location—only in test constants.
- impact: The 700-char limit is inaccessible to code that needs to document or validate it: executor briefs cannot name it, a new agent writing CLAUDE.md changes cannot reference it, and a future maintainer wanting to adjust the limit must search for it in a test file, not in production code or documentation.
- acceptance: autoloop/note_merge.py exports MAX_NOTE_LINE constant; autoloop/tests/test_docs_merge.py imports and uses it; CLAUDE.md §12 mentions the 700-char limit; executor.py can reference the limit when generating briefs (optional use, but it must be accessible)
- validation: grep -n 'MAX_NOTE_LINE' autoloop/note_merge.py autoloop/tests/test_docs_merge.py; grep -c '700' CLAUDE.md

#### ingestion_pipeline:ing-03 — Update the import_result.json example in docs/INGESTION_PIPELINE.md §4 to match ImportResult.as_dict
- severity **low**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `docs/INGESTION_PIPELINE.md`, `lexy-app/backend/services/document_package/result.py`
- symbols: `build_result:79-133`
- evidence: docs/INGESTION_PIPELINE.md:289 example `counts` has `pages` and a single `blocks_written`; result.py:104-113 actually emits `elements_in`, `blocks_planned`, `elements_skipped`, `pages_written`, `blocks_written`, `sentences` — no `pages` total-page-count field, and `blocks_planned` coexists with `blocks_written` rather than one field.
- impact: Roadmap task C1 (versioned JSON Schemas for import_result.json) or any consumer built off the §4 example would code against a `pages` field that doesn't exist in the real payload.
- acceptance: §4's import_result.json example field names match ImportResult.as_dict() output exactly
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k result


## Missing regression protection (1)
#### tests_ci:brief-02 — Add an assertion to test_claude_md_tells_a_task_to_append_one_line (or a new test) that verifies CLA
- severity **low**, confidence **confirmed**, domain `tests_ci`
- files: `autoloop/tests/test_docs_merge.py`, `CLAUDE.md`
- symbols: `test_docs_merge.py:898-911`, `CLAUDE.md:320`
- evidence: test_docs_merge.py:898-911 (test_claude_md_tells_a_task_to_append_one_line) validates that CLAUDE.md mentions 'ONE NEW LINE', 'merge=union', and the test file itself, but it does NOT check that CLAUDE.md documents the 700-character limit or cites any line-length bound. The test pins the instruction shape but not the limit.
- impact: An agent reading CLAUDE.md to learn the change-note rules finds the shape constraint ('ONE NEW LINE') but no quantitative bound. A 700-char line is valid JSON and valid Markdown, so the only enforcement is the test suite, not agent guidance or live documentation.
- acceptance: CLAUDE.md §12 includes '700' and 'change note' in the same sentence or nearby sentences; test_docs_merge.py::test_claude_md_tells_a_task_to_append_one_line passes with an assertion on that text
- validation: python3 -m pytest autoloop/tests/test_docs_merge.py::test_claude_md_tells_a_task_to_append_one_line -v


## Optional improvements (3)
#### security_paths:sec-01 — Move MAX_NOTE_LINE into an importable constant in autoloop/note_merge.py (the module that already do
- severity **low**, confidence **confirmed**, domain `security_paths`
- files: `autoloop/tests/test_docs_merge.py`, `autoloop/note_merge.py`, `CLAUDE.md`
- symbols: `test_docs_merge.py:73 (MAX_NOTE_LINE)`, `test_docs_merge.py:885-895 (test_every_change_note_line_is_short_enough_to_merge_by_line)`, `note_merge.py:116-141 (_added_lines)`
- evidence: test_docs_merge.py:73 defines the sole literal MAX_NOTE_LINE = 700, consumed only at :892. note_merge.py:125-129 explicitly states the resolver enforces NO length cap and names this test as the only enforcement point. CLAUDE.md §12 (the rule docs/SECURITY.md S35 tracks) tells a task to record 'ONE NEW LINE' but states no numeric ceiling anywhere.
- impact: An authoring agent can write a technically-compliant single change-note line over 700 chars to docs/SUMMARY.md or docs/TESTS.md with no way to know the ceiling in advance; it only surfaces as a failing autoloop/tests/test_docs_merge.py post-commit, costing a review/revise round for a rule that was knowable up front.
- acceptance: autoloop/tests/test_docs_merge.py imports the limit from autoloop/note_merge.py rather than redefining it locally; CLAUDE.md §12 states the exact numeric character ceiling for a change-note line; test_docs_merge.py::test_every_change_note_line_is_short_enough_to_merge_by_line still passes unchanged in behavior
- validation: python3 -m pytest autoloop/tests/test_docs_merge.py -q; rg -n 'MAX_NOTE_LINE' autoloop CLAUDE.md

#### db_migrations:db-03 — Author a new append-only migration dropping ix_word_table_lang_lower_word once EXPLAIN confirms _sug
- severity **low**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/032_word_frequency.py`, `lexy-app/backend/migrations/versions/036_add_word_norm_for_unicode_autocomplete.py`, `lexy-app/backend/services/search_service.py`
- symbols: `032_word_frequency.py:upgrade:60-65`, `036_add_word_norm_for_unicode_autocomplete.py:upgrade:84-89`, `search_service.py:_suggest_words:431-468`
- evidence: search_service.py:458-465 queries only word_norm (migration 036's column/index); grep for 'lower(word)' across lexy-app/backend/services returns zero hits, so ix_word_table_lang_lower_word (032:62) is never read by any production query anymore.
- impact: Every word_table write (scraper bulk ingest, word_seed_service, word_service) maintains an index no query reads, costing write amplification and disk with no read benefit.
- acceptance: New migration drops ix_word_table_lang_lower_word; EXPLAIN on the _suggest_words query still shows an index scan via ix_word_table_lang_word_norm
- validation: rg -n "lower\(word\)" lexy-app/backend/services; psql "$DATABASE_URL" -c "EXPLAIN SELECT word, frequency FROM word_table WHERE language='de' AND word_norm LIKE 'ab%'"

#### db_migrations:db-04 — Add a new append-only migration creating idx_content_request_user_created ON content_request (user_i
- severity **low**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/services/content_request_service.py`, `lexy-app/backend/migrations/versions/018_content_request.py`, `lexy-app/backend/migrations/versions/029_content_request_per_user_unique.py`
- symbols: `content_request_service.py:list_for_user:59-69`, `018_content_request.py:upgrade:30`, `029_content_request_per_user_unique.py:upgrade:60-68`
- evidence: list_for_user (content_request_service.py:61-66) runs WHERE user_id=$1 ORDER BY created_at DESC; 018:30 indexes only (status), and 029's UNIQUE(user_id, request_type, content_id) has no created_at column, so the ORDER BY needs an explicit sort.
- impact: As a user's content-request history grows, listing it degrades from an index-only scan to an index scan plus sort.
- acceptance: EXPLAIN for the list_for_user query shows an index scan satisfying both the filter and the sort
- validation: psql "$DATABASE_URL" -c "EXPLAIN SELECT * FROM content_request WHERE user_id='00000000-0000-0000-0000-000000000000' ORDER BY created_at DESC"


## Human decisions needed (1)
#### db_migrations:db-01 — Obtain an authoritative pg_dump --schema-only of video_category/user_video_category (and confirm lan
- severity **high**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/006_video_channel_genre.py`, `lexy-app/backend/migrations/versions/013_video_category_cleanup.py`, `lexy-app/backend/migrations/versions/014_drop_video_genre.py`
- symbols: `013_video_category_cleanup.py:upgrade:39-76`, `014_drop_video_genre.py:upgrade:21-40`
- evidence: Grep for 'CREATE TABLE' across all 37 files in migrations/versions/ has no hit for video_category or user_video_category, yet 013:39-76 ALTERs both and adds FKs to them, and 014:21-40 ALTERs user_video_category and references video.category — both assumed pre-existing.
- impact: CLAUDE.md §11's documented dev quickstart (`alembic upgrade head` on a fresh DB) fails once the chain reaches migration 006/013, blocking any new environment or CI runner that lacks these pre-Alembic tables.
- acceptance: createdb fresh_test && alembic upgrade head succeeds against an empty database; psql fresh_test -c '\dt' lists video_category and user_video_category
- validation: rg -n 'CREATE TABLE' lexy-app/backend/migrations/versions | rg -i video_category; createdb lexy_fresh_test && cd lexy-app/backend && DB_NAME=lexy_fresh_test alembic upgrade head


## Rejected items (0)
(none)

## Proposed task graph (11 tasks)
To adopt this graph, reply with a `plan` decision containing the tasks below (edit freely — this is a proposal):
```json
[
  {
    "id": "au-001",
    "title": "Guard validate_profile with isinstance(profile, str) before the ACCEPTED_PROFILES membership test...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding ingestion_pipeline:ing-01\nScope: Guard validate_profile with isinstance(profile, str) before the ACCEPTED_PROFILES membership test, emitting a fatal ValidationIssue (unsupported_profile) for non-string values.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A manifest with a non-string extractor.profile (e.g. a JSON list, from an untrusted package per loader.py's own docstring) raises an unhandled TypeError in validate_package; nothing in book_import_service or main.py catches it, so POST /api/v1/books/import returns a raw 500 instead of the documented 200-with-rejected diagnostic.\nEvidence: loader.py:75-76 returns the raw manifest value with no type check; loader.py:528 tests frozenset membership with no isinstance guard, unlike validators.py:91 which short-circuits on non-str before the membership test. tests/test_document_package.py covers only a valid string profile and a missing profile \u2014 no non-string case.\nExpected files: lexy-app/backend/services/document_package/loader.py\nAcceptance criteria: A package whose manifest.extractor.profile is a list or dict is rejected via a ValidationIssue, not an exception; POST /api/v1/books/import against such a package returns 200 with status=rejected\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k profile\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-002",
    "title": "Align test_document_package.py's imports with the `from backend.services...` convention used by e...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding ingestion_pipeline:ing-02\nScope: Align test_document_package.py's imports with the `from backend.services...` convention used by every other backend test file, removing the cwd-on-sys.path dependency.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any invocation of the backend suite that isn't exactly `python3 -m pytest` (a CI runner, an IDE test button, a future contributor typing `pytest`) silently drops A2's 107-test file with only a swallow-able collection error, not a visible failure.\nEvidence: test_document_package.py:13 `from services import book_import_service` is the only backend test file not using `from backend.services import ...` (contrast test_progression.py:23); COMMON_ERRORS.md:496-513 documents this open trap: `pytest` (console script) silently collects 1151 instead of 1258 tests, dropping all 107 A2 tests, while `python3 -m pytest` collects them because cwd lands on sys.path. No conftest.py sys.path fix exists (checked lexy-app/backend/tests/conftest.py).\nExpected files: lexy-app/backend/tests/test_document_package.py, docs/COMMON_ERRORS.md\nAcceptance criteria: test_document_package.py collects successfully under both `pytest` and `python3 -m pytest` from lexy-app/backend; docs/COMMON_ERRORS.md entry marked resolved with date\nValidation: cd lexy-app/backend && pytest tests/test_document_package.py -q; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-003",
    "title": "Either revert to adding the guard via a new migration/runner-level check instead of editing 025 i...",
    "description": "Priority: P4 | severity medium | confidence confirmed | from audit finding db_migrations:db-02\nScope: Either revert to adding the guard via a new migration/runner-level check instead of editing 025 in place, or explicitly ratify a narrow 'downgrade()-only' exception in CLAUDE.md \u00a712.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: CLAUDE.md \u00a712's 'Migrations are append-only \u2014 never edit an existing migration once it's been run anywhere' has no stated exception; this precedent risks silent divergence between environments that ran 025 before vs after the edit and weakens the guarantee the rule exists to give.\nEvidence: Docstring lines 20-24 self-documents 'the 2026-08-05 edit that added the downgrade guard touched only downgrade()' on a migration whose Create Date is 2026-03-29 \u2014 an in-place edit to an already-applied migration.\nExpected files: lexy-app/backend/migrations/versions/025_block_token_ids.py\nAcceptance criteria: CLAUDE.md explicitly states whether downgrade()-only edits to applied migrations are permitted; no future migration's already-applied logic is edited in place without following that stated rule\nValidation: git log --follow -p -- lexy-app/backend/migrations/versions/025_block_token_ids.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-004",
    "title": "Export the cap from autoloop/note_merge.py as a public constant that test_docs_merge.py imports i...",
    "description": "Priority: P4 | severity low | confidence confirmed | from audit finding repo_structure:arch-01\nScope: Export the cap from autoloop/note_merge.py as a public constant that test_docs_merge.py imports instead of redefining, and cite the same number in CLAUDE.md \u00a712 beside 'ONE NEW LINE'.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: The one number that decides whether a change note is compliant lives in a single test-local constant with no importable link to note_merge.py's docstring or to CLAUDE.md's instruction text; a future rename of the test or change to the cap has nothing forcing the three to stay consistent, and the agent writing notes is never shown a concrete target length.\nEvidence: MAX_NOTE_LINE=700 is defined and read only at test_docs_merge.py:73/885-895 (repo-wide grep for `700` finds no other definition). note_merge.py:125-128 references that test by dotted-name string, not an import. CLAUDE.md \u00a712 states 'ONE NEW LINE' with no numeric bound. implement_executor.py:320-350's `_agent_prompt` (the task brief a coding agent receives) never restates tracker rules \u2014 it only points at 'CLAUDE.md' by name \u2014 so CLAUDE.md is the sole place a task-writing agent could see a number, and it has none.\nExpected files: autoloop/tests/test_docs_merge.py, autoloop/note_merge.py, CLAUDE.md, autoloop/validation.py\nAcceptance criteria: autoloop/note_merge.py exposes the numeric change-note line cap as a named module constant; autoloop/tests/test_docs_merge.py imports that constant rather than defining its own MAX_NOTE_LINE; CLAUDE.md \u00a712 states the same concrete number; test_docs_merge.py's existing assertions and behavior are unchanged\nValidation: python3 -m pytest autoloop/tests/test_docs_merge.py -q; grep -rn 'MAX_NOTE_LINE\\|NOTE_LINE' autoloop/note_merge.py autoloop/tests/test_docs_merge.py CLAUDE.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-005",
    "title": "Add an assertion to test_claude_md_tells_a_task_to_append_one_line (or a new test) that verifies ...",
    "description": "Priority: P5 | severity low | confidence confirmed | from audit finding tests_ci:brief-02\nScope: Add an assertion to test_claude_md_tells_a_task_to_append_one_line (or a new test) that verifies CLAUDE.md \u00a712 names the 700-character limit for change-note lines, ensuring documentation stays in sync with validation.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: An agent reading CLAUDE.md to learn the change-note rules finds the shape constraint ('ONE NEW LINE') but no quantitative bound. A 700-char line is valid JSON and valid Markdown, so the only enforcement is the test suite, not agent guidance or live documentation.\nEvidence: test_docs_merge.py:898-911 (test_claude_md_tells_a_task_to_append_one_line) validates that CLAUDE.md mentions 'ONE NEW LINE', 'merge=union', and the test file itself, but it does NOT check that CLAUDE.md documents the 700-character limit or cites any line-length bound. The test pins the instruction shape but not the limit.\nExpected files: autoloop/tests/test_docs_merge.py, CLAUDE.md\nAcceptance criteria: CLAUDE.md \u00a712 includes '700' and 'change note' in the same sentence or nearby sentences; test_docs_merge.py::test_claude_md_tells_a_task_to_append_one_line passes with an assertion on that text\nValidation: python3 -m pytest autoloop/tests/test_docs_merge.py::test_claude_md_tells_a_task_to_append_one_line -v\nParallelizable: yes",
    "depends_on": [
      "au-010"
    ]
  },
  {
    "id": "au-006",
    "title": "Author a new append-only migration dropping ix_word_table_lang_lower_word once EXPLAIN confirms _...",
    "description": "Priority: P6 | severity low | confidence confirmed | from audit finding db_migrations:db-03\nScope: Author a new append-only migration dropping ix_word_table_lang_lower_word once EXPLAIN confirms _suggest_words uses only ix_word_table_lang_word_norm.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Every word_table write (scraper bulk ingest, word_seed_service, word_service) maintains an index no query reads, costing write amplification and disk with no read benefit.\nEvidence: search_service.py:458-465 queries only word_norm (migration 036's column/index); grep for 'lower(word)' across lexy-app/backend/services returns zero hits, so ix_word_table_lang_lower_word (032:62) is never read by any production query anymore.\nExpected files: lexy-app/backend/migrations/versions/032_word_frequency.py, lexy-app/backend/migrations/versions/036_add_word_norm_for_unicode_autocomplete.py, lexy-app/backend/services/search_service.py\nAcceptance criteria: New migration drops ix_word_table_lang_lower_word; EXPLAIN on the _suggest_words query still shows an index scan via ix_word_table_lang_word_norm\nValidation: rg -n \"lower\\(word\\)\" lexy-app/backend/services; psql \"$DATABASE_URL\" -c \"EXPLAIN SELECT word, frequency FROM word_table WHERE language='de' AND word_norm LIKE 'ab%'\"\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-007",
    "title": "Add a new append-only migration creating idx_content_request_user_created ON content_request (use...",
    "description": "Priority: P6 | severity low | confidence confirmed | from audit finding db_migrations:db-04\nScope: Add a new append-only migration creating idx_content_request_user_created ON content_request (user_id, created_at DESC).\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: As a user's content-request history grows, listing it degrades from an index-only scan to an index scan plus sort.\nEvidence: list_for_user (content_request_service.py:61-66) runs WHERE user_id=$1 ORDER BY created_at DESC; 018:30 indexes only (status), and 029's UNIQUE(user_id, request_type, content_id) has no created_at column, so the ORDER BY needs an explicit sort.\nExpected files: lexy-app/backend/services/content_request_service.py, lexy-app/backend/migrations/versions/018_content_request.py, lexy-app/backend/migrations/versions/029_content_request_per_user_unique.py\nAcceptance criteria: EXPLAIN for the list_for_user query shows an index scan satisfying both the filter and the sort\nValidation: psql \"$DATABASE_URL\" -c \"EXPLAIN SELECT * FROM content_request WHERE user_id='00000000-0000-0000-0000-000000000000' ORDER BY created_at DESC\"\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-008",
    "title": "Move MAX_NOTE_LINE into an importable constant in autoloop/note_merge.py (the module that already...",
    "description": "Priority: P6 | severity low | confidence confirmed | from audit finding security_paths:sec-01\nScope: Move MAX_NOTE_LINE into an importable constant in autoloop/note_merge.py (the module that already documents the rule) and have the test import it instead of redefining it; state the exact character ceiling in CLAUDE.md \u00a712 next to 'ONE NEW LINE' so an authoring agent can self-check before writing.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: An authoring agent can write a technically-compliant single change-note line over 700 chars to docs/SUMMARY.md or docs/TESTS.md with no way to know the ceiling in advance; it only surfaces as a failing autoloop/tests/test_docs_merge.py post-commit, costing a review/revise round for a rule that was knowable up front.\nEvidence: test_docs_merge.py:73 defines the sole literal MAX_NOTE_LINE = 700, consumed only at :892. note_merge.py:125-129 explicitly states the resolver enforces NO length cap and names this test as the only enforcement point. CLAUDE.md \u00a712 (the rule docs/SECURITY.md S35 tracks) tells a task to record 'ONE NEW LINE' but states no numeric ceiling anywhere.\nExpected files: autoloop/tests/test_docs_merge.py, autoloop/note_merge.py, CLAUDE.md\nAcceptance criteria: autoloop/tests/test_docs_merge.py imports the limit from autoloop/note_merge.py rather than redefining it locally; CLAUDE.md \u00a712 states the exact numeric character ceiling for a change-note line; test_docs_merge.py::test_every_change_note_line_is_short_enough_to_merge_by_line still passes unchanged in behavior\nValidation: python3 -m pytest autoloop/tests/test_docs_merge.py -q; rg -n 'MAX_NOTE_LINE' autoloop CLAUDE.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-009",
    "title": "Add to CLAUDE.md \u00a712 after 'ONE NEW LINE': 'Each line must not exceed 700 characters \u2014 this is a ...",
    "description": "Priority: P7 | severity medium | confidence confirmed | from audit finding docs_drift:doc_drift_01\nScope: Add to CLAUDE.md \u00a712 after 'ONE NEW LINE': 'Each line must not exceed 700 characters \u2014 this is a guard against regression to the 19,410-char rows this system replaced.'\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Agents and users creating change notes per CLAUDE.md guidance may exceed 700 characters, violating the documented test constraint that exists to prevent documentation bloat.\nEvidence: autoloop/tests/test_docs_merge.py:73 defines MAX_NOTE_LINE=700; test_docs_merge.py:885-895 enforces this on every note in SUMMARY.md and TESTS.md. But CLAUDE.md:314 (the 'Record a change note as ONE NEW LINE' section) documents the shape rules without mentioning the 700-char limit. A task reading CLAUDE.md won't know notes must stay \u2264700 chars.\nExpected files: CLAUDE.md, docs/audit_charters.toml, autoloop/audit/executor.py\nAcceptance criteria: CLAUDE.md mentions 700-character limit for change notes; test_docs_merge.py passes without modification\nValidation: grep -n '700' CLAUDE.md; python3 -m pytest autoloop/tests/test_docs_merge.py::test_every_change_note_line_is_short_enough_to_merge_by_line -xvs\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-010",
    "title": "Extract MAX_NOTE_LINE = 700 from test_docs_merge.py into a shared constant in autoloop/note_merge...",
    "description": "Priority: P7 | severity medium | confidence confirmed | from audit finding tests_ci:brief-01\nScope: Extract MAX_NOTE_LINE = 700 from test_docs_merge.py into a shared constant in autoloop/note_merge.py so both the test and the executor can reference it. Update CLAUDE.md \u00a712 to cite the limit explicitly, and wire it into executor.py's agent prompts so briefs can document the canonical bound.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: The 700-char limit is inaccessible to code that needs to document or validate it: executor briefs cannot name it, a new agent writing CLAUDE.md changes cannot reference it, and a future maintainer wanting to adjust the limit must search for it in a test file, not in production code or documentation.\nEvidence: test_docs_merge.py:73 defines MAX_NOTE_LINE = 700 as the canonical limit for change-note lines in docs/SUMMARY.md and docs/TESTS.md. test_docs_merge.py:885-895 validates this limit, but only in the test suite. The resolver (note_merge.py:124-128) explicitly does NOT enforce the limit ('Deliberately NO length cap'), leaving the validation purely in test coverage. Meanwhile, executor.py:497-568's _agent_prompt briefs audit agents on change-note rules but cannot reference the limit because it is not in a shared, readable location\u2014only in test constants.\nExpected files: autoloop/tests/test_docs_merge.py, autoloop/note_merge.py, autoloop/audit/executor.py, CLAUDE.md\nAcceptance criteria: autoloop/note_merge.py exports MAX_NOTE_LINE constant; autoloop/tests/test_docs_merge.py imports and uses it; CLAUDE.md \u00a712 mentions the 700-char limit; executor.py can reference the limit when generating briefs (optional use, but it must be accessible)\nValidation: grep -n 'MAX_NOTE_LINE' autoloop/note_merge.py autoloop/tests/test_docs_merge.py; grep -c '700' CLAUDE.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-011",
    "title": "Update the import_result.json example in docs/INGESTION_PIPELINE.md \u00a74 to match ImportResult.as_d...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding ingestion_pipeline:ing-03\nScope: Update the import_result.json example in docs/INGESTION_PIPELINE.md \u00a74 to match ImportResult.as_dict()'s real field names, or rename the implementation to match the doc.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Roadmap task C1 (versioned JSON Schemas for import_result.json) or any consumer built off the \u00a74 example would code against a `pages` field that doesn't exist in the real payload.\nEvidence: docs/INGESTION_PIPELINE.md:289 example `counts` has `pages` and a single `blocks_written`; result.py:104-113 actually emits `elements_in`, `blocks_planned`, `elements_skipped`, `pages_written`, `blocks_written`, `sentences` \u2014 no `pages` total-page-count field, and `blocks_planned` coexists with `blocks_written` rather than one field.\nExpected files: docs/INGESTION_PIPELINE.md, lexy-app/backend/services/document_package/result.py\nAcceptance criteria: \u00a74's import_result.json example field names match ImportResult.as_dict() output exactly\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k result\nParallelizable: yes",
    "depends_on": []
  }
]
```

Not promoted to tasks:
- `db_migrations:db-01`: human decision — surfaced to the operator, not a task
