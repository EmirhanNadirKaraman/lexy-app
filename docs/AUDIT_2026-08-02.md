# Repository audit — 2026-08-02

Generated by the autoloop audit executor (deterministic rendering of reconciled subagent findings; raw agent reports preserved at `.autoloop/audit/20260802T214031Z-302eb1`).

- branch: `autoloop/audit-0001` | HEAD: `c121e264f5c6` | uncommitted files: 0
- requested scope: Architecture, tests and CI, security, database and migrations, documentation drift, and the ingestion pipeline; reconcile duplicate or conflicting findings and distinguish confirmed defects from risks and already-resolved issues.

## Domain coverage — 6/6 domains reported usable output

| domain | usable output | findings |
|---|---|---|
| `docs_drift` | yes | 1 |
| `tests_ci` | yes | 5 |
| `repo_structure` | yes | 2 |
| `security_paths` | yes | 4 |
| `db_migrations` | yes | 11 |
| `ingestion_pipeline` | yes | 5 |

## Validation baseline
- `ruff check .` — **PASS**
  - tail: `All checks passed!`
- `python3 -m pytest autoloop/tests -q` — **PASS**
  - tail: `[32m[32m[1m1048 passed[0m, [33m1 skipped[0m[32m in 233.55s (0:03:53)[0m[0m`
- `python3 -m pytest tests/ -q` — **PASS**
  - tail: `[33m[32m368 passed[0m, [33m[1m5 warnings[0m[33m in 10.35s[0m[0m`

## Confirmed defects (4)
#### tests_ci:ci-01 — Add GitHub Actions workflow to run: (1) `cd lexy-app/backend && python3 -m pytest -n auto` on Python
- severity **critical**, confidence **confirmed**, domain `tests_ci`
- files: `.github/workflows/dependency-audit.yml`
- evidence: .github/workflows/dependency-audit.yml is the ONLY workflow file. CLAUDE.md §11 'Lint' states 'Ruff is **part of backend validation, not an optional extra** — run it alongside `pytest` before calling any Python change done.' No workflow runs ruff, pytest, or vitest.
- impact: Code can be merged to main without passing linting or tests. CLAUDE.md mandates ruff/pytest checks that CI does not enforce, creating a gap between documented requirements and actual safeguards.
- acceptance: Workflow runs tests on all PRs to main; Ruff, backend pytest, and frontend tests all pass; At least one PR blocked by a test failure
- validation: ls -la .github/workflows/; grep -l 'pytest\|vitest\|ruff' .github/workflows/*.yml

#### repo_structure:arch-01 — Move the Docker build context to the repo root and COPY subtitle-scraper/ + data/ alongside backend/
- severity **critical**, confidence **confirmed**, domain `repo_structure`
- files: `lexy-app/Dockerfile`, `lexy-app/docker-compose.yml`, `lexy-app/backend/services/matcher_service.py`, `lexy-app/backend/main.py`, `lexy-app/backend/routers/phrases.py`, `lexy-app/backend/routers/matcher.py`
- symbols: `matcher_service.py:35-43`, `main.py:11-32`, `lexy-app/Dockerfile:21-31`
- evidence: docker-compose.yml:17 builds with context lexy-app/ (`build: .`); Dockerfile only COPYs backend/ and frontend dist, never subtitle-scraper/ or data/ (both live at repo root, outside the build context). matcher_service.py:35 computes `_SCRAPER_PATH = Path(__file__).resolve().parents[3] / "subtitle-scraper"`, line 43 does top-level `import phrase_finder as _pf`. routers/phrases.py:16 and routers/matcher.py:5 import matcher_service at module scope; main.py:16/22 import those routers at module scope before `app = FastAPI(...)`, with no try/except in the chain.
- impact: In an image built from lexy-app/Dockerfile, parents[3] resolves to the container filesystem root, so subtitle-scraper/ doesn't exist there; `import phrase_finder` raises ModuleNotFoundError at `uvicorn backend.main:app` import time and the whole backend fails to boot.
- acceptance: docker compose up from lexy-app/ boots the app and any /api/v1 route responds without a startup crash; A CI or test step builds the Docker image and hits a health endpoint
- validation: cd lexy-app && docker compose build && docker compose up -d && curl -f http://localhost:8000/api/v1/auth/login -X POST -d '{}' -H 'Content-Type: application/json'

#### db_migrations:ing-01 — Add a structural validator (e.g. validate_bbox_page_consistency) flagging bbox.page != element.page_
- severity **medium**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/services/document_package/validators.py`, `docs/INGESTION_PIPELINE.md`
- symbols: `validators.py:_BBOX_KEYS:21`, `validators.py:validate_bboxes:296-373`
- evidence: _BBOX_KEYS = ('x1','y1','x2','y2') (line 21) never includes 'page'; validate_bboxes (296-373) cross-checks bbox numerics against page_dims[str(element.page_index)] only, never bbox.get('page') vs element.page_index. Grep for bbox.get("page"/bbox["page"] across document_package/ returns no matches. docs/INGESTION_PIPELINE.md §4's own example shows page_index:11 against bbox.page:12 in the same element. Matches prior audit ing-01/sec-03/rt-03, still unresolved.
- impact: A worker bug or corrupted package with bbox.page != element.page_index passes every validation gate silently; inert today, but a live trap for roadmap A3 if a future persistence backend ever reads bbox.page instead of element.page_index.
- acceptance: A synthetic package with bbox.page != element.page_index fails validation with a new stable issue code; INGESTION_PIPELINE.md §4 example no longer shows disagreeing values
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k Structural

#### ingestion_pipeline:def-01 — Add a structural validator comparing bbox['page'] (when present) to element['page_index'] and raise 
- severity **medium**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/services/document_package/validators.py`, `lexy-app/backend/services/document_package/coordinates.py`, `docs/INGESTION_PIPELINE.md`
- symbols: `validators.py:validate_bboxes:296-373`, `coordinates.py:page_height_for:87-101`
- evidence: No file under services/document_package reads bbox['page'] (confirmed via grep); validate_bboxes and page_height_for key exclusively on element['page_index']. The contract's own worked example in docs/INGESTION_PIPELINE.md §4 (lines 238-254) shows page_index=11 but bbox.page=12 — an inconsistency nothing in the importer would catch.
- impact: A worker bug or transformation that leaves bbox.page disagreeing with element.page_index produces no fatal/warning issue; the importer silently converts the bbox using the wrong page's height/dims, misplacing the highlight for the reader once A3 lands — exactly the 'silent' failure §6 warns about.
- acceptance: A synthetic element with bbox.page != page_index produces a validation issue; docs/INGESTION_PIPELINE.md §4 example has bbox.page == page_index
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -q


## Security / data-loss risks (6)
#### db_migrations:sec-01 — Add Depends(require_admin) to both GET /books/packages and POST /books/import, matching the existing
- severity **high**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/routers/books.py`, `lexy-app/backend/services/book_import_service.py`, `docs/SECURITY.md`
- symbols: `routers/books.py:182-212 (list_packages, import_package)`
- evidence: routers/books.py:182-185 (GET /books/packages) and 188-212 (POST /books/import) depend only on Depends(get_current_user), no require_admin. book_import_service.list_packages() takes no user-scoping arg. docs/SECURITY.md:486's admin-gated-routes list omits both routes, confirming this is untracked, not an accepted risk. Matches prior audit sec-02/rt-01, still unresolved and still not logged in SECURITY.md despite CLAUDE.md §14 requiring it.
- impact: Any authenticated non-admin learner can enumerate every operator-curated package name and force checksum/structural validation of any of them on demand — information disclosure plus a compute-cost surface open to the whole user base.
- acceptance: Non-admin authenticated user gets 403 admin_required from both endpoints; Admin user's existing dry-run workflow unaffected; docs/SECURITY.md admin-gated routes list includes both routes
- validation: rg -n 'require_admin' lexy-app/backend/routers/books.py

#### security_paths:sec-01 — Apply require_admin (core/deps.py) to GET /books/packages and POST /books/import, add lexy-app/backe
- severity **medium**, confidence **confirmed**, domain `security_paths`
- files: `lexy-app/backend/routers/books.py`, `autoloop/seed_tasks.json`, `docs/SECURITY.md`
- symbols: `routers/books.py:182-212 list_packages, import_package`
- evidence: books.py:182-191 both routes depend only on get_current_user; seed_tasks.json rt-01 (priority 1) states the routes are 'auth-gated but not admin-gated'; no test_books_import_admin.py exists; SECURITY.md's admin-gated-routes list omits both.
- impact: Any logged-in (non-admin) user can list server-side document-package names and run full validation reports against them; no DB mutation is possible yet (blocked by PendingSchemaPersistence until roadmap A3), so today's impact is info disclosure, not data loss.
- acceptance: non-admin caller gets 403 admin_required on both routes; admin caller behavior unchanged; docs/SECURITY.md lists both routes under admin-gated routes
- validation: cd lexy-app/backend && python3 -m pytest tests/test_books_import_admin.py -q; ruff check .

#### db_migrations:db-02 — Either refuse downgrade() when any reading_selections row has anchors referencing existing tokens, o
- severity **medium**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/025_block_token_ids.py`, `docs/SCHEMA.md`
- symbols: `025_block_token_ids.py:_tokenize:21-27`, `025_block_token_ids.py:downgrade:64-65`
- evidence: _tokenize (21-27) calls uuid.uuid4() fresh for every token on every run; downgrade() (64-65) is a bare ALTER TABLE book_blocks DROP COLUMN tokens with no guard. docs/SCHEMA.md:145 documents reading_selections.anchors as [{block_id, token_id, surface}] referencing these ids, with no FK. Matches prior audit db-02/rt-02, still unresolved.
- impact: Running downgrade then upgrade on migration 025 regenerates all token_ids, silently orphaning every existing reading_selections.anchors reference with no error.
- acceptance: downgrade() either raises when orphaning anchors would occur, or the upgrade/downgrade round-trip preserves existing token_ids; A regression test exercises the round-trip against a reading_selections row
- validation: cat lexy-app/backend/migrations/versions/025_block_token_ids.py

#### ingestion_pipeline:sec-01 — Add Depends(require_admin) to POST /books/import and GET /books/packages, matching the S17 precedent
- severity **medium**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/routers/books.py`, `docs/SECURITY.md`
- symbols: `routers/books.py:import_package:188-212`, `routers/books.py:list_packages:182-185`
- evidence: routers/books.py:182-212 gates both endpoints with only Depends(get_current_user); no is_admin/require_admin check, unlike delete_book/delete_page (books.py:376-402) which pass is_admin=user.get('is_admin', False). tests/test_document_package.py:987-1034 (TestImportEndpoint) only asserts auth is required, never that a non-admin is refused. docs/SECURITY.md:484's A2 entry covers only path containment, not authorization scope.
- impact: Any authenticated non-admin user can enumerate all ingestion package names and trigger validation/hashing of packages that aren't theirs; once A3 wires real persistence this same gap lets any user consume another operator's worker output as their own book.
- acceptance: Non-admin authenticated user gets 403 admin_required from both endpoints; Admin user still succeeds; docs/SECURITY.md has a new dated entry
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k TestImportEndpoint -q; rg -n 'require_admin' lexy-app/backend/routers/books.py

#### db_migrations:db-04 — Add a one-line comment in downgrade() stating the restored language value is a placeholder default, 
- severity **low**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/013_video_category_cleanup.py`
- symbols: `013_video_category_cleanup.py:upgrade:39-43`, `013_video_category_cleanup.py:downgrade:82`
- evidence: upgrade() (39-43) drops video_category.language without saving prior values. downgrade() line 82 re-adds it via ALTER TABLE video_category ADD COLUMN IF NOT EXISTS language TEXT NOT NULL DEFAULT 'de' — every row gets the same hardcoded 'de' regardless of its actual original value, undocumented as a placeholder. Matches prior audit db-04/rt-08, still unresolved.
- impact: Downgrading migration 013 silently fabricates data for a column it appears to restore, unlike sibling migrations 022/035 which fail loudly or document the loss for the same class of change.
- acceptance: downgrade() carries a comment documenting the fabricated value
- validation: grep -n -B1 "DEFAULT 'de'" lexy-app/backend/migrations/versions/013_video_category_cleanup.py

#### security_paths:sec-02 — Also strip/escape backslashes in SAFE before the quote substitution, and log (not silently swallow) 
- severity **low**, confidence **probable**, domain `security_paths`
- files: `scripts/autoloop_health_notify.sh`
- symbols: `scripts/autoloop_health_notify.sh:63-65`
- evidence: Line 63's sed only replaces literal double-quotes with single-quotes, leaving backslashes untouched; SAFE is embedded in an AppleScript double-quoted string (line 64) where backslash-quote is an escape sequence; the script `exit 0`s unconditionally regardless of osascript's result.
- impact: A blocker/park detail or question string ending in an odd number of backslashes can break the AppleScript string boundary and make osascript fail to compile; since the script always exits 0, the 'needs_attention' desktop alert this tool exists to deliver is silently dropped instead of surfaced.
- acceptance: a detail string ending in a backslash still produces a valid, undistorted notification; a failed osascript call is logged as a failure rather than exiting 0
- validation: scripts/autoloop_health_notify.sh (craft a state.question ending in a backslash and inspect the log)


## Architectural risks (1)
#### security_paths:arch-01 — Add a CI workflow running `ruff check .`, backend `python3 -m pytest -n auto`, frontend `npm run tes
- severity **medium**, confidence **confirmed**, domain `security_paths`
- files: `.github/workflows/dependency-audit.yml`
- symbols: `.github/workflows/`
- evidence: Glob '.github/workflows/*.yml' returns only dependency-audit.yml (pip-audit + npm audit); no workflow runs pytest/ruff/vitest on push or PR, despite CLAUDE.md mandating ruff+pytest for every backend change and a 1258+/322+/207+/729-test suite across five test trees.
- impact: A PR that breaks backend, frontend, root-pipeline, or autoloop tests, or introduces a ruff violation, can merge to main with no automated CI gate catching it.
- acceptance: a workflow exists that fails on a broken test or ruff violation in a PR
- validation: gh workflow list; cat .github/workflows/*.yml


## Documentation drift (7)
#### tests_ci:doc-01 — Either: (A) add CI workflows per ci-01, or (B) document in CLAUDE.md that these checks are recommend
- severity **medium**, confidence **confirmed**, domain `tests_ci`
- files: `docs/TESTS.md`, `CLAUDE.md`
- symbols: `CLAUDE.md:208-217`, `CLAUDE.md:232-236`, `docs/TESTS.md:1-40`
- evidence: CLAUDE.md §11 documents three mandatory validation commands: `ruff check .`, `python3 -m pytest` (backend), `npm run test` (frontend). TESTS.md documents 1,137+ backend tests, 306+ frontend tests, and root-pipeline tests. But .github/workflows/ has zero workflows for any of these. Docs claim validation is required; CI does not require it.
- impact: Developers following CLAUDE.md will run the checks locally, but reviewers have no automated guarantee that a branch passed those checks before merge.
- acceptance: CI workflow exists and runs all checks, OR CLAUDE.md re-states validation is optional
- validation: grep -n 'part of backend validation' CLAUDE.md; ls -la .github/workflows/*.yml | wc -l

#### db_migrations:doc-01 — Rewrite line 128 to describe the actual path: status_override passed into apply_progression inside i
- severity **medium**, confidence **confirmed**, domain `db_migrations`
- files: `CLAUDE.md`
- symbols: `CLAUDE.md:128`, `CLAUDE.md:136`, `CLAUDE.md:162`
- evidence: CLAUDE.md:128 says status_marked_known's status flip is 'forced to known by word_service.upsert_word_status (called from the router before apply_progression)'. Lines 136 and 162 of the same file say that function 'has been removed'/'is gone'. grep 'def upsert_word_status' lexy-app/backend/services/*.py returns nothing. Matches prior audit doc_drift-01/rt-07, still unresolved.
- impact: A reader of §8b — the file's own recommended starting point for the progression state machine — is misled into believing a deleted function still gates status writes.
- acceptance: CLAUDE.md line 128 no longer mentions upsert_word_status; The description matches routers/words.py's actual call into apply_progression(..., status_override=...)
- validation: grep -n upsert_word_status CLAUDE.md

#### docs_drift:doc-drift-01 — Update line 97 in TESTS.md to state the true current frontend count and date (299 or 322 as of 2026-
- severity **low**, confidence **confirmed**, domain `docs_drift`
- files: `docs/TESTS.md`
- symbols: `line 97`, `line 676`, `line 688-691`
- evidence: docs/TESTS.md line 97 states 'Current count (2026-07-27): 261 tests across 43 files' for frontend, but line 676 reports 289 tests (278+11), contradicting the earlier baseline. Further, ROADMAP.md line 68 claims 322 frontend tests as of 2026-07-29, creating a 33-test gap from TESTS.md's reported 289 at the same date
- impact: Frontend test count is inconsistent across documentation sections. The 'Current count' in TESTS.md is stale and likely describes only the backend tallies (261 can be computed as some backend entries) while frontend continued growing. This mismatch obscures whether test coverage targets have been met
- acceptance: TESTS.md line 97 'Current count' reflects actual 2026-07-29 test totals or was struck as stale; No contradictory per-date test counts exist within TESTS.md itself
- validation: grep -n 'Current count' docs/TESTS.md; grep -n '2026-07-29.*passed' docs/TESTS.md docs/ROADMAP.md

#### tests_ci:doc-02 — Elevate the pytest-vs-python3-m-pytest trap to docs/COMMON_ERRORS.md as an entry (it's already docum
- severity **low**, confidence **confirmed**, domain `tests_ci`
- files: `CLAUDE.md`
- symbols: `CLAUDE.md:235-242`
- evidence: CLAUDE.md §11 states: 'Use `python3 -m pytest`, not the bare `pytest` entrypoint. `python -m` puts the cwd on `sys.path`, which `tests/test_document_package.py` (A2) needs for its bare `from services import …` imports; the `pytest` console script does not, and the file then fails collection (1151 instead of 1258…)'. This is buried in a long paragraph and easy to miss. It's not highlighted as a critical requirement.
- impact: A developer running `pytest` instead of `python3 -m pytest` will silently run only 1151 of 1258 backend tests and may think their change is safe when one test file was never run.
- acceptance: Entry added to COMMON_ERRORS.md; OR pytest hook/plugin installed that shows helpful error when bare pytest misses test_document_package.py
- validation: grep -i 'python3 -m pytest' CLAUDE.md; grep -i 'pytest.*not.*python' docs/COMMON_ERRORS.md

#### db_migrations:db-05 — Fix CLAUDE.md:38 to read 'lexy-app/backend/migrations/versions/' and update the file count to 37+.
- severity **low**, confidence **confirmed**, domain `db_migrations`
- files: `CLAUDE.md`, `lexy-app/backend/alembic.ini`
- symbols: `CLAUDE.md:38`, `alembic.ini:2`
- evidence: CLAUDE.md:38 says migrations live in '25+ files in alembic/versions/'. alembic.ini:2 sets script_location = migrations, and the actual 37 files are at lexy-app/backend/migrations/versions/ — no alembic/versions/ directory exists anywhere in the repo. Not present in the prior 2026-07-30 audit — newly found.
- impact: A contributor following CLAUDE.md's own stated path when looking for or authoring a migration searches a directory that doesn't exist.
- acceptance: CLAUDE.md's stated migrations path matches alembic.ini's script_location
- validation: grep -n 'alembic/versions' CLAUDE.md; grep script_location lexy-app/backend/alembic.ini

#### ingestion_pipeline:doc-01 — Update the '+89 backend' figure at docs/TESTS.md:956 to 107 (or explain the discrepancy) to match th
- severity **low**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `docs/TESTS.md`
- symbols: `docs/TESTS.md:956`, `docs/TESTS.md:2174-2176`
- evidence: Line 956 states A2 landed '+89 backend' tests for tests/test_document_package.py, but line 2174-2176 gives the same file 107 tests with arithmetic proof (1151+107=1258 matching the pinned backend baseline). The two figures for one file were never reconciled.
- impact: A reader trusting the '+89' figure under-estimates test_document_package.py's actual coverage by 18 tests; low-impact but avoidable confusion in a doc CLAUDE.md designates as the coverage index.
- acceptance: Both figures for tests/test_document_package.py agree in docs/TESTS.md
- validation: grep -n '89 backend\|107 tests' docs/TESTS.md

#### security_paths:doc-01 — Update the line references in SECURITY.md and CLAUDE.md §14 to 164,205,322, or drop specific line nu
- severity **info**, confidence **confirmed**, domain `security_paths`
- files: `docs/SECURITY.md`, `CLAUDE.md`, `lexy-app/backend/services/search_service.py`
- symbols: `search_service.py:164,205,322`
- evidence: SECURITY.md line 464 and CLAUDE.md §14's self-check grep both say 'Expected hits: search_service.py:62,103,216'; the three f-string SQL sites (still safe — only a loop index and the SIMILARITY_THRESHOLD constant are interpolated) are now at lines 164, 205, and 322.
- impact: Someone re-running the documented verification grep and eyeballing line numbers against the stale reference could be momentarily confused about whether the hits match the pre-verified-safe set; the grep pattern itself still finds them correctly.
- acceptance: SECURITY.md and CLAUDE.md no longer cite stale line numbers for this pattern
- validation: rg -n 'f"(SELECT|INSERT|UPDATE|DELETE)' lexy-app/backend --glob '!**/migrations/**'


## Missing regression protection (4)
#### repo_structure:arch-02 — Add CI job(s) that run `ruff check .`, `cd lexy-app/backend && python3 -m pytest -n auto`, `pytest t
- severity **high**, confidence **confirmed**, domain `repo_structure`
- files: `.github/workflows/dependency-audit.yml`
- symbols: `.github/workflows/dependency-audit.yml:1-53`
- evidence: The repo's only workflow (.github/workflows/dependency-audit.yml) runs pip-audit + npm audit only; no job runs pytest, ruff check ., npm run test, or npm run build. CLAUDE.md §11 mandates ruff + pytest (backend/root/frontend) as required validation, and no pre-commit hook or other workflow enforces it (confirmed: no .pre-commit-config.yaml, no other .github/workflows/*.yml).
- impact: A PR that fails ruff, breaks any of the ~1258 backend / ~322 frontend / ~370 root-pipeline tests, or breaks `npm run build` can merge to main with a fully green required-checks state, since CI only checks dependency CVEs.
- acceptance: A PR that fails a backend test or ruff shows a failing required CI check, not just dependency-audit
- validation: cat .github/workflows/*.yml

#### db_migrations:test-01 — Add .github/workflows/tests.yml running backend pytest -n auto, frontend npm run test, root pytest t
- severity **high**, confidence **confirmed**, domain `db_migrations`
- files: `.github/workflows/dependency-audit.yml`, `pytest.ini`, `CLAUDE.md`
- symbols: `pytest.ini:7`
- evidence: Glob of .github/workflows/*.yml returns only dependency-audit.yml (pip-audit/npm audit) — no workflow runs pytest or vitest. pytest.ini:7 sets testpaths = tests, excluding autoloop/tests, so bare `pytest` from repo root silently skips those too. Matches prior audit ci-01/test-01/rt-05/rt-10, still unresolved.
- impact: No CI signal exists for backend, frontend, root-pipeline, or autoloop test regressions before merge to main; breakage is only caught if someone manually runs the right command.
- acceptance: .github/workflows/tests.yml exists, committed, and runs on push/PR to main; All four suites (backend, frontend, root pipeline, autoloop) run and must pass for the workflow to succeed
- validation: test -f .github/workflows/tests.yml; grep -E 'pytest|npm run test' .github/workflows/tests.yml

#### ingestion_pipeline:test-01 — Add a CI workflow that runs `ruff check .` and `cd lexy-app/backend && python3 -m pytest -n auto` (p
- severity **medium**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `.github/workflows/dependency-audit.yml`
- evidence: Only .github/workflows/dependency-audit.yml exists (pip-audit + npm audit); no workflow runs `ruff check .` or the backend/root pytest suites, which CLAUDE.md §11 mandates for every Python change.
- impact: A regression in any of the five document-package import gates (schema, checksums, containment/zip-slip, structural, profile) would only be caught if a human or agent remembers to run pytest/ruff manually before merging.
- acceptance: A PR that breaks a document_package validator or checksum gate fails CI automatically
- validation: ls .github/workflows/

#### tests_ci:test-01 — Add lexy-app/backend/tests/test_alembic_migrations.py that (1) asserts the version files form one un
- severity **medium**, confidence **probable**, domain `tests_ci`
- files: `lexy-app/backend/alembic/versions/`
- symbols: `lexy-app/backend/alembic/versions/`
- evidence: CLAUDE.md §5 lists 37 migrations (head 037), but no migration-level test file exists: lexy-app/backend/tests/test_llm_cache_migration.py covers one cache migration only, and tests/test_chat_language.py touches migration 033 incidentally. No test_migrations.py or equivalent.
- impact: A migration that corrupts data or ships wrong ON DELETE behaviour can land untested; migrations are append-only and not easily rolled back, so the damage reaches every deployed user.
- acceptance: test_alembic_migrations.py exists and passes; At least one pre-existing migration is exercised by the test
- validation: ls lexy-app/backend/alembic/versions/ | wc -l; cd lexy-app/backend && python3 -m pytest tests/test_alembic_migrations.py -v


## Optional improvements (5)
#### db_migrations:db-03 — Add a new append-only migration (039, not an edit to 018) creating idx_content_request_user_created 
- severity **low**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/routers/content_requests.py`, `lexy-app/backend/migrations/versions/018_content_request.py`, `lexy-app/backend/migrations/versions/029_content_request_per_user_unique.py`
- symbols: `routers/content_requests.py:177-182 (list_requests)`, `018_content_request.py:30`, `029_content_request_per_user_unique.py:60-68`
- evidence: list_requests (content_requests.py:177-182) runs SELECT * FROM content_request WHERE user_id=$1 ORDER BY created_at DESC. 018:30 indexes only (status). 029:66-68 adds UNIQUE(user_id, request_type, content_id), whose column order supports the equality filter but not the ORDER BY created_at DESC sort. Matches prior audit db-03/rt-13, still unresolved.
- impact: The one user-facing query on this table has no index covering both its filter and its sort; degrades to a sort-after-scan as each user's request history grows.
- acceptance: A new migration file creates the composite index; EXPLAIN for the list_requests query shows an index scan rather than an unassisted sort
- validation: psql "$DB_NAME" -c "EXPLAIN SELECT * FROM content_request WHERE user_id='00000000-0000-0000-0000-000000000000' ORDER BY created_at DESC"

#### db_migrations:db-07 — In a new migration, DROP the redundant single-column index once EXPLAIN confirms the composite UNIQU
- severity **low**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/001_initial_user_tables.py`
- symbols: `001_initial_user_tables.py:43`, `001_initial_user_tables.py:109`
- evidence: Line 43's UNIQUE (user_id, item_id, item_type) constraint already creates a btree index with user_id as its leading column; line 109 creates a second, separate single-column index on user_id, which a query filtering on user_id alone could already satisfy via the composite index's prefix. Not present in the prior 2026-07-30 audit — newly found.
- impact: Every INSERT/UPDATE/DELETE on user_word_knowledge — one of the app's hottest per-event write paths — maintains one extra unnecessary index for the table's lifetime.
- acceptance: EXPLAIN for a WHERE user_id = $1 query on user_word_knowledge still uses an index scan after the redundant index is dropped
- validation: psql "$DB_NAME" -c "\di+ user_word_knowledge*"

#### ingestion_pipeline:imp-01 — Align test_document_package.py's imports to `from backend.services...`, closing the open follow-up n
- severity **low**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/tests/test_document_package.py`
- symbols: `test_document_package.py:13-43`
- evidence: tests/test_document_package.py is the only backend test file using bare `from services import ...` (confirmed: rg '^from services\.' matches only this file); every other test uses `from backend.services import ...`. Already documented as a known, mitigated trap in CLAUDE.md §11 and docs/COMMON_ERRORS.md §2, with alignment listed there as a still-open follow-up rather than done.
- impact: Running `pytest` (the bare console script) instead of `python3 -m pytest` silently drops this file's 107 tests from collection (1151 vs 1258) with no error surfaced beyond a collection error easy to miss in a large run.
- acceptance: File uses from backend.services... imports; `pytest tests/test_document_package.py` (bare console script) collects and passes from lexy-app/backend
- validation: cd lexy-app/backend && pytest tests/test_document_package.py -q

#### db_migrations:db-06 — For future schema changes on append-only/high-growth tables, add transaction_per_migration support f
- severity **medium**, confidence **probable**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/026_transcript_click_dedup.py`, `lexy-app/backend/migrations/env.py`
- symbols: `026_transcript_click_dedup.py:upgrade:34-43`, `env.py:45,58`
- evidence: 026:34-38 adds word_usage_events.event_day as GENERATED ALWAYS ... STORED, forcing a full-table rewrite under ACCESS EXCLUSIVE lock; 39-43 then CREATE UNIQUE INDEX (no CONCURRENTLY) on the same table. env.py:45/58 wraps every migration in context.begin_transaction(), and Postgres refuses CREATE INDEX CONCURRENTLY inside a transaction, so this setup structurally cannot do a non-blocking index build. word_usage_events is the fire-and-forget sink for every transcript click, chat turn, and SRS review (CLAUDE.md §8b) — this repo's most likely unbounded-growth table.
- impact: As word_usage_events grows, any future generated-column or index change on it blocks all reads/writes for the rewrite's duration, with no structural way in the current env.py setup to avoid it.
- acceptance: Migration guidance documents the ACCESS EXCLUSIVE-lock trade-off for GENERATED STORED columns and index builds on high-growth tables
- validation: grep -n 'begin_transaction' lexy-app/backend/migrations/env.py

#### tests_ci:ruff-01 — Add a comment block to ruff.toml stating that only E+F are selected and that I (import sort), E501, 
- severity **low**, confidence **probable**, domain `tests_ci`
- files: `ruff.toml`
- symbols: `ruff.toml:19`
- evidence: ruff.toml:19 selects ['E','F'] with a comment explaining only that full E is broader than ruff's default; nothing records why I (import sort), E501, formatting, and the async/style rule sets are off. CLAUDE.md §11 documents the E402/E501 traps but ruff.toml itself does not.
- impact: A future maintainer reads the narrow rule set as an oversight, enables I or E501, and triggers the repo-wide reflow / scraper-breaking import hoist that CLAUDE.md §11 warns against.
- acceptance: ruff.toml carries a comment explaining why only E+F are selected and why I/E501/format are deferred; ruff check . still reports 'All checks passed!'
- validation: head -30 ruff.toml; ruff check .


## Human decisions needed (1)
#### db_migrations:db-01 — Obtain an authoritative schema-only dump of the 10 missing tables from a real environment, then get 
- severity **critical**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/006_video_channel_genre.py`, `lexy-app/backend/migrations/versions/032_word_frequency.py`, `lexy-app/backend/migrations/versions/036_add_word_norm_for_unicode_autocomplete.py`, `docs/SCHEMA.md`
- symbols: `006_video_channel_genre.py:upgrade:16-18`, `032_word_frequency.py:upgrade:41-56`, `docs/SCHEMA.md:3`
- evidence: grep 'CREATE TABLE' across all 37 files in migrations/versions/ never creates video, video_category, user_video_category, language_table, word_table, phrase_blueprint, sentence, word_to_sentence, sentence_to_phrase, sentence_to_grammar_rule; 006:16 runs ALTER TABLE video ADD COLUMN against the never-created video table, first in the chain. docs/SCHEMA.md:3 still calls migrations/versions/ the schema's 'source of truth'. Matches prior audit hb-01/db-01 (2026-07-30), still unresolved.
- impact: alembic upgrade head fails on any empty/new/CI/DR Postgres database at revision 006 with relation "video" does not exist — roughly a third of the schema cannot be rebuilt from the repo.
- acceptance: A human has supplied a schema-only dump of the 10 tables and approved a baseline-migration exception; createdb test_db && alembic upgrade head against an empty Postgres DB completes with exit 0 and creates all 30+ tables; docs/SCHEMA.md no longer overclaims migrations/versions/ as sole authority for these tables
- validation: rg -i 'CREATE TABLE' lexy-app/backend/migrations/versions | wc -l; createdb lexy_schema_check && DB_NAME=lexy_schema_check alembic -c lexy-app/backend/alembic.ini upgrade head; echo $?


## Rejected items (2)
- [tests_ci] unsupported speculation — recorded, never promoted to a task — `tests_ci:test-02`
- [ingestion_pipeline] unsupported speculation — recorded, never promoted to a task — `ingestion_pipeline:def-02`

## Proposed task graph (27 tasks)
To adopt this graph, reply with a `plan` decision containing the tasks below (edit freely — this is a proposal):
```json
[
  {
    "id": "au-001",
    "title": "Move the Docker build context to the repo root and COPY subtitle-scraper/ + data/ alongside backe...",
    "description": "Priority: P1 | severity critical | confidence confirmed | from audit finding repo_structure:arch-01\nScope: Move the Docker build context to the repo root and COPY subtitle-scraper/ + data/ alongside backend/ (or make the phrase_finder import lazy/optional with a clear degraded-mode failure), and add a build-and-boot smoke test for the Docker image.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: In an image built from lexy-app/Dockerfile, parents[3] resolves to the container filesystem root, so subtitle-scraper/ doesn't exist there; `import phrase_finder` raises ModuleNotFoundError at `uvicorn backend.main:app` import time and the whole backend fails to boot.\nEvidence: docker-compose.yml:17 builds with context lexy-app/ (`build: .`); Dockerfile only COPYs backend/ and frontend dist, never subtitle-scraper/ or data/ (both live at repo root, outside the build context). matcher_service.py:35 computes `_SCRAPER_PATH = Path(__file__).resolve().parents[3] / \"subtitle-scraper\"`, line 43 does top-level `import phrase_finder as _pf`. routers/phrases.py:16 and routers/matcher.py:5 import matcher_service at module scope; main.py:16/22 import those routers at module scope before `app = FastAPI(...)`, with no try/except in the chain.\nExpected files: lexy-app/Dockerfile, lexy-app/docker-compose.yml, lexy-app/backend/services/matcher_service.py, lexy-app/backend/main.py, lexy-app/backend/routers/phrases.py, lexy-app/backend/routers/matcher.py\nAcceptance criteria: docker compose up from lexy-app/ boots the app and any /api/v1 route responds without a startup crash; A CI or test step builds the Docker image and hits a health endpoint\nValidation: cd lexy-app && docker compose build && docker compose up -d && curl -f http://localhost:8000/api/v1/auth/login -X POST -d '{}' -H 'Content-Type: application/json'\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-002",
    "title": "Add GitHub Actions workflow to run: (1) `cd lexy-app/backend && python3 -m pytest -n auto` on Pyt...",
    "description": "Priority: P1 | severity critical | confidence confirmed | from audit finding tests_ci:ci-01\nScope: Add GitHub Actions workflow to run: (1) `cd lexy-app/backend && python3 -m pytest -n auto` on Python changes; (2) `ruff check .` on Python changes; (3) `cd lexy-app/frontend && npm run test` on TS/React changes; (4) block merge if any fail.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Code can be merged to main without passing linting or tests. CLAUDE.md mandates ruff/pytest checks that CI does not enforce, creating a gap between documented requirements and actual safeguards.\nEvidence: .github/workflows/dependency-audit.yml is the ONLY workflow file. CLAUDE.md \u00a711 'Lint' states 'Ruff is **part of backend validation, not an optional extra** \u2014 run it alongside `pytest` before calling any Python change done.' No workflow runs ruff, pytest, or vitest.\nExpected files: .github/workflows/dependency-audit.yml\nAcceptance criteria: Workflow runs tests on all PRs to main; Ruff, backend pytest, and frontend tests all pass; At least one PR blocked by a test failure\nValidation: ls -la .github/workflows/; grep -l 'pytest\\|vitest\\|ruff' .github/workflows/*.yml\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-003",
    "title": "Either refuse downgrade() when any reading_selections row has anchors referencing existing tokens...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding db_migrations:db-02\nScope: Either refuse downgrade() when any reading_selections row has anchors referencing existing tokens, or persist/restore the original token_ids instead of regenerating them.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Running downgrade then upgrade on migration 025 regenerates all token_ids, silently orphaning every existing reading_selections.anchors reference with no error.\nEvidence: _tokenize (21-27) calls uuid.uuid4() fresh for every token on every run; downgrade() (64-65) is a bare ALTER TABLE book_blocks DROP COLUMN tokens with no guard. docs/SCHEMA.md:145 documents reading_selections.anchors as [{block_id, token_id, surface}] referencing these ids, with no FK. Matches prior audit db-02/rt-02, still unresolved.\nExpected files: lexy-app/backend/migrations/versions/025_block_token_ids.py, docs/SCHEMA.md\nAcceptance criteria: downgrade() either raises when orphaning anchors would occur, or the upgrade/downgrade round-trip preserves existing token_ids; A regression test exercises the round-trip against a reading_selections row\nValidation: cat lexy-app/backend/migrations/versions/025_block_token_ids.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-004",
    "title": "Add a structural validator (e.g. validate_bbox_page_consistency) flagging bbox.page != element.pa...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding db_migrations:ing-01\nScope: Add a structural validator (e.g. validate_bbox_page_consistency) flagging bbox.page != element.page_index with a stable issue code, and fix the disagreeing 11/12 example in INGESTION_PIPELINE.md \u00a74.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A worker bug or corrupted package with bbox.page != element.page_index passes every validation gate silently; inert today, but a live trap for roadmap A3 if a future persistence backend ever reads bbox.page instead of element.page_index.\nEvidence: _BBOX_KEYS = ('x1','y1','x2','y2') (line 21) never includes 'page'; validate_bboxes (296-373) cross-checks bbox numerics against page_dims[str(element.page_index)] only, never bbox.get('page') vs element.page_index. Grep for bbox.get(\"page\"/bbox[\"page\"] across document_package/ returns no matches. docs/INGESTION_PIPELINE.md \u00a74's own example shows page_index:11 against bbox.page:12 in the same element. Matches prior audit ing-01/sec-03/rt-03, still unresolved.\nExpected files: lexy-app/backend/services/document_package/validators.py, docs/INGESTION_PIPELINE.md\nAcceptance criteria: A synthetic package with bbox.page != element.page_index fails validation with a new stable issue code; INGESTION_PIPELINE.md \u00a74 example no longer shows disagreeing values\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k Structural\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-005",
    "title": "Add a structural validator comparing bbox['page'] (when present) to element['page_index'] and rai...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding ingestion_pipeline:def-01\nScope: Add a structural validator comparing bbox['page'] (when present) to element['page_index'] and raise an issue on mismatch; fix the doc's worked example to have matching values.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A worker bug or transformation that leaves bbox.page disagreeing with element.page_index produces no fatal/warning issue; the importer silently converts the bbox using the wrong page's height/dims, misplacing the highlight for the reader once A3 lands \u2014 exactly the 'silent' failure \u00a76 warns about.\nEvidence: No file under services/document_package reads bbox['page'] (confirmed via grep); validate_bboxes and page_height_for key exclusively on element['page_index']. The contract's own worked example in docs/INGESTION_PIPELINE.md \u00a74 (lines 238-254) shows page_index=11 but bbox.page=12 \u2014 an inconsistency nothing in the importer would catch.\nExpected files: lexy-app/backend/services/document_package/validators.py, lexy-app/backend/services/document_package/coordinates.py, docs/INGESTION_PIPELINE.md\nAcceptance criteria: A synthetic element with bbox.page != page_index produces a validation issue; docs/INGESTION_PIPELINE.md \u00a74 example has bbox.page == page_index\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-006",
    "title": "Add a one-line comment in downgrade() stating the restored language value is a placeholder defaul...",
    "description": "Priority: P1 | severity low | confidence confirmed | from audit finding db_migrations:db-04\nScope: Add a one-line comment in downgrade() stating the restored language value is a placeholder default, not the original per-row value.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Downgrading migration 013 silently fabricates data for a column it appears to restore, unlike sibling migrations 022/035 which fail loudly or document the loss for the same class of change.\nEvidence: upgrade() (39-43) drops video_category.language without saving prior values. downgrade() line 82 re-adds it via ALTER TABLE video_category ADD COLUMN IF NOT EXISTS language TEXT NOT NULL DEFAULT 'de' \u2014 every row gets the same hardcoded 'de' regardless of its actual original value, undocumented as a placeholder. Matches prior audit db-04/rt-08, still unresolved.\nExpected files: lexy-app/backend/migrations/versions/013_video_category_cleanup.py\nAcceptance criteria: downgrade() carries a comment documenting the fabricated value\nValidation: grep -n -B1 \"DEFAULT 'de'\" lexy-app/backend/migrations/versions/013_video_category_cleanup.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-007",
    "title": "Add Depends(require_admin) to both GET /books/packages and POST /books/import, matching the exist...",
    "description": "Priority: P2 | severity high | confidence confirmed | from audit finding db_migrations:sec-01\nScope: Add Depends(require_admin) to both GET /books/packages and POST /books/import, matching the existing /phrases/seed pattern, and record the route pair in docs/SECURITY.md's admin-gated list.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any authenticated non-admin learner can enumerate every operator-curated package name and force checksum/structural validation of any of them on demand \u2014 information disclosure plus a compute-cost surface open to the whole user base.\nEvidence: routers/books.py:182-185 (GET /books/packages) and 188-212 (POST /books/import) depend only on Depends(get_current_user), no require_admin. book_import_service.list_packages() takes no user-scoping arg. docs/SECURITY.md:486's admin-gated-routes list omits both routes, confirming this is untracked, not an accepted risk. Matches prior audit sec-02/rt-01, still unresolved and still not logged in SECURITY.md despite CLAUDE.md \u00a714 requiring it.\nExpected files: lexy-app/backend/routers/books.py, lexy-app/backend/services/book_import_service.py, docs/SECURITY.md\nAcceptance criteria: Non-admin authenticated user gets 403 admin_required from both endpoints; Admin user's existing dry-run workflow unaffected; docs/SECURITY.md admin-gated routes list includes both routes\nValidation: rg -n 'require_admin' lexy-app/backend/routers/books.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-008",
    "title": "Add Depends(require_admin) to POST /books/import and GET /books/packages, matching the S17 preced...",
    "description": "Priority: P2 | severity medium | confidence confirmed | from audit finding ingestion_pipeline:sec-01\nScope: Add Depends(require_admin) to POST /books/import and GET /books/packages, matching the S17 precedent for /phrases/seed, and record the finding/fix in docs/SECURITY.md per CLAUDE.md \u00a714.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any authenticated non-admin user can enumerate all ingestion package names and trigger validation/hashing of packages that aren't theirs; once A3 wires real persistence this same gap lets any user consume another operator's worker output as their own book.\nEvidence: routers/books.py:182-212 gates both endpoints with only Depends(get_current_user); no is_admin/require_admin check, unlike delete_book/delete_page (books.py:376-402) which pass is_admin=user.get('is_admin', False). tests/test_document_package.py:987-1034 (TestImportEndpoint) only asserts auth is required, never that a non-admin is refused. docs/SECURITY.md:484's A2 entry covers only path containment, not authorization scope.\nExpected files: lexy-app/backend/routers/books.py, docs/SECURITY.md\nAcceptance criteria: Non-admin authenticated user gets 403 admin_required from both endpoints; Admin user still succeeds; docs/SECURITY.md has a new dated entry\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k TestImportEndpoint -q; rg -n 'require_admin' lexy-app/backend/routers/books.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-009",
    "title": "Apply require_admin (core/deps.py) to GET /books/packages and POST /books/import, add lexy-app/ba...",
    "description": "Priority: P2 | severity medium | confidence confirmed | from audit finding security_paths:sec-01\nScope: Apply require_admin (core/deps.py) to GET /books/packages and POST /books/import, add lexy-app/backend/tests/test_books_import_admin.py, and add both routes to docs/SECURITY.md's admin-gated-routes list per rt-01's own plan.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any logged-in (non-admin) user can list server-side document-package names and run full validation reports against them; no DB mutation is possible yet (blocked by PendingSchemaPersistence until roadmap A3), so today's impact is info disclosure, not data loss.\nEvidence: books.py:182-191 both routes depend only on get_current_user; seed_tasks.json rt-01 (priority 1) states the routes are 'auth-gated but not admin-gated'; no test_books_import_admin.py exists; SECURITY.md's admin-gated-routes list omits both.\nExpected files: lexy-app/backend/routers/books.py, autoloop/seed_tasks.json, docs/SECURITY.md\nAcceptance criteria: non-admin caller gets 403 admin_required on both routes; admin caller behavior unchanged; docs/SECURITY.md lists both routes under admin-gated routes\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_books_import_admin.py -q; ruff check .\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-010",
    "title": "Also strip/escape backslashes in SAFE before the quote substitution, and log (not silently swallo...",
    "description": "Priority: P2 | severity low | confidence probable | from audit finding security_paths:sec-02\nScope: Also strip/escape backslashes in SAFE before the quote substitution, and log (not silently swallow) a nonzero osascript exit code.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A blocker/park detail or question string ending in an odd number of backslashes can break the AppleScript string boundary and make osascript fail to compile; since the script always exits 0, the 'needs_attention' desktop alert this tool exists to deliver is silently dropped instead of surfaced.\nEvidence: Line 63's sed only replaces literal double-quotes with single-quotes, leaving backslashes untouched; SAFE is embedded in an AppleScript double-quoted string (line 64) where backslash-quote is an escape sequence; the script `exit 0`s unconditionally regardless of osascript's result.\nExpected files: scripts/autoloop_health_notify.sh\nAcceptance criteria: a detail string ending in a backslash still produces a valid, undistorted notification; a failed osascript call is logged as a failure rather than exiting 0\nValidation: scripts/autoloop_health_notify.sh (craft a state.question ending in a backslash and inspect the log)\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-011",
    "title": "Add a CI workflow running `ruff check .`, backend `python3 -m pytest -n auto`, frontend `npm run ...",
    "description": "Priority: P4 | severity medium | confidence confirmed | from audit finding security_paths:arch-01\nScope: Add a CI workflow running `ruff check .`, backend `python3 -m pytest -n auto`, frontend `npm run test`, and root `pytest tests/` on push/PR to main.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A PR that breaks backend, frontend, root-pipeline, or autoloop tests, or introduces a ruff violation, can merge to main with no automated CI gate catching it.\nEvidence: Glob '.github/workflows/*.yml' returns only dependency-audit.yml (pip-audit + npm audit); no workflow runs pytest/ruff/vitest on push or PR, despite CLAUDE.md mandating ruff+pytest for every backend change and a 1258+/322+/207+/729-test suite across five test trees.\nExpected files: .github/workflows/dependency-audit.yml\nAcceptance criteria: a workflow exists that fails on a broken test or ruff violation in a PR\nValidation: gh workflow list; cat .github/workflows/*.yml\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-012",
    "title": "Add .github/workflows/tests.yml running backend pytest -n auto, frontend npm run test, root pytes...",
    "description": "Priority: P5 | severity high | confidence confirmed | from audit finding db_migrations:test-01\nScope: Add .github/workflows/tests.yml running backend pytest -n auto, frontend npm run test, root pytest tests/, and pytest autoloop/tests on push/PR to main; note db-01 blocks a fresh-DB-provisioned backend job.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: No CI signal exists for backend, frontend, root-pipeline, or autoloop test regressions before merge to main; breakage is only caught if someone manually runs the right command.\nEvidence: Glob of .github/workflows/*.yml returns only dependency-audit.yml (pip-audit/npm audit) \u2014 no workflow runs pytest or vitest. pytest.ini:7 sets testpaths = tests, excluding autoloop/tests, so bare `pytest` from repo root silently skips those too. Matches prior audit ci-01/test-01/rt-05/rt-10, still unresolved.\nExpected files: .github/workflows/dependency-audit.yml, pytest.ini, CLAUDE.md\nAcceptance criteria: .github/workflows/tests.yml exists, committed, and runs on push/PR to main; All four suites (backend, frontend, root pipeline, autoloop) run and must pass for the workflow to succeed\nValidation: test -f .github/workflows/tests.yml; grep -E 'pytest|npm run test' .github/workflows/tests.yml\nParallelizable: yes\nNote: unresolved dependency references from the audit: ['db-01']",
    "depends_on": []
  },
  {
    "id": "au-013",
    "title": "Add CI job(s) that run `ruff check .`, `cd lexy-app/backend && python3 -m pytest -n auto`, `pytes...",
    "description": "Priority: P5 | severity high | confidence confirmed | from audit finding repo_structure:arch-02\nScope: Add CI job(s) that run `ruff check .`, `cd lexy-app/backend && python3 -m pytest -n auto`, `pytest tests/`, and `cd lexy-app/frontend && npx tsc --noEmit && npx vitest run && npm run build` on push/PR to main.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A PR that fails ruff, breaks any of the ~1258 backend / ~322 frontend / ~370 root-pipeline tests, or breaks `npm run build` can merge to main with a fully green required-checks state, since CI only checks dependency CVEs.\nEvidence: The repo's only workflow (.github/workflows/dependency-audit.yml) runs pip-audit + npm audit only; no job runs pytest, ruff check ., npm run test, or npm run build. CLAUDE.md \u00a711 mandates ruff + pytest (backend/root/frontend) as required validation, and no pre-commit hook or other workflow enforces it (confirmed: no .pre-commit-config.yaml, no other .github/workflows/*.yml).\nExpected files: .github/workflows/dependency-audit.yml\nAcceptance criteria: A PR that fails a backend test or ruff shows a failing required CI check, not just dependency-audit\nValidation: cat .github/workflows/*.yml\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-014",
    "title": "Add a CI workflow that runs `ruff check .` and `cd lexy-app/backend && python3 -m pytest -n auto`...",
    "description": "Priority: P5 | severity medium | confidence confirmed | from audit finding ingestion_pipeline:test-01\nScope: Add a CI workflow that runs `ruff check .` and `cd lexy-app/backend && python3 -m pytest -n auto` (plus root `pytest tests/`) on push/PR to main.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A regression in any of the five document-package import gates (schema, checksums, containment/zip-slip, structural, profile) would only be caught if a human or agent remembers to run pytest/ruff manually before merging.\nEvidence: Only .github/workflows/dependency-audit.yml exists (pip-audit + npm audit); no workflow runs `ruff check .` or the backend/root pytest suites, which CLAUDE.md \u00a711 mandates for every Python change.\nExpected files: .github/workflows/dependency-audit.yml\nAcceptance criteria: A PR that breaks a document_package validator or checksum gate fails CI automatically\nValidation: ls .github/workflows/\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-015",
    "title": "Add lexy-app/backend/tests/test_alembic_migrations.py that (1) asserts the version files form one...",
    "description": "Priority: P5 | severity medium | confidence probable | from audit finding tests_ci:test-01\nScope: Add lexy-app/backend/tests/test_alembic_migrations.py that (1) asserts the version files form one unbroken sequential chain, (2) smoke-tests upgrade+downgrade per revision via alembic's API, (3) flags unguarded DROP and unscoped DELETE.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A migration that corrupts data or ships wrong ON DELETE behaviour can land untested; migrations are append-only and not easily rolled back, so the damage reaches every deployed user.\nEvidence: CLAUDE.md \u00a75 lists 37 migrations (head 037), but no migration-level test file exists: lexy-app/backend/tests/test_llm_cache_migration.py covers one cache migration only, and tests/test_chat_language.py touches migration 033 incidentally. No test_migrations.py or equivalent.\nExpected files: lexy-app/backend/alembic/versions/\nAcceptance criteria: test_alembic_migrations.py exists and passes; At least one pre-existing migration is exercised by the test\nValidation: ls lexy-app/backend/alembic/versions/ | wc -l; cd lexy-app/backend && python3 -m pytest tests/test_alembic_migrations.py -v\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-016",
    "title": "For future schema changes on append-only/high-growth tables, add transaction_per_migration suppor...",
    "description": "Priority: P6 | severity medium | confidence probable | from audit finding db_migrations:db-06\nScope: For future schema changes on append-only/high-growth tables, add transaction_per_migration support for a CONCURRENTLY-safe path, or batch backfills; document the trade-off in CLAUDE.md's migration guidance.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: As word_usage_events grows, any future generated-column or index change on it blocks all reads/writes for the rewrite's duration, with no structural way in the current env.py setup to avoid it.\nEvidence: 026:34-38 adds word_usage_events.event_day as GENERATED ALWAYS ... STORED, forcing a full-table rewrite under ACCESS EXCLUSIVE lock; 39-43 then CREATE UNIQUE INDEX (no CONCURRENTLY) on the same table. env.py:45/58 wraps every migration in context.begin_transaction(), and Postgres refuses CREATE INDEX CONCURRENTLY inside a transaction, so this setup structurally cannot do a non-blocking index build. word_usage_events is the fire-and-forget sink for every transcript click, chat turn, and SRS review (CLAUDE.md \u00a78b) \u2014 this repo's most likely unbounded-growth table.\nExpected files: lexy-app/backend/migrations/versions/026_transcript_click_dedup.py, lexy-app/backend/migrations/env.py\nAcceptance criteria: Migration guidance documents the ACCESS EXCLUSIVE-lock trade-off for GENERATED STORED columns and index builds on high-growth tables\nValidation: grep -n 'begin_transaction' lexy-app/backend/migrations/env.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-017",
    "title": "Add a new append-only migration (039, not an edit to 018) creating idx_content_request_user_creat...",
    "description": "Priority: P6 | severity low | confidence confirmed | from audit finding db_migrations:db-03\nScope: Add a new append-only migration (039, not an edit to 018) creating idx_content_request_user_created ON content_request (user_id, created_at DESC).\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: The one user-facing query on this table has no index covering both its filter and its sort; degrades to a sort-after-scan as each user's request history grows.\nEvidence: list_requests (content_requests.py:177-182) runs SELECT * FROM content_request WHERE user_id=$1 ORDER BY created_at DESC. 018:30 indexes only (status). 029:66-68 adds UNIQUE(user_id, request_type, content_id), whose column order supports the equality filter but not the ORDER BY created_at DESC sort. Matches prior audit db-03/rt-13, still unresolved.\nExpected files: lexy-app/backend/routers/content_requests.py, lexy-app/backend/migrations/versions/018_content_request.py, lexy-app/backend/migrations/versions/029_content_request_per_user_unique.py\nAcceptance criteria: A new migration file creates the composite index; EXPLAIN for the list_requests query shows an index scan rather than an unassisted sort\nValidation: psql \"$DB_NAME\" -c \"EXPLAIN SELECT * FROM content_request WHERE user_id='00000000-0000-0000-0000-000000000000' ORDER BY created_at DESC\"\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-018",
    "title": "In a new migration, DROP the redundant single-column index once EXPLAIN confirms the composite UN...",
    "description": "Priority: P6 | severity low | confidence confirmed | from audit finding db_migrations:db-07\nScope: In a new migration, DROP the redundant single-column index once EXPLAIN confirms the composite UNIQUE index's prefix serves user_id-only lookups.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Every INSERT/UPDATE/DELETE on user_word_knowledge \u2014 one of the app's hottest per-event write paths \u2014 maintains one extra unnecessary index for the table's lifetime.\nEvidence: Line 43's UNIQUE (user_id, item_id, item_type) constraint already creates a btree index with user_id as its leading column; line 109 creates a second, separate single-column index on user_id, which a query filtering on user_id alone could already satisfy via the composite index's prefix. Not present in the prior 2026-07-30 audit \u2014 newly found.\nExpected files: lexy-app/backend/migrations/versions/001_initial_user_tables.py\nAcceptance criteria: EXPLAIN for a WHERE user_id = $1 query on user_word_knowledge still uses an index scan after the redundant index is dropped\nValidation: psql \"$DB_NAME\" -c \"\\di+ user_word_knowledge*\"\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-019",
    "title": "Align test_document_package.py's imports to `from backend.services...`, closing the open follow-u...",
    "description": "Priority: P6 | severity low | confidence confirmed | from audit finding ingestion_pipeline:imp-01\nScope: Align test_document_package.py's imports to `from backend.services...`, closing the open follow-up noted in docs/COMMON_ERRORS.md \u00a72, so both invocation forms behave identically.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Running `pytest` (the bare console script) instead of `python3 -m pytest` silently drops this file's 107 tests from collection (1151 vs 1258) with no error surfaced beyond a collection error easy to miss in a large run.\nEvidence: tests/test_document_package.py is the only backend test file using bare `from services import ...` (confirmed: rg '^from services\\.' matches only this file); every other test uses `from backend.services import ...`. Already documented as a known, mitigated trap in CLAUDE.md \u00a711 and docs/COMMON_ERRORS.md \u00a72, with alignment listed there as a still-open follow-up rather than done.\nExpected files: lexy-app/backend/tests/test_document_package.py\nAcceptance criteria: File uses from backend.services... imports; `pytest tests/test_document_package.py` (bare console script) collects and passes from lexy-app/backend\nValidation: cd lexy-app/backend && pytest tests/test_document_package.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-020",
    "title": "Add a comment block to ruff.toml stating that only E+F are selected and that I (import sort), E50...",
    "description": "Priority: P6 | severity low | confidence probable | from audit finding tests_ci:ruff-01\nScope: Add a comment block to ruff.toml stating that only E+F are selected and that I (import sort), E501, formatter, and style/async rule sets are deliberately deferred, with the one-line reason for each.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A future maintainer reads the narrow rule set as an oversight, enables I or E501, and triggers the repo-wide reflow / scraper-breaking import hoist that CLAUDE.md \u00a711 warns against.\nEvidence: ruff.toml:19 selects ['E','F'] with a comment explaining only that full E is broader than ruff's default; nothing records why I (import sort), E501, formatting, and the async/style rule sets are off. CLAUDE.md \u00a711 documents the E402/E501 traps but ruff.toml itself does not.\nExpected files: ruff.toml\nAcceptance criteria: ruff.toml carries a comment explaining why only E+F are selected and why I/E501/format are deferred; ruff check . still reports 'All checks passed!'\nValidation: head -30 ruff.toml; ruff check .\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-021",
    "title": "Rewrite line 128 to describe the actual path: status_override passed into apply_progression insid...",
    "description": "Priority: P7 | severity medium | confidence confirmed | from audit finding db_migrations:doc-01\nScope: Rewrite line 128 to describe the actual path: status_override passed into apply_progression inside its single transaction, removing the upsert_word_status reference.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A reader of \u00a78b \u2014 the file's own recommended starting point for the progression state machine \u2014 is misled into believing a deleted function still gates status writes.\nEvidence: CLAUDE.md:128 says status_marked_known's status flip is 'forced to known by word_service.upsert_word_status (called from the router before apply_progression)'. Lines 136 and 162 of the same file say that function 'has been removed'/'is gone'. grep 'def upsert_word_status' lexy-app/backend/services/*.py returns nothing. Matches prior audit doc_drift-01/rt-07, still unresolved.\nExpected files: CLAUDE.md\nAcceptance criteria: CLAUDE.md line 128 no longer mentions upsert_word_status; The description matches routers/words.py's actual call into apply_progression(..., status_override=...)\nValidation: grep -n upsert_word_status CLAUDE.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-022",
    "title": "Either: (A) add CI workflows per ci-01, or (B) document in CLAUDE.md that these checks are recomm...",
    "description": "Priority: P7 | severity medium | confidence confirmed | from audit finding tests_ci:doc-01\nScope: Either: (A) add CI workflows per ci-01, or (B) document in CLAUDE.md that these checks are recommendations, not requirements. (A) is preferred \u2014 the language 'part of backend validation' indicates these are non-negotiable.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Developers following CLAUDE.md will run the checks locally, but reviewers have no automated guarantee that a branch passed those checks before merge.\nEvidence: CLAUDE.md \u00a711 documents three mandatory validation commands: `ruff check .`, `python3 -m pytest` (backend), `npm run test` (frontend). TESTS.md documents 1,137+ backend tests, 306+ frontend tests, and root-pipeline tests. But .github/workflows/ has zero workflows for any of these. Docs claim validation is required; CI does not require it.\nExpected files: docs/TESTS.md, CLAUDE.md\nAcceptance criteria: CI workflow exists and runs all checks, OR CLAUDE.md re-states validation is optional\nValidation: grep -n 'part of backend validation' CLAUDE.md; ls -la .github/workflows/*.yml | wc -l\nParallelizable: yes",
    "depends_on": [
      "au-002"
    ]
  },
  {
    "id": "au-023",
    "title": "Fix CLAUDE.md:38 to read 'lexy-app/backend/migrations/versions/' and update the file count to 37+.",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding db_migrations:db-05\nScope: Fix CLAUDE.md:38 to read 'lexy-app/backend/migrations/versions/' and update the file count to 37+.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A contributor following CLAUDE.md's own stated path when looking for or authoring a migration searches a directory that doesn't exist.\nEvidence: CLAUDE.md:38 says migrations live in '25+ files in alembic/versions/'. alembic.ini:2 sets script_location = migrations, and the actual 37 files are at lexy-app/backend/migrations/versions/ \u2014 no alembic/versions/ directory exists anywhere in the repo. Not present in the prior 2026-07-30 audit \u2014 newly found.\nExpected files: CLAUDE.md, lexy-app/backend/alembic.ini\nAcceptance criteria: CLAUDE.md's stated migrations path matches alembic.ini's script_location\nValidation: grep -n 'alembic/versions' CLAUDE.md; grep script_location lexy-app/backend/alembic.ini\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-024",
    "title": "Update line 97 in TESTS.md to state the true current frontend count and date (299 or 322 as of 20...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding docs_drift:doc-drift-01\nScope: Update line 97 in TESTS.md to state the true current frontend count and date (299 or 322 as of 2026-07-29), then verify ROADMAP.md line 68 matches if that section was regenerated after TESTS.md was last edited\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Frontend test count is inconsistent across documentation sections. The 'Current count' in TESTS.md is stale and likely describes only the backend tallies (261 can be computed as some backend entries) while frontend continued growing. This mismatch obscures whether test coverage targets have been met\nEvidence: docs/TESTS.md line 97 states 'Current count (2026-07-27): 261 tests across 43 files' for frontend, but line 676 reports 289 tests (278+11), contradicting the earlier baseline. Further, ROADMAP.md line 68 claims 322 frontend tests as of 2026-07-29, creating a 33-test gap from TESTS.md's reported 289 at the same date\nExpected files: docs/TESTS.md\nAcceptance criteria: TESTS.md line 97 'Current count' reflects actual 2026-07-29 test totals or was struck as stale; No contradictory per-date test counts exist within TESTS.md itself\nValidation: grep -n 'Current count' docs/TESTS.md; grep -n '2026-07-29.*passed' docs/TESTS.md docs/ROADMAP.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-025",
    "title": "Update the '+89 backend' figure at docs/TESTS.md:956 to 107 (or explain the discrepancy) to match...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding ingestion_pipeline:doc-01\nScope: Update the '+89 backend' figure at docs/TESTS.md:956 to 107 (or explain the discrepancy) to match the reconciled count at line 2174-2176.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A reader trusting the '+89' figure under-estimates test_document_package.py's actual coverage by 18 tests; low-impact but avoidable confusion in a doc CLAUDE.md designates as the coverage index.\nEvidence: Line 956 states A2 landed '+89 backend' tests for tests/test_document_package.py, but line 2174-2176 gives the same file 107 tests with arithmetic proof (1151+107=1258 matching the pinned backend baseline). The two figures for one file were never reconciled.\nExpected files: docs/TESTS.md\nAcceptance criteria: Both figures for tests/test_document_package.py agree in docs/TESTS.md\nValidation: grep -n '89 backend\\|107 tests' docs/TESTS.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-026",
    "title": "Elevate the pytest-vs-python3-m-pytest trap to docs/COMMON_ERRORS.md as an entry (it's already do...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding tests_ci:doc-02\nScope: Elevate the pytest-vs-python3-m-pytest trap to docs/COMMON_ERRORS.md as an entry (it's already documented but not in the error log). Also add a pytest configuration that makes bare `pytest` fail with a clear message directing to `python3 -m pytest`.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A developer running `pytest` instead of `python3 -m pytest` will silently run only 1151 of 1258 backend tests and may think their change is safe when one test file was never run.\nEvidence: CLAUDE.md \u00a711 states: 'Use `python3 -m pytest`, not the bare `pytest` entrypoint. `python -m` puts the cwd on `sys.path`, which `tests/test_document_package.py` (A2) needs for its bare `from services import \u2026` imports; the `pytest` console script does not, and the file then fails collection (1151 instead of 1258\u2026)'. This is buried in a long paragraph and easy to miss. It's not highlighted as a critical requirement.\nExpected files: CLAUDE.md\nAcceptance criteria: Entry added to COMMON_ERRORS.md; OR pytest hook/plugin installed that shows helpful error when bare pytest misses test_document_package.py\nValidation: grep -i 'python3 -m pytest' CLAUDE.md; grep -i 'pytest.*not.*python' docs/COMMON_ERRORS.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-027",
    "title": "Update the line references in SECURITY.md and CLAUDE.md \u00a714 to 164,205,322, or drop specific line...",
    "description": "Priority: P7 | severity info | confidence confirmed | from audit finding security_paths:doc-01\nScope: Update the line references in SECURITY.md and CLAUDE.md \u00a714 to 164,205,322, or drop specific line numbers in favor of the grep pattern plus the 'loop index + module constant only' description.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Someone re-running the documented verification grep and eyeballing line numbers against the stale reference could be momentarily confused about whether the hits match the pre-verified-safe set; the grep pattern itself still finds them correctly.\nEvidence: SECURITY.md line 464 and CLAUDE.md \u00a714's self-check grep both say 'Expected hits: search_service.py:62,103,216'; the three f-string SQL sites (still safe \u2014 only a loop index and the SIMILARITY_THRESHOLD constant are interpolated) are now at lines 164, 205, and 322.\nExpected files: docs/SECURITY.md, CLAUDE.md, lexy-app/backend/services/search_service.py\nAcceptance criteria: SECURITY.md and CLAUDE.md no longer cite stale line numbers for this pattern\nValidation: rg -n 'f\"(SELECT|INSERT|UPDATE|DELETE)' lexy-app/backend --glob '!**/migrations/**'\nParallelizable: yes",
    "depends_on": []
  }
]
```

Not promoted to tasks:
- `db_migrations:db-01`: human decision — surfaced to the operator, not a task
