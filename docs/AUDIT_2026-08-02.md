# Repository audit — 2026-08-02

Generated by the autoloop audit executor (deterministic rendering of reconciled subagent findings; raw agent reports preserved at `.autoloop/audit/20260802T223252Z-ac2907`).

- branch: `autoloop/audit-0001` | HEAD: `07b659b8e12d` | uncommitted files: 0
- requested scope: Audit architecture, tests and CI, security, database migrations, documentation drift, and the ingestion pipeline; reconcile duplicate findings across domains and against all 20 existing roadmap tasks, report each canonical finding as new, completed, ready, blocked, or quarantined, preserve human blockers and dependencies, verify paths and current migration head, measure current test totals, and give every proposed task exact minimal approved_paths plus executable validation.

## Domain coverage — 6/6 domains reported usable output

| domain | usable output | findings |
|---|---|---|
| `docs_drift` | yes | 4 |
| `tests_ci` | yes | 4 |
| `repo_structure` | yes | 12 |
| `security_paths` | yes | 3 |
| `db_migrations` | yes | 6 |
| `ingestion_pipeline` | yes | 5 |

## Validation baseline
- `ruff check .` — **PASS**
  - tail: `All checks passed!`
- `python3 -m pytest autoloop/tests -q` — **PASS**
  - tail: `[32m[32m[1m1067 passed[0m, [33m1 skipped[0m[32m in 234.80s (0:03:54)[0m[0m`
- `python3 -m pytest tests/ -q` — **PASS**
  - tail: `[33m[32m368 passed[0m, [33m[1m5 warnings[0m[33m in 10.77s[0m[0m`

## Confirmed defects (7)
#### tests_ci:ci-01 — Add GitHub Actions workflows that run (1) backend: `cd lexy-app/backend && python3 -m pytest -n auto
- severity **high**, confidence **confirmed**, domain `tests_ci`
- files: `.github/workflows/dependency-audit.yml`
- evidence: .github/workflows/dependency-audit.yml contains ONLY pip-audit and npm audit jobs (lines 20–52), with no pytest, coverage, linting, or TypeScript compilation gates. CLAUDE.md §11 documents three test suites (backend, frontend, root pipeline) that must be run before any change is complete; docs/TESTS.md inventories 1122 backend tests across 54 files, 261 frontend tests across 43 files, 221 root-pipeline tests. None are invoked by CI.
- impact: Test failures, lint violations, type errors, and migration failures reach main without detection. A pushed change can corrupt the database schema, break every test, or regress security — then pass CI.
- acceptance: CI blocks a failing backend test from reaching main; CI blocks a failing frontend test from reaching main; CI blocks a ruff violation from reaching main; CI blocks a TypeScript error from reaching main
- validation: ls .github/workflows/; grep -c 'pytest\|npm.*test\|tsc' .github/workflows/*.yml

#### tests_ci:lint-01 — Add `ruff check .` to the CI workflow, gating main. Run from repo root (covers backend, scraper, roo
- severity **medium**, confidence **confirmed**, domain `tests_ci`
- files: `.github/workflows/dependency-audit.yml`, `CLAUDE.md`
- evidence: CLAUDE.md §11 ('Lint') mandates `ruff check .` from repo root as part of every backend change, with a baseline of 0 findings as of 2026-07-27 (documented in ruff.toml comments). CI workflow contains no ruff invocation. Ruff is configured in ruff.toml (pinned to 0.14.1 in requirements.txt) and can run, but is never gated by CI.
- impact: A new ruff violation lands on main without detection, silently raising the baseline. The documented baseline (zero findings) degrades undetected. Developers rely on running `ruff check .` locally before pushing, which is error-prone and not enforced.
- acceptance: A ruff violation on the working branch causes CI to fail; CI passes only when `ruff check .` at repo root reports zero findings
- validation: ruff check . --select E,F; grep -c ruff .github/workflows/dependency-audit.yml

#### repo_structure:ing-01 — Add a structural validator flagging bbox['page'] != element['page_index'] with a stable issue code; 
- severity **medium**, confidence **confirmed**, domain `repo_structure`
- files: `lexy-app/backend/services/document_package/validators.py`, `docs/INGESTION_PIPELINE.md`
- symbols: `validators.py:_BBOX_KEYS`, `validators.py:validate_bboxes:296-373`
- evidence: Grep for 'page' in validators.py (2026-08-03) shows no bbox['page'] read anywhere; validate_bboxes:351-352 keys page_dims only off element['page_index']. INGESTION_PIPELINE.md §4's own worked example (lines 238-246) still shows page_index:11 vs bbox.page:12 in one element. Collapses db_migrations:ing-01 + ingestion_pipeline:def-01, still unresolved.
- impact: A worker bug or corrupted package with bbox.page != element.page_index passes every validation gate silently — a live trap once roadmap A3 wires real persistence.
- acceptance: Synthetic element with mismatched page fails validation with a new issue code; INGESTION_PIPELINE.md §4 example has matching values
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k Structural

#### db_migrations:db-02 — Guard downgrade() to refuse when reading_selections rows hold non-empty anchors referencing book_blo
- severity **medium**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/025_block_token_ids.py`, `lexy-app/backend/migrations/versions/009_reading_selections.py`
- symbols: `025_block_token_ids.py:_tokenize:21-27`, `025_block_token_ids.py:downgrade:64-65`, `009_reading_selections.py:31-32`
- evidence: _tokenize calls uuid.uuid4() fresh per token on every run (21-27); downgrade() is a bare DROP COLUMN tokens with no guard (64-65). reading_selections.anchors (009:31-32) stores {block_id, token_id, surface} with no FK, referencing those same UUIDs.
- impact: A downgrade-then-upgrade of migration 025 regenerates every token_id, silently orphaning every existing reading_selections.anchors reference with no error and no data-loss warning.
- acceptance: downgrade() raises when orphaning anchors would occur, or a round-trip test proves token_ids survive downgrade+upgrade
- validation: cat lexy-app/backend/migrations/versions/025_block_token_ids.py

#### ingestion_pipeline:ing-01 — Add a structural validator comparing bbox['page'] (when present) to element['page_index'], adding a 
- severity **medium**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/services/document_package/validators.py`, `docs/INGESTION_PIPELINE.md`
- symbols: `validators.py:_BBOX_KEYS:21`, `validators.py:validate_bboxes:296-373`, `INGESTION_PIPELINE.md:238-254`
- evidence: _BBOX_KEYS=('x1','y1','x2','y2') never includes 'page'; validate_bboxes:351-352 looks up page dims via element.get('page_index') only, never bbox.get('page'). Grep confirms no file under document_package/ reads bbox['page']. The doc's own §4 worked example shows page_index:11 with bbox.page:12 in the same element, still unreconciled.
- impact: A worker bug or corrupted package where bbox.page disagrees with element.page_index passes every validation gate silently; inert while nothing reads bbox.page today, but a live trap if a future consumer (A3 persistence, a review UI) ever keys off bbox.page instead of element.page_index.
- acceptance: A synthetic element with bbox.page != page_index produces a new stable validation issue code; INGESTION_PIPELINE.md §4 example has bbox.page == page_index
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k Structural -q

#### ingestion_pipeline:ing-02 — Add a structural validator checking manifest.page_dims keys form a contiguous 1..page_count range (o
- severity **medium**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/services/document_package/validators.py`, `lexy-app/backend/services/document_package/result.py`
- symbols: `validators.py:validate_page_indices:102-128`, `validators.py:validate_page_dims:131-157`
- evidence: validate_page_indices only fires (fatal, undeclared_page) when an *element* references a page missing from manifest.page_dims. If a worker bug drops a page's page_dims entry AND every element on that page, no element ever mentions the missing page, so nothing flags it. No validator cross-checks manifest.counts.pages against len(page_dims) or checks page_dims keys form a contiguous 1..N range, even though INGESTION_PIPELINE.md §5 lists 'page_index contiguous' among its five promised structural checks.
- impact: A whole page silently vanishing from the package — the worst-case failure per §2b's own stated priority order — produces zero fatal/warning issues and imports as if the book were complete.
- acceptance: A synthetic package whose page_dims skips a page number (with no elements referencing it) fails validation with a new stable issue code; manifest.counts.pages mismatch against len(page_dims) is also caught
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k Structural -q

#### db_migrations:db-03 — Add a one-line comment in downgrade() documenting that the restored value is a placeholder default, 
- severity **low**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/013_video_category_cleanup.py`
- symbols: `013_video_category_cleanup.py:upgrade:39-43`, `013_video_category_cleanup.py:downgrade:82`
- evidence: upgrade() drops video_category.language without saving prior per-row values (39-43); downgrade() line 82 re-adds it via DEFAULT 'de' for every row, undocumented as a fabricated placeholder, unlike sibling migrations 022/037 which fail loudly for the same class of change.
- impact: Downgrading migration 013 silently fabricates data for a column that appears to be restored, misleading anyone relying on downgrade for a real rollback.
- acceptance: downgrade() carries a comment documenting the fabricated value
- validation: grep -n -B1 "DEFAULT 'de'" lexy-app/backend/migrations/versions/013_video_category_cleanup.py


## Security / data-loss risks (4)
#### repo_structure:sec-01 — Add Depends(require_admin) to both routes (mirroring /phrases/seed); add test_books_import_admin.py;
- severity **high**, confidence **confirmed**, domain `repo_structure`
- files: `lexy-app/backend/routers/books.py`, `docs/SECURITY.md`
- symbols: `routers/books.py:182-185 (list_packages)`, `routers/books.py:188-212 (import_package)`
- evidence: books.py:182-212 depend only on Depends(get_current_user); no require_admin, unlike /phrases/seed (S17) and delete_book/delete_page. SECURITY.md:543's admin-gated-routes list still omits both routes on 2026-08-03 re-check. Collapses 4 duplicate prior write-ups (db_migrations:sec-01, security_paths:sec-01, ingestion_pipeline:sec-01, seed_tasks.json rt-01) into one finding — still unresolved.
- impact: Any authenticated non-admin user can enumerate every operator-curated package name and force validation of any of them — info disclosure plus an uncapped compute-cost surface open to the whole user base.
- acceptance: Non-admin gets 403 admin_required from both endpoints; Admin behavior unchanged; SECURITY.md lists both routes as admin-gated
- validation: rg -n 'require_admin' lexy-app/backend/routers/books.py; cd lexy-app/backend && python3 -m pytest tests/test_books_import_admin.py -q

#### ingestion_pipeline:ing-04 — Add Depends(require_admin) (or an equivalent is_admin check) to GET /books/packages and POST /books/
- severity **high**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/routers/books.py`, `docs/SECURITY.md`
- symbols: `routers/books.py:list_packages:182-185`, `routers/books.py:import_package:188-212`
- evidence: Both routes depend only on Depends(get_current_user); delete_book/delete_page in the same file (books.py:376-402) pass is_admin=user.get('is_admin', False) for comparison. test_document_package.py:984-1034 (TestImportEndpoint) asserts only 401/403 for an unauthenticated caller, never that an authenticated non-admin is refused. docs/SECURITY.md's admin-gated-routes list (line 543) omits both routes.
- impact: Any authenticated non-admin user can enumerate every server-side ingestion package name and force full checksum/structural validation of any of them on demand — information disclosure plus an unmetered compute-cost surface open to the whole user base, and it becomes a data-integrity issue once A3 wires real persistence.
- acceptance: Non-admin authenticated user gets 403 admin_required from both endpoints; Admin user's existing dry-run workflow unaffected; docs/SECURITY.md admin-gated routes list includes both routes
- validation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k TestImportEndpoint -q; rg -n 'require_admin' lexy-app/backend/routers/books.py

#### repo_structure:data-01 — Refuse downgrade() when any reading_selections row has anchors referencing existing tokens, or persi
- severity **medium**, confidence **confirmed**, domain `repo_structure`
- files: `lexy-app/backend/migrations/versions/025_block_token_ids.py`, `docs/SCHEMA.md`
- symbols: `025_block_token_ids.py:_tokenize:21-27`, `025_block_token_ids.py:downgrade:64-65`
- evidence: _tokenize (21-27) calls uuid.uuid4() fresh per token every run; downgrade() (64-65) is a bare `ALTER TABLE book_blocks DROP COLUMN tokens`, unchanged on 2026-08-03 re-read. SCHEMA.md documents reading_selections.anchors as [{block_id, token_id, surface}] with no FK. Matches prior db_migrations:db-02, still unresolved.
- impact: downgrade then upgrade on migration 025 regenerates every token_id, silently orphaning every existing reading_selections.anchors reference with no error surfaced.
- acceptance: downgrade() raises on would-be orphaning, or the round-trip preserves token_ids; A regression test exercises the round-trip against a reading_selections row
- validation: cat lexy-app/backend/migrations/versions/025_block_token_ids.py

#### security_paths:sec-01 — Gate both routes with Depends(require_admin), matching /phrases/seed and /admin/lemma-corrections/*;
- severity **medium**, confidence **confirmed**, domain `security_paths`
- files: `lexy-app/backend/routers/books.py`, `lexy-app/backend/services/book_import_service.py`, `lexy-app/backend/services/document_package/loader.py`, `docs/SECURITY.md`
- symbols: `routers.books.list_packages:182-185`, `routers.books.import_package:188-212`
- evidence: routers/books.py:182-212 gates both routes with only Depends(get_current_user) — no require_admin (contrast lemma_corrections.py:64,90,117,150 and phrases.py:36) and no ownership filter; document_package/loader.py:25 is a single shared package root with no per-user split, and contract.py:95-98 carries no owner field; docs/INGESTION_PIPELINE.md:306 labels the workflow operator-only; test_document_package.py:987,1033 assert only that some token is required, never admin or ownership.
- impact: Any signed-up learner (registration is open per SECURITY.md S2) can enumerate operator-ingested package names and read validation metadata (document_id, schema/profile, checksum results, structural issue text) for packages they did not create.
- acceptance: A non-admin authenticated user gets 403 admin_required from GET /books/packages and POST /books/import; an admin still gets today's 200 behavior.; tests/test_document_package.py gains a regression test asserting the 403 for a non-admin caller.; docs/SECURITY.md records this finding with file:line, severity, verification check, and suggested fix per CLAUDE.md §14.; The admin-gated-routes list at docs/SECURITY.md:543 names both /books/packages and /books/import.
- validation: cd lexy-app/backend && MOCK_LLM=true python3 -m pytest tests/test_document_package.py -q; ruff check .; rg -n 'require_admin' lexy-app/backend/routers/books.py


## Architectural risks (2)
#### repo_structure:arch-01 — Move the Docker build context to repo root (COPY subtitle-scraper/ + data/ alongside backend/), or m
- severity **critical**, confidence **confirmed**, domain `repo_structure`
- files: `lexy-app/Dockerfile`, `lexy-app/docker-compose.yml`, `lexy-app/backend/services/matcher_service.py`, `lexy-app/backend/main.py`, `lexy-app/backend/routers/phrases.py`, `lexy-app/backend/routers/matcher.py`
- symbols: `matcher_service.py:35-43`, `Dockerfile:21-31`, `docker-compose.yml:17`
- evidence: docker-compose.yml:17 `build: .` (context lexy-app/); Dockerfile:21-31 COPYs only backend/ + built frontend, never subtitle-scraper/ or data/ (both at repo root, outside context). matcher_service.py:35 `Path(__file__).resolve().parents[3]/'subtitle-scraper'` — in-image (WORKDIR /app, COPY backend/ ./backend/) parents[3] is '/', so path doesn't exist; line 43 `import phrase_finder` at module scope, pulled in by routers/phrases.py & routers/matcher.py before main.py builds `app=FastAPI()`.
- impact: A container built from lexy-app/Dockerfile crashes on boot with ModuleNotFoundError: phrase_finder — uvicorn never starts, backend unreachable for any docker-compose deployment.
- acceptance: docker compose up boots without a startup crash; CI builds the image and hits a health endpoint successfully
- validation: cd lexy-app && docker compose build && docker compose up -d && curl -f http://localhost:8000/api/v1/auth/login -X POST -d '{}' -H 'Content-Type: application/json'

#### db_migrations:db-05 — Document the ACCESS EXCLUSIVE trade-off for GENERATED STORED columns and index builds on high-growth
- severity **medium**, confidence **probable**, domain `db_migrations`
- files: `lexy-app/backend/migrations/env.py`, `lexy-app/backend/migrations/versions/026_transcript_click_dedup.py`
- symbols: `env.py:run_migrations_offline:43-46`, `env.py:run_migrations_online:49-59`, `026_transcript_click_dedup.py:upgrade:29-43`
- evidence: env.py wraps every migration in context.begin_transaction() (lines 45 and 58); Postgres refuses CREATE INDEX CONCURRENTLY inside a transaction. Migration 026 adds a GENERATED ALWAYS ... STORED column (full-table rewrite) and a non-concurrent CREATE UNIQUE INDEX on word_usage_events, the fire-and-forget sink for every transcript click, chat turn, and SRS review (CLAUDE.md §8b).
- impact: Any future schema change to word_usage_events, or another high-growth table, blocks all reads/writes for the rewrite/index-build duration, with no structural way under the current env.py to opt into a non-blocking build.
- acceptance: Migration authoring guidance documents the trade-off and the autocommit_block() escape hatch
- validation: grep -n 'begin_transaction' lexy-app/backend/migrations/env.py


## Documentation drift (14)
#### tests_ci:doc-01 — Remove the conftest.py reference from the SUMMARY.md entry for pytest.ini. Replace with accurate des
- severity **medium**, confidence **confirmed**, domain `tests_ci`
- files: `docs/SUMMARY.md`
- symbols: `docs/SUMMARY.md:349`
- evidence: SUMMARY.md line 349 states `pytest.ini`, `conftest.py` — Pytest config; conftest adds `src/` to path.` No root conftest.py exists (Glob found only autoloop/tests/conftest.py, lexy-app/backend/tests/conftest.py, tests/evaluation/conftest.py). Root pytest.ini at line 7 says `testpaths = tests` and declares no path manipulation.
- impact: New contributors read the documentation and look for a conftest.py that doesn't exist, wasting time. The stated mechanism (sys.path manipulation in conftest) is never explained and the actual mechanism (sys.path implicit from `python -m pytest`, documented in CLAUDE.md §11 and COMMON_ERRORS.md §1) is not obvious from SUMMARY.md.
- acceptance: SUMMARY.md line 349 no longer mentions a non-existent conftest.py
- validation: grep -n 'conftest adds' docs/SUMMARY.md

#### repo_structure:doc-01 — Rewrite the §8b sentence to describe the actual path: status_override passed into apply_progression 
- severity **medium**, confidence **confirmed**, domain `repo_structure`
- files: `CLAUDE.md`
- symbols: `CLAUDE.md §8b (status_marked_known paragraph)`
- evidence: Current CLAUDE.md §8b still reads the status flip is 'forced to known by word_service.upsert_word_status (called from the router before apply_progression)', while §10/§12 of the same file say that function 'has been removed'/'is gone', and grep 'def upsert_word_status' across services/*.py returns nothing. Matches db_migrations:doc-01, still unresolved.
- impact: A reader of §8b — the file's own recommended starting point (§13) — is misled into believing a deleted function still gates status writes.
- acceptance: CLAUDE.md no longer mentions upsert_word_status; Description matches routers/words.py's actual apply_progression(..., status_override=...) call
- validation: grep -n upsert_word_status CLAUDE.md

#### docs_drift:doc-01 — In TESTS.md line 2176, change to: '1151 passed, 2 skipped [+107 from A2 collected via sys.path impor
- severity **low**, confidence **confirmed**, domain `docs_drift`
- files: `docs/TESTS.md`
- symbols: `TESTS.md:2176-2187`
- evidence: TESTS.md line 2176 claims validation baseline is '1258 passed, 2 skipped', but lines 2180-2187 explain this includes 107 tests from test_document_package.py (A2) that only load with 'python3 -m pytest' due to sys.path handling. The doc notes the true baseline is 1151 (without A2), making '1258' a conditional count. This is correctly documented but the phrasing could be clearer for users seeing 1151 in ROADMAP.md or earlier TESTS.md sections.
- impact: Reader confusion: the same validation command produces 1151 or 1258 depending on pytest invocation method, but docs don't lead with that fact in the baseline expectations
- acceptance: Baseline expectation in validation commands is unambiguous about which test files are included
- validation: Read TESTS.md lines 2180-2187 for the explanation vs line 2176 for the expectation

#### docs_drift:doc-03 — Update TESTS.md line 2260 to reference the current frontend baseline: '(322 tests across 44 files)'
- severity **low**, confidence **confirmed**, domain `docs_drift`
- files: `docs/TESTS.md`
- symbols: `TESTS.md:2260`
- evidence: TESTS.md line 2260 claims 'Frontend changes additionally want `cd lexy-app/frontend && npx tsc --noEmit && npx vitest run && npm run build` (261 tests across 43 files).' But earlier in the same file, line 2201-2202 claims '**322 passed across 44 files**' as the current baseline as of 2026-07-29. The inline instruction uses a stale figure (261 tests / 43 files) instead of the current 322 / 44.
- impact: Developer running the validation commands per line 2260 will see a mismatch between expected (261/43) and actual (322/44) test counts, causing alarm
- acceptance: Inline validation instruction matches the current frontend test baseline elsewhere in the same file
- validation: Read TESTS.md lines 2201, 2260 and compare

#### repo_structure:doc-02 — Add exposure_service.py and pipeline_diagnostics.py to CLAUDE.md §2's pipeline-modules list.
- severity **low**, confidence **confirmed**, domain `repo_structure`
- files: `CLAUDE.md`
- symbols: `CLAUDE.md §2 table`, `exposure_service.py`, `pipeline_diagnostics.py`
- evidence: CLAUDE.md §2's pipeline-modules row lists pipeline.py, eligibility.py, exposure_counter.py, user_knowledge.py, learning_units.py, onboarding.py, subtitle_*.py, utterance_*.py, word_knowledge.py, validate_tier_lemmas.py. Root Glob confirms exposure_service.py and pipeline_diagnostics.py also exist and are imported by pipeline.py (grep confirms both), but neither appears in the table — new finding, not in either prior audit pass.
- impact: A reader following §2's 'root pipeline modules are the only pipeline tree, change them directly' guidance may overlook these two files when auditing or changing pipeline behaviour.
- acceptance: CLAUDE.md §2 lists both modules alongside the other root pipeline modules
- validation: grep -n 'exposure_service.py\|pipeline_diagnostics.py' CLAUDE.md

#### repo_structure:doc-03 — Fix CLAUDE.md:38 to read 'lexy-app/backend/migrations/versions/' and update the count to 37+.
- severity **low**, confidence **confirmed**, domain `repo_structure`
- files: `CLAUDE.md`, `lexy-app/backend/alembic.ini`
- symbols: `CLAUDE.md:38`, `alembic.ini:2`, `CLAUDE.md §2 migrations row`
- evidence: CLAUDE.md:38 says '25+ files in alembic/versions/'. alembic.ini sets script_location=migrations; Glob confirms 37 files (head 037) at lexy-app/backend/migrations/versions/ — no alembic/versions/ directory exists anywhere. Matches db_migrations:db-05, still unresolved.
  - also reported as db_migrations:db-07: CLAUDE.md's backend-layer table says migrations live in '25+ files in alembic/versions/'. alembic.ini:2 sets script_location = migrations, and the real 37 files live at lexy-app/backend/migrations/versions/ — no alembic/versions/ directory exists anywhere in the repo.
- impact: A contributor following CLAUDE.md's own stated path when authoring a migration searches a directory that doesn't exist.
  - also reported as db_migrations:db-07: A contributor following CLAUDE.md's own stated path to find or author a migration searches a directory that does not exist.
- acceptance: CLAUDE.md's stated migrations path matches alembic.ini's script_location
- validation: grep -n 'alembic/versions' CLAUDE.md; grep script_location lexy-app/backend/alembic.ini

#### repo_structure:doc-04 — Update TESTS.md:97 to the current count/date and reconcile against ROADMAP.md's figure.
- severity **low**, confidence **confirmed**, domain `repo_structure`
- files: `docs/TESTS.md`
- symbols: `TESTS.md:97`
- evidence: TESTS.md:97 states 'Current count (2026-07-27): 261 tests across 43 files' for frontend; a 2026-08-03 static grep of it(/test( calls under lexy-app/frontend/src counts 322 across 44 files, matching ROADMAP.md's 322-as-of-2026-07-29 figure — TESTS.md's line was never updated. Matches docs_drift:doc-drift-01, still unresolved.
- impact: Frontend test count is inconsistent across CLAUDE.md's designated coverage index, obscuring whether coverage targets were met.
- acceptance: TESTS.md's 'Current count' line matches a verifiable current total/date; No contradictory per-date counts remain in TESTS.md
- validation: grep -n 'Current count' docs/TESTS.md; grep -rn 'it(\|test(' lexy-app/frontend/src --include=*.test.tsx --include=*.test.ts | wc -l

#### repo_structure:doc-06 — Update SECURITY.md:546 to point at wherever the auth-surface claim now lives, or drop the note if CL
- severity **low**, confidence **confirmed**, domain `repo_structure`
- files: `docs/SECURITY.md`, `CLAUDE.md`
- symbols: `SECURITY.md:546`
- evidence: SECURITY.md:546 says 'CLAUDE.md §7 calls /api/search... public legacy endpoints' and flags that as stale — but current CLAUDE.md has no section numbered §7 (sections run 3→5→6→8, confirmed via grep for '## 4.'/'## 7.' returning nothing) and no 'public legacy endpoint'/'video-sentences' text exists anywhere in CLAUDE.md today. New finding: the cross-reference itself is unfollowable.
- impact: A reader trying to verify SECURITY.md's own self-correction note cannot locate the CLAUDE.md section it points to.
- acceptance: SECURITY.md's cross-reference to CLAUDE.md resolves to an actual section
- validation: grep -n '^## ' CLAUDE.md; grep -n 'CLAUDE.md §7' docs/SECURITY.md

#### security_paths:doc-01 — Update ROADMAP.md:68-70 to cite docs/TESTS.md's reconciled figures (backend 1258 passed/2 skipped vi
- severity **low**, confidence **confirmed**, domain `security_paths`
- files: `docs/ROADMAP.md`
- symbols: `ROADMAP.md:68-70`
- evidence: ROADMAP.md:68-70, inside the live 'Current state' section (not the SUPERSEDED block at :392), states 'Verified on 2026-07-29 (current): backend 1151 passed / 2 skipped ... root pipeline 221 passed'. CLAUDE.md:236,241 and docs/TESTS.md:2176-2187 already reconcile this: 1151 is what the bare `pytest` entrypoint reports because it drops test_document_package.py on collection (needs `python3 -m pytest` per CLAUDE.md §11), the correct count under the documented invocation is 1258, and root grew 221→368 once the A1 evaluation harness landed later the same day. ROADMAP.md was never updated to match.
- impact: A reader who re-measures with the wrong (bare pytest) invocation reproduces 1151 and appears to 'confirm' ROADMAP.md's stale figure instead of catching the collection failure; the root-pipeline figure as stated understates coverage by 147 evaluation-harness tests.
- acceptance: ROADMAP.md's 'Current state' section no longer states 1151/221 as current without noting the invocation/date caveat, and matches or points to docs/TESTS.md's validation-commands table.
- validation: grep -n '1151 passed\|221 passed' docs/ROADMAP.md; grep -n '1258 passed\|368 passed' docs/TESTS.md CLAUDE.md

#### security_paths:doc-02 — Fix the curl example in docs/INGESTION_PIPELINE.md §5 to use package_name instead of package_path.
- severity **low**, confidence **confirmed**, domain `security_paths`
- files: `docs/INGESTION_PIPELINE.md`
- symbols: `INGESTION_PIPELINE.md:318`
- evidence: INGESTION_PIPELINE.md:318's operator curl example posts {"package_path": "<document_id>"}, but PackageImportRequest (lexy-app/backend/models/schemas.py:1018) only defines a required `package_name` field (no default) and routers/books.py:203 reads body.package_name.
- impact: An operator following the documented walkthrough verbatim gets a 422 missing-required-field error on package_name instead of a successful import.
- acceptance: INGESTION_PIPELINE.md's example field name matches PackageImportRequest.package_name.
- validation: grep -n 'package_path\|package_name' docs/INGESTION_PIPELINE.md lexy-app/backend/models/schemas.py

#### ingestion_pipeline:ing-05 — Update the '+89 backend' figure at docs/TESTS.md:956 to 107 to match the reconciled figure already p
- severity **low**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `docs/TESTS.md`
- symbols: `docs/TESTS.md:956`, `docs/TESTS.md:2174-2176`
- evidence: Line 956 states A2 landed '+89 backend' tests for test_document_package.py. Static count (grep -c 'def test_' = 96 function defs, plus 3 @pytest.mark.parametrize sites with 4/5/5 cases adding 3+4+4=11 extra collected cases = 107 total) matches the '107 tests' figure already stated at lines 2174-2176, not '+89'.
- impact: A reader trusting the stale '+89' figure under-counts test_document_package.py's actual coverage by 18 tests in the file CLAUDE.md designates as the coverage index.
- acceptance: Both figures for test_document_package.py agree in docs/TESTS.md
- validation: grep -n '89 backend\|107 tests' docs/TESTS.md

#### docs_drift:doc-02 — Update ROADMAP.md line 68 with the most recent frontend test count and date from the top of TESTS.md
- severity **info**, confidence **confirmed**, domain `docs_drift`
- files: `docs/TESTS.md`, `docs/ROADMAP.md`
- symbols: `ROADMAP.md:68`, `TESTS.md:2201`
- evidence: ROADMAP.md line 68 claims 'frontend **322 passed across 44 files**' as of 2026-07-29. TESTS.md line 2201 repeats this same figure. However, the document records intermediate test counts in between (289, 296, 306 across lines 676-754), all occurring after 2026-07-27, with 322 being the final figure on line 835 dated within the 2026-07-29 window. The claim is correct but the intermediate history shows several test additions are not reflected in the ROADMAP summary.
- impact: Readers checking ROADMAP for test coverage status get a stale intermediate figure if frontend tests have been added since 2026-07-29
- acceptance: ROADMAP 'Current state' section frontend test count matches the latest in TESTS.md
- validation: Grep TESTS.md for the most recent 'frontend.*passed' claim and cross-check ROADMAP.md line 68

#### docs_drift:doc-04 — Clarify by either (a) moving N5b to 'Remaining work' section with explicit note that it is independe
- severity **info**, confidence **confirmed**, domain `docs_drift`
- files: `docs/ROADMAP.md`
- symbols: `ROADMAP.md:126-141`
- evidence: ROADMAP.md N5a claims status '✅ SHIPPED 2026-07-28/29' and 'What remains from this line of work is tracked separately, not here'. However, the next item, N5b, immediately following in ROADMAP, is titled 'Case-variant catalog duplication' and explicitly labeled as 'code, ready to implement' (line 264), appearing to contradict the 'remains' framing if N5b is part of the same N5a line of work. Reading the text, N5b is presented as separate implementation, but the organizational framing suggests N5a work is fully tracked separately.
- impact: Low — the separation is correct (N5b is unblocked and independent), but the phrasing 'tracked separately, not here' is misleading since N5b immediately follows in the ROADMAP under the same N5 category
- acceptance: A reader understands that N5b is independent and can be implemented regardless of N5a status
- validation: Read ROADMAP.md lines 126-265 and trace the logical relationship between N5a and N5b

#### repo_structure:doc-05 — Update the cited line numbers in SECURITY.md and CLAUDE.md §14 to 164/205/322, or drop line numbers 
- severity **info**, confidence **confirmed**, domain `repo_structure`
- files: `docs/SECURITY.md`, `CLAUDE.md`, `lexy-app/backend/services/search_service.py`
- symbols: `search_service.py:164,205,322`
- evidence: SECURITY.md and CLAUDE.md §14's self-check grep both still say 'Expected hits: search_service.py:62,103,216'; re-running that exact grep on 2026-08-03 shows the three (still-safe) f-string SQL sites now at lines 164, 205, 322. Matches security_paths:doc-01, still unresolved.
- impact: Someone re-running the documented grep and eyeballing line numbers against the stale reference could be momentarily unsure whether the hits match the pre-verified-safe set.
- acceptance: SECURITY.md and CLAUDE.md no longer cite stale line numbers for this pattern
- validation: rg -n 'f"(SELECT|INSERT|UPDATE|DELETE)' lexy-app/backend --glob '!**/migrations/**'


## Missing regression protection (4)
#### repo_structure:ci-01 — Add a NEW .github/workflows/tests.yml running `ruff check .`, backend `python3 -m pytest -n auto`, r
- severity **high**, confidence **confirmed**, domain `repo_structure`
- files: `.github/workflows/tests.yml`, `.github/workflows/dependency-audit.yml`
- symbols: `.github/workflows/`
- evidence: Glob '.github/workflows/*.yml' still returns only dependency-audit.yml on 2026-08-03 re-check; CLAUDE.md §11 calls ruff+pytest 'part of backend validation, not an optional extra.' Collapses 5+ duplicate prior write-ups (ci-01, arch-01, arch-02, test-01 x3) into one canonical finding.
- impact: A PR that fails ruff, breaks a backend/frontend/root/autoloop test, or breaks the frontend build can merge to main with a fully green required-checks state.
- acceptance: tests.yml exists and runs on PRs to main; A PR with a failing test or ruff violation shows a failing required check
- validation: ls -la .github/workflows/; cat .github/workflows/tests.yml

#### tests_ci:test-01 — Wire test suites into CI: add a GitHub Actions job that runs `cd lexy-app/backend && python3 -m pyte
- severity **medium**, confidence **confirmed**, domain `tests_ci`
- files: `.github/workflows/dependency-audit.yml`, `docs/TESTS.md`
- evidence: docs/TESTS.md inventories 1122 backend test functions across 54 test files (section 'Existing tests'), 261 frontend tests across 43 files (line 99), and ~221 root-pipeline tests (per docs/SUMMARY.md line 200). No CI workflow invokes any of them. The dependency-audit workflow (lines 19–52) runs only pip-audit and npm audit.
- impact: A test suite documented as mandatory (CLAUDE.md §11: 'run this as part of every backend change') is never run by CI. A breaking change to database schema, a flaky test that only fails intermittently, or a regression in test isolation (xdist, cleanup patterns) can land on main without detection.
- acceptance: Backend test job exists in .github/workflows/ and gates main; Exit code 0 is required for main branch; A single failing test blocks the job and the PR
- validation: cd lexy-app/backend && python3 -m pytest -n auto --co -q | wc -l; grep -A 20 'lexy-app/backend' .github/workflows/*.yml | grep -c pytest

#### ingestion_pipeline:ing-03 — Rewrite the imports in test_document_package.py to `from backend.services...`/`from backend.services
- severity **medium**, confidence **confirmed**, domain `ingestion_pipeline`
- files: `lexy-app/backend/tests/test_document_package.py`
- symbols: `test_document_package.py:13-43`
- evidence: Line 13 is `from services import book_import_service` and lines 14-35 use bare `from services.document_package import ...` — the only backend test file not using `from backend.services import ...` (grep 'from services\.' across tests/ matches only this file). CLAUDE.md §11 and docs/COMMON_ERRORS.md §2 already document this exact trap as an open follow-up.
- impact: Running the bare `pytest` console script (instead of `python3 -m pytest`) fails to put the repo cwd on sys.path, so this file's 107 tests — all of A2's test coverage — are silently dropped from collection with no clear error in a large test run.
- acceptance: File uses from backend.services... imports throughout; `cd lexy-app/backend && pytest tests/test_document_package.py -q` (bare console script) collects and passes all tests
- validation: cd lexy-app/backend && pytest tests/test_document_package.py -q; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -q

#### repo_structure:test-01 — Align test_document_package.py's imports to 'from backend.services...' so both invocation forms coll
- severity **low**, confidence **confirmed**, domain `repo_structure`
- files: `lexy-app/backend/tests/test_document_package.py`
- symbols: `test_document_package.py:13-23`
- evidence: test_document_package.py:13-23 still uses bare 'from services import ...' — confirmed unchanged 2026-08-03 — while every other backend test file uses 'from backend.services import ...'. Documented in CLAUDE.md §11 / COMMON_ERRORS.md §2 as a known trap. Matches ingestion_pipeline:imp-01, still open.
- impact: Running bare `pytest` instead of `python3 -m pytest` silently drops this file's ~107 tests from collection, so a contributor on the wrong entrypoint may believe an untested change is safe.
- acceptance: File uses from backend.services... imports; bare 'pytest tests/test_document_package.py' collects and passes from lexy-app/backend
- validation: cd lexy-app/backend && pytest tests/test_document_package.py -q


## Optional improvements (2)
#### db_migrations:db-04 — Add a new append-only migration creating idx_content_request_user_created ON content_request (user_i
- severity **low**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/routers/content_requests.py`, `lexy-app/backend/migrations/versions/018_content_request.py`, `lexy-app/backend/migrations/versions/029_content_request_per_user_unique.py`
- symbols: `content_requests.py:list_requests:171-185`, `018_content_request.py:30`, `029_content_request_per_user_unique.py:60-68`
- evidence: list_requests runs SELECT * FROM content_request WHERE user_id=$1 ORDER BY created_at DESC (content_requests.py:177-184). 018:30 indexes only (status); 029's UNIQUE(user_id, request_type, content_id) supports the equality filter but not the created_at DESC sort.
- impact: The one user-facing query on this table has no index covering both its filter and sort; degrades to a sort-after-scan as each user's request history grows.
- acceptance: EXPLAIN for the list_requests query shows an index scan rather than an unassisted sort
- validation: psql "$DATABASE_URL" -c "EXPLAIN SELECT * FROM content_request WHERE user_id='00000000-0000-0000-0000-000000000000' ORDER BY created_at DESC"

#### db_migrations:db-06 — In a new migration, DROP the redundant single-column index once EXPLAIN confirms the composite UNIQU
- severity **low**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/001_initial_user_tables.py`
- symbols: `001_initial_user_tables.py:43`, `001_initial_user_tables.py:109`
- evidence: Line 43's UNIQUE (user_id, item_id, item_type) already creates a btree index with user_id leading; line 109 creates a separate single-column index on user_id, redundant for any WHERE user_id = $1 query since it can use the composite index's prefix.
- impact: Every write to user_word_knowledge, one of the app's hottest per-event tables, maintains one unnecessary extra index for the table's lifetime.
- acceptance: EXPLAIN for WHERE user_id = $1 on user_word_knowledge still uses an index scan after the redundant index is dropped
- validation: psql "$DATABASE_URL" -c '\di+ user_word_knowledge*'


## Human decisions needed (1)
#### db_migrations:db-01 — Human decision needed: obtain an authoritative pg_dump --schema-only of the 10 content-core tables f
- severity **critical**, confidence **confirmed**, domain `db_migrations`
- files: `lexy-app/backend/migrations/versions/006_video_channel_genre.py`, `docs/COMMON_ERRORS.md`, `docs/SCHEMA.md`
- symbols: `006_video_channel_genre.py:upgrade:16-20`, `docs/COMMON_ERRORS.md:876-905`
- evidence: grep 'CREATE TABLE' across all 37 files in migrations/versions/ never creates video, video_category, user_video_category, language_table, word_table, phrase_blueprint, sentence, word_to_sentence, sentence_to_phrase, sentence_to_grammar_rule; 006:16 ALTER TABLE video ADD COLUMN is the first reference to any of them. docs/COMMON_ERRORS.md:876-905 documents the root cause: a root-level table.sql used to own these tables and was deleted in commit 25a194d; its last snapshot already reflects the 013-022 channel-refactor shape, so replaying it against today's chain now conflicts on existing constraints.
- impact: alembic upgrade head fails on any empty/CI/DR/new-contributor Postgres DB at revision 006 with relation "video" does not exist; the schema cannot currently be rebuilt from the repo by any documented path.
- acceptance: createdb test_db && alembic upgrade head against an empty Postgres DB exits 0 and creates all 30+ tables; docs/SCHEMA.md no longer claims migrations/versions/ alone is authoritative for these tables
- validation: rg -n 'CREATE TABLE' lexy-app/backend/migrations/versions | wc -l; createdb lexy_schema_check && alembic -c lexy-app/backend/alembic.ini upgrade head; echo $?


## Rejected items (0)
(none)

## Deduplicated (1)
- `db_migrations:db-07` folded into `repo_structure:doc-03`

## Proposed task graph (33 tasks)
To adopt this graph, reply with a `plan` decision containing the tasks below (edit freely — this is a proposal):
```json
[
  {
    "id": "au-001",
    "title": "Add GitHub Actions workflows that run (1) backend: `cd lexy-app/backend && python3 -m pytest -n a...",
    "description": "Priority: P1 | severity high | confidence confirmed | from audit finding tests_ci:ci-01\nScope: Add GitHub Actions workflows that run (1) backend: `cd lexy-app/backend && python3 -m pytest -n auto` + ruff check; (2) frontend: `cd lexy-app/frontend && npm run test && npm run build`; (3) root pipeline: `cd /repo && pytest tests/` at repo root; gate main on all three.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Test failures, lint violations, type errors, and migration failures reach main without detection. A pushed change can corrupt the database schema, break every test, or regress security \u2014 then pass CI.\nEvidence: .github/workflows/dependency-audit.yml contains ONLY pip-audit and npm audit jobs (lines 20\u201352), with no pytest, coverage, linting, or TypeScript compilation gates. CLAUDE.md \u00a711 documents three test suites (backend, frontend, root pipeline) that must be run before any change is complete; docs/TESTS.md inventories 1122 backend tests across 54 files, 261 frontend tests across 43 files, 221 root-pipeline tests. None are invoked by CI.\nExpected files: .github/workflows/dependency-audit.yml\nAcceptance criteria: CI blocks a failing backend test from reaching main; CI blocks a failing frontend test from reaching main; CI blocks a ruff violation from reaching main; CI blocks a TypeScript error from reaching main\nValidation: ls .github/workflows/; grep -c 'pytest\\|npm.*test\\|tsc' .github/workflows/*.yml\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-002",
    "title": "Guard downgrade() to refuse when reading_selections rows hold non-empty anchors referencing book_...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding db_migrations:db-02\nScope: Guard downgrade() to refuse when reading_selections rows hold non-empty anchors referencing book_blocks tokens (matching migration 020's abort-on-unmatched-row precedent), or persist/restore original token_ids instead of regenerating.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A downgrade-then-upgrade of migration 025 regenerates every token_id, silently orphaning every existing reading_selections.anchors reference with no error and no data-loss warning.\nEvidence: _tokenize calls uuid.uuid4() fresh per token on every run (21-27); downgrade() is a bare DROP COLUMN tokens with no guard (64-65). reading_selections.anchors (009:31-32) stores {block_id, token_id, surface} with no FK, referencing those same UUIDs.\nExpected files: lexy-app/backend/migrations/versions/025_block_token_ids.py, lexy-app/backend/migrations/versions/009_reading_selections.py\nAcceptance criteria: downgrade() raises when orphaning anchors would occur, or a round-trip test proves token_ids survive downgrade+upgrade\nValidation: cat lexy-app/backend/migrations/versions/025_block_token_ids.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-003",
    "title": "Add a structural validator comparing bbox['page'] (when present) to element['page_index'], adding...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding ingestion_pipeline:ing-01\nScope: Add a structural validator comparing bbox['page'] (when present) to element['page_index'], adding a stable issue code on mismatch; fix the disagreeing 11/12 example in INGESTION_PIPELINE.md \u00a74.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A worker bug or corrupted package where bbox.page disagrees with element.page_index passes every validation gate silently; inert while nothing reads bbox.page today, but a live trap if a future consumer (A3 persistence, a review UI) ever keys off bbox.page instead of element.page_index.\nEvidence: _BBOX_KEYS=('x1','y1','x2','y2') never includes 'page'; validate_bboxes:351-352 looks up page dims via element.get('page_index') only, never bbox.get('page'). Grep confirms no file under document_package/ reads bbox['page']. The doc's own \u00a74 worked example shows page_index:11 with bbox.page:12 in the same element, still unreconciled.\nExpected files: lexy-app/backend/services/document_package/validators.py, docs/INGESTION_PIPELINE.md\nAcceptance criteria: A synthetic element with bbox.page != page_index produces a new stable validation issue code; INGESTION_PIPELINE.md \u00a74 example has bbox.page == page_index\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k Structural -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-004",
    "title": "Add a structural validator checking manifest.page_dims keys form a contiguous 1..page_count range...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding ingestion_pipeline:ing-02\nScope: Add a structural validator checking manifest.page_dims keys form a contiguous 1..page_count range (or match manifest.counts.pages), flagging any gap as fatal.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A whole page silently vanishing from the package \u2014 the worst-case failure per \u00a72b's own stated priority order \u2014 produces zero fatal/warning issues and imports as if the book were complete.\nEvidence: validate_page_indices only fires (fatal, undeclared_page) when an *element* references a page missing from manifest.page_dims. If a worker bug drops a page's page_dims entry AND every element on that page, no element ever mentions the missing page, so nothing flags it. No validator cross-checks manifest.counts.pages against len(page_dims) or checks page_dims keys form a contiguous 1..N range, even though INGESTION_PIPELINE.md \u00a75 lists 'page_index contiguous' among its five promised structural checks.\nExpected files: lexy-app/backend/services/document_package/validators.py, lexy-app/backend/services/document_package/result.py\nAcceptance criteria: A synthetic package whose page_dims skips a page number (with no elements referencing it) fails validation with a new stable issue code; manifest.counts.pages mismatch against len(page_dims) is also caught\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k Structural -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-005",
    "title": "Refuse downgrade() when any reading_selections row has anchors referencing existing tokens, or pe...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding repo_structure:data-01\nScope: Refuse downgrade() when any reading_selections row has anchors referencing existing tokens, or persist/restore original token_ids instead of regenerating.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: downgrade then upgrade on migration 025 regenerates every token_id, silently orphaning every existing reading_selections.anchors reference with no error surfaced.\nEvidence: _tokenize (21-27) calls uuid.uuid4() fresh per token every run; downgrade() (64-65) is a bare `ALTER TABLE book_blocks DROP COLUMN tokens`, unchanged on 2026-08-03 re-read. SCHEMA.md documents reading_selections.anchors as [{block_id, token_id, surface}] with no FK. Matches prior db_migrations:db-02, still unresolved.\nExpected files: lexy-app/backend/migrations/versions/025_block_token_ids.py, docs/SCHEMA.md\nAcceptance criteria: downgrade() raises on would-be orphaning, or the round-trip preserves token_ids; A regression test exercises the round-trip against a reading_selections row\nValidation: cat lexy-app/backend/migrations/versions/025_block_token_ids.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-006",
    "title": "Add a structural validator flagging bbox['page'] != element['page_index'] with a stable issue cod...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding repo_structure:ing-01\nScope: Add a structural validator flagging bbox['page'] != element['page_index'] with a stable issue code; fix the disagreeing 11/12 example in INGESTION_PIPELINE.md \u00a74.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A worker bug or corrupted package with bbox.page != element.page_index passes every validation gate silently \u2014 a live trap once roadmap A3 wires real persistence.\nEvidence: Grep for 'page' in validators.py (2026-08-03) shows no bbox['page'] read anywhere; validate_bboxes:351-352 keys page_dims only off element['page_index']. INGESTION_PIPELINE.md \u00a74's own worked example (lines 238-246) still shows page_index:11 vs bbox.page:12 in one element. Collapses db_migrations:ing-01 + ingestion_pipeline:def-01, still unresolved.\nExpected files: lexy-app/backend/services/document_package/validators.py, docs/INGESTION_PIPELINE.md\nAcceptance criteria: Synthetic element with mismatched page fails validation with a new issue code; INGESTION_PIPELINE.md \u00a74 example has matching values\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k Structural\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-007",
    "title": "Add `ruff check .` to the CI workflow, gating main. Run from repo root (covers backend, scraper, ...",
    "description": "Priority: P1 | severity medium | confidence confirmed | from audit finding tests_ci:lint-01\nScope: Add `ruff check .` to the CI workflow, gating main. Run from repo root (covers backend, scraper, root pipeline, scripts in one pass per ruff.toml line 3). Fail if any checks are reported.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A new ruff violation lands on main without detection, silently raising the baseline. The documented baseline (zero findings) degrades undetected. Developers rely on running `ruff check .` locally before pushing, which is error-prone and not enforced.\nEvidence: CLAUDE.md \u00a711 ('Lint') mandates `ruff check .` from repo root as part of every backend change, with a baseline of 0 findings as of 2026-07-27 (documented in ruff.toml comments). CI workflow contains no ruff invocation. Ruff is configured in ruff.toml (pinned to 0.14.1 in requirements.txt) and can run, but is never gated by CI.\nExpected files: .github/workflows/dependency-audit.yml, CLAUDE.md\nAcceptance criteria: A ruff violation on the working branch causes CI to fail; CI passes only when `ruff check .` at repo root reports zero findings\nValidation: ruff check . --select E,F; grep -c ruff .github/workflows/dependency-audit.yml\nParallelizable: yes",
    "depends_on": [
      "au-001"
    ]
  },
  {
    "id": "au-008",
    "title": "Add a one-line comment in downgrade() documenting that the restored value is a placeholder defaul...",
    "description": "Priority: P1 | severity low | confidence confirmed | from audit finding db_migrations:db-03\nScope: Add a one-line comment in downgrade() documenting that the restored value is a placeholder default, not a restoration of original data.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Downgrading migration 013 silently fabricates data for a column that appears to be restored, misleading anyone relying on downgrade for a real rollback.\nEvidence: upgrade() drops video_category.language without saving prior per-row values (39-43); downgrade() line 82 re-adds it via DEFAULT 'de' for every row, undocumented as a fabricated placeholder, unlike sibling migrations 022/037 which fail loudly for the same class of change.\nExpected files: lexy-app/backend/migrations/versions/013_video_category_cleanup.py\nAcceptance criteria: downgrade() carries a comment documenting the fabricated value\nValidation: grep -n -B1 \"DEFAULT 'de'\" lexy-app/backend/migrations/versions/013_video_category_cleanup.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-009",
    "title": "Add Depends(require_admin) (or an equivalent is_admin check) to GET /books/packages and POST /boo...",
    "description": "Priority: P2 | severity high | confidence confirmed | from audit finding ingestion_pipeline:ing-04\nScope: Add Depends(require_admin) (or an equivalent is_admin check) to GET /books/packages and POST /books/import, matching the delete_book/delete_page precedent in the same file, and add both routes to docs/SECURITY.md's admin-gated list.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any authenticated non-admin user can enumerate every server-side ingestion package name and force full checksum/structural validation of any of them on demand \u2014 information disclosure plus an unmetered compute-cost surface open to the whole user base, and it becomes a data-integrity issue once A3 wires real persistence.\nEvidence: Both routes depend only on Depends(get_current_user); delete_book/delete_page in the same file (books.py:376-402) pass is_admin=user.get('is_admin', False) for comparison. test_document_package.py:984-1034 (TestImportEndpoint) asserts only 401/403 for an unauthenticated caller, never that an authenticated non-admin is refused. docs/SECURITY.md's admin-gated-routes list (line 543) omits both routes.\nExpected files: lexy-app/backend/routers/books.py, docs/SECURITY.md\nAcceptance criteria: Non-admin authenticated user gets 403 admin_required from both endpoints; Admin user's existing dry-run workflow unaffected; docs/SECURITY.md admin-gated routes list includes both routes\nValidation: cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -k TestImportEndpoint -q; rg -n 'require_admin' lexy-app/backend/routers/books.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-010",
    "title": "Add Depends(require_admin) to both routes (mirroring /phrases/seed); add test_books_import_admin....",
    "description": "Priority: P2 | severity high | confidence confirmed | from audit finding repo_structure:sec-01\nScope: Add Depends(require_admin) to both routes (mirroring /phrases/seed); add test_books_import_admin.py; add the route pair to SECURITY.md's admin-gated-routes list.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any authenticated non-admin user can enumerate every operator-curated package name and force validation of any of them \u2014 info disclosure plus an uncapped compute-cost surface open to the whole user base.\nEvidence: books.py:182-212 depend only on Depends(get_current_user); no require_admin, unlike /phrases/seed (S17) and delete_book/delete_page. SECURITY.md:543's admin-gated-routes list still omits both routes on 2026-08-03 re-check. Collapses 4 duplicate prior write-ups (db_migrations:sec-01, security_paths:sec-01, ingestion_pipeline:sec-01, seed_tasks.json rt-01) into one finding \u2014 still unresolved.\nExpected files: lexy-app/backend/routers/books.py, docs/SECURITY.md\nAcceptance criteria: Non-admin gets 403 admin_required from both endpoints; Admin behavior unchanged; SECURITY.md lists both routes as admin-gated\nValidation: rg -n 'require_admin' lexy-app/backend/routers/books.py; cd lexy-app/backend && python3 -m pytest tests/test_books_import_admin.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-011",
    "title": "Gate both routes with Depends(require_admin), matching /phrases/seed and /admin/lemma-corrections...",
    "description": "Priority: P2 | severity medium | confidence confirmed | from audit finding security_paths:sec-01\nScope: Gate both routes with Depends(require_admin), matching /phrases/seed and /admin/lemma-corrections/*; note in import_package's docstring that user_id is not yet an ownership scope, so roadmap A3 must add one before real persistence.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any signed-up learner (registration is open per SECURITY.md S2) can enumerate operator-ingested package names and read validation metadata (document_id, schema/profile, checksum results, structural issue text) for packages they did not create.\nEvidence: routers/books.py:182-212 gates both routes with only Depends(get_current_user) \u2014 no require_admin (contrast lemma_corrections.py:64,90,117,150 and phrases.py:36) and no ownership filter; document_package/loader.py:25 is a single shared package root with no per-user split, and contract.py:95-98 carries no owner field; docs/INGESTION_PIPELINE.md:306 labels the workflow operator-only; test_document_package.py:987,1033 assert only that some token is required, never admin or ownership.\nExpected files: lexy-app/backend/routers/books.py, lexy-app/backend/services/book_import_service.py, lexy-app/backend/services/document_package/loader.py, docs/SECURITY.md\nAcceptance criteria: A non-admin authenticated user gets 403 admin_required from GET /books/packages and POST /books/import; an admin still gets today's 200 behavior.; tests/test_document_package.py gains a regression test asserting the 403 for a non-admin caller.; docs/SECURITY.md records this finding with file:line, severity, verification check, and suggested fix per CLAUDE.md \u00a714.; The admin-gated-routes list at docs/SECURITY.md:543 names both /books/packages and /books/import.\nValidation: cd lexy-app/backend && MOCK_LLM=true python3 -m pytest tests/test_document_package.py -q; ruff check .; rg -n 'require_admin' lexy-app/backend/routers/books.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-012",
    "title": "Move the Docker build context to repo root (COPY subtitle-scraper/ + data/ alongside backend/), o...",
    "description": "Priority: P4 | severity critical | confidence confirmed | from audit finding repo_structure:arch-01\nScope: Move the Docker build context to repo root (COPY subtitle-scraper/ + data/ alongside backend/), or make the phrase_finder import lazy/optional with a clear degraded-mode failure; add a build-and-boot smoke test.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A container built from lexy-app/Dockerfile crashes on boot with ModuleNotFoundError: phrase_finder \u2014 uvicorn never starts, backend unreachable for any docker-compose deployment.\nEvidence: docker-compose.yml:17 `build: .` (context lexy-app/); Dockerfile:21-31 COPYs only backend/ + built frontend, never subtitle-scraper/ or data/ (both at repo root, outside context). matcher_service.py:35 `Path(__file__).resolve().parents[3]/'subtitle-scraper'` \u2014 in-image (WORKDIR /app, COPY backend/ ./backend/) parents[3] is '/', so path doesn't exist; line 43 `import phrase_finder` at module scope, pulled in by routers/phrases.py & routers/matcher.py before main.py builds `app=FastAPI()`.\nExpected files: lexy-app/Dockerfile, lexy-app/docker-compose.yml, lexy-app/backend/services/matcher_service.py, lexy-app/backend/main.py, lexy-app/backend/routers/phrases.py, lexy-app/backend/routers/matcher.py\nAcceptance criteria: docker compose up boots without a startup crash; CI builds the image and hits a health endpoint successfully\nValidation: cd lexy-app && docker compose build && docker compose up -d && curl -f http://localhost:8000/api/v1/auth/login -X POST -d '{}' -H 'Content-Type: application/json'\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-013",
    "title": "Document the ACCESS EXCLUSIVE trade-off for GENERATED STORED columns and index builds on high-gro...",
    "description": "Priority: P4 | severity medium | confidence probable | from audit finding db_migrations:db-05\nScope: Document the ACCESS EXCLUSIVE trade-off for GENERATED STORED columns and index builds on high-growth tables, and add per-migration autocommit_block() support (op.get_context().autocommit_block()) for future non-blocking index builds.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Any future schema change to word_usage_events, or another high-growth table, blocks all reads/writes for the rewrite/index-build duration, with no structural way under the current env.py to opt into a non-blocking build.\nEvidence: env.py wraps every migration in context.begin_transaction() (lines 45 and 58); Postgres refuses CREATE INDEX CONCURRENTLY inside a transaction. Migration 026 adds a GENERATED ALWAYS ... STORED column (full-table rewrite) and a non-concurrent CREATE UNIQUE INDEX on word_usage_events, the fire-and-forget sink for every transcript click, chat turn, and SRS review (CLAUDE.md \u00a78b).\nExpected files: lexy-app/backend/migrations/env.py, lexy-app/backend/migrations/versions/026_transcript_click_dedup.py\nAcceptance criteria: Migration authoring guidance documents the trade-off and the autocommit_block() escape hatch\nValidation: grep -n 'begin_transaction' lexy-app/backend/migrations/env.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-014",
    "title": "Add a NEW .github/workflows/tests.yml running `ruff check .`, backend `python3 -m pytest -n auto`...",
    "description": "Priority: P5 | severity high | confidence confirmed | from audit finding repo_structure:ci-01\nScope: Add a NEW .github/workflows/tests.yml running `ruff check .`, backend `python3 -m pytest -n auto`, root `pytest tests/`, and frontend `npm run test`+`npm run build` on push/PR; leave dependency-audit.yml untouched.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A PR that fails ruff, breaks a backend/frontend/root/autoloop test, or breaks the frontend build can merge to main with a fully green required-checks state.\nEvidence: Glob '.github/workflows/*.yml' still returns only dependency-audit.yml on 2026-08-03 re-check; CLAUDE.md \u00a711 calls ruff+pytest 'part of backend validation, not an optional extra.' Collapses 5+ duplicate prior write-ups (ci-01, arch-01, arch-02, test-01 x3) into one canonical finding.\nExpected files: .github/workflows/tests.yml, .github/workflows/dependency-audit.yml\nAcceptance criteria: tests.yml exists and runs on PRs to main; A PR with a failing test or ruff violation shows a failing required check\nValidation: ls -la .github/workflows/; cat .github/workflows/tests.yml\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-015",
    "title": "Rewrite the imports in test_document_package.py to `from backend.services...`/`from backend.servi...",
    "description": "Priority: P5 | severity medium | confidence confirmed | from audit finding ingestion_pipeline:ing-03\nScope: Rewrite the imports in test_document_package.py to `from backend.services...`/`from backend.services.document_package...`, matching every other backend test file.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Running the bare `pytest` console script (instead of `python3 -m pytest`) fails to put the repo cwd on sys.path, so this file's 107 tests \u2014 all of A2's test coverage \u2014 are silently dropped from collection with no clear error in a large test run.\nEvidence: Line 13 is `from services import book_import_service` and lines 14-35 use bare `from services.document_package import ...` \u2014 the only backend test file not using `from backend.services import ...` (grep 'from services\\.' across tests/ matches only this file). CLAUDE.md \u00a711 and docs/COMMON_ERRORS.md \u00a72 already document this exact trap as an open follow-up.\nExpected files: lexy-app/backend/tests/test_document_package.py\nAcceptance criteria: File uses from backend.services... imports throughout; `cd lexy-app/backend && pytest tests/test_document_package.py -q` (bare console script) collects and passes all tests\nValidation: cd lexy-app/backend && pytest tests/test_document_package.py -q; cd lexy-app/backend && python3 -m pytest tests/test_document_package.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-016",
    "title": "Wire test suites into CI: add a GitHub Actions job that runs `cd lexy-app/backend && python3 -m p...",
    "description": "Priority: P5 | severity medium | confidence confirmed | from audit finding tests_ci:test-01\nScope: Wire test suites into CI: add a GitHub Actions job that runs `cd lexy-app/backend && python3 -m pytest -n auto` and gates main on the exit code. Backend tests are the highest risk (schema changes, state mutations); frontend and root-pipeline tests should follow.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A test suite documented as mandatory (CLAUDE.md \u00a711: 'run this as part of every backend change') is never run by CI. A breaking change to database schema, a flaky test that only fails intermittently, or a regression in test isolation (xdist, cleanup patterns) can land on main without detection.\nEvidence: docs/TESTS.md inventories 1122 backend test functions across 54 test files (section 'Existing tests'), 261 frontend tests across 43 files (line 99), and ~221 root-pipeline tests (per docs/SUMMARY.md line 200). No CI workflow invokes any of them. The dependency-audit workflow (lines 19\u201352) runs only pip-audit and npm audit.\nExpected files: .github/workflows/dependency-audit.yml, docs/TESTS.md\nAcceptance criteria: Backend test job exists in .github/workflows/ and gates main; Exit code 0 is required for main branch; A single failing test blocks the job and the PR\nValidation: cd lexy-app/backend && python3 -m pytest -n auto --co -q | wc -l; grep -A 20 'lexy-app/backend' .github/workflows/*.yml | grep -c pytest\nParallelizable: yes",
    "depends_on": [
      "au-001"
    ]
  },
  {
    "id": "au-017",
    "title": "Align test_document_package.py's imports to 'from backend.services...' so both invocation forms c...",
    "description": "Priority: P5 | severity low | confidence confirmed | from audit finding repo_structure:test-01\nScope: Align test_document_package.py's imports to 'from backend.services...' so both invocation forms collect identically.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Running bare `pytest` instead of `python3 -m pytest` silently drops this file's ~107 tests from collection, so a contributor on the wrong entrypoint may believe an untested change is safe.\nEvidence: test_document_package.py:13-23 still uses bare 'from services import ...' \u2014 confirmed unchanged 2026-08-03 \u2014 while every other backend test file uses 'from backend.services import ...'. Documented in CLAUDE.md \u00a711 / COMMON_ERRORS.md \u00a72 as a known trap. Matches ingestion_pipeline:imp-01, still open.\nExpected files: lexy-app/backend/tests/test_document_package.py\nAcceptance criteria: File uses from backend.services... imports; bare 'pytest tests/test_document_package.py' collects and passes from lexy-app/backend\nValidation: cd lexy-app/backend && pytest tests/test_document_package.py -q\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-018",
    "title": "Add a new append-only migration creating idx_content_request_user_created ON content_request (use...",
    "description": "Priority: P6 | severity low | confidence confirmed | from audit finding db_migrations:db-04\nScope: Add a new append-only migration creating idx_content_request_user_created ON content_request (user_id, created_at DESC).\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: The one user-facing query on this table has no index covering both its filter and sort; degrades to a sort-after-scan as each user's request history grows.\nEvidence: list_requests runs SELECT * FROM content_request WHERE user_id=$1 ORDER BY created_at DESC (content_requests.py:177-184). 018:30 indexes only (status); 029's UNIQUE(user_id, request_type, content_id) supports the equality filter but not the created_at DESC sort.\nExpected files: lexy-app/backend/routers/content_requests.py, lexy-app/backend/migrations/versions/018_content_request.py, lexy-app/backend/migrations/versions/029_content_request_per_user_unique.py\nAcceptance criteria: EXPLAIN for the list_requests query shows an index scan rather than an unassisted sort\nValidation: psql \"$DATABASE_URL\" -c \"EXPLAIN SELECT * FROM content_request WHERE user_id='00000000-0000-0000-0000-000000000000' ORDER BY created_at DESC\"\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-019",
    "title": "In a new migration, DROP the redundant single-column index once EXPLAIN confirms the composite UN...",
    "description": "Priority: P6 | severity low | confidence confirmed | from audit finding db_migrations:db-06\nScope: In a new migration, DROP the redundant single-column index once EXPLAIN confirms the composite UNIQUE index alone services user_id-only lookups.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Every write to user_word_knowledge, one of the app's hottest per-event tables, maintains one unnecessary extra index for the table's lifetime.\nEvidence: Line 43's UNIQUE (user_id, item_id, item_type) already creates a btree index with user_id leading; line 109 creates a separate single-column index on user_id, redundant for any WHERE user_id = $1 query since it can use the composite index's prefix.\nExpected files: lexy-app/backend/migrations/versions/001_initial_user_tables.py\nAcceptance criteria: EXPLAIN for WHERE user_id = $1 on user_word_knowledge still uses an index scan after the redundant index is dropped\nValidation: psql \"$DATABASE_URL\" -c '\\di+ user_word_knowledge*'\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-020",
    "title": "Rewrite the \u00a78b sentence to describe the actual path: status_override passed into apply_progressi...",
    "description": "Priority: P7 | severity medium | confidence confirmed | from audit finding repo_structure:doc-01\nScope: Rewrite the \u00a78b sentence to describe the actual path: status_override passed into apply_progression inside its own transaction; drop the upsert_word_status reference.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A reader of \u00a78b \u2014 the file's own recommended starting point (\u00a713) \u2014 is misled into believing a deleted function still gates status writes.\nEvidence: Current CLAUDE.md \u00a78b still reads the status flip is 'forced to known by word_service.upsert_word_status (called from the router before apply_progression)', while \u00a710/\u00a712 of the same file say that function 'has been removed'/'is gone', and grep 'def upsert_word_status' across services/*.py returns nothing. Matches db_migrations:doc-01, still unresolved.\nExpected files: CLAUDE.md\nAcceptance criteria: CLAUDE.md no longer mentions upsert_word_status; Description matches routers/words.py's actual apply_progression(..., status_override=...) call\nValidation: grep -n upsert_word_status CLAUDE.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-021",
    "title": "Remove the conftest.py reference from the SUMMARY.md entry for pytest.ini. Replace with accurate ...",
    "description": "Priority: P7 | severity medium | confidence confirmed | from audit finding tests_ci:doc-01\nScope: Remove the conftest.py reference from the SUMMARY.md entry for pytest.ini. Replace with accurate description of how paths are managed: `python -m pytest` adds cwd to sys.path (CLAUDE.md \u00a711).\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: New contributors read the documentation and look for a conftest.py that doesn't exist, wasting time. The stated mechanism (sys.path manipulation in conftest) is never explained and the actual mechanism (sys.path implicit from `python -m pytest`, documented in CLAUDE.md \u00a711 and COMMON_ERRORS.md \u00a71) is not obvious from SUMMARY.md.\nEvidence: SUMMARY.md line 349 states `pytest.ini`, `conftest.py` \u2014 Pytest config; conftest adds `src/` to path.` No root conftest.py exists (Glob found only autoloop/tests/conftest.py, lexy-app/backend/tests/conftest.py, tests/evaluation/conftest.py). Root pytest.ini at line 7 says `testpaths = tests` and declares no path manipulation.\nExpected files: docs/SUMMARY.md\nAcceptance criteria: SUMMARY.md line 349 no longer mentions a non-existent conftest.py\nValidation: grep -n 'conftest adds' docs/SUMMARY.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-022",
    "title": "In TESTS.md line 2176, change to: '1151 passed, 2 skipped [+107 from A2 collected via sys.path im...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding docs_drift:doc-01\nScope: In TESTS.md line 2176, change to: '1151 passed, 2 skipped [+107 from A2 collected via sys.path import]' or add a sidenote clarifying the conditional count.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Reader confusion: the same validation command produces 1151 or 1258 depending on pytest invocation method, but docs don't lead with that fact in the baseline expectations\nEvidence: TESTS.md line 2176 claims validation baseline is '1258 passed, 2 skipped', but lines 2180-2187 explain this includes 107 tests from test_document_package.py (A2) that only load with 'python3 -m pytest' due to sys.path handling. The doc notes the true baseline is 1151 (without A2), making '1258' a conditional count. This is correctly documented but the phrasing could be clearer for users seeing 1151 in ROADMAP.md or earlier TESTS.md sections.\nExpected files: docs/TESTS.md\nAcceptance criteria: Baseline expectation in validation commands is unambiguous about which test files are included\nValidation: Read TESTS.md lines 2180-2187 for the explanation vs line 2176 for the expectation\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-023",
    "title": "Update TESTS.md line 2260 to reference the current frontend baseline: '(322 tests across 44 files)'",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding docs_drift:doc-03\nScope: Update TESTS.md line 2260 to reference the current frontend baseline: '(322 tests across 44 files)'\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Developer running the validation commands per line 2260 will see a mismatch between expected (261/43) and actual (322/44) test counts, causing alarm\nEvidence: TESTS.md line 2260 claims 'Frontend changes additionally want `cd lexy-app/frontend && npx tsc --noEmit && npx vitest run && npm run build` (261 tests across 43 files).' But earlier in the same file, line 2201-2202 claims '**322 passed across 44 files**' as the current baseline as of 2026-07-29. The inline instruction uses a stale figure (261 tests / 43 files) instead of the current 322 / 44.\nExpected files: docs/TESTS.md\nAcceptance criteria: Inline validation instruction matches the current frontend test baseline elsewhere in the same file\nValidation: Read TESTS.md lines 2201, 2260 and compare\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-024",
    "title": "Update the '+89 backend' figure at docs/TESTS.md:956 to 107 to match the reconciled figure alread...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding ingestion_pipeline:ing-05\nScope: Update the '+89 backend' figure at docs/TESTS.md:956 to 107 to match the reconciled figure already present at lines 2174-2176.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A reader trusting the stale '+89' figure under-counts test_document_package.py's actual coverage by 18 tests in the file CLAUDE.md designates as the coverage index.\nEvidence: Line 956 states A2 landed '+89 backend' tests for test_document_package.py. Static count (grep -c 'def test_' = 96 function defs, plus 3 @pytest.mark.parametrize sites with 4/5/5 cases adding 3+4+4=11 extra collected cases = 107 total) matches the '107 tests' figure already stated at lines 2174-2176, not '+89'.\nExpected files: docs/TESTS.md\nAcceptance criteria: Both figures for test_document_package.py agree in docs/TESTS.md\nValidation: grep -n '89 backend\\|107 tests' docs/TESTS.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-025",
    "title": "Add exposure_service.py and pipeline_diagnostics.py to CLAUDE.md \u00a72's pipeline-modules list.",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding repo_structure:doc-02\nScope: Add exposure_service.py and pipeline_diagnostics.py to CLAUDE.md \u00a72's pipeline-modules list.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A reader following \u00a72's 'root pipeline modules are the only pipeline tree, change them directly' guidance may overlook these two files when auditing or changing pipeline behaviour.\nEvidence: CLAUDE.md \u00a72's pipeline-modules row lists pipeline.py, eligibility.py, exposure_counter.py, user_knowledge.py, learning_units.py, onboarding.py, subtitle_*.py, utterance_*.py, word_knowledge.py, validate_tier_lemmas.py. Root Glob confirms exposure_service.py and pipeline_diagnostics.py also exist and are imported by pipeline.py (grep confirms both), but neither appears in the table \u2014 new finding, not in either prior audit pass.\nExpected files: CLAUDE.md\nAcceptance criteria: CLAUDE.md \u00a72 lists both modules alongside the other root pipeline modules\nValidation: grep -n 'exposure_service.py\\|pipeline_diagnostics.py' CLAUDE.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-026",
    "title": "Fix CLAUDE.md:38 to read 'lexy-app/backend/migrations/versions/' and update the count to 37+.",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding repo_structure:doc-03\nScope: Fix CLAUDE.md:38 to read 'lexy-app/backend/migrations/versions/' and update the count to 37+.\n  - also reported as db_migrations:db-07: Fix CLAUDE.md to read 'lexy-app/backend/migrations/versions/' and update the file count to 37+.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A contributor following CLAUDE.md's own stated path when authoring a migration searches a directory that doesn't exist.\n  - also reported as db_migrations:db-07: A contributor following CLAUDE.md's own stated path to find or author a migration searches a directory that does not exist.\nEvidence: CLAUDE.md:38 says '25+ files in alembic/versions/'. alembic.ini sets script_location=migrations; Glob confirms 37 files (head 037) at lexy-app/backend/migrations/versions/ \u2014 no alembic/versions/ directory exists anywhere. Matches db_migrations:db-05, still unresolved.\n  - also reported as db_migrations:db-07: CLAUDE.md's backend-layer table says migrations live in '25+ files in alembic/versions/'. alembic.ini:2 sets script_location = migrations, and the real 37 files live at lexy-app/backend/migrations/versions/ \u2014 no alembic/versions/ directory exists anywhere in the repo.\nExpected files: CLAUDE.md, lexy-app/backend/alembic.ini\nAcceptance criteria: CLAUDE.md's stated migrations path matches alembic.ini's script_location\nValidation: grep -n 'alembic/versions' CLAUDE.md; grep script_location lexy-app/backend/alembic.ini\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-027",
    "title": "Update TESTS.md:97 to the current count/date and reconcile against ROADMAP.md's figure.",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding repo_structure:doc-04\nScope: Update TESTS.md:97 to the current count/date and reconcile against ROADMAP.md's figure.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Frontend test count is inconsistent across CLAUDE.md's designated coverage index, obscuring whether coverage targets were met.\nEvidence: TESTS.md:97 states 'Current count (2026-07-27): 261 tests across 43 files' for frontend; a 2026-08-03 static grep of it(/test( calls under lexy-app/frontend/src counts 322 across 44 files, matching ROADMAP.md's 322-as-of-2026-07-29 figure \u2014 TESTS.md's line was never updated. Matches docs_drift:doc-drift-01, still unresolved.\nExpected files: docs/TESTS.md\nAcceptance criteria: TESTS.md's 'Current count' line matches a verifiable current total/date; No contradictory per-date counts remain in TESTS.md\nValidation: grep -n 'Current count' docs/TESTS.md; grep -rn 'it(\\|test(' lexy-app/frontend/src --include=*.test.tsx --include=*.test.ts | wc -l\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-028",
    "title": "Update SECURITY.md:546 to point at wherever the auth-surface claim now lives, or drop the note if...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding repo_structure:doc-06\nScope: Update SECURITY.md:546 to point at wherever the auth-surface claim now lives, or drop the note if CLAUDE.md no longer makes that claim.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A reader trying to verify SECURITY.md's own self-correction note cannot locate the CLAUDE.md section it points to.\nEvidence: SECURITY.md:546 says 'CLAUDE.md \u00a77 calls /api/search... public legacy endpoints' and flags that as stale \u2014 but current CLAUDE.md has no section numbered \u00a77 (sections run 3\u21925\u21926\u21928, confirmed via grep for '## 4.'/'## 7.' returning nothing) and no 'public legacy endpoint'/'video-sentences' text exists anywhere in CLAUDE.md today. New finding: the cross-reference itself is unfollowable.\nExpected files: docs/SECURITY.md, CLAUDE.md\nAcceptance criteria: SECURITY.md's cross-reference to CLAUDE.md resolves to an actual section\nValidation: grep -n '^## ' CLAUDE.md; grep -n 'CLAUDE.md \u00a77' docs/SECURITY.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-029",
    "title": "Update ROADMAP.md:68-70 to cite docs/TESTS.md's reconciled figures (backend 1258 passed/2 skipped...",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding security_paths:doc-01\nScope: Update ROADMAP.md:68-70 to cite docs/TESTS.md's reconciled figures (backend 1258 passed/2 skipped via `python3 -m pytest`, root 368) or replace the restated numbers with a pointer to docs/TESTS.md as the single source of truth.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: A reader who re-measures with the wrong (bare pytest) invocation reproduces 1151 and appears to 'confirm' ROADMAP.md's stale figure instead of catching the collection failure; the root-pipeline figure as stated understates coverage by 147 evaluation-harness tests.\nEvidence: ROADMAP.md:68-70, inside the live 'Current state' section (not the SUPERSEDED block at :392), states 'Verified on 2026-07-29 (current): backend 1151 passed / 2 skipped ... root pipeline 221 passed'. CLAUDE.md:236,241 and docs/TESTS.md:2176-2187 already reconcile this: 1151 is what the bare `pytest` entrypoint reports because it drops test_document_package.py on collection (needs `python3 -m pytest` per CLAUDE.md \u00a711), the correct count under the documented invocation is 1258, and root grew 221\u2192368 once the A1 evaluation harness landed later the same day. ROADMAP.md was never updated to match.\nExpected files: docs/ROADMAP.md\nAcceptance criteria: ROADMAP.md's 'Current state' section no longer states 1151/221 as current without noting the invocation/date caveat, and matches or points to docs/TESTS.md's validation-commands table.\nValidation: grep -n '1151 passed\\|221 passed' docs/ROADMAP.md; grep -n '1258 passed\\|368 passed' docs/TESTS.md CLAUDE.md\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-030",
    "title": "Fix the curl example in docs/INGESTION_PIPELINE.md \u00a75 to use package_name instead of package_path.",
    "description": "Priority: P7 | severity low | confidence confirmed | from audit finding security_paths:doc-02\nScope: Fix the curl example in docs/INGESTION_PIPELINE.md \u00a75 to use package_name instead of package_path.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: An operator following the documented walkthrough verbatim gets a 422 missing-required-field error on package_name instead of a successful import.\nEvidence: INGESTION_PIPELINE.md:318's operator curl example posts {\"package_path\": \"<document_id>\"}, but PackageImportRequest (lexy-app/backend/models/schemas.py:1018) only defines a required `package_name` field (no default) and routers/books.py:203 reads body.package_name.\nExpected files: docs/INGESTION_PIPELINE.md\nAcceptance criteria: INGESTION_PIPELINE.md's example field name matches PackageImportRequest.package_name.\nValidation: grep -n 'package_path\\|package_name' docs/INGESTION_PIPELINE.md lexy-app/backend/models/schemas.py\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-031",
    "title": "Update ROADMAP.md line 68 with the most recent frontend test count and date from the top of TESTS.md",
    "description": "Priority: P7 | severity info | confidence confirmed | from audit finding docs_drift:doc-02\nScope: Update ROADMAP.md line 68 with the most recent frontend test count and date from the top of TESTS.md\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Readers checking ROADMAP for test coverage status get a stale intermediate figure if frontend tests have been added since 2026-07-29\nEvidence: ROADMAP.md line 68 claims 'frontend **322 passed across 44 files**' as of 2026-07-29. TESTS.md line 2201 repeats this same figure. However, the document records intermediate test counts in between (289, 296, 306 across lines 676-754), all occurring after 2026-07-27, with 322 being the final figure on line 835 dated within the 2026-07-29 window. The claim is correct but the intermediate history shows several test additions are not reflected in the ROADMAP summary.\nExpected files: docs/TESTS.md, docs/ROADMAP.md\nAcceptance criteria: ROADMAP 'Current state' section frontend test count matches the latest in TESTS.md\nValidation: Grep TESTS.md for the most recent 'frontend.*passed' claim and cross-check ROADMAP.md line 68\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-032",
    "title": "Clarify by either (a) moving N5b to 'Remaining work' section with explicit note that it is indepe...",
    "description": "Priority: P7 | severity info | confidence confirmed | from audit finding docs_drift:doc-04\nScope: Clarify by either (a) moving N5b to 'Remaining work' section with explicit note that it is independent of N5a, or (b) add a sentence in the N5a closing note: 'N5b (case-variant dedup) is listed below as a separate, ready-to-implement task independent of this line of work.'\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Low \u2014 the separation is correct (N5b is unblocked and independent), but the phrasing 'tracked separately, not here' is misleading since N5b immediately follows in the ROADMAP under the same N5 category\nEvidence: ROADMAP.md N5a claims status '\u2705 SHIPPED 2026-07-28/29' and 'What remains from this line of work is tracked separately, not here'. However, the next item, N5b, immediately following in ROADMAP, is titled 'Case-variant catalog duplication' and explicitly labeled as 'code, ready to implement' (line 264), appearing to contradict the 'remains' framing if N5b is part of the same N5a line of work. Reading the text, N5b is presented as separate implementation, but the organizational framing suggests N5a work is fully tracked separately.\nExpected files: docs/ROADMAP.md\nAcceptance criteria: A reader understands that N5b is independent and can be implemented regardless of N5a status\nValidation: Read ROADMAP.md lines 126-265 and trace the logical relationship between N5a and N5b\nParallelizable: yes",
    "depends_on": []
  },
  {
    "id": "au-033",
    "title": "Update the cited line numbers in SECURITY.md and CLAUDE.md \u00a714 to 164/205/322, or drop line numbe...",
    "description": "Priority: P7 | severity info | confidence confirmed | from audit finding repo_structure:doc-05\nScope: Update the cited line numbers in SECURITY.md and CLAUDE.md \u00a714 to 164/205/322, or drop line numbers in favor of the grep pattern plus description.\nNon-goals: nothing beyond the scope above \u2014 no drive-by refactors, no changes to files outside the expected list without re-approval.\nImpact if unfixed: Someone re-running the documented grep and eyeballing line numbers against the stale reference could be momentarily unsure whether the hits match the pre-verified-safe set.\nEvidence: SECURITY.md and CLAUDE.md \u00a714's self-check grep both still say 'Expected hits: search_service.py:62,103,216'; re-running that exact grep on 2026-08-03 shows the three (still-safe) f-string SQL sites now at lines 164, 205, 322. Matches security_paths:doc-01, still unresolved.\nExpected files: docs/SECURITY.md, CLAUDE.md, lexy-app/backend/services/search_service.py\nAcceptance criteria: SECURITY.md and CLAUDE.md no longer cite stale line numbers for this pattern\nValidation: rg -n 'f\"(SELECT|INSERT|UPDATE|DELETE)' lexy-app/backend --glob '!**/migrations/**'\nParallelizable: yes",
    "depends_on": []
  }
]
```

Not promoted to tasks:
- `db_migrations:db-01`: human decision — surfaced to the operator, not a task
